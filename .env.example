# Root environment example — copy to `.env` and fill in real values locally.
# `.env`/`.env.*` are gitignored (except this file); never commit real secrets.
# Real secret encryption (SOPS/age) is phase 4 (INFRA-03) — placeholders only here.

# Password for the Compose Postgres `imbau` (owner/migration) role.
# Consumed by compose.yml via ${POSTGRES_PASSWORD:-dev}. Placeholder only.
POSTGRES_PASSWORD=dev
