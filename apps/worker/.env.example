# apps/worker — variables de entorno (placeholders, NUNCA valores reales).
# Doc viva: mantener consistente con el schema de src/env.ts (D-04).
DATABASE_URL=postgres://user:password@localhost:5432/imbau
REDIS_URL=redis://localhost:6379
NODE_ENV=development
