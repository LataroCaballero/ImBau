# apps/worker — variables de entorno (placeholders, NUNCA valores reales).
# Doc viva: mantener consistente con el schema de src/env.ts (D-04).
# El worker es un shell BullMQ↔Redis (APP-03): NO abre conexiones a Postgres,
# así que NO declara DATABASE_URL (WR-01). Solo necesita REDIS_URL + NODE_ENV.
REDIS_URL=redis://localhost:6379
NODE_ENV=development
