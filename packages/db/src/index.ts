// Placeholder typed export for @imbau/db. Drizzle schema, RLS policies and the
// `postgres` driver land in phase 2 — nothing speculative here (D-12).
export type DbPackage = "@imbau/db";

export const dbPackage: DbPackage = "@imbau/db";
