---
phase: 04-motor-de-cotizaci-n-puro-packages-quoting
plan: 04
type: execute
wave: 4
depends_on: [04-02, 04-03]
files_modified:
  - packages/quoting/src/compare.ts
  - packages/quoting/src/compare.test.ts
  - packages/quoting/src/serialize.ts
  - packages/quoting/src/serialize.test.ts
  - packages/quoting/src/index.ts
autonomous: true
requirements: [ENGINE-03, ENGINE-04]
must_haves:
  truths:
    - "compareQuotes(contado, financiado) returns the derived figures ahorroUsd (int) and ahorroPct (2-decimal string) — surfaces never recompute the comparison (D-11)"
    - "toWhatsAppText(result) and toPdfModel(result) both consume the SAME QuoteResult (never re-run the engine, never re-read data) so UI == PDF == WhatsApp can't drift (ENGINE-03 / D-12)"
    - "toWhatsAppText produces a short es-AR voseo summary (WA-01: resumen, not the full table) using the shared formatter; toPdfModel returns a pure data model for fase 7 (no JSX, no render)"
    - "The barrel index.ts exports the full public API: calcQuote, compareQuotes, toWhatsAppText, toPdfModel, ENGINE_VERSION, QuoteError, formatUsd/formatArs, and all public types via export type"
    - "pnpm --filter @imbau/quoting test -- --coverage is GREEN at 100% (lines+branches+functions+statements) across the package, with no coverage-ignore pragma anywhere in src"
  artifacts:
    - path: "packages/quoting/src/compare.ts"
      provides: "compareQuotes(contado, financiado): QuoteComparison — derived ahorro figures (D-11)"
      contains: "compareQuotes"
    - path: "packages/quoting/src/serialize.ts"
      provides: "toWhatsAppText(result): string and toPdfModel(result): PdfModel — pure serializers off one QuoteResult"
      contains: "toWhatsAppText"
    - path: "packages/quoting/src/index.ts"
      provides: "Public barrel: full engine API + types (verbatimModuleSyntax export type)"
      contains: "calcQuote"
  key_links:
    - from: "packages/quoting/src/serialize.ts"
      to: "packages/quoting/src/format.ts"
      via: "toWhatsAppText/toPdfModel render amounts via formatUsd/formatArs (one shared es-AR formatter)"
      pattern: "format"
    - from: "packages/quoting/src/serialize.ts"
      to: "packages/quoting/src/types.ts (QuoteResult)"
      via: "serializers derive from the same QuoteResult — never re-run calcQuote"
      pattern: "QuoteResult"
    - from: "packages/quoting/src/index.ts"
      to: "engine.ts + compare.ts + serialize.ts + version.ts + errors.ts + format.ts + types.ts"
      via: "single public surface re-export; runtime via export {}, types via export type {}"
      pattern: "export"
---

<objective>
Finish the engine's public surface and lock the quality gate: `compareQuotes` (the derived contado-vs-financiado figures, D-11), the two pure serializers `toWhatsAppText` / `toPdfModel` (both off the SAME `QuoteResult`, D-12), the assembled barrel `index.ts` (the full public API), and the enforced package-wide 100% coverage gate. This is the last piece that makes `packages/quoting` the finalized contract every downstream surface (fase 5 API, fase 6 UI/WhatsApp, fase 7 PDF) consumes.

Purpose: ENGINE-03 (one output shape feeding UI, PDF, WhatsApp identically — "las tres superficies nunca difieren") is completed by deriving all surface outputs from `QuoteResult` via the shared formatter; ENGINE-04's 100% coverage gate is proven green across the whole package. D-11's `compareQuotes` gives fase 6 the ahorro figures without recompute.
Output: `compare.ts` + tests, `serialize.ts` + tests, the completed `index.ts` barrel, and a green `--coverage` run.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md
@CLAUDE.md

# Modules this plan composes into the public surface (read before editing):
@packages/quoting/src/types.ts
@packages/quoting/src/format.ts
@packages/quoting/src/engine.ts
@packages/quoting/src/version.ts
@packages/quoting/src/errors.ts
@packages/quoting/src/index.ts
@packages/db/src/index.ts
</context>

<artifacts_this_phase_produces>
This plan contributes the following NEW symbols (plans 01-03 shipped money/version/types/errors/format/engine):
- File `packages/quoting/src/compare.ts` — export `compareQuotes(contado: ContadoResult, financiado: FinanciadoResult): QuoteComparison`.
- File `packages/quoting/src/serialize.ts` — export `toWhatsAppText(result: QuoteResult): string` and `toPdfModel(result: QuoteResult): PdfModel`.
- `packages/quoting/src/index.ts` — completed public barrel re-exporting `calcQuote`, `compareQuotes`, `toWhatsAppText`, `toPdfModel`, `ENGINE_VERSION`, `QuoteError`, `formatUsd`, `formatArs`, and all public types + `QuoteErrorCode` (via `export type`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compareQuotes — derived contado-vs-financiado ahorro figures</name>
  <files>packages/quoting/src/compare.ts, packages/quoting/src/compare.test.ts</files>
  <read_first>
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md — D-11 (compareQuotes helper computes derived figures, ahorro USD + %, tipadas y 100% cubiertas; surfaces never recompute), D-10 (comparison is two runs of the engine, not a container)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "compare.ts / format.ts / serialize.ts" (compareQuotes → { ahorroUsd, ahorroPct }; surfaces never recompute)
    - packages/quoting/src/types.ts — ContadoResult, FinanciadoResult, QuoteComparison
    - packages/quoting/src/money.ts — reuse the decimal.js discipline for the % (never float)
  </read_first>
  <behavior>
    - `compareQuotes(contado{precioUsd:100000}, financiado{precioUsd:120000})` returns `{ ahorroUsd: 20000, ahorroPct: "16.67" }` — ahorroUsd = financiado.precioUsd − contado.precioUsd (what paying cash saves), ahorroPct = ahorroUsd / financiado.precioUsd × 100 as a 2-decimal string (20000/120000×100 = 16.666… → "16.67").
    - When both prices are equal, `ahorroUsd === 0` and `ahorroPct === "0.00"`.
    - ahorroUsd is an integer USD; ahorroPct is a 2-decimal string computed via decimal.js (never a JS float).
  </behavior>
  <action>
    Create `packages/quoting/src/compare.ts` exporting `compareQuotes(contado: ContadoResult, financiado: FinanciadoResult): QuoteComparison`. Compute `ahorroUsd = financiado.precioUsd - contado.precioUsd` (integer). Compute `ahorroPct` with decimal.js (reuse the `ROUND_HALF_UP` discipline — import a helper from `./money` or a local Decimal clone; never `parseFloat`) as `ahorroUsd / financiado.precioUsd × 100` formatted to a 2-decimal string. Document the sign convention in an English header (positive ahorro = paying contado is cheaper than financiado; D-11). This helper is the SINGLE place the comparison is computed — surfaces (fase 6, UI-03) consume it, never recompute.

    Create `packages/quoting/src/compare.test.ts` asserting the behavior-block cases exactly (`toEqual`, never `toBeCloseTo`), including the equal-price zero case and a repeating-decimal rounding case ("16.67").
  </action>
  <verify>
    <automated>pnpm --filter @imbau/quoting test compare && ! grep -q 'toBeCloseTo' packages/quoting/src/compare.test.ts && ! grep -Eq 'parseFloat|Number\(' packages/quoting/src/compare.ts && echo COMPARE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quoting/src/compare.ts` exports `compareQuotes` returning `{ ahorroUsd: number, ahorroPct: string }`.
    - `compareQuotes` for prices 100000/120000 returns `ahorroUsd: 20000, ahorroPct: "16.67"`; equal prices return `0` / `"0.00"`.
    - `ahorroPct` is computed via decimal.js (no `parseFloat`/`Number(` in `compare.ts`); tests use exact assertions (no `toBeCloseTo`).
    - `pnpm --filter @imbau/quoting test compare` passes.
  </acceptance_criteria>
  <done>`compareQuotes` is the single pure source of the ahorro USD + % figures, fully tested with exact decimal math — surfaces read it, never recompute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure serializers — toWhatsAppText + toPdfModel off one QuoteResult</name>
  <files>packages/quoting/src/serialize.ts, packages/quoting/src/serialize.test.ts</files>
  <read_first>
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md — "Pattern 3: One QuoteResult, many serializers (no drift)" + Open Question 2 (initial copy is a reasonable draft, does NOT bump ENGINE_VERSION)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "compare.ts / format.ts / serialize.ts" (serialize.ts role-match to src/index.ts; consume the SAME QuoteResult, never re-run the engine)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md — D-12 (serializers + shared es-AR formatter in scope), D-13 (copy changes do NOT bump ENGINE_VERSION), WA-01 wording (resumen + link, short — NOT the full table)
    - packages/quoting/src/format.ts — formatUsd/formatArs (the ONLY money-string source; both serializers use it)
    - packages/quoting/src/types.ts — QuoteResult (discriminated union), PdfModel
    - CLAUDE.md — user output in es-AR voseo; identifiers/comments in English
  </read_first>
  <behavior>
    - `toWhatsAppText(financiadoResult)` returns a short es-AR voseo string summarizing precio, anticipo, cuotas and the first cuota ARS (a resumen, NOT the full amortization table — WA-01), with all amounts rendered via `formatUsd`/`formatArs` (so `US$`/`$` labels and grouping match the on-screen values).
    - `toWhatsAppText(contadoResult)` returns a short summary of the contado precio (no cuotas).
    - `toPdfModel(result)` returns a plain `PdfModel` data object (label/value string pairs + leyendas) — no JSX, no rendering, no I/O — derived from the SAME result the WhatsApp text used.
    - Both serializers take an already-computed `QuoteResult` and NEVER call `calcQuote` (no re-computation, no drift — Pattern 3).
  </behavior>
  <action>
    Create `packages/quoting/src/serialize.ts` exporting `toWhatsAppText(result: QuoteResult): string` and `toPdfModel(result: QuoteResult): PdfModel`. Both branch on `result.modalidad` (exhaustive over the discriminated union) and render every amount through `formatUsd`/`formatArs` from `./format` — never build a currency string inline (single formatter, ENGINE-03). `toWhatsAppText`: a short es-AR voseo resumen (precio; for financiado: anticipo, N cuotas, "primera cuota" = `formatArs(cuotas[0].ars)` when present) — keep it short (WA-01 is resumen + link; the surface appends the wa.me link in fase 6). `toPdfModel`: a pure data object matching the `PdfModel` type (label/value pairs already formatted es-AR + the leyendas placeholders "cotización no vinculante" and the CAC-adjustment note) for fase 7 to render. Neither function imports `./engine` or calls `calcQuote`; neither does any I/O. Copy is a reasonable draft (D-12/D-13) — it does NOT bump `ENGINE_VERSION`; final wording is validated against the surfaces in fases 6/7.

    Create `packages/quoting/src/serialize.test.ts`: build a real `QuoteResult` by calling `calcQuote` (from `./engine`) ONCE in the test setup, then assert `toWhatsAppText` output contains the expected `US$`/`$`-formatted figures and stays a short resumen, and that `toPdfModel` returns the expected `PdfModel` shape with formatted values and the two leyendas. Assert both serializers, given the same result object, reference the same figures (no drift). Cover both modalidad branches so `serialize.ts` reaches full coverage.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/quoting test serialize && ! grep -q 'calcQuote' packages/quoting/src/serialize.ts && grep -Eq 'formatUsd|formatArs' packages/quoting/src/serialize.ts && echo SERIALIZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quoting/src/serialize.ts` exports `toWhatsAppText(result: QuoteResult): string` and `toPdfModel(result: QuoteResult): PdfModel`, both branching exhaustively on `modalidad`.
    - Both serializers render amounts via `formatUsd`/`formatArs`; `grep -q 'calcQuote' packages/quoting/src/serialize.ts` finds nothing (serializers never re-run the engine).
    - `toWhatsAppText` is a short es-AR resumen (not the full cuota table); `toPdfModel` returns a pure `PdfModel` data object with the "cotización no vinculante" + CAC-adjustment leyendas.
    - `serialize.test.ts` builds one `QuoteResult` via `calcQuote` and asserts both serializers off it; both modalidad branches covered.
    - `pnpm --filter @imbau/quoting test serialize` passes.
  </acceptance_criteria>
  <done>Two pure serializers derive WhatsApp text and a PDF data model from the SAME `QuoteResult` via the shared es-AR formatter — one output shape, three surfaces that can't drift.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the public barrel and enforce the package-wide 100% coverage gate</name>
  <files>packages/quoting/src/index.ts</files>
  <read_first>
    - packages/quoting/src/index.ts — the current minimal barrel (exports ENGINE_VERSION only, from plan 01) — REPLACE with the full public surface
    - packages/db/src/index.ts — the repo barrel convention: `export { ... }` for runtime, `export type { ... }` for type-only (verbatimModuleSyntax)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "packages/quoting/src/index.ts (barrel — REPLACE placeholder)" (re-export runtime with export {}, every type + QuoteError type-surface with export type {})
    - packages/quoting/vitest.config.ts — the coverage gate (thresholds { 100: true }, exclude index.ts) that this run enforces
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-VALIDATION.md — "Full suite command: pnpm --filter @imbau/quoting test -- --coverage (fails if any metric < 100%)"
  </read_first>
  <action>
    Replace `packages/quoting/src/index.ts` with the complete public barrel following the db barrel convention. Runtime exports (`export { ... } from ...`): `calcQuote` from `./engine`; `compareQuotes` from `./compare`; `toWhatsAppText`, `toPdfModel` from `./serialize`; `ENGINE_VERSION` from `./version`; `QuoteError` from `./errors`; `formatUsd`, `formatArs` from `./format` (fase 6 needs the shared formatter, D-12). Type-only exports (`export type { ... } from ...`, required by verbatimModuleSyntax): `QuoteInput`, `PlanInput`, `RefuerzoInput`, `CacInput`, `QuoteResult`, `ContadoResult`, `FinanciadoResult`, `CuotaLine`, `RefuerzoLine`, `QuoteTotals`, `QuoteComparison`, `PdfModel` from `./types`; `QuoteErrorCode` from `./errors`. Keep `money.ts` internal (do NOT export the primitives — they are implementation detail). The barrel is re-export only and is excluded from coverage.

    Then run the FULL package gate: `pnpm --filter @imbau/quoting test -- --coverage` and confirm it reports 100% on lines, branches, functions and statements. If any file is below 100%, add the missing unit assertion in the OWNING module's test file (do NOT add a coverage-ignore pragma — Pitfall 4). Also run `pnpm --filter @imbau/quoting typecheck` and `pnpm --filter @imbau/quoting lint` — all green. Confirm no coverage-ignore pragma exists anywhere under `packages/quoting/src`.
  </action>
  <verify>
    <automated>grep -q 'export { calcQuote' packages/quoting/src/index.ts && grep -q 'export type {' packages/quoting/src/index.ts && ! grep -Eq 'export .*roundHalfUpUsd|export .*allocateCuotas' packages/quoting/src/index.ts && ! grep -rEq 'c8 ignore|v8 ignore' packages/quoting/src && pnpm --filter @imbau/quoting typecheck && pnpm --filter @imbau/quoting lint && pnpm --filter @imbau/quoting test -- --coverage && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quoting/src/index.ts` exports (runtime) `calcQuote`, `compareQuotes`, `toWhatsAppText`, `toPdfModel`, `ENGINE_VERSION`, `QuoteError`, `formatUsd`, `formatArs`, and (type-only, via `export type`) all public types + `QuoteErrorCode`.
    - The money primitives (`roundHalfUpUsd`/`allocateCuotas`/`decimal2`) are NOT exported from the barrel (internal).
    - `pnpm --filter @imbau/quoting test -- --coverage` is GREEN at 100% (lines, branches, functions, statements).
    - `pnpm --filter @imbau/quoting typecheck` and `lint` pass.
    - `grep -rEq 'c8 ignore|v8 ignore' packages/quoting/src` finds nothing (no coverage-ignore escape hatch anywhere in src).
  </acceptance_criteria>
  <done>The finalized public barrel exposes the full engine API (runtime + types) with money internals hidden, and the package-scoped 100% coverage gate is green — the `QuoteResult` contract is locked for fases 5/6/7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuoteResult → surfaces (serializers) | The serializers are the last transform before UI/PDF/WhatsApp; all three MUST derive from one result and one formatter to stay identical. |
| package → downstream consumers (barrel) | The barrel is the public contract fase 5/6/7 import against; leaking internals or a drifting figure would surface in production quotes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `serialize.ts` (a surface recomputing figures and drifting from the on-screen number) | high | mitigate | Serializers consume the SAME `QuoteResult` and never call `calcQuote` (grep-verified); both use the one shared formatter; tests assert off one result. |
| T-04-04 | Information Disclosure | `serialize.ts`/`compare.ts` (currency-label / grouping drift) | medium | mitigate | All amounts rendered via `formatUsd`/`formatArs` (no inline currency strings); the formatter's U+202F/ICU mitigation from plan 02 applies. |
| T-04-07 | Tampering | coverage gate bypass (partial tests + coverage-ignore pragma) | medium | mitigate | 100% package-scoped gate enforced via `--coverage`; no coverage-ignore pragma allowed in `src` (grep-verified); missing coverage fixed by real assertions, not ignores. |
</threat_model>

<verification>
- `pnpm --filter @imbau/quoting test -- --coverage` GREEN at 100% (lines+branches+functions+statements).
- `pnpm --filter @imbau/quoting typecheck` and `lint` pass.
- Barrel exports the full public API (runtime + `export type`), money internals hidden.
- Serializers never import `./engine`/call `calcQuote`; both use the shared formatter.
- No coverage-ignore pragma anywhere under `packages/quoting/src`.
</verification>

<success_criteria>
- ENGINE-03 (complete): one `QuoteResult` shape feeds `toWhatsAppText` + `toPdfModel` + `compareQuotes` identically via the shared formatter — the three surfaces cannot differ.
- ENGINE-04 (complete): the package-scoped 100% coverage gate is green across the whole package, with property invariants (plan 03) + unit tables, no coverage-ignore escape hatch.
- D-11: `compareQuotes` supplies the ahorro USD + % figures for fase 6 (UI-03) without recompute.
- The public barrel locks the contract fases 5/6/7 build on.
</success_criteria>

<output>
Create `.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-04-SUMMARY.md` when done.
</output>
