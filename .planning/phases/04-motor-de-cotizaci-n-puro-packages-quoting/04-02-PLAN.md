---
phase: 04-motor-de-cotizaci-n-puro-packages-quoting
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/quoting/src/types.ts
  - packages/quoting/src/errors.ts
  - packages/quoting/src/errors.test.ts
  - packages/quoting/src/format.ts
  - packages/quoting/src/format.test.ts
autonomous: true
requirements: [ENGINE-03]
must_haves:
  truths:
    - "QuoteResult is a discriminated union on modalidad ('contado' | 'financiado') — one result per modalidad, never a container object holding both (D-10)"
    - "QuoteInput carries BOTH resolved USD prices (contado + financiado) plus an optional plan and cac; the engine does NO pricing math — the contado discount is the difference between the two supplied prices (D-09)"
    - "Every degenerate/out-of-domain QuoteInput maps to a typed QuoteError with an exhaustive code union — never a silent normalization (D-07)"
    - "The es-AR formatter owns the currency label (US$ / $) as a literal with a normal space and groups digits with Intl decimal style — never Intl currency style (defeats U+202F/ICU drift so UI == PDF == WhatsApp)"
  artifacts:
    - path: "packages/quoting/src/types.ts"
      provides: "QuoteInput, QuoteResult (ContadoResult | FinanciadoResult), CuotaLine, RefuerzoLine, PlanInput, CacInput, RefuerzoInput, QuoteTotals, PdfModel, QuoteComparison — the single output contract all surfaces consume"
      contains: "QuoteResult"
    - path: "packages/quoting/src/errors.ts"
      provides: "QuoteError class + exhaustive QuoteErrorCode union for degenerate-plan rejection (D-07)"
      contains: "QuoteError"
    - path: "packages/quoting/src/format.ts"
      provides: "Deterministic es-AR formatter: formatUsd (US$) / formatArs ($) literal labels + Intl decimal grouping"
      contains: "es-AR"
  key_links:
    - from: "packages/quoting/src/types.ts"
      to: "packages/db/src/schema/payment-plans.ts + cac-index.ts + unit-prices.ts"
      via: "QuoteInput field scalar types are structurally compatible with the Drizzle row shapes (precio int, anticipoPct/valor string, cuotas int, ajuste enum, refuerzos JSONB)"
      pattern: "anticipoPct"
    - from: "packages/quoting/src/format.ts"
      to: "Intl.NumberFormat('es-AR', { style: 'decimal' })"
      via: "digits-only grouping under a hand-owned US$/$ label — NOT style:'currency'"
      pattern: "decimal"
---

<objective>
Author the load-bearing type contract and its guardrails: `types.ts` (the single `QuoteResult` discriminated union + `QuoteInput` + line/total/serializer-model types), `errors.ts` (the exhaustive `QuoteError` domain-rejection idiom, D-07), and `format.ts` (the deterministic es-AR formatter that owns the currency label to defeat ICU drift, D-12). Nothing in `packages/quoting` reads or writes I/O; these are pure declarations + one pure formatter.

Purpose: ENGINE-03 requires a single typed output shape that feeds UI, PDF and WhatsApp identically — "ninguna superficie puede construirse hasta que la forma de salida esté finalizada" (ROADMAP goal). This is that shape. `calcQuote` (plan 03) produces `QuoteResult` and throws `QuoteError`; the serializers (plan 04) consume `QuoteResult` and the formatter.
Output: `types.ts`, `errors.ts` + tests, `format.ts` + tests — all inside the 100% coverage gate from plan 01.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md
@.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md
@CLAUDE.md

# The schema shapes QuoteInput must mirror structurally (the engine never imports @imbau/db):
@packages/db/src/schema/json-schemas.ts
@packages/db/src/schema/payment-plans.ts
@packages/db/src/schema/cac-index.ts
@packages/db/src/schema/unit-prices.ts
@packages/db/src/schema/price-lists.ts

# Typed-contract co-location analog + barrel convention:
@packages/db/src/index.ts
@packages/quoting/src/version.ts
</context>

<artifacts_this_phase_produces>
This plan contributes the following NEW symbols (plan 01 shipped money + version; plans 03-04 add the engine + serializers + barrel):
- File `packages/quoting/src/types.ts` — exported types: `QuoteInput`, `PlanInput`, `RefuerzoInput`, `CacInput`, `QuoteResult` (= `ContadoResult | FinanciadoResult`), `ContadoResult`, `FinanciadoResult`, `CuotaLine`, `RefuerzoLine`, `QuoteTotals`, `QuoteComparison`, `PdfModel`.
- File `packages/quoting/src/errors.ts` — `class QuoteError extends Error`, exported `type QuoteErrorCode` union.
- File `packages/quoting/src/format.ts` — exported `formatUsd(amount: number): string`, `formatArs(amount: string): string`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Define the QuoteInput / QuoteResult type contract as a discriminated union</name>
  <files>packages/quoting/src/types.ts</files>
  <read_first>
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "packages/quoting/src/types.ts (model — NEW)" incl. the exact source-column → QuoteInput field mapping table
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md — the data-flow diagram (QuoteInput / ContadoResult / FinanciadoResult shapes), Assumptions A1/A2/A3, Pattern 1
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md — D-08 (refuerzo schedule = index + monto, no dates), D-09 (two resolved prices, no pricing logic), D-10 (one result per modalidad, discriminated union), Claude's Discretion (union discriminated by modalidad)
    - packages/db/src/schema/payment-plans.ts — anticipoPct numeric→string, cuotas int, ajuste enum 'CAC'|'fijo', refuerzos JSONB Refuerzo[]
    - packages/db/src/schema/cac-index.ts — valor numeric→string, periodo text "YYYY-MM"
    - packages/db/src/schema/unit-prices.ts — precio integer USD
    - packages/db/src/schema/json-schemas.ts — the `Refuerzo` shape ({cuota:int, montoUsd:int}) QuoteInput.plan.refuerzos mirrors
  </read_first>
  <action>
    Create `packages/quoting/src/types.ts` as pure type declarations (NO runtime code — keep it declaration-only so it is trivially covered; any type-narrowing helper with a runtime body must move to engine.ts or be tested). English identifiers/comments with a header explaining this is the single output contract every downstream surface consumes (ENGINE-03).

    Input types (structurally compatible with Drizzle rows — the engine never imports @imbau/db; callers in fase 5/6 map rows in):
    - `RefuerzoInput` = `{ cuota: number; montoUsd: number }` (mirrors db `Refuerzo`; both integers, D-08).
    - `PlanInput` = `{ anticipoPct: string; cuotas: number; ajuste: 'CAC' | 'fijo'; refuerzos: RefuerzoInput[] }` (anticipoPct is a STRING — numeric from Drizzle).
    - `CacInput` = `{ periodo: string; valor: string }` (valor is a STRING — numeric(12,4) from Drizzle).
    - `QuoteInput` = `{ modalidad: 'contado' | 'financiado'; precioContadoUsd: number; precioFinanciadoUsd: number; plan?: PlanInput; cac?: CacInput }`. Per D-09/A2 BOTH resolved USD prices are always supplied even for a single-modalidad run; the engine does NO discount math (the contado discount is the difference between the two supplied prices). `plan` is required for financiado, `cac` required for financiado + `ajuste:'CAC'` (the engine enforces this at runtime via QuoteError — the types stay permissive with optionals).

    Output contract (discriminated union on `modalidad`, D-10):
    - `CuotaLine` = `{ indice: number; usd: number; ars: string | null }`. Per A1/A3 every cuota line carries an `ars` field: for `ajuste:'CAC'` it is the 2-decimal ARS string (usd × CAC), for `ajuste:'fijo'` it is `null` (CAC multiplier applies only to the CAC path). The "primera cuota en ARS" (ENGINE-02) is `cuotas[0].ars`.
    - `RefuerzoLine` = `{ indice: number; montoUsd: number }` (D-08: cuota index + USD amount, NO calendar dates — refuerzos are USD-fixed, D-05).
    - `QuoteTotals` = `{ cuotasUsd: number; refuerzosUsd: number; totalUsd: number }` (all integers; `totalUsd` reconciles to the financiado price).
    - `ContadoResult` = `{ modalidad: 'contado'; precioUsd: number; version: number }`.
    - `FinanciadoResult` = `{ modalidad: 'financiado'; precioUsd: number; anticipoUsd: number; saldoUsd: number; cuotas: CuotaLine[]; refuerzos: RefuerzoLine[]; cac: CacInput | null; totals: QuoteTotals; version: number }`.
    - `QuoteResult` = `ContadoResult | FinanciadoResult`.

    Serializer-model + comparison types (shapes consumed in plan 04; drafts OK per D-12, do NOT bump ENGINE_VERSION):
    - `QuoteComparison` = `{ ahorroUsd: number; ahorroPct: string }` (D-11 derived figures; pct as a 2-decimal string).
    - `PdfModel` = a plain data object for fase 7 (e.g. `{ modalidad: string; lineas: { label: string; valor: string }[]; leyendas: string[] }` — reasonable draft; the worker owns the JSX, this is data only). Keep it a pure data shape with es-AR-ready string fields.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/quoting typecheck && grep -q "modalidad: 'contado' | 'financiado'" packages/quoting/src/types.ts && grep -q 'FinanciadoResult' packages/quoting/src/types.ts && grep -q 'ars: string | null' packages/quoting/src/types.ts && ! grep -Eq "from ['\"]@imbau/db" packages/quoting/src/types.ts && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quoting/src/types.ts` declares `QuoteInput`, `PlanInput`, `RefuerzoInput`, `CacInput`, `QuoteResult`, `ContadoResult`, `FinanciadoResult`, `CuotaLine`, `RefuerzoLine`, `QuoteTotals`, `QuoteComparison`, `PdfModel`.
    - `QuoteResult` is a union of `ContadoResult | FinanciadoResult` discriminated on `modalidad` (grep confirms the literal `'contado' | 'financiado'`).
    - `QuoteInput.plan.anticipoPct` and `QuoteInput.cac.valor` are typed `string` (numeric-from-Drizzle); `precioContadoUsd`/`precioFinanciadoUsd`/`montoUsd`/`cuotas`/`usd` are `number`.
    - `CuotaLine.ars` is `string | null`; `RefuerzoLine` has NO date field.
    - `types.ts` imports nothing from `@imbau/db` and contains no runtime executable code.
    - `pnpm --filter @imbau/quoting typecheck` passes.
  </acceptance_criteria>
  <done>The single `QuoteResult` output contract exists as a modalidad-discriminated union with the `QuoteInput`, line, total, comparison and PDF-model types every surface will consume — no I/O, no pricing math, both prices supplied.</done>
</task>

<task type="auto">
  <name>Task 2: Define the QuoteError domain-rejection idiom with an exhaustive code union</name>
  <files>packages/quoting/src/errors.ts, packages/quoting/src/errors.test.ts</files>
  <read_first>
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md — "Open Questions" #1 (domain-error idiom recommendation: a discriminated QuoteError with a code union thrown from calcQuote) + Security Domain V5 (typed rejection of degenerate QuoteInput)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "packages/quoting/src/errors.ts (model — NEW, novel idiom)"
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md — D-07 (reject degenerate plans with a typed domain error; never normalize silently; the surface decides how to present it)
    - packages/quoting/src/types.ts — the contract these errors guard (read after Task 1)
  </read_first>
  <action>
    Create `packages/quoting/src/errors.ts` implementing the RESEARCH-recommended idiom (Open Question 1): export `type QuoteErrorCode` as an exhaustive string-literal union and a `class QuoteError extends Error` carrying a `readonly code: QuoteErrorCode`. Codes (name the domain conditions the pure engine can detect — every one must be reachable and unit-tested for the 100% gate): `'PLAN_REQUERIDO'` (financiado without a plan), `'CAC_REQUERIDO'` (ajuste:'CAC' without a cac value), `'ANTICIPO_PCT_FUERA_DE_RANGO'` (anticipoPct not in [0,100]), `'CUOTAS_INVALIDAS'` (cuotas < 1), `'REFUERZO_FUERA_DE_PLAZO'` (a refuerzo.cuota outside [1, cuotas]), `'REFUERZO_DUPLICADO'` (two refuerzos on the same cuota index), `'SALDO_NO_POSITIVO'` (precio − anticipo − Σrefuerzos ≤ 0). The constructor sets `this.name = 'QuoteError'`, assigns `this.code`, and accepts an optional human message (English; the es-AR presentation is the surface's job, D-07). Do NOT log, do NOT import Sentry/pino — the engine rejects, it does not observe (CLAUDE.md).

    Create `packages/quoting/src/errors.test.ts` constructing a `QuoteError` for EACH code and asserting `err instanceof Error`, `err.code === <code>`, and `err.name === 'QuoteError'`. This guarantees each code and the constructor branch are covered before the engine (plan 03) throws them.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/quoting test errors && grep -q 'SALDO_NO_POSITIVO' packages/quoting/src/errors.ts && grep -q 'class QuoteError' packages/quoting/src/errors.ts && ! grep -Eiq 'sentry|pino|console\.' packages/quoting/src/errors.ts && echo ERRORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/quoting/src/errors.ts` exports `class QuoteError extends Error` with a `readonly code: QuoteErrorCode` and a `QuoteErrorCode` union containing at least `SALDO_NO_POSITIVO`, `REFUERZO_FUERA_DE_PLAZO`, `REFUERZO_DUPLICADO`, `CAC_REQUERIDO`, `PLAN_REQUERIDO`, `ANTICIPO_PCT_FUERA_DE_RANGO`, `CUOTAS_INVALIDAS`.
    - `errors.ts` has no logging/observability imports (`grep -Eiq 'sentry|pino|console\.'` finds nothing).
    - `packages/quoting/src/errors.test.ts` constructs a `QuoteError` for every code and asserts `code`, `name`, and `instanceof Error`.
    - `pnpm --filter @imbau/quoting test errors` passes.
  </acceptance_criteria>
  <done>A typed, exhaustive `QuoteError` idiom exists for explicit domain rejection (D-07), with every code unit-tested and no observability coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the deterministic es-AR formatter (owns the currency label, defeats ICU drift)</name>
  <files>packages/quoting/src/format.ts, packages/quoting/src/format.test.ts</files>
  <read_first>
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-RESEARCH.md — "Pattern 4: es-AR formatter owns symbol + grouping deterministically" + "Pitfall 5" (Intl currency style U+202F / ICU drift; $ on a USD value reads as pesos)
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-PATTERNS.md — "compare.ts / format.ts / serialize.ts" (format.ts: US$ / $ literal labels + Intl decimal grouping, never style:'currency')
    - .planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-CONTEXT.md — D-12 (shared es-AR formatter, same output server and client), UI-06 wording (miles con punto, decimales con coma; US$ vs $)
    - packages/quoting/src/money.ts — decimal2 returns the 2-decimal ARS string this formatter renders
  </read_first>
  <behavior>
    - `formatUsd(1234)` returns `"US$ 1.234"` (thousands grouped with a dot, whole USD, `US$ ` literal label with a normal ASCII space).
    - `formatUsd(1000000)` returns `"US$ 1.000.000"`.
    - `formatUsd(0)` returns `"US$ 0"`.
    - `formatArs("1234560.00")` returns `"$ 1.234.560,00"` (thousands with dot, decimals with comma, `$ ` literal label, exactly 2 decimals preserved from the input string).
    - `formatArs("0.50")` returns `"$ 0,50"`.
    - No output contains the narrow no-break space U+202F (\u202f) — only normal ASCII spaces.
    - The USD label is `US$ ` and the ARS label is `$ ` — a USD amount is never rendered with a bare `$` (Pitfall 5, 1000× misread).
  </behavior>
  <action>
    Create `packages/quoting/src/format.ts` following RESEARCH Pattern 4. Export `formatUsd(amount: number): string` and `formatArs(amount: string): string`. Own the currency label as a literal with a NORMAL ASCII space: `"US$ "` for USD, `"$ "` for ARS. Group the DIGITS with `Intl.NumberFormat('es-AR', { style: 'decimal', ... })` (or a hand grouper) — NEVER `{ style: 'currency' }` (its U+202F narrow no-break space + symbol vary across ICU versions, making browser fase-6 output differ from Node/worker fase-7 output by an invisible character, D-12/Pitfall 5). `formatArs` takes the 2-decimal ARS STRING (as produced by `decimal2`) and must preserve exactly 2 decimals with a comma decimal separator and dot thousands separator (`minimumFractionDigits: 2, maximumFractionDigits: 2`). `formatUsd` renders whole USD (no decimals). English identifiers/comments; the visible label text (`US$`, `$`) is the only es-AR-facing string here.

    Create `packages/quoting/src/format.test.ts` asserting every behavior-block case with exact string equality, including an assertion that the output contains no `\u202f` character (guards the ICU-drift pitfall) and that USD uses the `US$ ` label. These exact-string assertions are the deterministic contract fase 6 and fase 7 both rely on.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/quoting test format && ! grep -q "style: 'currency'" packages/quoting/src/format.ts && ! grep -q 'style: "currency"' packages/quoting/src/format.ts && grep -q "US\$" packages/quoting/src/format.ts && echo FORMAT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/quoting test format` passes with all behavior-block cases.
    - `formatUsd(1234) === "US$ 1.234"` and `formatArs("1234560.00") === "$ 1.234.560,00"`.
    - `packages/quoting/src/format.ts` does NOT use `style: 'currency'` (grep finds neither quote style); it uses a hand-owned `US$ `/`$ ` label.
    - A test asserts no output contains `\u202f` (the narrow no-break space).
    - USD amounts render with the `US$ ` label, ARS with `$ ` (never a bare `$` on a USD value).
  </acceptance_criteria>
  <done>A shared, deterministic es-AR formatter renders `US$ 1.234` / `$ 1.234.560,00` with a hand-owned label and no U+202F, so the same string is produced in both later runtimes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → engine (QuoteInput) | The only input surface; degenerate/out-of-domain inputs must be rejected here (typed), never normalized silently. |
| engine output → surfaces (string rendering) | The formatter's exact output is the contract UI/PDF/WhatsApp all render; an invisible-char divergence would break "las tres superficies nunca difieren". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Repudiation | `types.ts` + `errors.ts` (degenerate plan yields a plausible-but-wrong quote) | high | mitigate | Exhaustive `QuoteError` code union (D-07); the input contract makes each degenerate combination detectable; every code unit-tested. Enforcement in `engine.ts` lands plan 03. |
| T-04-04 | Information Disclosure | `format.ts` (es-AR ICU / U+202F cross-runtime drift; `$` on USD misread 1000×) | medium | mitigate | Hand-owned `US$ `/`$ ` literal labels + Intl decimal grouping (never currency style); test asserts no `\u202f` and correct label per currency. |
</threat_model>

<verification>
- `pnpm --filter @imbau/quoting test` green (types compile, errors + format suites pass).
- `pnpm --filter @imbau/quoting typecheck` and `lint` pass.
- `QuoteResult` is a modalidad-discriminated union; `types.ts` imports no `@imbau/db`.
- Every `QuoteErrorCode` is constructed and asserted; no observability imports in `errors.ts`.
- `format.ts` never uses `style:'currency'`; output has no U+202F and correct `US$ `/`$ ` labels.
</verification>

<success_criteria>
- ENGINE-03 (shape half): the single `QuoteResult` discriminated union + `QuoteInput` contract exist and are the only output shape; serializers in plan 04 derive from it.
- D-07: a typed, exhaustive `QuoteError` idiom exists for explicit domain rejection.
- D-12: a deterministic es-AR formatter exists, ready for both later runtimes.
</success_criteria>

<output>
Create `.planning/phases/04-motor-de-cotizaci-n-puro-packages-quoting/04-02-SUMMARY.md` when done.
</output>
