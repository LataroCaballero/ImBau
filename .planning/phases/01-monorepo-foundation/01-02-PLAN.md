---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/config/package.json
  - packages/config/tsconfig/base.json
  - packages/config/tsconfig/next.json
  - packages/config/tsconfig/node.json
  - packages/config/eslint.js
  - packages/config/prettier.js
  - packages/config/env/presets.ts
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/src/index.ts
  - packages/api/package.json
  - packages/api/tsconfig.json
  - packages/api/src/index.ts
  - packages/ui/package.json
  - packages/ui/tsconfig.json
  - packages/ui/src/index.tsx
  - packages/quoting/package.json
  - packages/quoting/tsconfig.json
  - packages/quoting/src/index.ts
  - packages/quoting/src/index.test.ts
  - eslint.config.js
  - prettier.config.js
  - vitest.config.ts
autonomous: true
requirements: [MONO-01, MONO-02]
must_haves:
  truths:
    - "packages/config es la única fuente de verdad de tsconfig presets, ESLint 9 flat config, Prettier config y env presets"
    - "Los packages db/api/ui/quoting extienden el tsconfig de config y dependen de @imbau/config vía workspace:*"
    - "Los packages son JIT: sus exports apuntan a src/*.ts crudo, sin dist/ ni paso de build"
    - "packages/quoting exporta una función pura con un test Vitest que pasa"
    - "packages/ui exporta un componente React trivial consumible por las apps"
    - "pnpm lint y pnpm typecheck corren desde packages/config y pasan en los packages"
  artifacts:
    - path: "packages/config/eslint.js"
      provides: "ESLint 9 flat config array compartida (typescript-eslint + eslint-config-prettier last)"
      exports: ["config"]
    - path: "packages/config/tsconfig/base.json"
      provides: "Preset TS 5.9 estricto (strict, noUncheckedIndexedAccess)"
      contains: "\"strict\": true"
    - path: "packages/config/env/presets.ts"
      provides: "Presets parciales de env (baseEnv, dbEnv, redisEnv) consumidos por las apps"
      exports: ["baseEnv", "dbEnv", "redisEnv"]
    - path: "packages/quoting/src/index.ts"
      provides: "Función pura placeholder (roundUsd) sin I/O"
      exports: ["roundUsd"]
    - path: "packages/ui/src/index.tsx"
      provides: "Componente React trivial usado por web+panel"
    - path: "packages/db/src/index.ts"
      provides: "Placeholder tipado (sin Drizzle — fase 2)"
    - path: "packages/api/src/index.ts"
      provides: "Placeholder tipado (sin tRPC — fase 3)"
  key_links:
    - from: "packages/db/package.json, packages/api/package.json, packages/ui/package.json, packages/quoting/package.json"
      to: "@imbau/config"
      via: "devDependency workspace:*"
      pattern: "@imbau/config.*workspace:\\*"
    - from: "eslint.config.js (root)"
      to: "@imbau/config/eslint"
      via: "import del array compartido"
      pattern: "@imbau/config/eslint"
    - from: "packages/quoting/src/index.test.ts"
      to: "roundUsd"
      via: "import + assertion Vitest"
      pattern: "roundUsd"
---

<objective>
Crear `packages/config` —la raíz del DAG de dependencias que todo el monorepo importa— con presets de tsconfig (TS 5.9 estricto), ESLint 9 flat config, Prettier config y los presets parciales de env. Luego crear los cuatro packages leaf (`db`, `api`, `ui`, `quoting`) como esqueletos JIT genuinos que prueban la cadena de deps descendente (MONO-01) y que `pnpm lint` + `pnpm typecheck` corren desde la config compartida (MONO-02).

Purpose: MONO-02 exige que tooling (TS estricto + ESLint 9 flat) corra desde `packages/config` en todo el workspace; `config` debe existir antes que cualquier consumidor. Los packages leaf establecen el patrón JIT (exports → `src/*.ts` crudo, sin `dist/`) que las fases 2-3 heredan, y cada uno exporta algo pequeño pero genuino (D-12) que valida las aristas del grafo de deps sin código especulativo.
Output: `packages/config` con presets + flat config + env presets; `db`/`api`/`ui`/`quoting` esqueleto; `eslint.config.js`/`prettier.config.js`/`vitest.config.ts` root cableados; `pnpm lint`, `pnpm typecheck` y `pnpm test` (quoting) verdes en los packages.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (newly introduced — not drift):
- Package `@imbau/config` — exports: `./eslint` (`config` array), `./prettier` (config object), `./tsconfig/base.json`, `./tsconfig/next.json`, `./tsconfig/node.json`, `./env/presets` (`baseEnv`, `dbEnv`, `redisEnv`)
- Package `@imbau/db` — placeholder typed export from `src/index.ts`
- Package `@imbau/api` — placeholder typed export from `src/index.ts`
- Package `@imbau/ui` — exports trivial React component from `src/index.tsx`
- Package `@imbau/quoting` — exports `roundUsd(amount: number): number` from `src/index.ts`; test `src/index.test.ts`
- Root files: `eslint.config.js`, `prettier.config.js`, `vitest.config.ts`
- Function `roundUsd` (pure, integer-friendly money signature)
- Exports `baseEnv`, `dbEnv`, `redisEnv` (Zod preset objects)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: packages/config — presets de tsconfig, ESLint 9 flat config, Prettier y env presets (MONO-02)</name>
  <files>packages/config/package.json, packages/config/tsconfig/base.json, packages/config/tsconfig/next.json, packages/config/tsconfig/node.json, packages/config/eslint.js, packages/config/prettier.js, packages/config/env/presets.ts, eslint.config.js, prettier.config.js</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md ("Pattern 2: Shared config package" con base.json estricto; "Pattern 3: ESLint 9 flat config" con `tseslint.config(...)` + `projectService` + `prettier` last; "Code Examples → Shared env presets" con baseEnv/dbEnv/redisEnv; Pitfall 2: `projectService: true` para type-aware lint en monorepo; "Standard Stack": typescript-eslint 8.x, prettier 3.8.4, eslint-config-prettier 10.1.8)
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-11: Prettier compartido en config + integrado con ESLint 9 vía eslint-config-prettier; Claude's Discretion: detalles de ESLint flat config, tsconfig presets, naming `@imbau/*`)
    - CLAUDE.md (sección "Technology Stack": ESLint 9 flat config, TS 5.9 `strict: true` + `noUncheckedIndexedAccess: true`; "What NOT to Use": no flotar a TS 6 / ESLint 10)
  </read_first>
  <action>
    Crear `packages/config` como package `@imbau/config` (`"private": true`, `"type": "module"`) — la raíz del DAG (MONO-02, D-11):
    - `exports` map apuntando a src crudo (JIT, D-05): `"./eslint"` → `./eslint.js`, `"./prettier"` → `./prettier.js`, `"./tsconfig/base.json"`, `"./tsconfig/next.json"`, `"./tsconfig/node.json"`, `"./env/presets"` → `./env/presets.ts`.
    - devDependencies con versiones pinneadas verificadas al planear: `typescript@5.9`, `eslint@9`, `typescript-eslint@8.61.0`, `eslint-config-prettier@10.1.8`, `prettier@3.8.4`. Agregar `zod@4` como dependency (para los env presets).
    - `tsconfig/base.json`: TS 5.9 estricto — `strict: true`, `noUncheckedIndexedAccess: true`, `noImplicitOverride: true`, `module: ESNext`, `moduleResolution: Bundler`, `target: ES2022`, `esModuleInterop: true`, `skipLibCheck: true`, `forceConsistentCasingInFileNames: true`, `verbatimModuleSyntax: true`.
    - `tsconfig/next.json`: extiende base.json, ajustes para Next/JSX (`jsx: preserve`, `lib: [DOM, DOM.Iterable, ES2022]`, `noEmit: true`).
    - `tsconfig/node.json`: extiende base.json para el worker (`lib: [ES2022]`, `types: [node]`).
    - `eslint.js`: exporta `config` construido con `tseslint.config(...tseslint.configs.recommendedTypeChecked, { languageOptions: { parserOptions: { projectService: true } } }, prettier)` con `prettier` (eslint-config-prettier) SIEMPRE último (Pattern 3, Pitfall 2). `projectService: true` es el reemplazo monorepo-friendly de los `project` arrays.
    - `prettier.js`: exporta un objeto de config Prettier compartido (default sensato: `singleQuote: false`, `semi: true`, `trailingComma: all` — Discretion).
    - `env/presets.ts`: exporta `baseEnv` (`server: { NODE_ENV: z.enum([...]).default("development") }`), `dbEnv` (`server: { DATABASE_URL: z.string().url() }`), `redisEnv` (`server: { REDIS_URL: z.string().url() }`), cada uno `as const`. NO incluir presets especulativos de fases 2-3.
    Crear también en la raíz: `eslint.config.js` que importa `config` desde `@imbau/config/eslint` y lo re-exporta default; `prettier.config.js` que re-exporta la config desde `@imbau/config/prettier`. NO escribir helper de env propio (D-01 — eso es t3-env en el plan 03).
  </action>
  <acceptance_criteria>
    - `packages/config/tsconfig/base.json` contiene `"strict": true` y `"noUncheckedIndexedAccess": true`
    - `packages/config/eslint.js` contiene `tseslint.config(` y referencia `eslint-config-prettier` (importado como `prettier`) como último elemento del array
    - `packages/config/eslint.js` contiene `projectService`
    - `packages/config/env/presets.ts` exporta `baseEnv`, `dbEnv` y `redisEnv` (al menos 3 `export const` en líneas no comentadas)
    - `packages/config/package.json` tiene un `exports` con `./eslint`, `./prettier`, `./env/presets` y los tres tsconfig
    - `eslint.config.js` (root) importa desde `@imbau/config/eslint`
    - `pnpm install` resuelve `@imbau/config` como package del workspace
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"strict": true' packages/config/tsconfig/base.json && grep -q 'tseslint.config(' packages/config/eslint.js && grep -q 'projectService' packages/config/eslint.js && test "$(grep -v '^\s*//' packages/config/env/presets.ts | grep -c 'export const')" -ge 3 && grep -q '@imbau/config/eslint' eslint.config.js && echo OK</automated>
  </verify>
  <done>`packages/config` existe como `@imbau/config` con tsconfig estricto, ESLint 9 flat config (prettier last, projectService), Prettier config y los tres env presets; la raíz cablea eslint/prettier desde config y `pnpm install` resuelve el package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Packages leaf db/api/ui/quoting (JIT, deps descendentes) + Vitest root (MONO-01)</name>
  <files>packages/db/package.json, packages/db/tsconfig.json, packages/db/src/index.ts, packages/api/package.json, packages/api/tsconfig.json, packages/api/src/index.ts, packages/ui/package.json, packages/ui/tsconfig.json, packages/ui/src/index.tsx, packages/quoting/package.json, packages/quoting/tsconfig.json, packages/quoting/src/index.ts, packages/quoting/src/index.test.ts, vitest.config.ts</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md ("JIT package exports (D-05)" con `exports` → raw src; "Pure placeholder + test in packages/quoting" con `roundUsd` + test; "Recommended Project Structure": db/api typed placeholder, ui componente trivial, quoting función pura + test; Architectural Responsibility Map: db/api typed placeholder sin Drizzle/tRPC)
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-05: JIT, exports → src/*.ts crudo sin dist/; D-10: Vitest cableado con tests reales del esqueleto, `pnpm test` verde desde día uno; D-12: cada package exporta algo pequeño pero genuino — ui componente, quoting función pura, db/api solo tipos/placeholder; nada especulativo de fases 2-3)
    - packages/config/tsconfig/base.json (se acaba de crear en Task 1 — los packages lo extienden)
    - CLAUDE.md ("Money: integers/decimal, never floats" — la firma de quoting debe ser integer-friendly; idioma: código/identificadores en inglés)
  </read_first>
  <behavior>
    - Test 1 (quoting): `roundUsd(1234.6)` retorna `1235` (redondeo a USD entero, función pura sin I/O)
    - Test 2 (quoting): `roundUsd(1234.4)` retorna `1234` (redondea hacia abajo correctamente)
  </behavior>
  <action>
    Crear los cuatro packages leaf, todos `"type": "module"`, `"private": true`, con naming `@imbau/<name>`, exports JIT apuntando a `src/*` crudo (D-05 — NUNCA `dist/`), un `tsconfig.json` que `extends "@imbau/config/tsconfig/base.json"` (ui usa next.json por el JSX), y `@imbau/config` como devDependency `workspace:*`:
    - `packages/quoting`: `src/index.ts` exporta `roundUsd(amount: number): number` (pura, `Math.round`, integer-friendly per CLAUDE.md money rule). `src/index.test.ts` con las assertions del bloque `<behavior>` usando Vitest. Sin I/O, sin lógica especulativa de fase 3 (la cobertura 100% + property-based llega con QUOT-01).
    - `packages/ui`: `src/index.tsx` exporta un componente React trivial (ej: `AppStatus` que recibe props simples y renderiza texto) consumible por web+panel (D-12). Agregar `react@19.2` + `react-dom@19.2` como peerDependencies y `@types/react` como devDependency. `tsconfig.json` extiende `@imbau/config/tsconfig/next.json`.
    - `packages/db`: `src/index.ts` exporta solo un placeholder tipado (ej: un `type` o una constante tipada que marca el package), SIN Drizzle ni schema (fase 2).
    - `packages/api`: `src/index.ts` exporta solo un placeholder tipado, SIN tRPC (fase 3).
    Crear `vitest.config.ts` en la raíz (D-10) que descubra los tests de los packages del workspace y use `@vitest/coverage-v8@4.1.8` como provider de cobertura (gate de cobertura cableado, no exigir umbral todavía — eso es fase 3). Agregar a cada package un script `lint` (`eslint .`), `typecheck` (`tsc --noEmit`, D-07) y donde aplique `test` (`vitest run`), para que Turbo los orqueste. `pnpm test` debe pasar verde con el test de quoting (D-10).
  </action>
  <acceptance_criteria>
    - `packages/quoting/src/index.ts` contiene `export function roundUsd`
    - `packages/quoting/src/index.test.ts` importa `roundUsd` y assertea `1235` para `1234.6`
    - Los cuatro `packages/*/package.json` (db, api, ui, quoting) contienen `@imbau/config` con `workspace:*` (al menos 4 archivos matchean)
    - Ningún `packages/*/package.json` define un script `build` que produzca `dist/` (JIT — exports apuntan a `src`)
    - `packages/ui/src/index.tsx` exporta un componente React (contiene `export` y JSX)
    - `pnpm test` pasa con el test de quoting verde
    - `pnpm typecheck` sale 0 en los packages (extienden el base estricto)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'export function roundUsd' packages/quoting/src/index.ts && grep -q 'roundUsd' packages/quoting/src/index.test.ts && test "$(grep -l 'workspace:\*' packages/db/package.json packages/api/package.json packages/ui/package.json packages/quoting/package.json | wc -l | tr -d ' ')" -ge 4 && pnpm test >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>Los cuatro packages leaf existen como esqueletos JIT con deps descendentes hacia `@imbau/config`, quoting exporta una función pura con test Vitest verde, ui exporta un componente React, db/api son placeholders tipados, y `pnpm test` pasa desde el día uno.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package consumer → @imbau/config | Toda la config de tooling fluye desde un único package; un error acá se propaga al workspace entero |
| código de package → bundle de app | Los packages son JIT (src crudo); lo que exporten lo transpila el consumidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | typescript-eslint en flat config type-checked | mitigate | `projectService: true` (no `project` arrays) evita el no-op silencioso de reglas type-aware (Pitfall 2); versión verificada 8.61.0 al planear (soporta ESLint 9 + TS 5.9). |
| T-02-02 | Information Disclosure | env presets en packages/config | accept | Los presets solo declaran NOMBRES y schemas Zod de variables (DATABASE_URL, REDIS_URL, NODE_ENV), nunca valores. Ningún secret cruza al repo. Riesgo bajo. |
| T-02-03 | Denial of Service | `tsconfig` estricto roto | mitigate | `tsc --noEmit` por package como gate de typecheck (D-07) detecta config rota antes de que se propague a las apps del plan 03. |
</threat_model>

<verification>
- `pnpm lint` corre con la flat config compartida desde `@imbau/config/eslint`
- `pnpm typecheck` corre `tsc --noEmit` por package extendiendo el base estricto
- `pnpm test` pasa con el test de quoting (D-10)
- Los exports de los packages apuntan a `src` crudo (JIT, sin `dist/`)
- `@imbau/config` es devDependency `workspace:*` en los cuatro packages leaf
</verification>

<success_criteria>
- MONO-02: TS 5.9 estricto + ESLint 9 flat config corren desde `packages/config` y pasan en los packages
- MONO-01 (parcial): cadena de deps descendente packages → config establecida y compilando
- Cada package leaf exporta algo genuino (D-12) sin código especulativo de fases 2-3
- `pnpm test` verde desde el día uno (prerequisito de CI fase 4)
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md` when done
</output>
