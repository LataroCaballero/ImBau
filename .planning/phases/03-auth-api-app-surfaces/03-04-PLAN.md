---
phase: 03-auth-api-app-surfaces
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/package.json
  - apps/web/env.ts
  - apps/web/next.config.ts
  - apps/web/app/page.tsx
  - apps/worker/package.json
  - apps/worker/src/index.ts
  - apps/worker/src/index.test.ts
autonomous: true
requirements: [APP-02, APP-03]

must_haves:
  truths:
    - "apps/web renders a list of published projects read via the anon role (withAnon) and shows ONLY estado='publicado' rows"
    - "apps/web has NO auth handler and NO tRPC client — it is anon-only (D-03)"
    - "apps/worker boots as a BullMQ Worker connected to Redis with NO job logic (D-16/APP-03)"
    - "the worker's ioredis connection uses maxRetriesPerRequest: null (required by BullMQ)"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "RSC published-projects list via tRPC server caller → withAnon"
      contains: "listPublished"
    - path: "apps/worker/src/index.ts"
      provides: "BullMQ Queue + Worker on 'health' wired to REDIS_URL, no job logic"
      contains: "maxRetriesPerRequest"
    - path: "apps/worker/src/index.test.ts"
      provides: "smoke test: worker connects to Compose Redis and boots"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "@imbau/api createCaller"
      via: "createCaller({ headers }).projects.listPublished()"
      pattern: "createCaller|listPublished"
    - from: "apps/worker/src/index.ts"
      to: "Redis (REDIS_URL)"
      via: "new IORedis(env.REDIS_URL, { maxRetriesPerRequest: null })"
      pattern: "IORedis|REDIS_URL"
---

<objective>
Deliver the two app surfaces that prove the anonymous read path and the deployable background-worker shell: `apps/web` renders a published-projects list via the anon role (APP-02), and `apps/worker` becomes a BullMQ Worker connected to Redis with no job logic (APP-03).

`apps/web` reads through the tRPC server caller → `projects.listPublished` → `withAnon` → RLS returns only `estado='publicado'` rows. Web has NO auth handler and NO tRPC client (D-03). The worker keeps its env-first boot and adds a BullMQ Queue/Worker on `health` with `maxRetriesPerRequest: null` on the ioredis connection (required by BullMQ); no real jobs (D-16).

Purpose: APP-02 (anon published-only read) and APP-03 (deployable worker shell) are the last two non-Docker requirements; both are independent of the panel and run in parallel with it.
Output: the web published-projects page + the worker BullMQ shell + a Redis-connect smoke test.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-auth-api-app-surfaces/03-CONTEXT.md
@.planning/phases/03-auth-api-app-surfaces/03-RESEARCH.md
@.planning/phases/03-auth-api-app-surfaces/03-PATTERNS.md
@.planning/phases/03-auth-api-app-surfaces/03-02-SUMMARY.md
@apps/web/app/page.tsx
@apps/web/env.ts
@apps/web/next.config.ts
@apps/web/package.json
@apps/worker/src/index.ts
@apps/worker/src/env.ts
@apps/worker/src/env.test.ts
@apps/worker/package.json
@apps/worker/tsup.config.ts
@packages/db/src/with-tenant.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: apps/web published-projects list via anon read</name>
  <read_first>
    - apps/web/app/page.tsx (modify target: keep the RSC + es-AR + validated-env shape; swap the status body for a published list)
    - apps/web/env.ts (modify: add DATABASE_ANON_URL to the server block so the server caller's withAnon path validates at boot)
    - apps/web/next.config.ts (modify: add "@imbau/api" to transpilePackages)
    - apps/web/package.json (add @imbau/api workspace dep)
    - packages/api/src/index.ts (createCaller — direct server caller for a pure RSC read, D-14)
    - packages/db/src/with-tenant.ts (withAnon — anon policy filters publicado globally)
    - 03-RESEARCH.md Pattern 3 (RSC server caller: createCaller({headers}).projects.listPublished()), D-03 (NO auth handler in web), D-14 (name/slug/estado list)
    - 03-PATTERNS.md (apps/web page analog = itself; anon read, no tRPC client)
  </read_first>
  <action>
    Add `@imbau/api` (workspace:*) to `apps/web/package.json`. Add `"@imbau/api"` to `apps/web/next.config.ts` `transpilePackages`. Modify `apps/web/env.ts` to add `DATABASE_ANON_URL: dbEnv.server.DATABASE_ANON_URL` to the server block (the anon read path needs it validated at boot); keep the client NEXT_PUBLIC_APP_ENV split (do NOT add server secrets to the client).
    Modify `apps/web/app/page.tsx` to a published-projects RSC: `const caller = await createCaller({ headers: await headers() }); const projects = await caller.projects.listPublished();` then render nombre/slug/estado in a flat es-AR list (D-13/D-14). Do NOT add any auth handler, auth client, or tRPC client to web (anon-only — D-03). Keep `React.JSX.Element` return type, validated `env` import.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/web typecheck && pnpm --filter @imbau/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/page.tsx` reads via `createCaller(...).projects.listPublished()` (grep confirms `listPublished`) and renders nombre/slug/estado.
    - `apps/web/next.config.ts` `transpilePackages` includes `@imbau/api`; `apps/web/env.ts` validates `DATABASE_ANON_URL`.
    - No `toNextJsHandler` / auth route / tRPC client exists anywhere under `apps/web/` (grep returns nothing).
    - `pnpm --filter @imbau/web build` succeeds (Next standalone output).
  </acceptance_criteria>
  <done>apps/web lists published projects via the anon read path with no auth surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apps/worker BullMQ shell + Redis-connect smoke test</name>
  <read_first>
    - apps/worker/src/index.ts (modify: keep the env-FIRST import + structured JSON boot log; add BullMQ after)
    - apps/worker/src/env.ts (REDIS_URL already validated via redisEnv)
    - apps/worker/src/env.test.ts (analog: Vitest describe/it/expect; composes presets directly)
    - apps/worker/package.json (add bullmq + ioredis deps)
    - apps/worker/tsup.config.ts (noExternal bundles @imbau/* — the worker stays bundleable for Docker)
    - 03-RESEARCH.md Pattern 6 (new IORedis(env.REDIS_URL, { maxRetriesPerRequest: null }); Queue/Worker on 'health'; no job logic; worker.on('ready')), Pitfall (maxRetriesPerRequest null REQUIRED)
  </read_first>
  <behavior>
    - The worker constructs an ioredis connection with `maxRetriesPerRequest: null` and a BullMQ Worker on the `health` queue.
    - The worker boots and the Worker reaches a connected/ready state against the Compose Redis (host :6380 → 6379).
    - No real job processing logic exists (shell only — D-16/APP-03).
  </behavior>
  <action>
    Add `bullmq` and `ioredis` deps to `apps/worker/package.json`. Modify `apps/worker/src/index.ts` per RESEARCH Pattern 6, preserving the env-first import and the boot JSON log: construct `const connection = new IORedis(env.REDIS_URL, { maxRetriesPerRequest: null })` (the `null` is REQUIRED by BullMQ), a `Queue("health", { connection })`, and a `Worker("health", async () => "ok", { connection })` with a `worker.on("ready", ...)` structured log. NO job logic beyond the no-op handler (D-16/APP-03). Keep it tsup-bundleable.
    Create `apps/worker/src/index.test.ts` (analog: `env.test.ts`): a smoke test asserting the worker connects to the Compose Redis and reaches ready (needs Compose Redis up). If isolating the connection is cleaner, factor a small `createWorker()`/`createConnection()` export from index.ts and test that — keep the shell behavior unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/worker typecheck && pnpm --filter @imbau/worker test -t "worker connects"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/src/index.ts` constructs `new IORedis(env.REDIS_URL, { maxRetriesPerRequest: null })` (grep confirms `maxRetriesPerRequest`) and a BullMQ `Queue`/`Worker` on `health`; no real job logic.
    - The env-first import and boot JSON log are preserved.
    - `apps/worker/src/index.test.ts` proves the worker connects to Compose Redis and boots.
    - `pnpm --filter @imbau/worker build` (tsup) still produces `dist/`.
    - typecheck + smoke test pass (Compose Redis up).
  </acceptance_criteria>
  <done>The worker is a deployable BullMQ shell connected to Redis with no job logic, and a smoke test proves the connection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → web | Untrusted public visitors read here with no session. |
| worker → Redis | The worker connects to Redis (no auth secrets, internal network). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Information Disclosure | web leaks non-published (borrador/archivado) projects | mitigate | Web reads ONLY via `withAnon` → the anon RLS policy returns only `estado='publicado'`; the Plan 03-02 anon test asserts borrador absence. No tenant data path in web. |
| T-03-17 | Elevation of Privilege | web gaining an authenticated/tenant data path | mitigate | Web has no auth handler, no tRPC client, no `withTenant` import (anon-only, D-03); acceptance criteria grep-assert their absence. |
| T-03-18 | Denial of Service | worker connection retry storm | accept | Shell only this phase; `maxRetriesPerRequest: null` is BullMQ's required setting. Backoff/queue hardening lands with real jobs (future milestone). |
| T-03-19 | Information Disclosure | server-only env leaking into the web client bundle | mitigate | `DATABASE_ANON_URL` stays in the `server` block of the t3-env split; only `NEXT_PUBLIC_*` reaches the client. |
</threat_model>

<verification>
- `pnpm --filter @imbau/web typecheck && build` and `pnpm --filter @imbau/worker typecheck && test` pass.
- grep: `listPublished` present in `apps/web/app/page.tsx`; no `toNextJsHandler`/`withTenant`/tRPC client under `apps/web/`.
- grep: `maxRetriesPerRequest` present in `apps/worker/src/index.ts`.
</verification>

<success_criteria>
- apps/web renders published projects via the anon path and shows only publicado rows (APP-02).
- apps/web has no auth surface.
- apps/worker boots as a BullMQ shell connected to Redis with no job logic (APP-03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

**Files created:**
- `apps/worker/src/index.test.ts`

**Files modified:**
- `apps/web/package.json`, `apps/web/env.ts`, `apps/web/next.config.ts`, `apps/web/app/page.tsx`
- `apps/worker/package.json`, `apps/worker/src/index.ts`

**Symbols/exports:** published-projects RSC (web `page.tsx`); BullMQ `healthQueue`/worker shell (worker `index.ts`)
</artifacts_produced>

<output>
Create `.planning/phases/03-auth-api-app-surfaces/03-04-SUMMARY.md` when done.
</output>
