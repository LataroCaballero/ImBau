---
phase: 04-staging-observability-ci-cd
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - secrets/.sops.yaml
  - secrets/staging.env.example
  - secrets/staging.enc.yaml
  - .gitignore
autonomous: false
requirements: [INFRA-03]
user_setup:
  - service: sops-age
    why: "SOPS+age encrypts the staging runtime secrets in the repo; the age keypair and real secret values can only be produced by the user (INFRA-03)"
    env_vars:
      - name: SOPS_AGE_KEY
        source: "Generated by `age-keygen`; private key. Add as a GitHub Actions secret AND place at ~/.config/sops/age/keys.txt on the VPS"
    dashboard_config:
      - task: "Generate age keypair (`age-keygen -o keys.txt`), put the PUBLIC key as the recipient in secrets/.sops.yaml, then `sops -e` the real values into secrets/staging.enc.yaml"
        location: "Local machine + GitHub → Settings → Secrets and variables → Actions (SOPS_AGE_KEY); VPS ~/.config/sops/age/keys.txt"
must_haves:
  truths:
    - "Staging runtime secrets live encrypted in the repo as secrets/staging.enc.yaml (SOPS+age); nothing sensitive appears in plaintext (INFRA-03)"
    - "`sops -d --output-type dotenv secrets/staging.enc.yaml` round-trips to a runtime .env that is gitignored and never committed"
    - "The full set of staging secret KEYS is documented (names only, no values) so the deploy is reproducible"
    - "secrets/*.enc.yaml is committed (NOT gitignored) while .env / decrypted output is gitignored"
  artifacts:
    - secrets/.sops.yaml
    - secrets/staging.env.example
    - secrets/staging.enc.yaml
  key_links:
    - ".sops.yaml creation_rules (path_regex staging.enc.yaml) → age recipient (public key)"
    - "SOPS_AGE_KEY (Actions secret + VPS ~/.config/sops/age/keys.txt) → sops -d → runtime .env (deploy.sh, plan 04-06)"
    - "staging.env.example (key contract) → the values encrypted in staging.enc.yaml and consumed by compose env_file"
---

<objective>
Establish SOPS+age secret management (INFRA-03): a `.sops.yaml` creation-rule, a documented key contract (`staging.env.example`, names only), the encrypted `secrets/staging.enc.yaml`, and `.gitignore` discipline so nothing sensitive is ever committed in plaintext while the encrypted file IS committed.

Purpose: INFRA-03 literal — secrets encrypted in repo, env-separated, nothing sensitive in plaintext. The deploy (04-06) decrypts `staging.enc.yaml` → runtime `.env` consumed by the staging compose.
Output: `secrets/.sops.yaml`, `secrets/staging.env.example`, `secrets/staging.enc.yaml` (encrypted), verified `.gitignore`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-staging-observability-ci-cd/04-RESEARCH.md
@.planning/phases/04-staging-observability-ci-cd/04-CONTEXT.md
@.gitignore
@packages/config/env/presets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .sops.yaml + staging.env.example key contract + .gitignore discipline</name>
  <files>secrets/.sops.yaml, secrets/staging.env.example, .gitignore</files>
  <read_first>
    - .gitignore (lines 17-20 — `.env`/`.env.*` already ignored, `!.env.example` allowed; confirm decrypted runtime `.env` is covered and add an allow-rule so `secrets/*.enc.yaml` stays committed)
    - packages/config/env/presets.ts (the authoritative variable NAMES the apps validate: dbEnv DATABASE_URL/APP_URL/ANON_URL, redisEnv REDIS_URL, authEnv BETTER_AUTH_SECRET/URL/RESEND_API_KEY/INVITE_FROM, + new sentryEnv/lokiEnv from 04-01)
    - 04-RESEARCH.md §D-09 / Runtime State Inventory (line 698 — the full secret list) and §Recommended Repo Structure (lines 263-265 — secrets/ layout)
    - 04-PATTERNS.md §"secrets/.sops.yaml ..." (lines 250-252 — creation_rules → age recipient; verify enc.yaml NOT gitignored)
  </read_first>
  <action>Create `secrets/.sops.yaml` with a `creation_rules` entry whose `path_regex` matches `secrets/.*\.enc\.yaml$` and an `age:` recipient set to a clearly-marked placeholder public key (e.g. `age1REPLACE_WITH_REAL_RECIPIENT`) with a comment instructing the user to replace it with their real age public key in Task 2. Create `secrets/staging.env.example` listing every staging runtime secret KEY with a short comment and NO real value: `POSTGRES_PASSWORD`, `DATABASE_URL` (owner imbau role), `DATABASE_APP_URL` (app_authenticated), `DATABASE_ANON_URL` (anon), `APP_DB_PASSWORD`, `ANON_DB_PASSWORD` (consumed by bootstrap-roles.sql in 04-06), `REDIS_URL` (`redis://redis:6379`), `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` (`https://panel.staging.tours.andescode.com.ar`), `RESEND_API_KEY`, `INVITE_FROM`, `SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN`, `NEXT_PUBLIC_APP_ENV` (`staging`), `LOKI_URL` (`http://loki:3100`), `LOKI_BASIC_AUTH` (optional — only for the Grafana Cloud fallback). Add a header comment naming the build-time/CI-only secrets that do NOT go in this file (`SENTRY_AUTH_TOKEN`, the GHCR PAT, `VPS_SSH_KEY`, `SOPS_AGE_KEY` — those are GitHub Actions / VPS secrets, plan 04-06). In `.gitignore`, confirm `.env`/`.env.*` are ignored and append an explicit allow for the committed encrypted secrets if needed (`!secrets/*.enc.yaml`) and a comment that the decrypted runtime `.env` produced by deploy.sh must never be committed. Do NOT put any real secret value in any committed file.</action>
  <verify>
    <automated>test -f secrets/.sops.yaml && grep -q "creation_rules" secrets/.sops.yaml && grep -q "enc.yaml" secrets/.sops.yaml && test -f secrets/staging.env.example && grep -q "APP_DB_PASSWORD" secrets/staging.env.example && grep -q "DATABASE_APP_URL" secrets/staging.env.example && grep -q "LOKI_URL" secrets/staging.env.example && grep -qE '(^|/)\.env(\.|$|\*)' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `secrets/.sops.yaml` has a `creation_rules` entry with `path_regex` matching `secrets/*.enc.yaml` and an `age` recipient placeholder clearly marked for replacement.
    - `secrets/staging.env.example` lists every staging runtime secret key (incl. `APP_DB_PASSWORD`/`ANON_DB_PASSWORD` for the role bootstrap) with comments and zero real values.
    - `.gitignore` keeps `.env`/`.env.*` ignored and does NOT ignore `secrets/*.enc.yaml`.
    - No committed file contains a real secret value.
  </acceptance_criteria>
  <done>The SOPS rule + secret key contract + gitignore discipline are in place; encryption of real values is the human-action in Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Generate age keypair + encrypt real staging secrets (INFRA-03 human step)</name>
  <action>BLOCKING human-only step: generate the age keypair (`age-keygen`), set the real public key as the recipient in `secrets/.sops.yaml`, encrypt the real staging secret values into `secrets/staging.enc.yaml` with `sops -e` (from the `staging.env.example` key contract), confirm the `sops -d` round-trip, commit the encrypted file (only), and load the age private key as the `SOPS_AGE_KEY` Actions secret AND at `~/.config/sops/age/keys.txt` on the VPS. Real secret values and the age key can only come from the user.</action>
  <what-built>The `.sops.yaml` rule and `staging.env.example` key contract are authored. Encrypting REAL secret values requires the age keypair and the real values — only you can provide these (DB passwords, Sentry DSN, Resend key). This is INFRA-03's load-bearing human step.</what-built>
  <how-to-verify>
    1. Generate the keypair: `age-keygen -o keys.txt` (keep the private key safe).
    2. Put the PUBLIC key (`age1...`) as the recipient in `secrets/.sops.yaml`, replacing the placeholder.
    3. Create `secrets/staging.enc.yaml` from the key contract with REAL values and encrypt it: author a plaintext temp from `secrets/staging.env.example`, then `sops -e --input-type dotenv --output-type yaml <temp> > secrets/staging.enc.yaml` (SOPS picks the recipient from `.sops.yaml`). Delete the plaintext temp.
    4. Confirm round-trip: `SOPS_AGE_KEY_FILE=keys.txt sops -d --output-type dotenv secrets/staging.enc.yaml` prints the dotenv (do NOT commit that output).
    5. Confirm the encrypted file is committed and encrypted: `git status` shows `secrets/staging.enc.yaml` tracked; the file contains SOPS markers (`ENC[` / a `sops:` block) and no readable secret values.
    6. Load the private key as the GitHub Actions secret `SOPS_AGE_KEY`, and place it at `~/.config/sops/age/keys.txt` on the VPS (`root@31.97.175.128`).
  </how-to-verify>
  <acceptance_criteria>
    - `secrets/staging.enc.yaml` exists, is committed, and contains SOPS/age ciphertext (no plaintext secret values).
    - `sops -d` round-trips to the full dotenv with all keys from the contract.
    - `SOPS_AGE_KEY` is set as a GitHub Actions secret and present on the VPS keyfile.
  </acceptance_criteria>
  <resume-signal>Type "approved" once staging.enc.yaml is encrypted+committed and the age key is loaded in Actions + the VPS, or describe the blocker.</resume-signal>
</task>

</tasks>

<artifacts>
- `secrets/.sops.yaml` — creation_rule binding `secrets/*.enc.yaml` to the age recipient
- `secrets/staging.env.example` — staging secret key contract (names only)
- `secrets/staging.enc.yaml` — SOPS+age-encrypted staging runtime secrets (committed)
- `.gitignore` — verified: `.env`/`.env.*` ignored, `secrets/*.enc.yaml` committed
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (public-ish) → secret values | Encrypted secrets are committed; only age-key holders can decrypt |
| Actions/VPS → age private key | The decrypt key transits CI + the VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-SECRET | Information Disclosure | secrets in repo | high | mitigate | SOPS+age field encryption; `.env`/decrypted output gitignored; key contract has names-only; only `*.enc.yaml` committed |
| T-4-CRED | Spoofing/Tampering | age private key (SOPS_AGE_KEY) | high | mitigate | Stored only as a GitHub Actions secret + VPS keyfile (chmod-restricted); rotate the age key and re-encrypt if leaked |
| T-4-SC | Tampering | sops/age binaries | medium | mitigate | Install from official GitHub releases / distro packages; verify checksums; pin versions |
</threat_model>

<verification>
- `secrets/.sops.yaml` + `secrets/staging.env.example` exist with the full key contract; `.gitignore` discipline confirmed.
- After the checkpoint: `sops -d` round-trips; `secrets/staging.enc.yaml` is encrypted + committed; no plaintext secret is tracked by git.
</verification>

<success_criteria>
INFRA-03 is satisfied: staging secrets are SOPS+age-encrypted in the repo, env-separated, with nothing sensitive in plaintext, and the deploy can decrypt them with the age key held only in Actions + the VPS.
</success_criteria>

<output>
Create `.planning/phases/04-staging-observability-ci-cd/04-04-SUMMARY.md` when done.
</output>
