---
phase: 04-staging-observability-ci-cd
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/instrumentation.ts
  - apps/web/instrumentation-client.ts
  - apps/web/sentry.server.config.ts
  - apps/web/sentry.edge.config.ts
  - apps/web/next.config.ts
  - apps/web/env.ts
  - apps/panel/instrumentation.ts
  - apps/panel/instrumentation-client.ts
  - apps/panel/sentry.server.config.ts
  - apps/panel/sentry.edge.config.ts
  - apps/panel/next.config.ts
  - apps/panel/env.ts
  - apps/worker/src/instrument.ts
  - apps/worker/src/index.ts
  - apps/worker/src/env.ts
autonomous: true
requirements: [OBS-01, OBS-02]
must_haves:
  truths:
    - "Server Component / route-handler / middleware errors (incl. RSC) in web and panel are captured by Sentry via the `onRequestError = Sentry.captureRequestError` hook (OBS-01)"
    - "Client-side errors and router transitions in web and panel are captured via instrumentation-client.ts (not the deprecated sentry.client.config.ts)"
    - "The worker initializes @sentry/node before any other module (instrument.ts imported first) and emits structured pino logs instead of console.log (OBS-01/OBS-02)"
    - "All three apps log through the shared @imbau/observability pino logger so logs ship to Loki when LOKI_URL is set"
    - "Apps still fail-fast at boot on missing/invalid env; SENTRY_DSN/LOKI_* are optional so dev runs with zero external deps"
  artifacts:
    - apps/web/instrumentation.ts
    - apps/panel/instrumentation.ts
    - apps/worker/src/instrument.ts
  key_links:
    - "instrumentation.ts register() → sentry.server.config / sentry.edge.config by NEXT_RUNTIME"
    - "onRequestError → Sentry.captureRequestError (RSC error capture, OBS-01)"
    - "worker index.ts → @imbau/observability logger (replaces console.log JSON, OBS-02)"
    - "next.config.ts → withSentryConfig + transpilePackages includes @imbau/observability"
---

<objective>
Instrument the three apps with Sentry (incl. the RSC `onRequestError` hook) and route all logging through the shared `@imbau/observability` pino logger, completing OBS-01 and OBS-02 at the code level.

Purpose: OBS-01 requires errors from web/panel/worker to reach Sentry with context including RSC errors; OBS-02 requires structured pino logs to reach Loki. The foundation (SDKs, presets, logger) landed in 04-01; this plan does the per-app wiring.
Output: Sentry instrumentation files + `withSentryConfig` wrap for web/panel, `@sentry/node` init + pino logger for the worker, and the env presets composed into each app's `env.ts`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-staging-observability-ci-cd/04-RESEARCH.md
@.planning/phases/04-staging-observability-ci-cd/04-PATTERNS.md
@.planning/phases/04-staging-observability-ci-cd/04-01-SUMMARY.md
@apps/web/next.config.ts
@apps/panel/next.config.ts
@apps/web/env.ts
@apps/panel/env.ts
@apps/worker/src/env.ts
@apps/worker/src/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire Sentry + pino into apps/web (anon surface)</name>
  <files>apps/web/instrumentation.ts, apps/web/instrumentation-client.ts, apps/web/sentry.server.config.ts, apps/web/sentry.edge.config.ts, apps/web/next.config.ts, apps/web/env.ts</files>
  <read_first>
    - apps/web/next.config.ts (lines 13-22 — existing `transpilePackages`, `output:'standalone'`, `turbopack.root`, `outputFileTracingRoot`, `import "./env"` MUST be preserved and wrapped, not replaced)
    - apps/web/env.ts (full — composes baseEnv + DATABASE_ANON_URL + NEXT_PUBLIC_APP_ENV; add sentryEnv/lokiEnv here)
    - 04-RESEARCH.md Pattern 5 (lines 487-517 — instrumentation.ts register()+onRequestError, instrumentation-client.ts init+onRouterTransitionStart, sentry.server/edge.config.ts, withSentryConfig)
    - 04-PATTERNS.md §"apps/{web,panel}/next.config.ts (MODIFY)" (lines 168-180) and §"instrumentation.ts ... (NEW)" (lines 203-208)
  </read_first>
  <action>Create `apps/web/instrumentation.ts` exporting `register()` (dynamic-import `./sentry.server.config` when `NEXT_RUNTIME==="nodejs"`, `./sentry.edge.config` when `"edge"`) and `export const onRequestError = Sentry.captureRequestError` (the RSC error hook OBS-01 requires). Create `apps/web/instrumentation-client.ts` calling `Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 0.1, environment: process.env.NEXT_PUBLIC_APP_ENV })` and `export const onRouterTransitionStart = Sentry.captureRouterTransitionStart`. Create `apps/web/sentry.server.config.ts` and `apps/web/sentry.edge.config.ts` each calling `Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1, environment: process.env.NEXT_PUBLIC_APP_ENV })`. Wrap the existing `nextConfig` export in `apps/web/next.config.ts` with `withSentryConfig(nextConfig, { org: process.env.SENTRY_ORG, project: process.env.SENTRY_PROJECT, authToken: process.env.SENTRY_AUTH_TOKEN, silent: !process.env.CI, widenClientFileUpload: true })` — preserve every existing field and add `@imbau/observability` to `transpilePackages`. In `apps/web/env.ts`, compose `sentryEnv` (server `SENTRY_DSN`, client `NEXT_PUBLIC_SENTRY_DSN`) and `lokiEnv` (server `LOKI_URL`, `LOKI_BASIC_AUTH`) from `@imbau/config/env/presets`, keeping `NEXT_PUBLIC_SENTRY_DSN` under `client` + wired in `experimental__runtimeEnv`; all new vars optional so dev still boots with zero external deps. Use `instrumentation-client.ts`, NOT `sentry.client.config.ts` (deprecated). Do NOT inline a second pino logger — web's server reads use the shared `@imbau/observability` logger.</action>
  <verify>
    <automated>pnpm --filter @imbau/web typecheck && pnpm --filter @imbau/web lint && grep -q "captureRequestError" apps/web/instrumentation.ts && grep -q "withSentryConfig" apps/web/next.config.ts && test -f apps/web/instrumentation-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/instrumentation.ts` exports `register` and `onRequestError = Sentry.captureRequestError`.
    - `apps/web/instrumentation-client.ts` exists (NOT `sentry.client.config.ts`) and exports `onRouterTransitionStart`.
    - `apps/web/next.config.ts` default export is `withSentryConfig(nextConfig, {...})` with all original fields intact and `@imbau/observability` in `transpilePackages`.
    - `apps/web/env.ts` composes `sentryEnv` + `lokiEnv`; all new vars optional; `NEXT_PUBLIC_SENTRY_DSN` is under `client`.
    - `pnpm --filter @imbau/web typecheck lint` exits 0.
  </acceptance_criteria>
  <done>web captures server (incl. RSC) and client errors in Sentry and validates the new optional env without breaking dev boot.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire Sentry + pino into apps/panel (auth surface)</name>
  <files>apps/panel/instrumentation.ts, apps/panel/instrumentation-client.ts, apps/panel/sentry.server.config.ts, apps/panel/sentry.edge.config.ts, apps/panel/next.config.ts, apps/panel/env.ts</files>
  <read_first>
    - apps/panel/next.config.ts (lines 11-22 — existing config to preserve and wrap; `transpilePackages` already lists `@imbau/api`)
    - apps/panel/env.ts (full — composes baseEnv + authEnv + all three DB URLs + NEXT_PUBLIC_APP_ENV)
    - 04-RESEARCH.md Pattern 5 (lines 487-517) — identical wiring to web
    - apps/web result from Task 1 (mirror it)
  </read_first>
  <action>Mirror Task 1 for the panel: create `apps/panel/instrumentation.ts` (`register()` + `onRequestError = Sentry.captureRequestError`), `apps/panel/instrumentation-client.ts` (init with `NEXT_PUBLIC_SENTRY_DSN` + `onRouterTransitionStart`), `apps/panel/sentry.server.config.ts`, `apps/panel/sentry.edge.config.ts`. Wrap the existing `nextConfig` in `apps/panel/next.config.ts` with `withSentryConfig(...)` (same options as web) — preserve `transpilePackages` (`@imbau/api`,`@imbau/ui`,`@imbau/config`) and ADD `@imbau/observability`, keep `output:'standalone'`, turbopack/outputFileTracing roots, and `import "./env"`. In `apps/panel/env.ts`, compose `sentryEnv` + `lokiEnv` (all optional). `BETTER_AUTH_URL` already exists; the staging value (`https://panel.staging.tours.andescode.com.ar`) is supplied by the runtime `.env`, not changed here. Use `instrumentation-client.ts`.</action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint && grep -q "captureRequestError" apps/panel/instrumentation.ts && grep -q "withSentryConfig" apps/panel/next.config.ts && grep -q "@imbau/observability" apps/panel/next.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/panel/instrumentation.ts` exports `register` and `onRequestError = Sentry.captureRequestError`.
    - `apps/panel/instrumentation-client.ts` exists and exports `onRouterTransitionStart`.
    - `apps/panel/next.config.ts` wrapped with `withSentryConfig`, `transpilePackages` includes `@imbau/observability` alongside the existing entries.
    - `apps/panel/env.ts` composes `sentryEnv` + `lokiEnv`, all optional.
    - `pnpm --filter @imbau/panel typecheck lint` exits 0.
  </acceptance_criteria>
  <done>panel captures server (incl. RSC), middleware, and client errors in Sentry; env stays fail-fast with optional Sentry/Loki vars.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire @sentry/node + pino logger into apps/worker (replace console.log)</name>
  <files>apps/worker/src/instrument.ts, apps/worker/src/index.ts, apps/worker/src/env.ts</files>
  <read_first>
    - apps/worker/src/index.ts (full — lines 3 `import "./env"` first; lines 44-57 the two `console.log(JSON.stringify({...}))` calls to replace; auto-boot guard lines 65-67)
    - apps/worker/src/env.ts (full — composes baseEnv + redisEnv via createEnv; add sentryEnv/lokiEnv here)
    - 04-RESEARCH.md Pattern 5 (lines 519-521 — @sentry/node init) and Pattern 6 (worker uses the shared logger)
    - 04-PATTERNS.md §"apps/worker/src/index.ts (MODIFY) + instrument.ts (NEW)" (lines 184-199)
    - packages/observability/src/logger.ts (from 04-01 — the `logger` export to import)
  </read_first>
  <action>Create `apps/worker/src/instrument.ts` calling `Sentry.init({ dsn: process.env.SENTRY_DSN, tracesSampleRate: 0.1, environment: process.env.NODE_ENV })` using `@sentry/node`. In `apps/worker/src/index.ts`, import `./instrument` on the FIRST line (before `./env`) so Sentry instruments before any other module loads (analogous to the env-first pattern). Replace the two existing stdout JSON log statements (the BullMQ "ready" handler that logs "worker connected to Redis…" and the "worker boot ok" line) with `logger.info({...}, "<message>")` from `@imbau/observability`, preserving the same fields (`node_env`, `queue`) — no direct stdout logging may remain. In `apps/worker/src/env.ts`, compose `sentryEnv.server` (`SENTRY_DSN`) and `lokiEnv.server` (`LOKI_URL`, `LOKI_BASIC_AUTH`) from the presets alongside the existing `baseEnv`+`redisEnv`; all optional (worker still boots with zero external deps in dev). Set `APP_NAME` for the worker's log labels via the runtime `.env` (compose), not hardcoded. Keep the `maxRetriesPerRequest: null` smoke-test contract intact — do not change the BullMQ wiring.</action>
  <verify>
    <automated>pnpm --filter @imbau/worker typecheck && pnpm --filter @imbau/worker lint && pnpm --filter @imbau/worker test && grep -q "instrument" apps/worker/src/index.ts && ! grep -q "console.log" apps/worker/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/src/instrument.ts` initializes `@sentry/node` and is imported on the first line of `index.ts` (before `./env`).
    - `apps/worker/src/index.ts` has no direct stdout logging remaining; both prior JSON log lines now go through the `@imbau/observability` `logger` with the same fields (the verify gate negative-greps for the removed stdout call).
    - `apps/worker/src/env.ts` composes `sentryEnv` + `lokiEnv`, all optional; `REDIS_URL` + `NODE_ENV` still required.
    - The existing Redis-connect smoke test still passes (`maxRetriesPerRequest: null` unchanged).
    - `pnpm --filter @imbau/worker typecheck lint test` exits 0.
  </acceptance_criteria>
  <done>worker initializes Sentry first and emits structured pino logs (Loki-bound when LOKI_URL is set), with the fase-3 smoke test still green.</done>
</task>

</tasks>

<artifacts>
- `apps/web/{instrumentation.ts,instrumentation-client.ts,sentry.server.config.ts,sentry.edge.config.ts}` + wrapped `next.config.ts` + `env.ts`
- `apps/panel/{instrumentation.ts,instrumentation-client.ts,sentry.server.config.ts,sentry.edge.config.ts}` + wrapped `next.config.ts` + `env.ts`
- `apps/worker/src/instrument.ts` + pino-wired `index.ts` + `env.ts`
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime → Sentry cloud | Error payloads (stack traces, request context) leave the box |
| app runtime → Loki | Structured logs leave the app process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-PII | Information Disclosure | Sentry error payloads / pino logs | medium | mitigate | tracesSampleRate 0.1; never log secret values (V7); DSN/LOKI creds from env, optional in dev |
| T-4-CLIENTLEAK | Information Disclosure | NEXT_PUBLIC_SENTRY_DSN in browser bundle | low | accept | Public DSNs are designed to be client-exposed; kept under `client` so t3-env guards server-only secrets from leaking |
</threat_model>

<verification>
- `pnpm -w typecheck && pnpm -w lint` pass.
- `pnpm --filter @imbau/worker test` passes (Redis smoke test green).
- grep confirms `onRequestError`/`captureRequestError` in both web and panel instrumentation, `withSentryConfig` in both next.config.ts, and no `console.log` in the worker.
- Operational (Sentry/Loki live) verification is gated on the VPS — see plan 04-07.
</verification>

<success_criteria>
OBS-01 and OBS-02 are satisfied at the code level: all three apps capture errors to Sentry (incl. RSC via onRequestError) and log structured pino lines bound for Loki when LOKI_URL is configured, while dev still runs with zero external dependencies.
</success_criteria>

<output>
Create `.planning/phases/04-staging-observability-ci-cd/04-02-SUMMARY.md` when done.
</output>
