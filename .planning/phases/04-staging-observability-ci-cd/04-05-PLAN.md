---
phase: 04-staging-observability-ci-cd
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/migrate.ts
  - packages/db/package.json
  - deploy/migrate.Dockerfile
  - deploy/compose.staging.yml
  - deploy/loki/loki-config.yml
  - deploy/nginx/staging.tours.andescode.com.ar.conf
autonomous: true
requirements: [INFRA-01, INFRA-02, OBS-02, OBS-03]
must_haves:
  truths:
    - "A programmatic drizzle-orm migrator (packages/db/migrate.ts) applies the SAME journal the harness uses, connecting as the OWNER role from raw DATABASE_URL (never drizzle-kit, which is a devDep absent from runtime images)"
    - "deploy/compose.staging.yml runs the full staging topology with Postgres/Redis INTERNAL-only (no host ports), web/panel bound to loopback 127.0.0.1:8090/8091, every observability service under a `profiles` gate with a per-container mem_limit (D-04)"
    - "The staging Postgres starts in production mode (imbau.env=production) so 0001_rls.sql does NOT set dev passwords — real app/anon passwords are provisioned out-of-band (Pattern 4, completed in 04-06)"
    - "Loki runs single-binary on the filesystem with 168h retention; Grafana + Uptime Kuma run internal-only (not publicly exposed)"
  artifacts:
    - packages/db/migrate.ts
    - deploy/migrate.Dockerfile
    - deploy/compose.staging.yml
    - deploy/loki/loki-config.yml
    - deploy/nginx/staging.tours.andescode.com.ar.conf
  key_links:
    - "migrate.ts → process.env.DATABASE_URL (owner) → drizzle-orm/postgres-js/migrator → packages/db/migrations"
    - "compose postgres `command: -c imbau.env=production` → 0001_rls.sql skips the dev-password block (Pattern 4)"
    - "nginx vhost 127.0.0.1:8090/8091 → web/panel loopback binds (D-01/D-02)"
    - "compose web/panel/worker → env_file .env (decrypted by deploy.sh, plan 04-06); APP_NAME per service → pino-loki labels → loki:3100"
---

<objective>
Author every staging runtime artifact (no GitHub, no live VPS): the programmatic migrate runner + its image, the full staging Compose topology (internal network, loopback binds, profiles, mem_limits) with Loki config, and the host-nginx vhost. These are consumed by the deploy pipeline (04-06) and brought up on the VPS (04-07).

Purpose: INFRA-01 (full Compose behind a TLS proxy), INFRA-02 (migrate-before-swap runner), OBS-02 (Loki), OBS-03 (Uptime Kuma) all need their config authored. D-01/D-02/D-04 dictate host-nginx integration, internal-only DB/Redis, loopback binds, and mandatory RAM mitigations.
Output: `packages/db/migrate.ts`, `deploy/migrate.Dockerfile`, `deploy/compose.staging.yml`, `deploy/loki/loki-config.yml`, `deploy/nginx/staging.tours.andescode.com.ar.conf`.
</objective>

<phase_decisions>
- **pino-loki over Promtail** (refines D-03): the Loki config + compose carry NO Promtail/Alloy scraper — apps push via the in-app pino-loki transport (RAM-optimal, Grafana-Cloud-symmetric). See 04-01.
- **Postgres backups DEFERRED** (FOUND-01): `pgdata` is a named volume (persists restarts) but no pgBackRest/wal-g — staging DB is reconstructible from migrations (+future seed).
- **Edge rate-limit for events/leads DEFERRED**: the nginx vhost carries a COMMENTED `limit_req` placeholder noting the mechanism for when those tables land (future milestone).
- **Grafana / Uptime-Kuma NOT publicly exposed**: internal docker network only; Grafana anonymous auth off; access via SSH tunnel (04-07).
- ⚠ **TWO DNS A records required** (one SAN cert): `staging.tours.andescode.com.ar` AND `panel.staging.tours.andescode.com.ar` → 31.97.175.128. Flagged in 04-07 prerequisites.
</phase_decisions>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-staging-observability-ci-cd/04-RESEARCH.md
@.planning/phases/04-staging-observability-ci-cd/04-PATTERNS.md
@.planning/phases/04-staging-observability-ci-cd/04-CONTEXT.md
@compose.yml
@packages/db/tests/db.ts
@packages/db/tests/setup.ts
@apps/worker/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Programmatic migrate runner (packages/db/migrate.ts) + deploy/migrate.Dockerfile</name>
  <files>packages/db/migrate.ts, packages/db/package.json, deploy/migrate.Dockerfile</files>
  <read_first>
    - packages/db/tests/db.ts (lines 14-20 migrationsFolder resolution; lines 76-80 connectAs `postgres(url,{max:1})` + drizzle)
    - packages/db/tests/setup.ts (lines 24-26, 69-76 — the exact `migrate(db,{migrationsFolder})` call to copy; drop the role-guard, that is test-only)
    - packages/db/src/client.ts (lines 28-31 createOwnerDb — owner pool shape)
    - packages/db/src/env.ts (DO NOT import this in migrate.ts — it requires all three URLs; migrate.ts reads raw process.env.DATABASE_URL only)
    - packages/db/migrations/0001_rls.sql (lines 22-46 — the imbau.env production gate; the owner role must run CREATE ROLE, so migrate connects as owner)
    - apps/worker/Dockerfile (lines 7-30 — prune→install multi-stage to mirror, targeting @imbau/db; no tsup build)
    - 04-RESEARCH.md Pattern 3 (lines 431-455) + 04-PATTERNS.md §"packages/db/migrate.ts" (lines 38-72) and §"deploy/migrate.Dockerfile" (lines 76-96)
  </read_first>
  <action>Create `packages/db/migrate.ts`: import `postgres`, `drizzle` from `drizzle-orm/postgres-js`, and `migrate` from `drizzle-orm/postgres-js/migrator`; resolve `migrationsFolder` from `import.meta.url` to `packages/db/migrations` (mirror tests/db.ts but one level up: the file sits at packages/db/, so resolve `dirname(fileURLToPath(import.meta.url))` + `"migrations"`); open `postgres(process.env.DATABASE_URL, { max: 1 })` as the OWNER role (read raw process.env — NOT `./env`, which would force APP/ANON URLs the migrate container does not have); run `await migrate(drizzle(sql), { migrationsFolder })`; `await sql.end()`. Add a `db:migrate:deploy` script to `packages/db/package.json` running `node --experimental-strip-types migrate.ts`. Create `deploy/migrate.Dockerfile` mirroring apps/worker/Dockerfile's pruner+builder stages but `pnpm dlx turbo prune @imbau/db --docker`, `pnpm install --frozen-lockfile` on out/json, copy out/full, and `CMD ["node","--experimental-strip-types","packages/db/migrate.ts"]` (no tsup build — migrate.ts runs from source via Node type-stripping). Use the drizzle-orm migrator (the migrations folder + drizzle-orm are all it needs); do NOT use the drizzle-kit CLI (devDep, absent from runtime images).</action>
  <verify>
    <automated>pnpm --filter @imbau/db typecheck && grep -q "drizzle-orm/postgres-js/migrator" packages/db/migrate.ts && grep -q "process.env.DATABASE_URL" packages/db/migrate.ts && grep -q "db:migrate:deploy" packages/db/package.json && test -f deploy/migrate.Dockerfile && grep -q "turbo prune @imbau/db" deploy/migrate.Dockerfile && grep -q "packages/db/migrate.ts" deploy/migrate.Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/migrate.ts` imports the drizzle-orm programmatic migrator and connects via raw `process.env.DATABASE_URL` (owner, `max:1`) — it does NOT import `packages/db/src/env.ts`.
    - `migrationsFolder` resolves to `packages/db/migrations` from `import.meta.url`.
    - `packages/db/package.json` has a `db:migrate:deploy` script running the migrator via Node type-stripping.
    - `deploy/migrate.Dockerfile` prunes `@imbau/db`, installs frozen, and its CMD runs `packages/db/migrate.ts`; no tsup/drizzle-kit step.
    - `pnpm --filter @imbau/db typecheck` exits 0.
  </acceptance_criteria>
  <done>A runtime-image-buildable migrate runner applies the prod journal as owner, gating the deploy in 04-06.</done>
</task>

<task type="auto">
  <name>Task 2: Staging Compose topology (deploy/compose.staging.yml) + Loki config</name>
  <files>deploy/compose.staging.yml, deploy/loki/loki-config.yml</files>
  <read_first>
    - compose.yml (root — lines 9-36; the postgres/redis image pins + healthcheck shapes to keep; staging DEVIATES: no host ports, loopback binds, mem_limit, profiles)
    - 04-RESEARCH.md Pattern 2 (lines 344-424 — the full staging skeleton incl. internal network, loopback `127.0.0.1:8090:3000`, mem_limits, profiles deploy/observability, staged bring-up) and Pattern 6 Loki config (lines 552-572)
    - 04-PATTERNS.md §"deploy/compose.staging.yml" (lines 100-123) — service names postgres/redis stay stable; Traefik wording in compose.yml superseded by D-01
    - 04-CONTEXT.md D-02/D-04 + Environment reality (taken ports 80,443,22,3000,3001,5432,5680,6379,5433)
    - packages/db/migrations/0001_rls.sql (lines 28-46 — why imbau.env=production must be set on the staging server)
  </read_first>
  <action>Create `deploy/compose.staging.yml` with `name: imbau-staging` and one `internal` network. Services: (1) `postgres` image `postgres:16-alpine`, env POSTGRES_USER=imbau / POSTGRES_PASSWORD=${POSTGRES_PASSWORD} / POSTGRES_DB=imbau, NO `ports:` (internal only — D-02), `command: ["postgres","-c","imbau.env=production"]` (custom GUC accepted at startup so 0001_rls.sql skips the dev-password block — Pattern 4), `pgdata` volume, the pg_isready healthcheck, `mem_limit: 512m`. (2) `redis` image `redis:7-alpine`, NO `ports:`, redis-cli ping healthcheck, `mem_limit: 128m`. (3) `web` image `ghcr.io/latarocaballero/imbau-web:${IMAGE_TAG:-latest}`, `env_file: [.env]`, `environment: { APP_NAME: web }`, networks internal, `ports: ["127.0.0.1:8090:3000"]` (loopback prefix MANDATORY — a bare port publishes on all interfaces), `mem_limit: 512m`, depends_on postgres healthy. (4) `panel` same pattern, image `imbau-panel`, `APP_NAME: panel`, `ports: ["127.0.0.1:8091:3000"]`, `mem_limit: 512m`. (5) `worker` image `imbau-worker`, `env_file: [.env]`, `APP_NAME: worker`, internal, `mem_limit: 384m`, depends_on redis. (6) `migrate` image `ghcr.io/latarocaballero/imbau-migrate:${IMAGE_TAG:-latest}`, `env_file: [.env]`, internal, `profiles: ["deploy"]`, `restart: "no"` (one-off via `compose run --rm migrate`). (7) `loki` image `grafana/loki:3.5` (verify current minor), `command: -config.file=/etc/loki/loki-config.yml`, volumes `./loki/loki-config.yml:/etc/loki/loki-config.yml:ro` + `lokidata:/loki`, internal, `profiles: ["observability"]`, `mem_limit: 256m`. (8) `grafana` image `grafana/grafana:11.6.0` (verify), environment GF_ANALYTICS_REPORTING_ENABLED=false + GF_AUTH_ANONYMOUS_ENABLED=false (NOT publicly exposed — no host port), `grafanadata` volume, internal, `profiles: ["observability"]`, `mem_limit: 256m`. (9) `uptime-kuma` image `louislam/uptime-kuma:1`, `kumadata` volume, internal, `profiles: ["observability"]`, `mem_limit: 128m`. Volumes: pgdata, lokidata, grafanadata, kumadata (named — backups deferred). Add a header comment recording the deferred decisions (backups, rate-limit) and the staged bring-up order. Create `deploy/loki/loki-config.yml` per RESEARCH Pattern 6: auth_enabled false, http_listen_port 3100, tsdb+filesystem schema v13, `limits_config.retention_period: 168h`, compactor with retention_enabled. Verify image tags at author time (they drift).</action>
  <verify>
    <automated>test -f deploy/compose.staging.yml && grep -q "127.0.0.1:8090:3000" deploy/compose.staging.yml && grep -q "127.0.0.1:8091:3000" deploy/compose.staging.yml && grep -q "imbau.env=production" deploy/compose.staging.yml && grep -c "mem_limit" deploy/compose.staging.yml | awk '$1>=8{exit 0} {exit 1}' && grep -q 'profiles' deploy/compose.staging.yml && grep -q "louislam/uptime-kuma" deploy/compose.staging.yml && test -f deploy/loki/loki-config.yml && grep -q "retention_period: 168h" deploy/loki/loki-config.yml && ! grep -E '^\s*-\s*"?(5432|6379):' deploy/compose.staging.yml</automated>
  </verify>
  <acceptance_criteria>
    - Postgres and Redis have NO host-published ports (internal network only); a port-publish scan for 5432/6379 finds nothing.
    - web binds `127.0.0.1:8090:3000`, panel binds `127.0.0.1:8091:3000` (loopback-prefixed).
    - Postgres `command` sets `imbau.env=production`.
    - Every service that consumes RAM has a `mem_limit` (>=8 services limited); Loki/Grafana/Uptime-Kuma are under `profiles: ["observability"]`; migrate is under `profiles: ["deploy"]`.
    - Grafana has anonymous auth + analytics disabled and is not host-published; Uptime Kuma present.
    - `deploy/loki/loki-config.yml` is single-binary filesystem with 168h retention.
  </acceptance_criteria>
  <done>The full staging topology is authored with RAM mitigations, internal-only data services, loopback app binds, and observability gated behind a profile.</done>
</task>

<task type="auto">
  <name>Task 3: Host-nginx vhost (deploy/nginx/staging.tours.andescode.com.ar.conf)</name>
  <files>deploy/nginx/staging.tours.andescode.com.ar.conf</files>
  <read_first>
    - 04-RESEARCH.md Pattern 1 (lines 269-337 — webroot ACME server block, the two :443 proxy blocks to 127.0.0.1:8090/8091, one SAN cert paths, the COMMENTED SSE block; certonly --webroot never --nginx)
    - 04-PATTERNS.md §"deploy/nginx/*.conf" (lines 246-248)
    - 04-CONTEXT.md D-01/D-02 + guardrail: the VPS host nginx owns :80/:443 and runs prod sites — this file is ADDED by hand, existing vhosts are never touched
  </read_first>
  <action>Create `deploy/nginx/staging.tours.andescode.com.ar.conf` containing: a header comment that this file is ADDED to the host nginx by hand (04-07), that cert issuance uses `certbot certonly --webroot -w /var/www/certbot` (NEVER `--nginx`, which rewrites prod configs), and that TWO DNS A records are required. An HTTP `server` block listening :80 for both `server_name`s that serves `location /.well-known/acme-challenge/ { root /var/www/certbot; }` and otherwise 301-redirects to https. Two `server` blocks listening `443 ssl` with `http2 on`: (a) `server_name staging.tours.andescode.com.ar;` → `location / { proxy_pass http://127.0.0.1:8090; ... }`, (b) `server_name panel.staging.tours.andescode.com.ar;` → `location / { proxy_pass http://127.0.0.1:8091; ... }`. Both reference the single SAN cert at `/etc/letsencrypt/live/staging.tours.andescode.com.ar/fullchain.pem` + `privkey.pem`. Set proxy headers Host=$host, X-Real-IP=$remote_addr, X-Forwarded-For=$proxy_add_x_forwarded_for, X-Forwarded-Proto=$scheme, and `proxy_http_version 1.1`. Include a COMMENTED SSE `location /api/stream/` block (proxy_buffering off, proxy_read_timeout 3600s) marked "not active until phase 2", and a COMMENTED `limit_req` placeholder marked "edge rate-limit for events/leads deferred (future milestone)". Do not run nginx here — `nginx -t`/reload happen on the VPS in 04-07.</action>
  <verify>
    <automated>test -f deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "server_name staging.tours.andescode.com.ar" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "panel.staging.tours.andescode.com.ar" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "127.0.0.1:8090" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "127.0.0.1:8091" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "acme-challenge" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -q "fullchain.pem" deploy/nginx/staging.tours.andescode.com.ar.conf</automated>
  </verify>
  <acceptance_criteria>
    - The vhost has an HTTP :80 block serving `/.well-known/acme-challenge/` from `/var/www/certbot` (for `certonly --webroot`) and redirecting otherwise to https.
    - Two :443 ssl blocks: `staging.tours.andescode.com.ar` → `127.0.0.1:8090`, `panel.staging.tours.andescode.com.ar` → `127.0.0.1:8091`, both using the single SAN cert under `/etc/letsencrypt/live/staging.tours.andescode.com.ar/`.
    - X-Forwarded-* + Host proxy headers and `proxy_http_version 1.1` are set on the proxy locations.
    - The SSE block and the `limit_req` rate-limit are present but COMMENTED with their deferral notes; the header states the file is hand-applied and never via `certbot --nginx`.
  </acceptance_criteria>
  <done>The host-nginx vhost is authored (two subdomains, one SAN cert, loopback proxy) ready to be applied by hand on the VPS in 04-07.</done>
</task>

</tasks>

<artifacts>
- `packages/db/migrate.ts` + `db:migrate:deploy` script — programmatic drizzle-orm owner-role migrator
- `deploy/migrate.Dockerfile` — runtime-image-buildable migrate runner (prune @imbau/db → run migrate.ts)
- `deploy/compose.staging.yml` — full staging topology (internal net, loopback 8090/8091, profiles deploy/observability, per-container mem_limits, imbau.env=production)
- `deploy/loki/loki-config.yml` — single-binary filesystem Loki, 168h retention
- `deploy/nginx/staging.tours.andescode.com.ar.conf` — host-nginx vhost (web + panel subdomains, one SAN cert, webroot ACME)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImBau compose ↔ shared prod box | Containers must not collide with prod ports/RAM |
| public internet → nginx → loopback apps | nginx is the sole public ingress; backends are loopback/internal only |
| admin UIs (Grafana/Kuma) → operator | Admin surfaces must not be publicly reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-PROD-DOS | Denial of Service | RAM/ports on the shared VPS | high | mitigate | Per-container `mem_limit`; observability under `profiles` (staged bring-up); Postgres/Redis internal-only (no host ports); loopback binds outside the taken port set |
| T-4-ADMIN | Information Disclosure / Elevation | Grafana / Uptime Kuma | medium | mitigate | Not host-published (internal network); Grafana anonymous auth + analytics off; access via SSH tunnel only (04-07) |
| T-4-PROXY | Tampering | host nginx (shared with prod) | high | mitigate | vhost is ADDED by hand; `certonly --webroot` (never `--nginx`); `nginx -t` before reload (04-07) |
| T-4-AUTH-PW | Spoofing | staging app/anon DB auth | high | mitigate | `imbau.env=production` set here so dev passwords are skipped; real passwords provisioned in 04-06 (Pattern 4) |
</threat_model>

<verification>
- `pnpm --filter @imbau/db typecheck` passes; migrate.ts uses the drizzle-orm migrator + raw DATABASE_URL.
- `deploy/compose.staging.yml` parses; loopback binds, mem_limits, profiles, internal-only DB/Redis, and `imbau.env=production` are present; no host port-publish for 5432/6379.
- `deploy/loki/loki-config.yml` is single-binary with 168h retention.
- nginx vhost has both subdomains, loopback proxy, webroot ACME, one SAN cert. Real `nginx -t` + image build/run are gated on the VPS (04-06/04-07).
</verification>

<success_criteria>
All staging runtime artifacts are authored and self-consistent: a migrate runner (INFRA-02), a RAM-safe internal-network Compose with loopback app binds + observability profiles (INFRA-01, OBS-02, OBS-03), Loki config (OBS-02), and the host-nginx vhost (INFRA-01) — ready for the deploy pipeline (04-06) and VPS bring-up (04-07).
</success_criteria>

<output>
Create `.planning/phases/04-staging-observability-ci-cd/04-05-SUMMARY.md` when done.
</output>