---
phase: 04-staging-observability-ci-cd
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-05", "04-06"]
files_modified: []
autonomous: false
requirements: [INFRA-01, INFRA-02, OBS-01, OBS-02, OBS-03]
user_setup:
  - service: dns
    why: "certbot HTTP-01 (webroot) issuance and public reachability require DNS. ⚠ TWO A records are needed (one SAN cert covers both subdomains)"
    dashboard_config:
      - task: "Create A record `staging.tours.andescode.com.ar` → 31.97.175.128"
        location: "DNS provider for andescode.com.ar"
      - task: "Create A record `panel.staging.tours.andescode.com.ar` → 31.97.175.128"
        location: "DNS provider for andescode.com.ar"
  - service: vps
    why: "First bring-up is operator-run on the live prod box (only ADD; never touch existing services)"
    dashboard_config:
      - task: "git clone repo to /opt/imbau; docker login ghcr.io with a PAT read:packages; place age key at ~/.config/sops/age/keys.txt; install sops + age binaries"
        location: "VPS root@31.97.175.128"
must_haves:
  truths:
    - "https://staging.tours.andescode.com.ar and https://panel.staging.tours.andescode.com.ar both return 200 over TLS with a valid SAN cert, with web/panel/worker/postgres/redis healthy (INFRA-01)"
    - "A merge to main auto-deploys to staging; a forced bad migration aborts the deploy before the app swap (old containers keep running) — migrate-before-swap proven (INFRA-02)"
    - "The staging app_authenticated/anon pools authenticate against the staging DB (no scram-sha-256 failures in logs) — Pattern 4 confirmed live"
    - "Test errors from web, panel, and worker (incl. an RSC error) appear in Sentry with stack + context (OBS-01)"
    - "pino logs from the apps are queryable in Grafana/Loki (LogQL returns recent lines) (OBS-02)"
    - "Uptime Kuma shows green monitors for the staging endpoints (OBS-03)"
    - "After staged observability bring-up, `free -m` shows headroom retained for the shared prod box (D-04)"
  artifacts: []
  key_links:
    - "DNS A records (×2) → certbot certonly --webroot → one SAN cert → nginx :443 blocks → loopback apps"
    - "merge to main → deploy-staging.yml → deploy.sh (migrate gate + role bootstrap) → running staging stack"
    - "apps pino-loki → loki:3100 → Grafana LogQL; apps Sentry SDK → Sentry cloud; Uptime Kuma → https endpoints"
---

<objective>
Bring the staging stack up on the live VPS for the first time and verify the whole phase end-to-end (the phase gate). This plan is entirely operational and GATED on user prerequisites (two DNS A records, age key + real secrets, SSH deploy key, GHCR PAT). Authoring of everything it exercises is done in 04-01..04-06.

Purpose: convert the authored artifacts into a running, observable, auto-deployed staging environment and prove all four ROADMAP success criteria on the real box without disturbing prod.
Output: a verified staging deployment (no new repo files) + a SUMMARY recording the live evidence.
</objective>

<prerequisites>
⚠ **TWO DNS A records are required** (this changed the original single-record assumption): `staging.tours.andescode.com.ar` AND `panel.staging.tours.andescode.com.ar`, both → 31.97.175.128. One SAN cert covers both.
- age private key loaded on the VPS at `~/.config/sops/age/keys.txt` and as the `SOPS_AGE_KEY` Actions secret (04-04).
- `secrets/staging.enc.yaml` encrypted + committed (04-04).
- `VPS_SSH_KEY` Actions secret set (04-06); branch protection requiring `quality` enabled (04-03).
- The VPS is a LIVE PROD box (chatwoot/n8n/waha/2×Postgres, host nginx owns :80/:443, ~4.9 GiB shared RAM). Only ADD; never edit/restart existing services, vhosts, or certs.
</prerequisites>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-staging-observability-ci-cd/04-RESEARCH.md
@.planning/phases/04-staging-observability-ci-cd/04-VALIDATION.md
@.planning/phases/04-staging-observability-ci-cd/04-CONTEXT.md
@deploy/nginx/staging.tours.andescode.com.ar.conf
@deploy/deploy.sh
@deploy/compose.staging.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: First VPS bring-up — DNS, nginx vhost, certbot, staged compose (only ADD)</name>
  <read_first>
    - deploy/nginx/staging.tours.andescode.com.ar.conf (the vhost to add by hand)
    - deploy/deploy.sh + deploy/compose.staging.yml (the staged bring-up the operator runs / the deploy runs)
    - 04-RESEARCH.md Pattern 1 (lines 282-297 — the safe issuance order on a live box) and Pattern 2 staged bring-up (lines 412-424) and Pitfall 2/3 (lines 710-722)
  </read_first>
  <action>Operator-run first bring-up on the live prod VPS, strictly additive: create the two DNS A records; bootstrap the VPS (clone to /opt/imbau, install sops+age, place age key, docker login ghcr with read:packages PAT); add the nginx vhost additively (ACME :80 block first, `nginx -t`, reload), issue ONE SAN cert via `certbot certonly --webroot` (never `--nginx`), enable the :443 blocks + renewal deploy-hook; bring up the compose stack staged with `free -m` at each step (postgres/redis → migrate → app → observability profile). Never edit/restart existing prod services, vhosts, or certs.</action>
  <what-built>04-01..04-06 authored the compose stack, the host-nginx vhost, the migrate runner, the deploy pipeline, and the SOPS secrets. Nothing is running on the VPS yet. This step is the operator-run first bring-up on the shared prod box — strictly additive.</what-built>
  <how-to-verify>
    1. Confirm BOTH DNS A records resolve to 31.97.175.128 (`dig +short staging.tours.andescode.com.ar panel.staging.tours.andescode.com.ar`).
    2. On the VPS: `git clone` (or pull) the repo into `/opt/imbau`; install `sops` + `age`; place the age key at `~/.config/sops/age/keys.txt`; `docker login ghcr.io` with a PAT scoped `read:packages`.
    3. Add the nginx vhost ADDITIVELY: copy `deploy/nginx/staging.tours.andescode.com.ar.conf` into the host nginx (e.g. `/etc/nginx/sites-available/` + symlink), starting with ONLY the :80 ACME block; `sudo mkdir -p /var/www/certbot`; `sudo nginx -t` (MUST pass — it also validates prod) then `sudo systemctl reload nginx`. Never use `certbot --nginx`.
    4. Issue ONE SAN cert: `sudo certbot certonly --webroot -w /var/www/certbot -d staging.tours.andescode.com.ar -d panel.staging.tours.andescode.com.ar --non-interactive --agree-tos -m <admin-email>`.
    5. Enable the :443 server blocks (referencing the issued cert), `sudo nginx -t` then reload; add a renewal deploy-hook (`systemctl reload nginx`).
    6. Bring the stack up staged (or trigger a merge to main so deploy.sh runs): `free -m`; `docker compose -f deploy/compose.staging.yml up -d postgres redis`; `docker compose -f deploy/compose.staging.yml run --rm migrate`; run the role bootstrap; `up -d web panel worker`; `free -m`; `docker compose -f deploy/compose.staging.yml --profile observability up -d`; `free -m`.
  </how-to-verify>
  <acceptance_criteria>
    - Both DNS A records resolve; the SAN cert is issued via webroot (no existing vhost/cert was modified; `nginx -t` passed each time).
    - `docker compose -f deploy/compose.staging.yml ps` shows web/panel/worker/postgres/redis healthy; Postgres/Redis are NOT published on host ports; web/panel are on loopback 8090/8091 only.
    - `free -m` after observability bring-up shows headroom retained for prod (no OOM in `dmesg`).
    - No prod service (chatwoot/n8n/waha) was touched or restarted.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the stack is up behind TLS without disturbing prod, or describe what blocked you.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end phase gate — TLS, auto-deploy/migrate-gate, Sentry, Loki, Kuma, RAM</name>
  <read_first>
    - 04-VALIDATION.md §Manual-Only Verifications (the operational checks per requirement) and §Sampling Rate Phase gate
    - 04-RESEARCH.md §Validation Architecture (lines 825-843) and Pitfall 1 (app/anon scram auth signs)
  </read_first>
  <action>Run the seven end-to-end phase-gate checks on the live staging box and record evidence in the SUMMARY: (1) TLS curl 200 on both subdomains; (2) merge-to-main auto-deploy + a forced bad migration aborting the swap (old containers stay up); (3) no scram auth failures for app_authenticated/anon in logs; (4) Sentry captures errors from web/panel/worker incl. an RSC error; (5) Grafana LogQL returns pino lines (SSH tunnel); (6) Uptime Kuma green monitors; (7) `free -m` headroom retained. Revert the forced-fail migration after the test.</action>
  <what-built>The staging stack is up behind TLS (Task 1) and the deploy pipeline is wired (04-06). This step proves all four ROADMAP success criteria live on the box.</what-built>
  <how-to-verify>
    1. **TLS (INFRA-01):** `curl -I https://staging.tours.andescode.com.ar` and `curl -I https://panel.staging.tours.andescode.com.ar` → 200 with a valid cert.
    2. **Auto-deploy + migrate-before-swap (INFRA-02):** merge a trivial change to main → confirm `deploy-staging.yml` builds/pushes and the SSH deploy runs. Then push a deliberately broken migration on a throwaway branch deploy → confirm `docker compose run --rm migrate` exits non-zero and `deploy.sh` ABORTS before the app swap (old containers stay up; `docker compose ps` unchanged). Revert the broken migration.
    3. **App-pool auth (Pattern 4):** check web/panel/worker logs for the absence of `password authentication failed for user "app_authenticated"` / `"anon"` — the pools connect cleanly.
    4. **Sentry (OBS-01):** trigger a test error in web (an RSC/server-component error), panel, and worker → each appears in Sentry with stack + context (RSC error captured via `onRequestError`).
    5. **Loki (OBS-02):** SSH-tunnel to Grafana (`ssh -L 3000:127.0.0.1:<grafana-loopback-or-exec> root@31.97.175.128`, Grafana is internal-only) and run LogQL `{app="panel"}` (and `{app="worker"}`) → recent pino lines returned.
    6. **Uptime Kuma (OBS-03):** open Kuma (SSH tunnel), add monitors for the two HTTPS endpoints (and internal container health) → monitors show green.
    7. **RAM (D-04):** `free -m` confirms headroom retained for prod after everything is up.
  </how-to-verify>
  <acceptance_criteria>
    - Both HTTPS endpoints return 200 with a valid SAN cert; `docker compose ps` all healthy (INFRA-01).
    - Auto-deploy runs on merge; a forced bad migration aborts the deploy before the swap and leaves the old containers running (INFRA-02).
    - No scram-sha-256 auth failures for app_authenticated/anon in the container logs (Pattern 4).
    - Sentry shows captured errors from all three apps including an RSC error (OBS-01).
    - Grafana LogQL returns recent pino lines for panel and worker (OBS-02).
    - Uptime Kuma shows green monitors for the staging endpoints (OBS-03).
    - `free -m` shows prod headroom retained (D-04).
  </acceptance_criteria>
  <resume-signal>Type "approved" once all seven checks pass, or list which failed.</resume-signal>
</task>

</tasks>

<artifacts>
- No new repo files. Produces a verified, running staging deployment and a SUMMARY capturing the live evidence (curl output, Sentry issue links, LogQL screenshots, Kuma status, `free -m`, forced-migration-fail proof).
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live prod box | First bring-up must not disturb chatwoot/n8n/waha/host nginx |
| public internet → staging endpoints | TLS-terminated; backends loopback/internal only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-PROXY | Tampering/DoS | host nginx + certbot on the shared box | high | mitigate | Additive vhost only; `certonly --webroot` (never `--nginx`); `nginx -t` before each reload; verify prod sites still serve after reload |
| T-4-PROD-DOS | Denial of Service | shared RAM | high | mitigate | Staged `profiles` bring-up with `free -m` at each step; Grafana Cloud fallback documented if RAM is tight (D-04) |
| T-4-MIGRATE | Availability | deploy swap | high | mitigate | Forced-failure test proves migrate-before-swap aborts before swapping app containers |
| T-4-ADMIN | Information Disclosure | Grafana/Kuma | medium | mitigate | Verified internal-only; accessed via SSH tunnel; Grafana anon auth off |
</threat_model>

<verification>
- All checks are operational and recorded in the SUMMARY with evidence. This is the phase gate per 04-VALIDATION.md: CI green on a PR (04-03) PLUS one real merge-to-main deploy verified end-to-end (200 over TLS, Sentry test error incl. RSC, Loki log line, Kuma green, `free -m` headroom, forced-migration-fail aborts swap, app-pool auth clean).
</verification>

<success_criteria>
INFRA-01, INFRA-02, OBS-01, OBS-02, OBS-03 are verified LIVE on staging: the full Compose runs behind host-nginx TLS at the two subdomains, merges auto-deploy with migrate-before-swap, errors reach Sentry (incl. RSC), pino logs reach Loki, and Uptime Kuma monitors the endpoints — all without disturbing the shared prod box.
</success_criteria>

<output>
Create `.planning/phases/04-staging-observability-ci-cd/04-07-SUMMARY.md` when done.
</output>
