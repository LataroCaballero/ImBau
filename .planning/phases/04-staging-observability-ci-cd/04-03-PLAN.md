---
phase: 04-staging-observability-ci-cd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements: [CI-01, CI-02]
user_setup:
  - service: github
    why: "Branch protection requires repo admin; CI secrets are not needed for the quality job (uses built-in GITHUB_TOKEN)"
    dashboard_config:
      - task: "Enable branch protection on `main` requiring the `quality` status check to pass before merge"
        location: "GitHub → Settings → Branches → Branch protection rules (or `gh api`); the check name appears only after the workflow runs once on a PR"
must_haves:
  truths:
    - "Every pull request and every push to main runs `lint + typecheck + test` via GitHub Actions (CI-01)"
    - "The RLS cross-tenant isolation suite runs in CI against a real postgres:16-alpine service using the fase-2 harness UNCHANGED — pointed at the service via TEST_DATABASE_* env (CI-02)"
    - "The RLS tests connect as the unprivileged app_authenticated/anon roles (never superuser); the harness role-guard asserts rolbypassrls=false/rolsuper=false so isolation cannot pass for the wrong reason"
    - "A red `quality` check blocks merge once branch protection requires it (CI-01)"
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "ci.yml services.postgres (imbau_test) → TEST_DATABASE_URL/APP_URL/ANON_URL → fase-2 harness setup.ts (migrate + role-guard)"
    - "imbau.env unset in CI Postgres → 0001_rls.sql sets :dev passwords → TEST_DATABASE_APP_URL=app_authenticated:dev authenticates for free"
    - "setup-github-actions-caching-for-turbo → turbo run lint typecheck test (cache restore)"
---

<objective>
Author the GitHub Actions PR gate (`.github/workflows/ci.yml`) that runs `lint + typecheck + test` on every PR and push to main, with a real `postgres:16-alpine` service so the fase-2 RLS cross-tenant isolation suite runs in CI exactly as designed, restoring the Turbo cache before the run (CI-01, CI-02, D-08). Wire branch protection so a red check blocks merge.

Purpose: CI-01 (red CI blocks merge) and CI-02 (RLS tests vs a real Postgres) are the milestone exit-gate's automation. The fase-2 harness is already endpoint-parametrizable, so CI only needs to point its `TEST_DATABASE_*` env at the Actions Postgres service — no testcontainers, no divergence.
Output: a committed CI workflow + branch-protection requiring the `quality` check.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-staging-observability-ci-cd/04-RESEARCH.md
@.planning/phases/04-staging-observability-ci-cd/04-PATTERNS.md
@packages/db/tests/db.ts
@packages/db/tests/setup.ts
@packages/db/vitest.config.ts
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/ci.yml (PR gate + Postgres service + RLS harness env + Turbo cache)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - 04-RESEARCH.md Pattern 7 (lines 580-612 — the exact workflow: triggers, services.postgres block with health options, TEST_DATABASE_* env, steps incl. dtinth/setup-github-actions-caching-for-turbo, `pnpm turbo run lint typecheck test`)
    - 04-PATTERNS.md §".github/workflows/ci.yml (NEW — greenfield)" (lines 220-228) — why the harness reuse works (imbau_test satisfies requireTestDb `_test` guard; imbau.env unset ⇒ :dev passwords)
    - packages/db/tests/db.ts (lines 43-60 requireTestDb `_test` assertion; lines 62-67 owner/app/anon URL precedence TEST_DATABASE_* || DATABASE_*)
    - packages/db/tests/setup.ts (globalSetup runs migrate() then assertUnprivileged for app_authenticated/anon — role-guard)
    - package.json (packageManager pnpm@11.6.0; scripts lint/typecheck/test → turbo)
    - 04-RESEARCH.md Pitfall 4 (GH Actions cache v2 only — use current docker/build actions; not relevant here but cache action must be current)
  </read_first>
  <action>Create `.github/workflows/ci.yml` per RESEARCH Pattern 7. Triggers: `pull_request: {}` and `push: { branches: [main] }`. One job `quality` on `ubuntu-latest` with a `services.postgres` of `postgres:16-alpine`, env `POSTGRES_USER=postgres`, `POSTGRES_PASSWORD=postgres`, `POSTGRES_DB=imbau_test`, `ports: ["5432:5432"]`, and a `pg_isready -U postgres -d imbau_test` health check (interval 5s, retries 10). Job-level `env`: `TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/imbau_test` (owner — runs migrate + CREATE ROLE), `TEST_DATABASE_APP_URL=postgres://app_authenticated:dev@localhost:5432/imbau_test`, `TEST_DATABASE_ANON_URL=postgres://anon:dev@localhost:5432/imbau_test` (the harness applies the `:dev` passwords because `imbau.env` is unset). Steps: `actions/checkout@v4`; `pnpm/action-setup@v4` (reads `packageManager`); `actions/setup-node@v4` with `node-version: 22` + `cache: pnpm`; `dtinth/setup-github-actions-caching-for-turbo@v1`; `pnpm install --frozen-lockfile`; `pnpm turbo run lint typecheck test`. Pin action majors as in RESEARCH; verify current majors at author time. Do NOT add testcontainers. Do NOT point the app/anon URLs at the `postgres` superuser (Pitfall 5) — they MUST be `app_authenticated`/`anon`.</action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "postgres:16-alpine" .github/workflows/ci.yml && grep -q "imbau_test" .github/workflows/ci.yml && grep -q "app_authenticated:dev" .github/workflows/ci.yml && grep -q "anon:dev" .github/workflows/ci.yml && grep -q "turbo run lint typecheck test" .github/workflows/ci.yml && grep -q "setup-github-actions-caching-for-turbo" .github/workflows/ci.yml && python3 -c "import sys,yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || node -e "const f=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/jobs:\s/.test(f)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` triggers on `pull_request` and `push` to `main`.
    - The `quality` job declares a `postgres:16-alpine` service named so the harness reaches it at `localhost:5432`, DB `imbau_test`.
    - Job env sets `TEST_DATABASE_URL` (owner), `TEST_DATABASE_APP_URL` (`app_authenticated:dev`), `TEST_DATABASE_ANON_URL` (`anon:dev`) — never the superuser for app/anon.
    - Steps install with `--frozen-lockfile` and run `pnpm turbo run lint typecheck test` with the Turbo cache action restored first.
    - The file parses as valid YAML.
  </acceptance_criteria>
  <done>A PR/push triggers lint+typecheck+test in CI with the RLS suite running against a real Postgres service using the unprivileged roles.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable branch protection requiring the `quality` check on main</name>
  <read_first>
    - .github/workflows/ci.yml (from Task 1 — the job is named `quality`; that exact name is the required status check)
    - 04-RESEARCH.md Pattern 7 (line 612 — "mark `quality` a required status check on `main` so red CI blocks merge (CI-01)")
  </read_first>
  <action>One-time GitHub admin action: after the `quality` workflow has run once (so the check name registers), enable branch protection on `main` requiring the `quality` status check to pass before merge — via GitHub Settings → Branches or `gh api` (command provided below). This is what makes CI-01 ("red CI blocks merge") real. Cannot be done before the workflow has run at least once.</action>
  <what-built>`.github/workflows/ci.yml` is committed and will run on the next PR. The `quality` check name only exists in GitHub after the workflow has run at least once, so requiring it as a merge gate is a one-time admin action you take on GitHub.</what-built>
  <how-to-verify>
    1. Push this branch and open a PR (or push to a branch) so the `quality` workflow runs once and the check name registers.
    2. Enable branch protection on `main` requiring the `quality` status check before merge — either GitHub → Settings → Branches → Add rule (require status checks, select `quality`), or via CLI: `gh api -X PUT repos/LataroCaballero/ImBau/branches/main/protection -f required_status_checks.strict=true -f 'required_status_checks.contexts[]=quality' -F enforce_admins=true -f required_pull_request_reviews= -f restrictions=` (adjust fields to your org policy).
    3. Confirm a PR with a deliberately failing test cannot be merged (the merge button is blocked by the red `quality` check).
  </how-to-verify>
  <resume-signal>Type "approved" once branch protection requires the `quality` check, or describe what blocked you.</resume-signal>
</task>

</tasks>

<artifacts>
- `.github/workflows/ci.yml` — PR + push:main gate running `lint + typecheck + test` with a `postgres:16-alpine` service and Turbo cache; reuses the fase-2 RLS harness via `TEST_DATABASE_*` env
- Branch protection on `main` requiring the `quality` check (GitHub setting — human action)
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI test runner → Postgres service | RLS isolation must be proven under the unprivileged role, not the superuser |
| PR author → main branch | Untrusted change must pass the quality gate before merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-RLS | Information Disclosure | RLS isolation tests in CI | high | mitigate | App/anon URLs connect as `app_authenticated`/`anon` (never superuser); fase-2 harness role-guard asserts rolbypassrls=false/rolsuper=false and fails loudly otherwise (Pitfall 5) |
| T-4-MERGE | Tampering | merge to main | high | mitigate | Branch protection requires the `quality` check green before merge (CI-01) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` parses and contains the Postgres service, the three `TEST_DATABASE_*` URLs with unprivileged roles, and `turbo run lint typecheck test`.
- Operational (CI-01/CI-02 truly green) is verified by the first PR run — the harness `globalSetup` migrates the service and the role-guard + absence tests pass.
- Branch protection requiring `quality` is confirmed via the checkpoint.
</verification>

<success_criteria>
CI-01 and CI-02 are satisfied: every PR runs the full quality suite (incl. RLS isolation vs a real Postgres under unprivileged roles) and a red check blocks merge once branch protection is enabled.
</success_criteria>

<output>
Create `.planning/phases/04-staging-observability-ci-cd/04-03-SUMMARY.md` when done.
</output>
