---
phase: 11-d2-bandeja-de-leads-notificaci-n-por-email
plan: 05
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - apps/panel/app/proyectos/[id]/leads/page.tsx
  - apps/panel/app/proyectos/[id]/leads/leads-board.tsx
  - apps/panel/app/proyectos/[id]/leads/lead-card.tsx
  - apps/panel/app/proyectos/[id]/leads/lead-drawer.tsx
  - apps/panel/app/proyectos/[id]/leads/desenlace-prompt.tsx
  - apps/panel/app/proyectos/[id]/leads/alta-lead-form.tsx
  - apps/panel/app/proyectos/[id]/leads/notify-email-field.tsx
autonomous: true
requirements: [LEADS-01, LEADS-02, LEADS-03, LEADS-04]
must_haves:
  truths:
    - "The kanban board renders 4 fixed columns (`nuevo`→`contactado`→`negociacion`→`cerrado`), one per stage, on the Hormigón canvas — the visual anchor (D-08)."
    - "Moving a card = a pipeline transition via `leads.updateEstado`, with TWO equivalent paths: drag a card between columns (mouse) AND an accessible `<select>` in the drawer (the keyboard-complete path — a11y parity; drag is a mouse enhancement, never the only way) (LEADS-02/D-02)."
    - "Transitions are free between the 4 enum values (advance/retrocede/reopen a cerrado); the destination is always one of the 4 (never a free/arbitrary state)."
    - "Any transition INTO `cerrado` opens the desenlace prompt (`Ganado`/`Perdido`) BEFORE committing; cancel aborts the move (card returns to origin, estado unchanged); confirm writes estado=`cerrado`+`desenlace` (required-choice, D-03)."
    - "The drawer opens over the board (no navigation away, D-09): header (nombre + estado select + close `aria-label=\"Cerrar\"`), `Actividad` timeline (chronological transitions+notes merged by `ts`, autor visible), `Agregar nota` form."
    - "A lead card shows nombre (primary line, emphasis via Grafito+size not weight), contacto (muted Gris-500), the origen chip resolved by join (broker→`Broker · {nombre}` / unidad→`Unidad {identificador}` / cotización→`Cotización` / null→`Directo`), and an 8px stage/desenlace dot; the card carries `aria-label=\"Arrastrar para mover\"` on the drag handle (LEADS-01)."
    - "Alta-manual (`Registrar lead`) inserts a lead + enqueues the idempotent email; the success message asserts the queued email (`Listo. El lead quedó en Nuevo y avisamos por email.`) and the UI never blocks on the send (idempotency-feedback, LEADS-04/D-06)."
    - "The notify-email settings field edits `projects.leadsNotifyEmail` via the extended `projects.updateSettings` (Zod `.email()`, nullable); invalid input shows `Ingresá un email válido` inline; helper explains the org-owner fallback (D-05)."
    - "Feedback is inline `role=\"status\"` (success) / `role=\"alert\"` (error) — NO global toast (Phase 10 D-04); tokens/fonts consumed from the existing `tokens.css` as-is (no new token file)."
    - "empty/board: 0 leads → board empty-state heading `Todavía no hay leads` + body + `Registrar lead` CTA."
    - "empty/column: a stage with 0 cards → `Sin leads en esta etapa`, still a valid drop target."
    - "empty/timeline: defensive `Sin actividad todavía`; seeded/timeline: every lead has a creation `LeadNote` at t=0 so a fresh drawer is never empty on first open."
    - "empty/add-note: empty textarea → `Agregar nota` submit disabled."
    - "loading/board: skeleton cards per column on initial fetch; loading/drawer: drawer loading state before timeline resolves; loading/alta+note+estado: submitting/saving disables the control + shows a spinner; drag uses an optimistic move."
    - "populated/board: 14 seed leads distributed; populated/card: nombre+contacto+origen chip+stage dot; populated/timeline: chronological merge by ts with autor; populated/cerrado card: `Ganado`/`Perdido` badge via desenlace split colors (green/red)."
    - "partial/board: uneven column distribution is the normal shape; per-column count reflects it. zero-one-many/column count: 0 → `Sin leads` · N → `{N}` badge. zero-one-many/board: 0 → empty state · ≥1 → columns with cards."
    - "overflow/board: narrow laptop → horizontal scroll, 320px columns hold (desktop-first). overflow/column: many cards → vertical scroll within the column body."
    - "error/board: data-fetch failure → `No pudimos cargar los leads. Reintentá.` + retry. error/estado transition: `No se pudo guardar. Reintentá.` and the optimistic move reverts. error/add-note: inline `role=\"alert\"`, note not appended, textarea preserved. error/notify-email: `Ingresá un email válido` inline."
    - "The estado enum is stored ASCII (`negociacion`) but always rendered accented (`Negociación`); the raw enum value is never surfaced to the user."
    - statement: "long-text/lead card: a long `nombre`/`contacto` truncates + shows a `title` tooltip; it must NOT break the 88px card min-height or push the origen chip out."
      verification: backstop
    - statement: "long-text/timeline: a very long free note wraps/clamps inside the drawer without pushing the `Agregar nota` form out of view."
      verification: backstop
    - statement: "overflow/drawer: at 480px on a short viewport the timeline scrolls internally while the header + note form remain visible (no whole-page scroll hijack)."
      verification: backstop
  artifacts:
    - apps/panel/app/proyectos/[id]/leads/leads-board.tsx
    - apps/panel/app/proyectos/[id]/leads/lead-card.tsx
    - apps/panel/app/proyectos/[id]/leads/lead-drawer.tsx
    - apps/panel/app/proyectos/[id]/leads/desenlace-prompt.tsx
    - apps/panel/app/proyectos/[id]/leads/alta-lead-form.tsx
    - apps/panel/app/proyectos/[id]/leads/notify-email-field.tsx
  key_links:
    - "leads.listForProject / create / updateEstado / addNote (Plan 03) ← consumed via useTRPC islands."
    - "projects.updateSettings (extended, Plan 03) ← notify-email settings field."
    - "leads/page.tsx server spine (Phase 9) ← kept verbatim; only the <main> body mounts the client board."
  prohibitions:
    - statement: "The UI MUST NOT surface the raw ASCII enum value `negociacion` to the user — the column title, stage label, and timeline copy always render the accented `Negociación`."
      category: transparency
      verification: judgment
---

<objective>
Build the leads bandeja: a drag-and-drop kanban board with a lead drawer (timeline + add-note + estado
select), the desenlace prompt, the alta-manual form, and the notify-email settings field — a sibling
styled surface to the units grid, reusing the settled panel tokens/wiring/feedback (D-08/D-09/D-10).

Purpose: the developer-facing surface for LEADS-01..04. Output: the replaced server spine body + six
client islands, all copy verbatim from the UI-SPEC.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-CONTEXT.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan)

- Replaced `leads/page.tsx` body mounting the client board (server spine kept verbatim).
- `leads-board.tsx` (kanban root: columns, counts, drag, states), `lead-card.tsx`, `lead-drawer.tsx`
  (timeline + add-note + estado select), `desenlace-prompt.tsx`, `alta-lead-form.tsx`,
  `notify-email-field.tsx`.

<tasks>

<task type="auto">
  <name>Task 1: Server spine + kanban board (columns/cards/counts/states) + drawer read</name>
  <read_first>
    - apps/panel/app/proyectos/[id]/leads/page.tsx (KEEP the spine verbatim: await params → z.uuid() → notFound → resolveProject → notFound → resolveActiveRole → canWrite; replace only the <main> body)
    - apps/panel/app/proyectos/[id]/unidades/page.tsx (how the server page mounts a client island in TRPCReactProvider)
    - apps/panel/app/proyectos/[id]/unidades/units-grid.tsx (L1-95 — useTRPC()/useQuery/useMutation/invalidateQueries wiring, ESTADO_DOT record idiom, canWrite cosmetic comment, inline role=status/alert feedback)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-UI-SPEC.md (Component Inventory, Color stage table, Spacing fixed dims, Copywriting Contract — verbatim copy)
  </read_first>
  <files>apps/panel/app/proyectos/[id]/leads/page.tsx, apps/panel/app/proyectos/[id]/leads/leads-board.tsx, apps/panel/app/proyectos/[id]/leads/lead-card.tsx, apps/panel/app/proyectos/[id]/leads/lead-drawer.tsx</files>
  <action>
    Keep the `leads/page.tsx` server spine verbatim; replace the placeholder `<main>` body with the H1
    `{project.nombre} · Leads` (Space Grotesk) and `<TRPCReactProvider><LeadsBoard projectId={id}
    canWrite={canWrite} /></TRPCReactProvider>` (mirror `unidades/page.tsx`). Build `leads-board.tsx`
    (`"use client"`) consuming `useQuery(trpc.leads.listForProject.queryOptions({ projectId }))`: render
    4 fixed 320px columns in stage order with a header (title + 8px stage dot + count badge:
    0→`Sin leads`, N→`{N}`), each column a scrollable card list and a drop target. Render `LeadCard`
    (nombre primary line, contacto muted Gris-500, origen chip resolved from the row, stage/desenlace
    dot; drag handle `aria-label="Arrastrar para mover"`; click opens the drawer). Build `lead-drawer.tsx`
    (right 480px panel) opening on card click with header (nombre + estado `<select>` + close
    `aria-label="Cerrar"`) and the `Actividad` timeline rendering `leads.timeline` ordered by `ts` with
    autor visible. Implement the state coverage: board loading (skeleton cards), board empty
    (`Todavía no hay leads` + body + `Registrar lead` CTA), column empty (`Sin leads en esta etapa`,
    still droppable), board error (`No pudimos cargar los leads. Reintentá.` + retry), overflow
    (horizontal board scroll on narrow width; vertical scroll within a column). Render `Negociación`
    accented everywhere (never the raw `negociacion`). Stage dot colors per the UI-SPEC Color table
    (Blueprint/Gris-500/amber; cerrado split green/red by desenlace). Consume `tokens.css` as-is.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `leads/page.tsx` spine (z.uuid guard, resolveProject notFound, resolveActiveRole/canWrite) unchanged; body mounts `<LeadsBoard>` in `TRPCReactProvider`.
    - Source assertion: the board reads via `trpc.leads.listForProject`; the 4 columns render in stage order with count badges (0 → `Sin leads`).
    - Source assertion: column titles + labels render `Negociación` accented; no user-facing render of the literal `negociacion`.
    - Behavior assertion: board loading/empty/error and column empty states render the exact UI-SPEC copy.
    - `pnpm --filter @imbau/panel typecheck` + `lint` pass.
  </acceptance_criteria>
  <done>Server spine mounts the board; 4-column kanban with cards, counts, drawer read, and all read-side states.</done>
</task>

<task type="auto">
  <name>Task 2: Transitions (drag + estado select parity) + desenlace prompt + add-note + optimistic feedback</name>
  <read_first>
    - apps/panel/app/proyectos/[id]/unidades/units-grid.tsx (useMutation + invalidateQueries refetch; inline role=status/alert feedback; saving/disabled patterns)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-UI-SPEC.md (Interaction Contracts: move=transition, cerrado requires desenlace, every transition leaves a trail; Copywriting Contract: desenlace prompt copy, note copy, save error)
    - packages/api/src/trpc/routers/leads.ts (Plan 03 — updateEstado/addNote input shapes, cerrado refine)
  </read_first>
  <files>apps/panel/app/proyectos/[id]/leads/leads-board.tsx, apps/panel/app/proyectos/[id]/leads/lead-drawer.tsx, apps/panel/app/proyectos/[id]/leads/desenlace-prompt.tsx</files>
  <action>
    Wire `leads.updateEstado` from BOTH paths: native HTML5 drag on cards (drop-target column shows a
    2px cobre ring while hovered; on drop → optimistic move → saving → confirmed or revert on error) AND
    the drawer estado `<select>` over the 4 stages (the keyboard-complete path — identical mutation).
    Any transition INTO `cerrado` (drop on Cerrado column OR select→Cerrado) opens `desenlace-prompt.tsx`
    (modal: heading `¿Cómo cerró este lead?`, options `Ganado`/`Perdido`, body copy, confirm
    `Guardar desenlace` / cancel `Cancelar cierre`) BEFORE the write; cancel aborts the move (card
    returns, estado unchanged); confirm calls updateEstado with `estado:"cerrado"` + `desenlace`. In the
    drawer, add the `Agregar nota` textarea (placeholder `Escribí una nota…`, submit disabled while
    empty) → `leads.addNote`; on success invalidate the board query so the timeline refreshes. Feedback:
    inline `role="status"` on success, `role="alert"` (`No se pudo guardar. Reintentá.`) on error with
    the optimistic move reverting and the note textarea preserved. NO global toast. The desenlace prompt
    is a NEUTRAL required-choice (cobre confirm), never a red/danger confirmation.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both drag-drop AND the drawer `<select>` call `leads.updateEstado` (a11y parity); the select path does not depend on drag.
    - Source assertion: entering `cerrado` opens the desenlace prompt before the write; cancel does not call updateEstado; confirm passes `desenlace`.
    - Source assertion: add-note submit is disabled on empty textarea and calls `leads.addNote`; error path uses `role="alert"` and preserves the textarea.
    - Behavior assertion: a transition error reverts the optimistic move.
    - `pnpm --filter @imbau/panel typecheck` + `lint` pass.
  </acceptance_criteria>
  <done>Both transition paths, the desenlace gate, add-note, and optimistic inline feedback wired to the router.</done>
</task>

<task type="auto">
  <name>Task 3: Alta-manual form + notify-email settings field</name>
  <read_first>
    - apps/panel/app/proyectos/[id]/unidades/units-grid.tsx (form/mutation/feedback idiom to mirror)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-UI-SPEC.md (Copywriting: alta-manual fields/validation/success; settings notify-email label/helper/invalid/save)
    - packages/api/src/trpc/routers/leads.ts (create input) + projects.ts (updateSettings extended, Plan 03)
  </read_first>
  <files>apps/panel/app/proyectos/[id]/leads/alta-lead-form.tsx, apps/panel/app/proyectos/[id]/leads/notify-email-field.tsx</files>
  <action>
    Build `alta-lead-form.tsx` (modal or drawer, opened by the `Registrar lead` CTA): fields `Nombre`
    (req), `Contacto` (req, teléfono o email), `Origen` (Directo / Broker / Unidad / Cotización) →
    `leads.create`. Empty-required validation copy `Ingresá el nombre` / `Ingresá un contacto (teléfono
    o email)`. On success show inline `role="status"` `Listo. El lead quedó en Nuevo y avisamos por
    email.` and invalidate the board query; the UI never blocks on the email send. Build
    `notify-email-field.tsx`: a minimal field editing `projects.leadsNotifyEmail` via
    `projects.updateSettings` (label `Email para avisos de leads`, helper `Te avisamos acá cada vez que
    entra un lead nuevo. Si lo dejás vacío, el aviso va a los owners de la organización.`, invalid copy
    `Ingresá un email válido`, save `Guardar email`, cobre primary button + focus ring). Both forms use
    the cobre focused-input ring per the UI-SPEC accent reservation.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint && pnpm --filter @imbau/panel build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `alta-lead-form.tsx` calls `leads.create` and renders the success copy asserting the queued email (`…avisamos por email`).
    - Source assertion: `notify-email-field.tsx` calls `projects.updateSettings` with `leadsNotifyEmail`; invalid input shows `Ingresá un email válido`; helper explains the owner fallback.
    - `pnpm --filter @imbau/panel typecheck` + `lint` + `build` pass.
  </acceptance_criteria>
  <done>Alta-manual form (create→queued email) and notify-email settings field wired; panel builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC / tRPC | client renders + writes; `canWrite` is cosmetic only |
| rendered board → screen | tenant-scoped lead PII displayed to the developer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Elevation of Privilege | UI write affordances | medium | mitigate | `canWrite` hides controls as defense-in-depth ONLY; the server `requireRole` (Plan 03) is the authority — a viewer's direct call is rejected FORBIDDEN regardless of UI. |
| T-11-13 | Information Disclosure | rendered lead PII | medium | mitigate | Board data comes solely from the tenant-scoped `leads.listForProject` (RLS); no cross-tenant lead can be fetched or rendered. |
| T-11-SC | Tampering | npm/pnpm installs | low | mitigate | No new external packages required; `lucide-react` (optional icons) is MIT and already vetted in Phase 10; inline SVG is acceptable. |
</threat_model>

<verification>
- `pnpm --filter @imbau/panel typecheck` + `lint` + `build` green.
- All UI copy verbatim from the UI-SPEC Copywriting Contract; `Negociación` rendered accented.
- The 3 backstop considerations (long-text card, long-text timeline, drawer overflow) route to a
  held-out visual check at phase verify time (abstain → human_needed if unconfirmed).
</verification>

<success_criteria>
The bandeja delivers LEADS-01..04 in the panel: origen-traced cards across a 4-stage kanban, free
transitions with the desenlace gate, an append-only timeline with add-note, alta-manual that enqueues
the idempotent email, and the notify-email settings field — all on the settled tokens/wiring/feedback.
</success_criteria>

<output>
Create `.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-05-SUMMARY.md` when done.
</output>
