---
phase: 08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - ".planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md"
autonomous: true
requirements: [DEBT-02]
must_haves:
  truths:
    - "A parallel burst above rate=10r/s + burst=20 to `https://staging.tours.andescode.com.ar/api/trpc/quotes.compute?batch=1` returns HTTP 429 for the excess and ZERO HTTP 503; requests within the burst budget pass through to the app as real tRPC responses (fase-5 code is now live, not the pre-merge 404)."
    - "The nginx `quotes` zone hit is confirmed in `/var/log/nginx/error.log` (`limiting requests ... by zone \"quotes\"`) over SSH, and the on-box vhost equals the repo source-of-truth `deploy/nginx/staging.tours.andescode.com.ar.conf` (v1.2 D-12) — the vhost stays intact after the burst (web 200 / panel 307)."
    - "The end-to-end PDF flow on staging produces a downloadable PDF whose text contains correct es-AR accented glyphs (\"Cotización\", \"índice\") rendered with embedded Roboto fonts (pdffonts shows Roboto-Regular/Bold, no tofu)."
    - "The PDF footer deep-link and the embedded QR both point to `https://staging.tours.andescode.com.ar/...` (not localhost, not a pre-fase-5 image) (D-11)."
    - "Smoke of live surfaces passes: web home returns 200, `/p/brigos-recoleta/cotizador` returns 200, panel login returns 307, the worker is alive in its logs, and Sentry/Loki are receiving (D-04)."
    - "The full run is captured as command + real output in `08-UAT.md` following the 05/07-UAT.md evidence pattern (D-10)."
    - statement: "A concurrent (parallel) burst never surfaces a 503 — every rejection is a 429 (concurrency guarantee under load)."
      verification: backstop
    - statement: "At the limit, a request within the rate=10r/s + burst=20 budget passes and the next request over budget is rejected 429 (one step either side of the threshold)."
      verification: backstop
  prohibitions:
    - statement: "The rate-limit rejection status must be 429, never 503 — the fase-6 retry client and the QUOTE-03 contract key off 429 (nginx conf CITED)."
      status: flagged-unverified
    - statement: "Do not reconfigure the box nginx rate-limit divergent from the repo file; the repo file is the source of truth — sync back if tuned in place (v1.2 D-12)."
      status: flagged-unverified
    - statement: "On the shared VPS, do not touch the prod nginx/host; inspect any process's identity before any kill (D-08, MEMORY never-kill-unverified-pid)."
      status: flagged-unverified
  artifacts:
    - ".planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md"
  key_links:
    - "quotes.compute path → nginx `^~ /api/trpc/quotes` location → `quotes` zone throttle (429, not 503)"
    - "quote create → worker BullMQ job → R2 PDF → presigned GET → download"
    - "quotes.pdfStatus poll → ready → download URL → pdftotext/pdffonts/QR inspection"
---

<objective>
Re-verify all of v1.2 live in staging now that the merge (08-01) put the real images on the VPS, and record the evidence. Prove the three live gaps from the ROADMAP success criteria: (2) the nginx edge rate-limit fires 429 (never 503) under a burst to `quotes.*`, with the box vhost matching the repo source of truth (v1.2 D-12); (3) the end-to-end PDF flow works — cotización → async PDF → download with correct es-AR accents and embedded Roboto; (4) the PDF QR and deep-link point to the staging URL (D-11). Add a light smoke of the live surfaces (home, cotizador, panel login, worker alive, Sentry/Loki receiving — D-04). Do NOT re-run the full v1.2 UAT — only the infra changed, the code is already verified (D-04). All checks are scripted/repeatable where trivial (parallel burst for the 429, `quotes.pdfStatus` poll) with captured output (D-10), and the result is documented in `08-UAT.md` in the phase directory following the 05/07-UAT.md pattern. Claude runs the whole cycle via curl + SSH (`root@31.97.175.128`, key `id_vps_andescode`), only escalating on real infra blockers (D-09). This satisfies DEBT-02.

Purpose: DEBT-02 is the live confirmation that the merged infra actually behaves — the rate-limit contract, the PDF pipeline, and staging-correct links — before any panel feature builds on it.
Output: `08-UAT.md` with pass/fail evidence for the 429 burst, the PDF e2e, the QR/deep-link, and the surface smoke.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md
@deploy/nginx/staging.tours.andescode.com.ar.conf
@.planning/milestones/v1.2-phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-UAT.md
@.planning/milestones/v1.2-phases/07-pdf-as-ncrono-en-el-worker/07-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the 429 edge rate-limit burst on staging + nginx conf parity (DEBT-02 crit. 2, v1.2 D-12)</name>
  <files>deploy/nginx/staging.tours.andescode.com.ar.conf (read-only source of truth; sync-back edit ONLY if box tuned)</files>
  <read_first>
    - deploy/nginx/staging.tours.andescode.com.ar.conf (lines 29-34 burst recipe; line 44 `limit_req_zone ... zone=quotes:10m rate=10r/s`; lines 83-85 `location ^~ /api/trpc/quotes { limit_req zone=quotes burst=20 nodelay; limit_req_status 429; }` — the source of truth)
    - .planning/milestones/v1.2-phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-UAT.md (the prior burst run: 40 parallel POSTs → excess 429, zero 503)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md (D-08 shared-VPS safety, D-10 scripted checks)
  </read_first>
  <action>
    Run a parallel burst of 40 requests to `https://staging.tours.andescode.com.ar/api/trpc/quotes.compute?batch=1` with parallelism above the burst budget (e.g. `seq 1 40 | xargs -P 20 -I{} curl -s -o /dev/null -w "%{http_code}\n" "<url>"`, per 05-UAT.md), tallying the status distribution. Assert the excess above rate=10r/s + burst=20 returns 429 and that ZERO responses are 503 — the reject status MUST be 429 (nginx conf CITED). Now that fase-5 is live, the passing requests return real tRPC responses (200 / 4xx), NOT the pre-merge 404 the 05-UAT saw. Over SSH (`root@31.97.175.128`, key `id_vps_andescode`, D-08 — never touch prod nginx/host, inspect identity before any kill) confirm the zone fired in `/var/log/nginx/error.log` (`limiting requests ... by zone "quotes"`) and assert the box vhost equals the repo file `deploy/nginx/staging.tours.andescode.com.ar.conf` (diff the box copy in `/etc/nginx/sites-available/` against the repo file — v1.2 D-12; if the box was tuned, sync the value back into the repo file). Confirm the vhost survived the burst: web home 200, panel 307. Capture the raw status tally + error.log lines + diff result as evidence for 08-UAT.md (D-10). Interpret the edge boundary explicitly: the request within budget passes (200/4xx) and the next over budget is rejected 429 (one step either side), and under concurrency every rejection is 429 not 503 (backstop guarantees).
  </action>
  <verify>
    <automated>c=$(seq 1 40 | xargs -P 20 -I{} curl -s -o /dev/null -w "%{http_code}\n" "https://staging.tours.andescode.com.ar/api/trpc/quotes.compute?batch=1"); echo "$c" | grep -qx 429 && ! echo "$c" | grep -qx 503</automated>
  </verify>
  <acceptance_criteria>
    - The 40-request parallel burst yields at least one HTTP 429 and zero HTTP 503 (exact contract; 429 not 503).
    - Passing requests return real tRPC responses (200 or 4xx-class), not 404 (confirms fase-5 code is live post-merge).
    - `/var/log/nginx/error.log` on the VPS contains a `limiting requests ... by zone "quotes"` line dated to the burst.
    - `diff` of `/etc/nginx/sites-available/staging.tours.andescode.com.ar.conf` (box) against the repo `deploy/nginx/...conf` shows the `quotes` zone (`rate=10r/s`) and location (`burst=20 nodelay; limit_req_status 429`) identical — or, if tuned, the repo file is updated to match (v1.2 D-12).
    - Post-burst: `curl -s -o /dev/null -w "%{http_code}" https://staging.tours.andescode.com.ar/` = 200 and the panel host returns 307 (vhost intact).
  </acceptance_criteria>
  <done>The staging edge rate-limit rejects excess quotes traffic with 429 (never 503), the zone hit is logged, and the box vhost matches the repo source of truth (v1.2 D-12).</done>
</task>

<task type="auto">
  <name>Task 2: Verify the end-to-end PDF flow + QR/deep-link + surface smoke on staging (DEBT-02 crit. 3-4, D-04, D-11)</name>
  <files>cotizacion.pdf (transient download for inspection) — no repo file modified; reads packages/api/src/trpc/routers/quotes.ts</files>
  <read_first>
    - .planning/milestones/v1.2-phases/07-pdf-as-ncrono-en-el-worker/07-UAT.md (the PDF e2e recipe: trigger → poll quotes.pdfStatus → download → pdffonts/pdftotext, QR/deep-link, soft-fail path)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md (D-04 smoke scope, D-11 QR/deep-link tooling is executor's choice)
    - packages/api/src/trpc/routers/quotes.ts (procedures `compute`, `create`, `pdfStatus` — the exact tRPC surface to drive)
  </read_first>
  <action>
    Drive the full PDF flow against staging: create a quote for a seeded 'Brigos Recoleta' unit (via `quotes.create` through the public cotizador `/p/brigos-recoleta/cotizador`, or a scripted tRPC POST), then poll `quotes.pdfStatus` until the job reports ready (D-10) and download the generated `cotizacion.pdf` from its presigned R2 URL. Inspect the PDF: `pdffonts cotizacion.pdf` must show `Roboto-Regular`/`Roboto-Bold` embedded (CID TrueType, no tofu); `pdftotext cotizacion.pdf -` must contain the es-AR accented glyphs "Cotización" and "índice", the two legal legends, and the full header (proyecto/unidad/piso/tipología/m²/fecha/CAC/ref). Verify the QR + deep-link (D-11): extract the `?u=&plan=` deep-link from the PDF text and assert it starts with `https://staging.tours.andescode.com.ar/` (NOT localhost, NOT a pre-fase-5 host); decode the embedded QR by rasterizing the PDF page and running a local decoder (e.g. `pdftoppm` + `zbarimg`) if the tooling is available — executor picks the tooling — and assert the decoded URL also targets the staging host. Then run the surface smoke (D-04): web home 200, `/p/brigos-recoleta/cotizador` 200, panel login 307, worker alive (SSH: `docker compose -f deploy/compose.staging.yml logs --tail=50 worker` shows recent activity / no crash loop — inspect identity, never kill, D-08), and Sentry/Loki receiving (worker/app logs reaching Loki, no Sentry init error in logs). Capture every command + output as evidence for 08-UAT.md (D-10).
  </action>
  <verify>
    <automated>test -f cotizacion.pdf && pdffonts cotizacion.pdf | grep -q Roboto && pdftotext cotizacion.pdf - | grep -q "Cotización" && pdftotext cotizacion.pdf - | grep -o 'https://staging.tours.andescode.com.ar/[^ ]*' | head -1 | grep -q '^https://staging.tours.andescode.com.ar/'</automated>
  </verify>
  <acceptance_criteria>
    - A quote created on staging reaches `quotes.pdfStatus` = ready and the `cotizacion.pdf` downloads from its presigned R2 URL.
    - `pdffonts cotizacion.pdf` lists `Roboto-Regular` and `Roboto-Bold` as embedded (no tofu).
    - `pdftotext cotizacion.pdf -` contains "Cotización" and "índice" (correct es-AR ó/í glyphs), the two legal legends, and the full header fields.
    - The PDF deep-link extracted from the text starts with `https://staging.tours.andescode.com.ar/` and the decoded QR (if decoded) targets the same staging host — never localhost or a pre-fase-5 host (D-11).
    - Surface smoke: web `/` = 200, `/p/brigos-recoleta/cotizador` = 200, panel login = 307; worker logs show live activity without a crash loop; Loki/Sentry show recent ingest (D-04).
  </acceptance_criteria>
  <done>The staging PDF pipeline works end-to-end with correct es-AR accents and embedded fonts, the QR/deep-link target the staging host, and the live surfaces smoke-pass — DEBT-02 criteria 3-4 met.</done>
</task>

<task type="auto">
  <name>Task 3: Author 08-UAT.md evidence document (D-10)</name>
  <files>.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-UAT.md (frontmatter + per-test `expected:`/`result:`/`evidence: |` + Summary/Gaps tail pattern)
    - .planning/milestones/v1.2-phases/07-pdf-as-ncrono-en-el-worker/07-UAT.md (multi-test evidence structure)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-PATTERNS.md (`08-UAT.md` analog + structure spec)
  </read_first>
  <action>
    Write `.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md` following the 05/07-UAT.md pattern: frontmatter (`status: complete`, `phase: 08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging`, `source: [08-01-SUMMARY.md, 08-02-SUMMARY.md]`, `started`/`updated` ISO8601), a `## Current Test` = `[testing complete]`, then a `## Tests` section with one block per check — the 429 burst (Task 1), the PDF e2e + QR/deep-link (Task 2), and the surface smoke (Task 2) — each with `expected:`, `result: pass|fail`, and a multiline `evidence: |` capturing the exact commands run (SSH host/key named, exact URLs, observed status distribution, pdffonts/pdftotext output, decoded QR/deep-link, diff result) plus real output, matching the disciplined style of 05-UAT.md (cross-reference any non-obvious pass, e.g. "passing requests now 200/4xx not 404 because fase-5 is live"). Close with the `## Summary` totals block (`total`/`passed`/`issues`/`pending`/`skipped`/`blocked`) and `## Gaps`. Record the deferred fase-6 visual pass under Gaps as an explicitly deferred item (D-06), not a fail. Never paste secret values into the evidence (T-08-04). Use `Write` for this net-new file.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md && grep -q '^status: complete' .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md && grep -cE '^result: pass' .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `08-UAT.md` exists in the phase directory with frontmatter `status: complete` and `phase: 08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging`.
    - It contains one evidence block per check (429 burst, PDF e2e, surface smoke), each with `expected:`, `result:`, and a multiline `evidence: |` holding the real captured commands + output.
    - The `## Summary` totals reconcile (`passed` + `issues` + `pending` + `skipped` + `blocked` = `total`) and `## Gaps` lists the deferred fase-6 visual pass (D-06) as deferred, not failed.
    - No secret values appear in the evidence (T-08-04).
  </acceptance_criteria>
  <done>`08-UAT.md` records reproducible command+output evidence for all DEBT-02 checks, closing the phase's verification with an auditable artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → nginx edge → quotes tRPC | Untrusted burst traffic crosses the edge into the anonymous quotes funnel. |
| operator SSH → shared prod VPS | Log inspection on a box that also hosts live prod. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06 | Denial of Service | `/api/trpc/quotes*` anonymous funnel | high | mitigate | Verify live that the edge `quotes` zone (10r/s + burst=20, `limit_req_status 429`) rejects excess with 429 and zero 503; confirm box vhost == repo source of truth (Task 1). |
| T-08-07 | Information Disclosure | generated PDF deep-link / QR | low | accept | The deep-link is a public showroom URL by design; assert it targets the staging host only (D-11); no secrets are embedded in the PDF. |
| T-08-08 | Spoofing / Tampering | staging host response provenance | low | mitigate | Assert responses come from `staging.tours.andescode.com.ar` over TLS (single SAN cert); confirm vhost intact (web 200 / panel 307) after the burst. |
| T-08-09 | Elevation of Privilege | SSH `root@` shared prod VPS (log reads) | medium | mitigate | Read-only log/container inspection of imbau services; never touch prod nginx/host; inspect process identity before any kill (D-08). |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this verification phase; PDF-inspection tooling (pdffonts/pdftotext/zbarimg) is local OS tooling, not a project dependency. |
</threat_model>

<verification>
- DEBT-02 satisfied: burst → 429 with zero 503 + zone logged + vhost parity (crit. 2); PDF e2e with es-AR accents + embedded Roboto (crit. 3); QR/deep-link target staging host (crit. 4); surface smoke green; all recorded in `08-UAT.md`.
- Edge-probe coverage (spec-less fallback): DEBT-02 boundary (request within budget passes / next over budget 429) → truth + one-step backstop; precision (es-AR accented glyphs render correctly) → truth; concurrency (parallel burst → 429 never 503) → truth + backstop. No edge item dropped.
- Prohibitions recorded (descriptor-less): reject must be 429 not 503; no box/repo nginx divergence (v1.2 D-12); no prod-host touch / inspect before kill (D-08).
</verification>

<success_criteria>
ROADMAP Phase 8 criteria 2-4 are met live on `staging.tours.andescode.com.ar`: a burst to `quotes.*` fires 429 (no 503), the full PDF flow works with correct es-AR accents, and the QR + deep-link point to the staging URL — all documented in `08-UAT.md`.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08-02)

- **`.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-UAT.md`** — the verification evidence document with per-check `expected/result/evidence` blocks (429 burst, PDF e2e, QR/deep-link, surface smoke), Summary totals, and Gaps (deferred fase-6 visual pass, D-06).
- **(transient, not committed)** downloaded `cotizacion.pdf` from staging used for pdffonts/pdftotext/QR inspection.
- **(transient, not committed)** captured burst status tally + `/var/log/nginx/error.log` lines + box-vs-repo nginx diff, folded into `08-UAT.md`.
- **(possible)** a sync-back edit to `deploy/nginx/staging.tours.andescode.com.ar.conf` ONLY if the box rate-limit was tuned during UAT (v1.2 D-12) — otherwise unchanged.
</artifacts_produced>

<output>
Create `.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-02-SUMMARY.md` when done (link `08-UAT.md`, record the burst status distribution, the PDF quoteId, and the QR/deep-link decode result; note whether the nginx repo file needed a sync-back).
</output>
