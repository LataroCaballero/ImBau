---
phase: 05-emisi-n-y-persistencia-server-side-api-rls-rate-limit
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/api/tests/quotes-router.test.ts
autonomous: true
requirements: [QUOTE-01, QUOTE-02]
must_haves:
  truths:
    - "An anon tRPC caller (empty headers) invoking quotes.compute on a publicado project gets a financiado QuoteResult whose version === ENGINE_VERSION, using the max(periodo) CAC row — proven against real Postgres (QUOTE-01, D-03, D-07)."
    - "quotes.create persists exactly one quotes row whose snapshot is {version:1, inputs, result, cacPeriodo}; the row is readable under withTenant(org) — proven by test (QUOTE-02, D-04)."
    - "quotes.compute writes NO quotes row (D-01) — proven by a row-count assertion."
    - "A non-publicado project rejects with NOT_FOUND; an org with no CAC rejects with PRECONDITION_FAILED (not a 500); a degenerate plan rejects with BAD_REQUEST carrying data.quoteErrorCode (D-08)."
    - "cac_index and quotes remain tenant-private: an anon-pool SELECT on cac_index and an anon-pool INSERT into quotes both raise Postgres 42501 (no anon policy was added — Pitfall 5)."
  artifacts:
    - packages/api/tests/quotes-router.test.ts
  key_links:
    - "createCaller({ headers: new Headers() }) → appRouter.quotes — the anonymous production context shape"
    - "owner-pool seed (project publicado + floor + unit + price_lists + unit_prices + payment_plan + cac_index) → the rows resolveAndQuote re-reads via withTenant"
    - "withAnon direct-table probes prove 42501 on the tenant-private tables"
---

<objective>
Prove the QUOTE-01/QUOTE-02 behavior end-to-end with an integration test through the tRPC caller against real Postgres (`_test` DB), cloning the `trpc-tenant.test.ts` harness: owner-pool seeding, `makeUserWithActiveOrg` org minting, anon caller via empty `Headers`. This is the phase's Nyquist validation contract — the automated evidence that the anonymous emission path resolves the org server-side, reads CAC via the app pool, persists the versioned snapshot, and never opens `quotes`/`cac_index` to the anon role.

Purpose: 05-01's router is only structurally verified (typecheck/lint/grep); this plan supplies the behavioral proof for every QUOTE-01/QUOTE-02 success criterion.
Output: `packages/api/tests/quotes-router.test.ts` — green under `pnpm --filter @imbau/api test`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-RESEARCH.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-01-SUMMARY.md

# Harness + analogs to clone
@packages/api/tests/trpc-tenant.test.ts
@packages/api/tests/fixtures.ts
@packages/api/tests/db.ts
@packages/api/tests/setup.ts
@packages/api/src/trpc/routers/quotes.ts
@packages/db/src/schema/quotes.ts
@packages/db/src/schema/cac-index.ts
@packages/db/src/schema/payment-plans.ts
@packages/db/src/schema/unit-prices.ts
@packages/db/src/schema/price-lists.ts
@packages/db/src/schema/floors.ts
@packages/db/src/schema/units.ts
@packages/quoting/src/index.ts

# Local run prerequisite (not needed in CI — CI env is already wired):
# the `_test` DB + env exports per the developer's local-test-env-recipe
# (TEST_DATABASE_URL / DATABASE_APP_URL / DATABASE_ANON_URL + auth vars).
</context>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift/source-grounding):

- File `packages/api/tests/quotes-router.test.ts` (NEW)
- Test helper `seedQuoteFixtures(orgId, ...)` (local to the test file) seeding project/floor/unit/price_lists/unit_prices/payment_plan/cac_index through the owner pool
- Test helper constants for the seeded fixture (two price list rows named to match the "contado"/"financiado" classification, a CAC-adjusted payment plan, a degenerate plan with an out-of-range anticipo)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture seeding + happy-path compute/create tests</name>
  <files>packages/api/tests/quotes-router.test.ts</files>
  <read_first>
    - packages/api/tests/trpc-tenant.test.ts (the harness to clone: `createCaller`, `ownerSql()`, `makeUserWithActiveOrg`, `seedProject` owner-SQL insert shape, anon caller via `new Headers()`)
    - packages/api/tests/fixtures.ts (SessionFixture shape; org minting through the real auth runtime)
    - packages/api/tests/db.ts (ownerSql / `_test` DB guard)
    - packages/api/src/trpc/routers/quotes.ts (the exact input shape `{projectId, unitId, paymentPlanId, modalidad}`, the price-list classification rule, and the CAC max-periodo read the fixtures must satisfy)
    - packages/db/src/schema/floors.ts and packages/db/src/schema/units.ts (NOT NULL columns the owner-SQL seed must provide — units require organization_id, project_id, floor_id, identificador, estado default)
    - packages/db/src/schema/price-lists.ts, unit-prices.ts, payment-plans.ts, cac-index.ts (NOT NULL columns + natural keys for the owner-SQL seed; anticipoPct/valor are numeric → insert as strings; refuerzos is jsonb)
    - packages/quoting/src/index.ts (import `ENGINE_VERSION` for the version assertion)
  </read_first>
  <behavior>
    - Test "anon compute returns a financiado QuoteResult from the publicado project": caller with `new Headers()` calls `quotes.compute` with the seeded IDs + modalidad "financiado" → result.modalidad === "financiado", result.version === ENGINE_VERSION, result.cuotas.length === the seeded plan's cuotas, result.cuotas[0].ars is a non-null string, and result.cac.periodo equals the NEWEST seeded período (max-periodo rule D-07 — seed at least two periods and assert the later one is used).
    - Test "compute writes no quotes row": count quotes rows for the org via owner SQL before and after compute → identical (D-01).
    - Test "create persists the versioned snapshot": `quotes.create` returns `{ quoteId, result }`; owner SQL select of that row shows snapshot.version === 1, snapshot.result.version === ENGINE_VERSION, snapshot.cacPeriodo === the max período, snapshot.inputs.precioFinanciadoUsd === the seeded financiado price; the row is also readable through `withTenant(orgId)` (import withTenant from @imbau/db) proving the app-pool path sees it (QUOTE-02).
    - Test "contado compute returns the contado price": modalidad "contado" → result.precioUsd === seeded contado price, result.modalidad === "contado".
  </behavior>
  <action>
    Create the test file cloning the trpc-tenant harness verbatim: `const owner = ownerSql();`, `beforeAll` mints `orgA = await makeUserWithActiveOrg()` (and `orgB` for Task 2), `afterAll` ends the owner client. Write a local `seedQuoteFixtures(orgId)` helper that owner-SQL-inserts, in FK order: a `publicado` project (clone `seedProject`), one floor (numero 1), one unit on that floor (identificador e.g. "1A"), two price_lists — one whose `nombre` contains "Contado" (moneda USD) and one whose `nombre` contains "Financiado" (moneda USD) so the router's classification resolves both — two unit_prices for the unit (concrete integers, e.g. contado 88000 / financiado 100000, vigencia any fixed timestamp), one payment_plan (nombre "Plan test 30/70 CAC", anticipoPct '30.00' as string, cuotas 12, ajuste 'CAC', refuerzos '[{"cuota":6,"montoUsd":5000}]'::jsonb, notasLegales any text), and cac_index rows for TWO periods (e.g. ('2026-05','500000.0000') and ('2026-06','520000.0000')) so the max-periodo assertion is meaningful. Use `randomUUID()` ids returned from the helper as `{ projectId, unitId, paymentPlanId }`. Then write the four happy-path tests from <behavior> using `await createCaller({ headers: new Headers() })`. Import `ENGINE_VERSION` from `@imbau/quoting` and `withTenant`, `schema` from `@imbau/db` for the tenant-visibility assertion. All money assertions are exact integer/string equality — never approximate matchers.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && pnpm --filter @imbau/api test -- quotes-router</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api/tests/quotes-router.test.ts` exists and `pnpm --filter @imbau/api test -- quotes-router` exits 0 with the happy-path tests green.
    - The file contains `new Headers()` anon-caller usage and `makeUserWithActiveOrg` fixture usage.
    - An assertion compares `result.version` (or `snapshot.result.version`) to `ENGINE_VERSION` imported from `@imbau/quoting`.
    - An assertion proves the max-periodo CAC was used (the later seeded período appears in the result/snapshot).
    - A row-count assertion proves compute inserted zero quotes rows.
    - No approximate numeric matcher appears in the file (money is exact — integers/strings).
  </acceptance_criteria>
  <done>Anonymous compute/create happy paths are proven against real Postgres with the real RLS roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error-surface + tenant-privacy negative tests</name>
  <files>packages/api/tests/quotes-router.test.ts</files>
  <read_first>
    - packages/api/tests/quotes-router.test.ts (the file as left by Task 1 — extend, do not restructure)
    - packages/api/src/trpc/routers/quotes.ts (exact TRPCError codes/messages thrown: NOT_FOUND on unresolved project, PRECONDITION_FAILED on missing CAC, BAD_REQUEST + cause on engine rejection)
    - packages/api/src/trpc/init.ts (errorFormatter — data.quoteErrorCode surfaces on the caller's thrown TRPCClientError-like shape; with createCaller the raw TRPCError is thrown, so assert on the thrown error's `code` and `cause`)
    - packages/db/src/with-tenant.ts (withAnon — used to probe the anon pool directly)
    - packages/quoting/src/errors.ts (expected quoteErrorCode value for an out-of-range anticipo: ANTICIPO_PCT_FUERA_DE_RANGO)
  </read_first>
  <behavior>
    - Test "borrador project → NOT_FOUND": seed a borrador project (reuse the Task-1 project-insert shape with estado 'borrador') and call compute with its id → `.rejects.toMatchObject({ code: "NOT_FOUND" })`.
    - Test "org without CAC → PRECONDITION_FAILED": seed full fixtures for orgB but insert NO cac_index rows; compute with modalidad "financiado" → `.rejects.toMatchObject({ code: "PRECONDITION_FAILED" })` (the es-AR missing-CAC message, not a 500).
    - Test "degenerate plan → BAD_REQUEST with machine code": seed an extra payment_plan for orgA with anticipoPct '150.00' (out of range); compute financiado with it → rejects with `code: "BAD_REQUEST"` and the thrown error's `cause` is a QuoteError whose `.code === "ANTICIPO_PCT_FUERA_DE_RANGO"` (this is what the errorFormatter surfaces as data.quoteErrorCode over the wire).
    - Test "cac_index stays anon-invisible": `withAnon((tx) => tx.select().from(schema.cacIndex))` rejects with a Postgres permission error (code 42501).
    - Test "quotes stays anon-unwritable": a withAnon insert attempt into schema.quotes rejects with 42501.
  </behavior>
  <action>
    Extend the Task-1 file with a second describe block covering the five negative tests above. For the 42501 probes, import `withAnon` from `@imbau/db` and assert the rejection's Postgres error code — match on the driver error's `code` property equaling `"42501"` (postgres.js exposes it); if the property shape differs, assert the rejection message contains the permission-denied SQLSTATE semantics by checking `err.code === "42501"` via a caught-error inspection rather than a string match. For the degenerate-plan test, insert the out-of-range plan through the owner pool exactly like Task 1's plan insert but with anticipoPct '150.00'. Keep every rejection assertion on the typed `code` — never assert on stack traces or internal messages.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && pnpm --filter @imbau/api test -- quotes-router && pnpm --filter @imbau/api test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/api test` (full api suite) exits 0 — the new tests are green and no existing test regressed.
    - The file asserts rejection codes NOT_FOUND, PRECONDITION_FAILED, and BAD_REQUEST for the three error paths.
    - The degenerate-plan test asserts the machine-readable engine code ANTICIPO_PCT_FUERA_DE_RANGO on the error cause.
    - Two 42501 probes exist: an anon SELECT on cac_index and an anon INSERT into quotes, both asserted to reject.
  </acceptance_criteria>
  <done>Every QUOTE-01/QUOTE-02 success-criterion behavior — including the "no anon policy" invariant — has green automated evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test caller (anon Headers) → quotesRouter | Exercises the exact production anonymous entry path |
| withAnon probes → tenant-private tables | Deliberate boundary violation attempts proving RLS holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Information Disclosure | cac_index anon exposure | high | mitigate | Regression test: anon SELECT raises 42501 — any future anon policy addition turns the suite red |
| T-05-04 | Denial of Service / Spam | compute write-amplification | medium | mitigate | Regression test: compute inserts zero rows (D-01) |
| T-05-06 | Information Disclosure | error surface | high | mitigate | Tests assert typed codes only; missing CAC is PRECONDITION_FAILED, never an unhandled 500 |
| T-05-08 | Tampering | test DB safety | medium | mitigate | ownerSql guard refuses any DB whose name does not end in `_test` (existing harness, reused) |
| T-05-SC | Tampering | npm installs | high | accept | No packages installed — reuses the existing Vitest 4.1.8 harness |
</threat_model>

<verification>
- `pnpm --filter @imbau/api test` green (full suite, includes the new file).
- The suite covers all rows of the RESEARCH "Phase Requirements → Test Map" except the manual QUOTE-03 429 burst (human UAT, plan 05-05).
- `pnpm test` (turbo, per-wave merge gate) stays green — the quoting 100% coverage gate is untouched.
</verification>

<success_criteria>
- QUOTE-01: anon compute resolves the publicado org, reads max-periodo CAC, returns QuoteResult; borrador → NOT_FOUND; no-CAC → PRECONDITION_FAILED; tenant-private tables still raise 42501 for anon.
- QUOTE-02: create persists `{version:1, inputs, result, cacPeriodo}` with `result.version === ENGINE_VERSION`; compute persists nothing.
</success_criteria>

<output>
Create `.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-02-SUMMARY.md` when done.
</output>
