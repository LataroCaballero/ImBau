---
phase: 05-emisi-n-y-persistencia-server-side-api-rls-rate-limit
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/env.ts
  - apps/web/app/api/trpc/[trpc]/route.ts
  - .planning/PROJECT.md
autonomous: true
requirements: [QUOTE-01]
must_haves:
  truths:
    - "apps/web mounts the tRPC route handler at /api/trpc/[trpc] (mirror of panel), so the anonymous fase-6 client can reach quotes.compute/quotes.create over HTTP (D-06/A1)."
    - "apps/web/env.ts validates DATABASE_APP_URL in its server block at boot, and its header comment documents the A1 widening: web holds the app pool ONLY through withTenant inside quotesRouter procedures (no longer anon-only)."
    - "The T-03-09 grep-fence holds: no file under apps/web imports the elevated/owner-pool clients from @imbau/db — data access stays behind withTenant/withAnon inside packages/api routers."
    - "PROJECT.md Key Decisions records the A1-vs-A2 resolution (A1) with rationale — closing the Key Decision left open by the v1.2 roadmap."
  artifacts:
    - apps/web/app/api/trpc/[trpc]/route.ts
    - apps/web/env.ts
    - .planning/PROJECT.md
  key_links:
    - "route.ts: fetchRequestHandler → appRouter (includes quotesRouter from 05-01) with createTRPCContext({ headers })"
    - "env.ts server block: DATABASE_APP_URL from dbEnv.server preset — the app-pool URL @imbau/db's client boot requires (Pitfall 6)"
    - "compose.staging.yml web env_file [.env] already carries DATABASE_APP_URL — no new secret, no infra change"
---

<objective>
Resolve the A1-vs-A2 Key Decision as A1 (D-06): `apps/web` gains `DATABASE_APP_URL` in its validated env server block and mounts its own tRPC route handler (byte-mirror of the panel's), so the public web surface can serve the anonymous quote procedures server-side. Web stops being "anon-only" but ONLY through `withTenant` inside `quotesRouter` — the grep-fence (routers/apps import only `withTenant`/`withAnon`/`schema` from `@imbau/db`) is what keeps A1 safe. The decision is registered in PROJECT.md Key Decisions.

Purpose: Without the mount + env widening, the fase-6 UI has no HTTP path to quotes.compute/create; without the documented Key Decision, the open A1-vs-A2 note from the v1.2 roadmap stays unresolved.
Output: web route handler (new), env.ts widened + re-commented, PROJECT.md Key Decisions row.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-CONTEXT.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-01-SUMMARY.md

# Analogs / files to modify
@apps/panel/app/api/trpc/[trpc]/route.ts
@apps/web/env.ts
@apps/panel/env.ts
@packages/config/env/presets.ts
@deploy/compose.staging.yml
</context>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift/source-grounding):

- File `apps/web/app/api/trpc/[trpc]/route.ts` (NEW — GET/POST handler exports)
- `DATABASE_APP_URL` key added to the `server` block of `apps/web/env.ts`
- New row in the PROJECT.md `## Key Decisions` table documenting A1 (web holds the app pool, grep-fenced)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Surface DATABASE_APP_URL in apps/web/env.ts and rewrite the anon-only comment (D-06)</name>
  <files>apps/web/env.ts</files>
  <read_first>
    - apps/web/env.ts (current server block declares only DATABASE_ANON_URL; the header comment lines 6-14 document the now-superseded anon-only posture)
    - packages/config/env/presets.ts (dbEnv preset — DATABASE_APP_URL: z.string().url() under dbEnv.server)
    - apps/panel/env.ts (how panel composes multiple DB URLs and documents WHY each is needed — the commenting style to match)
    - deploy/compose.staging.yml (web service uses env_file [.env], so DATABASE_APP_URL is already provisioned in the container — verify, no compose change needed)
  </read_first>
  <action>
    In the `server` block of `apps/web/env.ts`, add `DATABASE_APP_URL: dbEnv.server.DATABASE_APP_URL,` directly below the existing `DATABASE_ANON_URL` line. Rewrite the header comment (the block currently explaining "it is anon-only (D-03)") to document the Phase-5 A1 widening: web now ALSO validates the app-pool URL because the quotesRouter's publicProcedures run withTenant(orgId) server-side for the anonymous quote path (QUOTE-01), and because importing @imbau/db in the web route handler boots the app client from this URL (fail-fast at boot, Pitfall 6). State the fence explicitly: apps/web reaches data ONLY through withTenant/withAnon inside packages/api routers — it never constructs or imports the elevated/owner-pool clients directly (T-03-09, grep-verified). Keep everything else unchanged: the client block, experimental__runtimeEnv, skipValidation, and all existing server keys stay as-is. Do NOT add DATABASE_URL (owner) — web never needs the owner pool.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && grep -c "DATABASE_APP_URL: dbEnv.server.DATABASE_APP_URL" apps/web/env.ts && pnpm --filter @imbau/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/env.ts` contains `DATABASE_APP_URL: dbEnv.server.DATABASE_APP_URL,` inside the `server` block.
    - `apps/web/env.ts` does NOT declare `DATABASE_URL` (owner pool) — grep for `DATABASE_URL:` matches only the APP/ANON lines.
    - The header comment mentions the quotesRouter/withTenant widening and no longer describes web as anon-only-by-design without qualification.
    - `pnpm --filter @imbau/web typecheck` exits 0.
  </acceptance_criteria>
  <done>Web's env boundary validates the app-pool URL at boot, with the A1 posture documented where the old anon-only comment lived.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the tRPC route handler in apps/web (mirror of panel)</name>
  <files>apps/web/app/api/trpc/[trpc]/route.ts</files>
  <read_first>
    - apps/panel/app/api/trpc/[trpc]/route.ts (the byte-mirror source: fetchRequestHandler, endpoint "/api/trpc", appRouter, createTRPCContext({ headers: req.headers }), export { handler as GET, handler as POST })
    - apps/web/next.config.ts (confirm @imbau/api is already in transpilePackages / the config the mount relies on)
  </read_first>
  <action>
    Create `apps/web/app/api/trpc/[trpc]/route.ts` as a byte-mirror of the panel's route handler: import `fetchRequestHandler` from `@trpc/server/adapters/fetch` and `appRouter`, `createTRPCContext` from `@imbau/api`; define `function handler(req: Request): Promise<Response>` calling `fetchRequestHandler({ endpoint: "/api/trpc", req, router: appRouter, createContext: () => createTRPCContext({ headers: req.headers }) })`; export it as both GET and POST. Adapt ONLY the header comment: this mount exists for the anonymous quote path (quotes.compute/quotes.create, QUOTE-01/D-06-A1) — an anonymous request carries no session, so createTRPCContext resolves session = null and publicProcedures resolve the org server-side from the publicado project, never from the client. `@imbau/web` already depends on `@imbau/api` (package.json verified) — no dependency change. Do not add any client provider/link — the browser client is fase 6.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && test -f "apps/web/app/api/trpc/[trpc]/route.ts" && grep -c "fetchRequestHandler" "apps/web/app/api/trpc/[trpc]/route.ts" && pnpm --filter @imbau/web typecheck && pnpm --filter @imbau/web lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/trpc/[trpc]/route.ts` exists, imports `appRouter` and `createTRPCContext` from `@imbau/api`, and exports `handler as GET` and `handler as POST`.
    - The `endpoint` option is exactly `"/api/trpc"` (must match the nginx location prefix `/api/trpc/quotes` from plan 05-05).
    - `pnpm --filter @imbau/web typecheck` and `pnpm --filter @imbau/web lint` exit 0.
    - Grep-fence: `grep -rn 'from "@imbau/db"' apps/web/` returns nothing (web imports data access only via @imbau/api).
  </acceptance_criteria>
  <done>The web app serves appRouter (including quotesRouter) under /api/trpc/* exactly like the panel does.</done>
</task>

<task type="auto">
  <name>Task 3: Register the A1 Key Decision in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the `## Key Decisions` table — match the existing row style: Decision | Rationale | Outcome)
    - .planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-CONTEXT.md (D-06 wording — the decision being registered)
  </read_first>
  <action>
    Append one row to the `## Key Decisions` table in `.planning/PROJECT.md` (use Edit — scoped insertion after the last existing row, never a whole-file rewrite). Decision: "A1 — apps/web hosts the app pool for the anonymous quote path (Phase 5 / D-06): DATABASE_APP_URL en el env de web + mount tRPC propio; web deja de ser anon-only SOLO vía withTenant dentro de quotesRouter". Rationale: "El pool app ya llegaba al contenedor web por env_file; A2 (emisión en panel/API dedicada) agregaba un hop cross-app sin ganancia de aislamiento real — el fence T-03-09 (solo withTenant/withAnon/schema desde @imbau/db, grep-verificable) mantiene la superficie de datos idéntica". Outcome: "— Pending (se valida con la UI de fase 6 en staging)". Update the trailing "*Last updated:*" line to reference Phase 5 planning/execution. Do not touch any other section.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && grep -c "A1 — apps/web" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` Key Decisions table contains a row starting with "A1 — apps/web" mentioning D-06 and the T-03-09 fence.
    - All pre-existing Key Decisions rows are still present (row count increased by exactly 1).
  </acceptance_criteria>
  <done>The A1-vs-A2 sub-decision left open by the v1.2 roadmap is closed and documented where PROJECT.md tracks decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → apps/web /api/trpc/* | Anonymous HTTP requests now reach appRouter on the public web surface |
| apps/web process → app pool (DATABASE_APP_URL) | The web runtime holds the NOSUPERUSER/NOBYPASSRLS app credentials (A1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Elevation of Privilege | web holding the app pool (A1) | high | mitigate | App role is NOSUPERUSER/NOBYPASSRLS; web reaches data only through withTenant/withAnon inside packages/api routers; grep-fence asserted in Task 2 acceptance criteria |
| T-05-12 | Information Disclosure | env secret in client bundle | high | mitigate | DATABASE_APP_URL declared in the t3-env `server` block only — the client/server split fails the build on a leak (existing T-03-19 pattern) |
| T-05-13 | Spoofing | anonymous callers hitting protectedProcedures via the new mount | medium | accept | protectedProcedure already rejects sessionless callers with UNAUTHORIZED (verified init.ts middleware) — the mount adds no bypass |
| T-05-14 | Denial of Service | public anonymous endpoint on web | high | mitigate | Edge rate limit on the quotes path — plan 05-05 (QUOTE-03) |
| T-05-SC | Tampering | npm installs | high | accept | No packages installed — @imbau/api was already a web dependency |
</threat_model>

<verification>
- `pnpm --filter @imbau/web typecheck` and `lint` green; full `pnpm test` (turbo) stays green.
- Route handler present and byte-equivalent to the panel's modulo the header comment.
- Grep-fence: no `@imbau/db` import anywhere under `apps/web/`.
- PROJECT.md carries the A1 Key Decision row.
</verification>

<success_criteria>
- The anonymous quote path is HTTP-reachable on the web surface (mount + validated app-pool env), completing the QUOTE-01 delivery chain from 05-01.
- A1 is documented as the resolved Key Decision; the old anon-only comment no longer misdescribes web's posture.
</success_criteria>

<output>
Create `.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-04-SUMMARY.md` when done.
</output>
