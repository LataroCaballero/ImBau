---
phase: 10-d1-grilla-de-unidades-editable-import-export-excel
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - packages/ui/src/tokens.css
  - apps/panel/postcss.config.mjs
  - apps/panel/app/globals.css
  - apps/panel/app/layout.tsx
  - apps/panel/app/proyectos/[id]/unidades/page.tsx
  - apps/panel/app/proyectos/[id]/unidades/units-grid.tsx
  - apps/panel/app/proyectos/[id]/unidades/import-wizard.tsx
  - apps/panel/app/proyectos/[id]/unidades/bulk-edit.tsx
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, GRID-06, GRID-07]
must_haves:
  truths:
    # --- 5 success criteria (behavioral) ---
    - "The developer edits a price cell inline (click → numeric input → Enter/blur saves) per list, and it persists (GRID-01, D-04 inline-cell edit). The grid is flat, one row per unit, with identificador/piso/tipología·m2 read-only reference columns + Financiado/Contado (USD) + Estado editable columns (D-03)."
    - "The developer changes a unit's estado from an inline dropdown (disponible/reservado/vendido) and selects rows via per-row checkboxes to build the bulk-edit selection (GRID-02, D-04)."
    - "The developer exports all units to a canonical-template Excel via Exportar a Excel (GRID-03)."
    - "The developer imports an Excel and sees a dry-run preview with field-by-field viejo→nuevo before applying; Aplicar cambios is disabled until the file is 100% valid (GRID-04, D-06/D-07)."
    - "The developer applies bulk price edits (% or monto fijo) over a selection, always through a mandatory preview modal before any write (GRID-06, D-12/D-13)."
    - "Success copy after any apply asserts the public web reflects the change instantly ('La web pública ya está actualizada') — GRID-07/D-14."
    # --- UI-SPEC ## UI Considerations: COVERED (26) ---
    - "units grid / empty: 0-unit project shows heading 'Todavía no hay unidades' + body + Importar Excel CTA."
    - "units grid / loading: skeleton rows on initial load."
    - "units grid / populated: 38 units × columns; money columns JetBrains Mono tabular-nums; estado dots."
    - "units grid / partial: a cell with no unit_price row renders '—' (em dash), never 0 or USD 0."
    - "units grid / overflow: horizontal scroll on narrow laptop; sticky checkbox + identificador columns hold."
    - "units grid / zero-one-many: 0 → empty state; 1..N → rows."
    - "price cell / empty: '—' em dash in gris-500 when no price."
    - "price cell / loading: 'saving' state — input disabled + spinner."
    - "price cell / error: vendido-red border + 'No se pudo guardar. Reintentá.'; value reverts."
    - "price cell / populated: idle JetBrains Mono tabular-nums integer USD, es-AR thousands separator."
    - "selection / empty: 0 selected → selection bar hidden."
    - "selection / populated: ≥1 selected → count + 'Editar precios en lote' + 'Limpiar selección'."
    - "selection / partial: header checkbox 'indeterminate' when a subset of visible rows is selected."
    - "selection / zero-one-many: 0 hidden · 1 '1 unidad seleccionada' · N '{N} unidades seleccionadas'."
    - "bulk edit / loading: preview modal 'applying' — disabled + spinner."
    - "bulk edit / error: preview modal 'error' — all-or-nothing restated; nothing written."
    - "bulk edit / populated: preview modal affected-units table, viejo → nuevo per unit, mono."
    - "bulk edit / overflow: affected-units list scrolls inside modal; Confirmar/Cancelar stay pinned."
    - "bulk edit / zero-one-many: count summary '{N} unidades' in the confirmation copy."
    - "import wizard / empty: Step 1 idle dropzone (no file selected)."
    - "import wizard / loading: 'parsing' (Step 1) and 'applying' (Step 4) states."
    - "import wizard / error: parse-error copy · per-row validation reasons · all-or-nothing apply failure writes nothing."
    - "import wizard / populated: Step 3 diff preview — Nuevas / Con cambios / Sin cambios groups."
    - "import wizard / partial: validation report classifies rows; errors > 0 disables Aplicar cambios."
    - "import wizard / overflow: invalid-rows list + diff scroll inside modal; summary bar + actions pinned."
    - "import wizard / zero-one-many: summary bar '{N} nuevas · {N} con cambios · {N} sin cambios · {N} con errores'; errors=0 vs >0 branch."
    # --- UI-SPEC focal point + token contract ---
    - "The Financiado/Contado price columns are the primary visual anchor (mono tabular-nums, cobre focus ring on edit); no other element out-weights them."
    - "Panel adopts the same Tailwind v4 @theme mirror as apps/web; the LIVE packages/ui/src/tokens.css is extended (un-prefixed naming) with the neutrals/radii/type-scale from docs/marca — consumers are NOT switched to --imbau-* and the two token files are not forked."
    # --- UI-SPEC ## UI Considerations: BACKSTOP (3) — held-out visual checks at verify time ---
    - { statement: "units grid / long-text: long tipología/identificador truncates + shows a title tooltip without breaking the 40px row height or the sticky columns", verification: backstop }
    - { statement: "units grid / error: a grid-level data-fetch/render failure shows a defined load-error state (message + retry), distinct from the per-cell save error", verification: backstop }
    - { statement: "import wizard / long-text: a long identificador/tipología inside a diff row truncates/tooltips without pushing the summary bar or action buttons out of view", verification: backstop }
  artifacts:
    - "apps/panel/app/proyectos/[id]/unidades/units-grid.tsx (client island)"
    - "apps/panel/app/proyectos/[id]/unidades/import-wizard.tsx (client modal)"
    - "apps/panel/app/proyectos/[id]/unidades/bulk-edit.tsx (client panel + preview modal)"
    - "apps/panel/app/proyectos/[id]/unidades/page.tsx (RSC body replaced)"
    - "packages/ui/src/tokens.css (extended), apps/panel/{postcss.config.mjs,app/globals.css,app/layout.tsx} (token wiring)"
  key_links:
    - "RSC page (resolveProject + canWrite) → <UnitsGrid projectId canWrite> → useTRPC units.* mutations"
    - "grid selection → <BulkEdit> preview modal → units.bulkPreview/bulkUpdatePrice"
    - "import-wizard → units.dryRunImport → units.importExcel (Aplicar disabled while errores > 0)"
---

<objective>
Build the panel's units-grid surface at `proyectos/[id]/unidades` — its highest-value, highest-risk (money) screen and its first STYLED surface — consuming the Plan 03 mutations and honoring the `10-UI-SPEC.md` contract (es-AR voseo, whole-USD JetBrains Mono figures, cobre-on-price focal point, mandatory previews for irreversible money ops). Replace only the body of the Phase 9 placeholder RSC; keep the inherited `params → z.uuid() → resolveProject → notFound` guard verbatim.

Purpose: Deliver GRID-01/02/03/04/06 as a usable tool and surface GRID-07's instant-reflection promise in the success copy. All authorization/validation authority lives server-side (Plan 03); the UI's `canWrite` is cosmetic defense-in-depth only.

**Planner assumption (UI-SPEC ## UI Considerations · unresolved row — toast/inline feedback):** the panel has no toast infrastructure and D-05 mandates functional UI with no design system. Decision: **fold all feedback into per-surface INLINE states** — single-cell edits use the inline saved/error cell states (no toast, per Copywriting); import-applied and bulk-applied success/error render as inline banners inside their own modal/panel (reusing the existing `role="status"`/`role="alert"` pattern from `(dashboard)/invite-form.tsx`). No global/floating toast system is introduced this phase. This resolves the under-specified error-toast shape without dropping the feedback requirement.

Output (artifacts this plan produces):
- Panel token wiring: extend `packages/ui/src/tokens.css`; add `apps/panel/postcss.config.mjs`, `apps/panel/app/globals.css`, font vars in `apps/panel/app/layout.tsx`.
- `units-grid.tsx`, `import-wizard.tsx`, `bulk-edit.tsx` client islands; `page.tsx` body.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-CONTEXT.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-PATTERNS.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-UI-SPEC.md
@apps/panel/app/proyectos/[id]/unidades/page.tsx
@apps/panel/lib/trpc-client.tsx
@apps/panel/app/(dashboard)/invite-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel token wiring + RSC body + units-grid island (grid, inline price cell, estado dropdown, selection, export)</name>
  <files>packages/ui/src/tokens.css, apps/panel/postcss.config.mjs, apps/panel/app/globals.css, apps/panel/app/layout.tsx, apps/panel/app/proyectos/[id]/unidades/page.tsx, apps/panel/app/proyectos/[id]/unidades/units-grid.tsx</files>
  <read_first>
    - apps/panel/app/proyectos/[id]/unidades/page.tsx (the Phase 9 placeholder — keep the guard L18-35 verbatim; replace only the &lt;main&gt; body L37-45 with the grid island; canWrite is cosmetic per D-08)
    - apps/panel/lib/trpc-client.tsx (useTRPC proxy + TRPCReactProvider — how client islands call tRPC) + apps/panel/app/(dashboard)/invite-form.tsx (the client-island useMutation + role="status"/role="alert" inline-feedback pattern to clone)
    - apps/web/app/globals.css (the Tailwind v4 CSS-first @import "tailwindcss" + @theme mirror pattern the panel must copy) + packages/ui/src/tokens.css (LIVE, un-prefixed --grafito/--cobre — the file to EXTEND) + docs/marca/tokens.css (brand ref: neutrals gris-700/500/300/100, type scale, radii, shadow — copy VALUES, keep LIVE un-prefixed naming)
    - apps/panel/app/layout.tsx (bare today — add globals.css import + the three next/font CSS vars --font-space-grotesk/--font-inter/--font-jetbrains on &lt;html&gt;)
    - 10-UI-SPEC.md §Design System/Wiring, §Token divergence FLAG, §Spacing, §Typography, §Color, §Copywriting Contract, §Component Inventory (grid + price cell + estado cell + selection bar states), §Interaction Contracts (price-cell state machine)
    - packages/quoting/src/format.ts (formatUsd for the money display — reuse, do not hand-roll grouping)
  </read_first>
  <action>
    Wire the panel to the brand tokens the SAME way apps/web does (do not invent): extend the LIVE `packages/ui/src/tokens.css` by adding the missing neutrals (`--gris-700/500/300/100`), radii (`--radius-*`), and type-scale tokens using the `docs/marca/tokens.css` VALUES but keeping the LIVE file's un-prefixed naming (do NOT switch consumers to `--imbau-*`, do NOT fork the files). Add `apps/panel/postcss.config.mjs` and `apps/panel/app/globals.css` (`@import "tailwindcss";` + an `@theme` block mirroring the tokens, copied from apps/web's globals.css), import globals.css in `apps/panel/app/layout.tsx`, and set the three next/font CSS variables on `<html>`. Replace the `<main>` body of `unidades/page.tsx` with `<UnitsGrid projectId={id} canWrite={canWrite} />`, keeping the inherited guard verbatim. Build `units-grid.tsx` as a `"use client"` island that loads rows via `useTRPC().units.listForProject` and renders: a sticky-header table (one row per unit — identificador, piso, tipología, m2 read-only in gris-500; Financiado + Contado price cells; estado cell) with the required states — loading (skeleton rows), populated (mono tabular-nums money, estado dot+label), empty (heading "Todavía no hay unidades" + body + Importar Excel CTA), a **grid-level load-error state with a retry affordance (backstop — define it; distinct from the per-cell save error)**, and horizontal-overflow with sticky checkbox + identificador columns; long tipología/identificador truncates with a `title` tooltip without breaking the 40px row height. Implement the **price cell state machine** (idle → hover → editing with a 2px cobre focus ring + numeric input → Enter/blur saving (disabled + spinner) → saved (brief tint) or error (vendido-red border + "No se pudo guardar. Reintentá.", value reverts; Escape cancels) via `units.updatePrice`; empty price renders "—"). Implement the **estado dropdown** (native select of the 3 enum options as dot+label) via `units.updateEstado`. Implement **row checkboxes + header select-all (indeterminate on partial)** feeding a selection, and the **selection bar** (hidden at 0; "1 unidad seleccionada"/"{N} unidades seleccionadas" + "Editar precios en lote" + "Limpiar selección"). Add the **Exportar a Excel** button calling `units.exportExcel` and triggering a browser download of the returned base64 workbook (all units, sanitized server-side). Preserve the focal-point hierarchy: the price columns are the strongest visual anchor; ref columns and toolbar stay lower-contrast. Use cobre only on the reserved primary actions + the editing price-cell ring (never on estado). Lift all es-AR copy verbatim from the Copywriting Contract — do not invent strings.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/panel typecheck` and `lint` exit 0.
    - `unidades/page.tsx` retains the `z.uuid()`/`resolveProject`/`notFound` guard and renders `<UnitsGrid ... />` in the body.
    - `units-grid.tsx` is a `"use client"` island calling `units.listForProject`, `units.updatePrice`, `units.updateEstado`, `units.exportExcel` via `useTRPC`.
    - `packages/ui/src/tokens.css` gains the neutrals/radii/type-scale tokens with un-prefixed names; `apps/panel/app/globals.css` and `postcss.config.mjs` exist and mirror the apps/web @theme pattern.
    - Empty-price cells render `—` (em dash), not `0`; money uses `formatUsd`.
    - A grid-level load-error state with retry is present, distinct from the per-cell save-error state.
  </acceptance_criteria>
  <done>The panel is styled with brand tokens, the units grid renders all required states with inline-edit price cells, estado dropdowns, selection, and export — all consuming the Plan 03 read/mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Import wizard (4-step modal) + bulk-edit panel + mandatory preview modal, with inline feedback</name>
  <files>apps/panel/app/proyectos/[id]/unidades/import-wizard.tsx, apps/panel/app/proyectos/[id]/unidades/bulk-edit.tsx</files>
  <read_first>
    - 10-UI-SPEC.md §Import Flow (Step 1 Upload → Step 2 Validation report → Step 3 Diff preview → Step 4 Confirm) + §Component Inventory (import wizard, bulk-edit panel, bulk-edit preview modal states) + §Copywriting Contract (summary bar, invalid-row reasons, apply-blocked hint, applied-success, both destructive confirmations) + §Interaction Contracts (selection → bulk always routes through preview)
    - apps/panel/app/proyectos/[id]/unidades/units-grid.tsx (Task 1 — the selection state + Importar Excel / Editar precios en lote triggers that open these modals)
    - apps/panel/app/(dashboard)/invite-form.tsx (client useMutation + inline role="status"/role="alert" feedback — the pattern for the folded-inline feedback assumption)
    - 10-RESEARCH.md Pattern 6 (import wizard states; lift copy verbatim, do not re-derive) + Pattern 5 (all-or-nothing apply contract the UI mirrors)
    - packages/api/src/excel/types.ts (DryRunResult + BulkPreview shapes the UI renders — via the tRPC return types)
  </read_first>
  <action>
    Build `import-wizard.tsx` as a `"use client"` multi-step modal opened from the grid's Importar Excel CTA: Step 1 Upload (dropzone + file input; states idle/dragover/file-selected/parsing/parse-error with the exact es-AR parse-error copy; read the file to base64 and call `units.dryRunImport`). Step 2 Validation report — render the summary bar `{N} nuevas · {N} con cambios · {N} sin cambios · {N} con errores`; if errores > 0 render the scrollable invalid-rows list (row nº + es-AR reason from the dry-run) and keep **Aplicar cambios disabled** with the apply-blocked hint copy. Step 3 Diff preview (only when errores = 0) — three groups Nuevas / Con cambios (field-by-field viejo→nuevo: old value gris-500 strikethrough, new value grafito, both mono for prices, separated by →) / Sin cambios (collapsed count). Step 4 Confirm — Aplicar cambios (cobre) with the all-or-nothing confirmation copy + Cancelar; on confirm call `units.importExcel`; states preview → applying (disabled+spinner) → success (applied count + "La web pública ya está actualizada" inline) or error (nothing written — restate all-or-nothing inline). Invalid-rows list + diff must scroll inside the modal without pushing the summary bar or actions out of view; long identificador/tipología in a diff row truncates/tooltips (backstop). Build `bulk-edit.tsx` as the selection-triggered panel (choose lista Financiado/Contado, mode % / monto fijo, value; Previsualizar cambios calls `units.bulkPreview`) plus the **mandatory preview modal** (affected-units table identificador + viejo→nuevo mono, count summary, the bulk destructive-confirmation copy, Confirmar/Cancelar; applying/success/error states; list scrolls with pinned actions) that calls `units.bulkUpdatePrice` — never a blind apply (D-13). Feedback is inline per surface (planner assumption); do NOT add a global toast system. Lift ALL copy verbatim from the Copywriting Contract.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/panel typecheck && pnpm --filter @imbau/panel lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/panel typecheck` and `lint` exit 0.
    - `import-wizard.tsx` calls `units.dryRunImport` then `units.importExcel`; Aplicar cambios is disabled while the dry-run reports errores > 0.
    - `bulk-edit.tsx` always routes through the preview modal (`units.bulkPreview`) before `units.bulkUpdatePrice`; no direct apply path exists.
    - Applied-success copy includes "La web pública ya está actualizada"; the all-or-nothing and bulk destructive-confirmation strings match the Copywriting Contract verbatim.
    - Feedback is rendered inline (role="status"/role="alert" style) within each surface; no global toast component is added.
  </acceptance_criteria>
  <done>The import wizard (4 steps, apply-blocked until 100% valid) and the bulk-edit flow (mandatory preview) are built with verbatim es-AR copy and inline per-surface feedback, consuming the Plan 03 dry-run/apply/bulk procedures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual/UX verification of the units grid + import wizard + bulk flow</name>
  <action>Pause for a human to visually verify the styled surface per the steps below (the browser-interaction and es-AR copy checks that unit tests cannot assert — 10-VALIDATION.md Manual-Only Verifications).</action>
  <what-built>The styled units grid at proyectos/[id]/unidades: inline price editing, estado dropdowns, row selection + bulk-edit-with-preview, Excel export, and the 4-step import wizard — all in es-AR voseo with brand tokens (mono money, cobre-on-price focal point). Manual visual/UX confirmation per 10-VALIDATION.md Manual-Only Verifications (browser interaction + es-AR copy rendering are not fully assertable in unit tests).</what-built>
  <how-to-verify>
    1. Boot the panel into a seeded org that owns a project (see memory "Panel local UAT setup": `pnpm dev`, then the SQL to place a logged-in user into an org owning the Brigos Recoleta project). Node 22 (`nvm use 22`); Docker Postgres/Redis up.
    2. Open `proyectos/[id]/unidades`. Confirm: 38 units render; money is JetBrains Mono with es-AR thousands (e.g. USD 185.000); empty prices show "—"; estado shows a colored dot + label; the price columns read as the primary visual anchor.
    3. Click a price cell → edit → Enter: it saves (brief tint). Type a non-integer / trigger a failure: vendido-red border + "No se pudo guardar. Reintentá." and the value reverts. Change an estado via the dropdown.
    4. Select rows (checkboxes; header indeterminate on partial) → "Editar precios en lote" → apply +10% → confirm the preview modal shows viejo→nuevo per unit → Confirmar → success.
    5. Click "Exportar a Excel" — a .xlsx downloads with all units. Edit a couple of prices in Excel, then "Importar Excel": confirm the dry-run summary bar, that an invalid row shows an es-AR reason and disables Aplicar cambios, and that a valid file shows the field-by-field diff and applies with "La web pública ya está actualizada".
    6. Open the public cotizador for that project in another tab and confirm an edited price/estado is reflected (GRID-07 Path A).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (copy, layout, token, or behavior) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Panel UI → units router | The UI's `canWrite` gate is cosmetic; the real authority is server-side `requireRole` (Plan 03). |
| Browser file → dryRunImport/importExcel | The client reads an uploaded file to base64; the server re-parses and re-validates (never trusts the client dry-run). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-UI-COSMETIC-GATE | Elevation | canWrite affordance | medium | mitigate | Treat `canWrite` as cosmetic only; every write is independently gated by `requireRole` server-side (Plan 03) — hiding a button is never the control |
| T-10-UI-CLIENTVALID | Integrity | import wizard client dry-run | medium | mitigate | UI dry-run is UX-only; `importExcel` re-validates server-side inside the tx (Plan 03) — the client isValid flag is never trusted |
| T-10-UI-BLINDMONEY | Integrity | bulk edit / import apply | high | mitigate | Every irreversible money op routes through a mandatory preview modal (viejo→nuevo) with all-or-nothing confirmation copy before any write (D-13/D-08) |
</threat_model>

<verification>
- `pnpm --filter @imbau/panel typecheck` + `lint` clean.
- Human-verify checkpoint approved (visual/UX + es-AR copy + GRID-07 reflection).
- Copy strings match the 10-UI-SPEC Copywriting Contract verbatim.
</verification>

<success_criteria>
The units grid + import wizard + bulk-edit flow are usable in es-AR with brand tokens, deliver GRID-01/02/03/04/06 through the Plan 03 mutations, surface GRID-07's instant-reflection promise in copy, cover every UI-SPEC covered/backstop state, and pass a human visual/UX verification.
</success_criteria>

<output>
Create `.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-04-SUMMARY.md` when done.
</output>
