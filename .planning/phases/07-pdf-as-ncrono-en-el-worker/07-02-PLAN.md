---
phase: 07-pdf-as-ncrono-en-el-worker
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/worker/src/quote-pdf-store.ts
  - apps/worker/src/quote-pdf-runtime.ts
  - apps/worker/src/quote-pdf.ts
  - apps/worker/src/index.ts
  - apps/worker/src/quote-pdf.test.ts
autonomous: true
requirements: [PDF-01, PDF-02]
tags: [worker, pdf, bullmq, processor, r2, withTenant, idempotency, qrcode]

must_haves:
  truths:
    - "processQuotePdf short-circuits when pdfKey is already set (D-11): it does not re-render or re-upload — the PDF is frozen forever, coherent with the immutable snapshot (PDF-02)."
    - "The PDF amounts come ONLY from snapshot.result via toPdfModel — the worker NEVER re-runs calcQuote (T-04-06); unit/project descriptors are read live via withTenant, montos never (D-07)."
    - "The render → R2 put → single withTenant pdfKey write-back happens in that order; the key is quotePdfKey(org, project, quoteId) (deterministic, overwrite on retry — PDF-02)."
    - "createQuotePdfWorker consumes QUOTE_PDF_QUEUE at concurrency 2 and its failed handler routes the final failure to Sentry + pino (reportQuotePdfFailure) — never swallowed (D-10)."
    - "The QR encodes the cotizador deep-link {WEB_PUBLIC_BASE_URL}/p/{slug}/cotizador?u={unitId}&plan={paymentPlanId} (D-08) built server-side."
  artifacts:
    - "apps/worker/src/quote-pdf-store.ts — readQuoteForPdf (snapshot+pdfKey+descriptors, withTenant) + writePdfKey"
    - "apps/worker/src/quote-pdf-runtime.ts — putPdf (R2) + WEB_PUBLIC_BASE_URL export"
    - "apps/worker/src/quote-pdf.ts — processQuotePdf + reportQuotePdfFailure + QR generation"
    - "apps/worker/src/index.ts — createQuotePdfWorker + QUOTE_PDF_QUEUE + failed handler wired into boot()"
    - "apps/worker/src/quote-pdf.test.ts — processor orchestration (seams + doc mocked)"
  key_links:
    - "processQuotePdf: readQuoteForPdf → (short-circuit if pdfKey) → toPdfModel(snapshot.result) → QRCode.toDataURL(deepLink) → renderToBuffer(QuoteDoc(...)) → putPdf(quotePdfKey(...)) → writePdfKey"
    - "index.ts boot(): new Queue(QUOTE_PDF_QUEUE) + createQuotePdfWorker(connection) + quotePdfWorker.on('failed', …reportQuotePdfFailure)"
    - "quote-pdf.ts delegates ALL I/O to quote-pdf-store (withTenant) + quote-pdf-runtime (R2) so quote-pdf.test.ts can vi.mock both + the doc"
    - "writePdfKey runs as app_authenticated via withTenant — never owner (quotes_tenant is FOR ALL TO appAuthenticated)"
  prohibitions:
    - statement: "The worker never re-runs calcQuote — PDF amounts come only from snapshot.result via toPdfModel"
      status: resolved
      verification: "quote-pdf.ts imports toPdfModel from @imbau/quoting and reads row.snapshot.result; it does not import or call calcQuote"
    - statement: "pdfKey write-back uses withTenant (app_authenticated), never the owner/BYPASSRLS pool"
      status: resolved
      verification: "quote-pdf-store.ts imports only withTenant/schema from @imbau/db and writes via withTenant(orgId)"
---

<objective>
Build the worker consumer that fulfils PDF-01/PDF-02: a BullMQ processor on `QUOTE_PDF_QUEUE` that reads
the frozen snapshot + descriptors under `withTenant`, short-circuits if the PDF already exists (D-11),
lays out the snapshot via `toPdfModel` (never recompute — D-07/T-04-06), builds the deep-link QR (D-08),
renders `QuoteDoc` to a Buffer, uploads it to the deterministic R2 key, and writes `pdfKey` back in one
`withTenant` UPDATE. This is a near-exact clone of the verified media pipeline (processMedia + writeVariants
+ putVariant + boot), so the seams stay mockable and observable-on-failure.

Purpose: This is the heart of the phase — the actual PDF generation. Triple idempotency (short-circuit +
deterministic key + jobId dedup) guarantees a BullMQ retry never duplicates an object (PDF-02).
Output: quote-pdf-store.ts, quote-pdf-runtime.ts, quote-pdf.ts, index.ts boot wiring, processor test.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-CONTEXT.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-RESEARCH.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-PATTERNS.md

# Contracts + document consumed (do NOT redefine)
@packages/storage/src/quote-pdf.ts
@packages/storage/src/keys.ts
@packages/quoting/src/serialize.ts

# Analog files to clone (exact) + the doc from 07-01
@apps/worker/src/media.ts
@apps/worker/src/media-store.ts
@apps/worker/src/media-runtime.ts
@apps/worker/src/index.ts
@apps/worker/src/media.test.ts
@apps/worker/src/env.ts
@packages/db/src/schema/quotes.ts
@packages/db/src/schema/units.ts
@packages/db/src/schema/floors.ts
@packages/db/src/schema/projects.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- **New files:** `apps/worker/src/quote-pdf-store.ts`, `apps/worker/src/quote-pdf-runtime.ts`, `apps/worker/src/quote-pdf.ts`, `apps/worker/src/quote-pdf.test.ts`
- **New exports:** `readQuoteForPdf(orgId, quoteId)`, `writePdfKey(orgId, quoteId, pdfKey)`, `putPdf(key, body)`, `WEB_PUBLIC_BASE_URL` (const from env), `processQuotePdf(job)`, `reportQuotePdfFailure(err, ctx)`, `createQuotePdfWorker(connection)`
- **Modified:** `apps/worker/src/index.ts` boot() adds `quotePdfQueue` + `quotePdfWorker` handles and a `failed` handler
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: withTenant read/write seam (quote-pdf-store) + R2 put seam (quote-pdf-runtime)</name>
  <files>apps/worker/src/quote-pdf-store.ts, apps/worker/src/quote-pdf-runtime.ts</files>
  <read_first>
    - apps/worker/src/media-store.ts (EXACT analog — writeVariants via withTenant, app_authenticated rationale)
    - apps/worker/src/media-runtime.ts (EXACT analog — module-level makeR2Client + putVariant + R2_BUCKET export)
    - apps/worker/src/env.ts (validated env incl. WEB_PUBLIC_BASE_URL from 07-01)
    - packages/db/src/schema/quotes.ts (snapshot $type<QuoteSnapshot>, pdfKey, org/project/unit/plan ids)
    - packages/db/src/schema/units.ts (identificador, tipologia, m2) + floors.ts (numero) + projects.ts (nombre, slug)
  </read_first>
  <action>
    Create `apps/worker/src/quote-pdf-store.ts` (clone media-store.ts). Import ONLY `withTenant, schema` from
    `@imbau/db` (fence T-03-09) + `eq, and` from drizzle-orm. Export:
      - `writePdfKey(orgId: string, quoteId: string, pdfKey: string): Promise<void>` = `withTenant(orgId, (tx) =>
        tx.update(schema.quotes).set({ pdfKey }).where(eq(schema.quotes.id, quoteId)))` (single atomic write-back,
        app_authenticated — never owner).
      - `readQuoteForPdf(orgId: string, quoteId: string)` returning `{ snapshot, pdfKey, header }` where header carries
        the D-07 descriptors. In ONE `withTenant(orgId)` transaction: select the quote's `{ snapshot, pdfKey, unitId,
        paymentPlanId, projectId }` by id; then read descriptors — unit `{ identificador, tipologia, m2 }` (units by
        unitId), floor `numero` (floors via the unit's floorId), and project `{ nombre, slug }` (projects by projectId).
        RLS scopes every read to orgId. Montos are NEVER read from these tables — only descriptors (D-07). Return a typed
        object exposing snapshot (typed QuoteSnapshot), pdfKey, and header fields (proyecto, unidad identificador, piso,
        tipologia, m2, projectSlug, unitId, paymentPlanId) the processor needs to build the header + deep-link.
      - If the quote row is missing (already-consumed / wrong org), throw so BullMQ marks the job failed (never a silent
        empty PDF).

    Create `apps/worker/src/quote-pdf-runtime.ts` (clone media-runtime.ts). Build the module-level `makeR2Client(env)`
    client (env already validated at boot via ./env). Export `R2_BUCKET = env.R2_BUCKET`, `putPdf(key: string, body: Buffer):
    Promise<void>` = `r2.send(new PutObjectCommand({ Bucket: R2_BUCKET, Key: key, Body: body, ContentType: "application/pdf" }))`
    (overwrite on retry — never duplicates), and `WEB_PUBLIC_BASE_URL = env.WEB_PUBLIC_BASE_URL` so the processor imports the
    deep-link base through this (mockable) seam rather than importing ./env directly — keeping quote-pdf.test.ts free of env load.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/worker typecheck</automated>
  </verify>
  <acceptance_criteria>
    - quote-pdf-store.ts exports `writePdfKey` and `readQuoteForPdf`; imports only `withTenant`/`schema` from `@imbau/db`
    - readQuoteForPdf reads snapshot + pdfKey + unit/floor/project descriptors under a single withTenant(orgId); reads no monetary column from those tables
    - writePdfKey updates schema.quotes.pdfKey via withTenant(orgId) (app_authenticated), never the owner pool
    - quote-pdf-runtime.ts exports `putPdf`, `R2_BUCKET`, and `WEB_PUBLIC_BASE_URL`; putPdf sends PutObject with ContentType application/pdf
    - `pnpm --filter @imbau/worker typecheck` exits 0
  </acceptance_criteria>
  <done>The worker has a tenant-scoped read (snapshot + descriptors) + write (pdfKey) seam and an R2 put seam, cloned from the verified media pipeline, both mockable in isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: processQuotePdf (short-circuit + QR + render + put + write) + reportQuotePdfFailure + worker boot wiring</name>
  <files>apps/worker/src/quote-pdf.ts, apps/worker/src/index.ts, apps/worker/src/quote-pdf.test.ts</files>
  <read_first>
    - apps/worker/src/media.ts (processMedia L124-148 + reportMediaFailure L166-177 — exact analog)
    - apps/worker/src/index.ts (createMediaWorker L68-75 + boot() media wiring L120-133 — exact analog)
    - apps/worker/src/media.test.ts (vi.mock seams + makeJob + orchestration assertions — the test style to mirror)
    - apps/worker/src/quote-pdf-store.ts + quote-pdf-runtime.ts (Task 1 seams)
    - apps/worker/src/quote-pdf-doc.ts (QuoteDoc + QuoteHeader/QuoteDocProps from 07-01)
    - packages/storage/src/keys.ts (quotePdfKey) + quote-pdf.ts (QuotePdfJobData, QUOTE_PDF_QUEUE)
    - packages/quoting/src/serialize.ts (toPdfModel)
  </read_first>
  <behavior>
    - Given a job whose quote already has pdfKey set, processQuotePdf returns WITHOUT calling putPdf or writePdfKey (short-circuit, D-11)
    - Given a job whose quote has pdfKey null, processQuotePdf calls putPdf once with key = quotePdfKey(org, project, quoteId) then writePdfKey once with that same key (order: put before write)
    - The QR data-URL is generated from the deep-link {WEB_PUBLIC_BASE_URL}/p/{slug}/cotizador?u={unitId}&plan={paymentPlanId}
    - toPdfModel is called with snapshot.result; calcQuote is never called
    - A failing render/put/write propagates (job marked failed); reportQuotePdfFailure(err, {quoteId, attempts}) routes to Sentry + pino
  </behavior>
  <action>
    Create `apps/worker/src/quote-pdf.ts` (clone media.ts structure). Import `Job` type (bullmq), `renderToBuffer`
    (@react-pdf/renderer), `QRCode` (qrcode), `quotePdfKey`/`QUOTE_PDF_QUEUE`/`QuotePdfJobData` (@imbau/storage),
    `toPdfModel` (@imbau/quoting), `logger` (@imbau/observability), `Sentry` (@sentry/node), the `readQuoteForPdf`/
    `writePdfKey` store seams, the `putPdf`/`WEB_PUBLIC_BASE_URL` runtime seams, and `QuoteDoc` from `./quote-pdf-doc`.

    `processQuotePdf(job: Job<QuotePdfJobData>): Promise<void>`:
      1. Destructure `{ quoteId, organizationId, projectId }` from job.data.
      2. `const row = await readQuoteForPdf(organizationId, quoteId)`.
      3. SHORT-CIRCUIT (D-11): `if (row.pdfKey) return;` — never re-render / re-upload.
      4. `const model = toPdfModel(row.snapshot.result)` (montos solo del snapshot — D-07; never calcQuote).
      5. Build the deep-link string `${WEB_PUBLIC_BASE_URL}/p/${row.projectSlug}/cotizador?u=${row.unitId}&plan=${row.paymentPlanId}`
         and `const qrPng = await QRCode.toDataURL(deepLink, { margin: 1, width: 256 })` (D-08).
      6. Build the header (QuoteHeader): proyecto = row.proyecto; unidad = row.unidadIdentificador; piso = String(row.piso);
         tipologia = row.tipologia ?? ""; m2 = row.m2 ?? ""; fecha = render-time date formatted es-AR (America/Argentina/Buenos_Aires) —
         NOTE: quotes has no createdAt column, so fecha de emisión is render-time, frozen at first render by the D-11 short-circuit;
         cacPeriodo = row.snapshot.cacPeriodo ?? "—"; ref = quoteId.slice(0, 8).toUpperCase() (traceable to quotes.id — D-07);
         deepLink = the deep-link string.
      7. `const pdf = await renderToBuffer(QuoteDoc({ model, header, qrPng }))`.
      8. `const key = quotePdfKey(organizationId, projectId, quoteId); await putPdf(key, pdf);` (deterministic overwrite).
      9. `await writePdfKey(organizationId, quoteId, key);` (single atomic write-back).
      10. `logger.info({ quoteId, organizationId }, "quote pdf rendered + persisted")`.
    Export `reportQuotePdfFailure(err, ctx: { quoteId?: string; attempts?: number })` cloning reportMediaFailure:
    Sentry.captureException + logger.error with structured fields only (quoteId, attempts, queue=QUOTE_PDF_QUEUE) — never the raw payload (V7).

    In `apps/worker/src/index.ts`: import `QUOTE_PDF_QUEUE`, `QuotePdfJobData` (@imbau/storage) and `processQuotePdf`,
    `reportQuotePdfFailure` (./quote-pdf). Add `createQuotePdfWorker(connection: IORedis): Worker<QuotePdfJobData>` cloning
    createMediaWorker (`new Worker<QuotePdfJobData>(QUOTE_PDF_QUEUE, (job) => processQuotePdf(job), { connection, concurrency: 2 })`).
    In boot(): declare `const quotePdfQueue = new Queue(QUOTE_PDF_QUEUE, { connection })`, stand up `const quotePdfWorker =
    createQuotePdfWorker(connection)`, wire `quotePdfWorker.on("failed", (job, err) => reportQuotePdfFailure(err, { quoteId:
    job?.data.quoteId, attempts: job?.attemptsMade }))`, and add both handles to the boot() return object + its return type.

    Create `apps/worker/src/quote-pdf.test.ts` mirroring media.test.ts orchestration: `vi.mock("./quote-pdf-store")`,
    `vi.mock("./quote-pdf-runtime", () => ({ putPdf: vi.fn(), WEB_PUBLIC_BASE_URL: "https://web.test", R2_BUCKET: "test" }))`,
    and `vi.mock("./quote-pdf-doc")` (QuoteDoc → a stub) plus `vi.mock("@react-pdf/renderer", () => ({ renderToBuffer:
    vi.fn().mockResolvedValue(Buffer.from("%PDF-1.7")) }))`. Cases: (a) short-circuit — readQuoteForPdf returns a row with pdfKey
    set → putPdf/writePdfKey NOT called; (b) happy path — pdfKey null → putPdf called once with quotePdfKey(org,project,quoteId),
    writePdfKey called once with the same key, in that order; (c) the deep-link passed to QRCode.toDataURL matches the expected
    {WEB_PUBLIC_BASE_URL}/p/{slug}/cotizador?u={unitId}&plan={paymentPlanId}. Mock QRCode.toDataURL. Use a makeJob helper like media.test.ts.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/worker test -t "processQuotePdf" && pnpm --filter @imbau/worker test</automated>
  </verify>
  <acceptance_criteria>
    - quote-pdf.ts exports `processQuotePdf` and `reportQuotePdfFailure`; imports `toPdfModel` from @imbau/quoting and does NOT import calcQuote
    - processQuotePdf returns early when row.pdfKey is truthy (short-circuit); otherwise puts then writes with key = quotePdfKey(org,project,quoteId)
    - The QR deep-link is built as `{WEB_PUBLIC_BASE_URL}/p/{slug}/cotizador?u={unitId}&plan={paymentPlanId}`
    - index.ts exports `createQuotePdfWorker` and boot() stands up QUOTE_PDF_QUEUE + a failed handler calling reportQuotePdfFailure
    - quote-pdf.test.ts mocks the store/runtime/doc/renderer seams (no live R2/DB/Redis); asserts short-circuit + single put + single write + deep-link
    - `pnpm --filter @imbau/worker test` exits 0 (all worker suites green, including the existing media + index smoke)
  </acceptance_criteria>
  <done>The worker renders + stores the PDF idempotently from the frozen snapshot, wires the queue consumer into boot with observable failure, and proves short-circuit + deterministic-key + deep-link behavior with mocked seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ job payload → worker | organizationId in the payload drives the withTenant scope (worker has no session) |
| worker → R2 / DB | Object write + pdfKey write-back under RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | client-influenced pdfKey / cross-tenant write | high | mitigate | Key derived server-side via quotePdfKey(org,project,quoteId); write via withTenant(org) as app_authenticated (quotes_tenant FOR ALL TO appAuthenticated) — an owner UPDATE hits default-deny. |
| T-07-10 | Repudiation / Silent failure | job fails and nobody notices | medium | mitigate | reportQuotePdfFailure → Sentry captureException + pino error on the final retry; error never swallowed (D-10 / CLAUDE.md). |
| T-07-13 | Information Disclosure | recompute drift leaks a different number than screen/WhatsApp | high | mitigate | Amounts come only from snapshot.result via toPdfModel; calcQuote never runs in the worker (T-04-06). Descriptors read live (non-monetary, D-07). |
| T-07-14 | DoS / duplication | BullMQ retry duplicates R2 objects | medium | mitigate | Triple idempotency: short-circuit on pdfKey (D-11) + deterministic quotePdfKey overwrite + jobId=quoteId dedup. |
</threat_model>

<verification>
- `pnpm --filter @imbau/worker test` green (processor + existing media + env + index smoke suites).
- `pnpm --filter @imbau/worker typecheck` green.
- Idempotency proven: short-circuit + single deterministic-key write asserted in quote-pdf.test.ts.
</verification>

<success_criteria>
- The worker generates the PDF from the frozen snapshot, uploads to the deterministic R2 key, and writes pdfKey back once — idempotent under retry (PDF-01/PDF-02).
- Failures are observable (Sentry + pino), never swallowed; amounts never recomputed.
</success_criteria>

<output>
Create `.planning/phases/07-pdf-as-ncrono-en-el-worker/07-02-SUMMARY.md` when done.
</output>
