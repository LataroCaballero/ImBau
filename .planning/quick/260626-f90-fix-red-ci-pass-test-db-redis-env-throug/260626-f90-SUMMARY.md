---
quick_id: 260626-f90
slug: fix-red-ci-pass-test-db-redis-env-throug
created: 2026-06-26
completed: 2026-06-26
status: complete
tasks_completed: 2
tasks_total: 2
key-files:
  modified:
    - turbo.json
    - .github/workflows/ci.yml
commits:
  - 59a88e8
  - d537fed
---

# Quick Task 260626-f90: Fix red CI — pass test DB/Redis env through Turbo strict mode + add Redis service Summary

Fixed the red `quality` CI check by declaring the test DB/Redis connection strings as `passThroughEnv` on the Turbo `test` task (Turbo 2.x strict mode was stripping them before vitest ran) and by adding a live `redis:7-alpine` service plus `REDIS_URL` to the CI quality job for the worker BullMQ smoke test.

## What Was Done

### Task 1 — Declare test env passthrough on `turbo.json` `test` task (commit `59a88e8`)
Added a `passThroughEnv` array to the `test` task (`dependsOn: ["^build"]` preserved) containing `TEST_DATABASE_URL`, `TEST_DATABASE_APP_URL`, `TEST_DATABASE_ANON_URL`, `DATABASE_URL`, `DATABASE_APP_URL`, `DATABASE_ANON_URL`, `REDIS_URL`, `NODE_ENV`. These are runtime connection strings, so `passThroughEnv` (not `env`) is correct — they reach the task without becoming part of the cache key. This resolves `@imbau/db#test` throwing "Missing test DB connection string". Verified: passThroughEnv contains the required vars and turbo.json is valid JSON.

### Task 2 — Add a Redis service to the CI quality job (commit `d537fed`)
Added a `redis` service to the `quality` job alongside the existing `postgres` service: image `redis:7-alpine`, healthcheck (`redis-cli ping`, interval 10s / timeout 5s / retries 5), and port mapping `6380:6379`. Added job-level `REDIS_URL: redis://localhost:6380` to the `env` block (matching the published port and the worker smoke test convention). The existing postgres service and `TEST_DATABASE_*` env were left untouched. Verified via regex checks and a Ruby YAML parse confirming `jobs.quality.services.redis.image = redis:7-alpine` and `jobs.quality.env.REDIS_URL = redis://localhost:6380`.

## Verification
- Task 1 `<verify>`: passThroughEnv includes the three `TEST_DATABASE_*` vars + `REDIS_URL`; valid JSON. PASSED.
- Task 2 `<verify>`: redis image, REDIS_URL env, and redis-cli healthcheck present; YAML parses cleanly. PASSED.

## Deviations from Plan
None — plan executed exactly as written. (The plan's verify step did not check YAML structurally; pyyaml was unavailable locally, so YAML validity was confirmed via a Ruby `YAML.load_file` parse instead — same outcome, different tool.)

## Self-Check: PASSED
- Commit `59a88e8` found in git log.
- Commit `d537fed` found in git log.
- `turbo.json` and `.github/workflows/ci.yml` present and committed (working tree clean for both files).
