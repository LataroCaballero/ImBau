---
phase: 10-d1-grilla-de-unidades-editable-import-export-excel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/unit-prices.ts
  - packages/db/migrations/0005_unit_prices_unit_list_uq.sql
  - packages/db/migrations/meta/_journal.json
  - packages/api/tests/unit-prices-unique.test.ts
autonomous: true
requirements: [GRID-05]
must_haves:
  truths:
    - "unit_prices enforces at most one row per (unit_id, price_list_id) — a second insert with the same natural key is rejected by the DB (GRID-05, D-01)."
    - "The v1.2 cotizador resolver invariant (one price row per unit×list) is now enforced at the schema, not merely by convention."
    - "The migration is a versioned Drizzle SQL file applied via pnpm db:migrate — never drizzle-kit push, never manual DDL (CLAUDE.md)."
  artifacts:
    - "packages/db/migrations/0005_unit_prices_unit_list_uq.sql (versioned, committed)"
    - "unit_prices_unit_list_uq UNIQUE constraint on (unit_id, price_list_id)"
    - "packages/api/tests/unit-prices-unique.test.ts (dup-insert-rejected proof)"
  key_links:
    - "unit-prices.ts unique().on(t.unitId, t.priceListId) → generated ALTER TABLE ... ADD CONSTRAINT → conflict target for every upsert in Plan 03"
---

<objective>
Add the versioned `UNIQUE(unit_id, price_list_id)` constraint to `unit_prices` so the grid/Excel upsert has a conflict target and the v1.2 cotizador's one-row-per-unit×list invariant is enforced at the database (GRID-05, D-01). This is the **[BLOCKING] schema gate**: it MUST land before any mutation that relies on `onConflictDoUpdate (unit_id, price_list_id)` (Plan 03).

Purpose: Downstream type/build passing is a false positive if the migration is never applied — so this plan proves the constraint is both present in a versioned migration AND enforced against a live `_test` Postgres (dup insert rejected).

Output (artifacts this plan produces):
- EDIT `packages/db/src/schema/unit-prices.ts` — `unique("unit_prices_unit_list_uq").on(t.unitId, t.priceListId)` in the `(t) => [ ... ]` extras array.
- NEW versioned migration `packages/db/migrations/0005_unit_prices_unit_list_uq.sql` (generated, committed).
- NEW `packages/api/tests/unit-prices-unique.test.ts` — integration proof the constraint rejects a duplicate natural key.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-CONTEXT.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-PATTERNS.md
@.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UNIQUE(unit_id, price_list_id) to unit_prices and generate + apply the versioned migration [BLOCKING]</name>
  <files>packages/db/src/schema/unit-prices.ts, packages/db/migrations/0005_unit_prices_unit_list_uq.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/src/schema/unit-prices.ts (the file being modified — extras array ends at L84, no UNIQUE today; import block L15-23; do NOT touch the three composite FKs L52-67 or the two policies L69/L78)
    - packages/db/src/schema/units.ts L9-18 (import of `unique` from drizzle-orm/pg-core) + L62 (the in-repo `unique().on(...)` idiom — the exact analog per 10-PATTERNS.md)
    - packages/db/drizzle.config.ts (schema array includes unit-prices.ts; events.ts is DELIBERATELY absent — generate will not touch it; `out: "./migrations"`, `entities.roles: true`)
    - packages/db/package.json scripts (`db:generate` = drizzle-kit generate, `db:migrate` = drizzle-kit migrate)
    - packages/db/src/seed/pricing.ts L66 ("unit_prices — one row per unit per price_list") — pre-flight: existing seed data is already unique, so the constraint applies cleanly
  </read_first>
  <action>
    Add `unique` to the existing `drizzle-orm/pg-core` import in unit-prices.ts (mirror units.ts L18), then add `unique("unit_prices_unit_list_uq").on(t.unitId, t.priceListId)` as a new entry in the `(t) => [ ... ]` extras array — placed alongside the existing constraints, leaving the three composite FKs and both `pgPolicy` blocks untouched (they carry tenant isolation + anon-published read that GRID-07 depends on). Generate the versioned migration with `pnpm --filter @imbau/db db:generate` (this maps to CLAUDE.md `pnpm db:migrate` tooling; drizzle-kit diffs schema vs the meta snapshot offline, emitting the next sequential file `0005_*`). Do NOT run `drizzle-kit push` and do NOT hand-edit DDL — versioned migrations only (CLAUDE.md). Confirm the generated SQL contains ONLY the unit_prices ADD CONSTRAINT (events.ts is absent from drizzle.config, so no partitioned-table drift is emitted). Apply against the running `_test` / local Postgres with `pnpm --filter @imbau/db db:migrate`. Rename the generated file to `0005_unit_prices_unit_list_uq.sql` only if drizzle-kit's default name is opaque; keep `meta/_journal.json` in sync (commit both).
  </action>
  <verify>
    <automated>cd packages/db && pnpm db:generate && grep -rli "unit_prices_unit_list_uq" migrations/ && grep -rl "unit_id" migrations/0005*.sql | head -1 && pnpm db:migrate</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema/unit-prices.ts` contains `unique(` referencing `t.unitId` and `t.priceListId`.
    - A file `packages/db/migrations/0005_*.sql` exists and its body adds a UNIQUE constraint/index on `unit_prices (unit_id, price_list_id)`; it does NOT contain a `CREATE TABLE "events"` statement.
    - `pnpm --filter @imbau/db db:migrate` exits 0 (migration applies clean against existing seed data).
    - No occurrence of `drizzle-kit push` anywhere in the change.
  </acceptance_criteria>
  <done>The UNIQUE(unit_id, price_list_id) constraint exists in a committed versioned migration and is applied to the DB; the schema edit is scoped to the extras array with FKs and policies intact.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the constraint is enforced — duplicate natural-key insert rejected vs real Postgres</name>
  <files>packages/api/tests/unit-prices-unique.test.ts</files>
  <read_first>
    - packages/api/tests/projects-role-gate.test.ts L15-56 (fixtures + ownerSql seeding pattern; `makeUserWithActiveOrg`, `seedProject`)
    - packages/api/tests/db.ts (`ownerSql()` — the owner pool used ONLY for seeding)
    - packages/api/tests/fixtures.ts (`makeUserWithActiveOrg`, `mintMemberInOrg`)
    - packages/api/tests/setup.ts (globalSetup wires the real `_test` Postgres)
    - packages/db/src/schema/unit-prices.ts (columns: organization_id, project_id, unit_id, price_list_id, precio integer, vigencia timestamptz — for the raw insert)
    - packages/db/src/seed/pricing.ts (how a unit_prices row is shaped; there are 2 price lists per project)
  </read_first>
  <action>
    Create `packages/api/tests/unit-prices-unique.test.ts`. Using `ownerSql()` for seeding only (never assert through the elevated pool for the isolation claims — that is Plan 03's job here we only prove the DB constraint), seed one org + one project + one floor + one unit + one price_list, then insert a first `unit_prices` row for `(unit_id, price_list_id)`. Assert that a SECOND raw insert with the SAME `(unit_id, price_list_id)` (different `id`, different `precio`) rejects with a Postgres unique-violation (SQLSTATE 23505 / error mentioning `unit_prices_unit_list_uq`). Keep `precio` an integer (USD, never float). Close the owner pool in `afterAll`. This is the enforcement proof that turns the schema gate from a possible false positive into a verified fact.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/api test -- unit-prices-unique</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/api test -- unit-prices-unique` exits 0.
    - The test asserts a second insert with a duplicate `(unit_id, price_list_id)` is rejected (unique violation), and a first insert succeeds.
    - Seeding uses `ownerSql()`; the assertion is on the DB error, not on an app-role read.
  </acceptance_criteria>
  <done>An automated integration test proves the DB rejects a duplicate `(unit_id, price_list_id)` — the constraint is enforced, not just declared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema → cotizador resolver | The one-row-per-unit×list invariant the v1.2 quoting resolver relies on is now a hard DB constraint, not a convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-DUPKEY | Integrity (Tampering) | `unit_prices` natural key | high | mitigate | `UNIQUE(unit_id, price_list_id)` enforced at DB + dup-insert-rejected test (Task 2); makes the Plan 03 upsert idempotent and protects the cotizador invariant |
| T-10-MIGRATE-FALSEPOS | Tampering | migration application | medium | mitigate | `[BLOCKING]` task applies via `pnpm db:migrate` (not push) and Task 2 exercises the live constraint, so a never-applied migration cannot pass as green |
</threat_model>

<verification>
- `pnpm --filter @imbau/db db:migrate` applies cleanly against existing seed data.
- `pnpm --filter @imbau/api test -- unit-prices-unique` green.
- Generated migration is versioned and committed (present in `migrations/` + `meta/_journal.json`).
</verification>

<success_criteria>
The `UNIQUE(unit_id, price_list_id)` constraint exists in a committed versioned migration, is applied to the DB, and is proven enforced by an integration test — unblocking the idempotent upsert in Plan 03 (GRID-05).
</success_criteria>

<output>
Create `.planning/phases/10-d1-grilla-de-unidades-editable-import-export-excel/10-01-SUMMARY.md` when done.
</output>
