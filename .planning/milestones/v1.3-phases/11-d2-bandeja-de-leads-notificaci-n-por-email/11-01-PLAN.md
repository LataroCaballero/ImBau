---
phase: 11-d2-bandeja-de-leads-notificaci-n-por-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/leads.ts
  - packages/db/src/schema/projects.ts
  - packages/db/migrations/0006_leads_desenlace_projects_notify_email.sql
  - packages/db/src/seed/content.ts
  - packages/db/src/seed/content-rows.ts
autonomous: true
requirements: [LEADS-02, LEADS-04]
must_haves:
  truths:
    - "A nullable `desenlace` column exists on `leads` (values `ganado`/`perdido`, null outside `cerrado`) without touching `leadEstadoEnum` — supports the cerrado ganado/perdido distinction (D-03)."
    - "A nullable `leadsNotifyEmail` (`leads_notify_email`) column exists on `projects`; null is the valid 'fallback to org owners' sentinel (D-05)."
    - "`drizzle-kit generate` emits exactly ONE additive migration adding the two nullable columns (no drop, no type change, no enum edit); `drizzle-kit migrate` applies it cleanly and a second `migrate` is a no-op (idempotent)."
    - "Re-running `pnpm db:seed` inserts the 14 fictitious leads via a direct `tx.insert(schema.leads)` that NEVER calls the create seam / `enqueueLeadEmail`, so a re-seed sends zero emails (belt-and-suspenders with the jobId dedup)."
    - "The seeded `cerrado` leads carry a non-null `desenlace` so the bandeja's Cerrado column renders Ganado/Perdido badges from real data."
  artifacts:
    - packages/db/src/schema/leads.ts
    - packages/db/src/schema/projects.ts
    - packages/db/migrations/0006_leads_desenlace_projects_notify_email.sql
    - packages/db/src/seed/content.ts
    - packages/db/src/seed/content-rows.ts
  key_links:
    - "leads.desenlace column ← consumed by leads.updateEstado refine (Plan 03) + the desenlace prompt UI (Plan 05)."
    - "projects.leadsNotifyEmail column ← written by projects.updateSettings (Plan 03), read by the worker recipient resolver (Plan 04)."
    - "seed direct-insert path structurally bypasses enqueueLeadEmail (Plan 03) — the re-seed no-email guarantee."
  prohibitions:
    - statement: "Re-seeding or any bulk/direct insert of leads MUST NOT send notification emails — the seed inserts leads outside the create seam so it can never enqueue."
      category: values
      verification: judgment
---

<objective>
Land the two additive nullable schema columns this phase needs and seed real Cerrado outcomes,
producing the versioned migration that all downstream plans verify against.

- `leads.desenlace` (nullable text, `ganado`/`perdido`) — the cheap, forward-compatible cerrado
  distinction (D-03) that does NOT touch `leadEstadoEnum`.
- `projects.leadsNotifyEmail` (nullable text) — the single load-bearing config field advanced from
  D5/fase 6 (D-05); null = fall back to org owners.
- Seed: attach `desenlace` to the seeded `cerrado` leads; confirm the seed's direct-insert path
  never touches the email seam (D-01/D-06).

Purpose: schema must be applied to the LIVE DB before any router/worker/UI verification, or build/types
pass against config-derived types while the live DB is stale (false-positive verification).
Output: two modified schema files, one generated migration SQL, seed updates.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-CONTEXT.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- `desenlace` nullable column on `leads` (`ganado`/`perdido`).
- `leadsNotifyEmail` (`leads_notify_email`) nullable column on `projects`.
- Generated migration `packages/db/migrations/0006_leads_desenlace_projects_notify_email.sql`.
- Seed `desenlace` values on the fictitious `cerrado` leads.

<tasks>

<task type="auto">
  <name>Task 1: Add nullable desenlace + leadsNotifyEmail columns</name>
  <read_first>
    - packages/db/src/schema/leads.ts (the `estado` enum col at L48 + `origen` plain text L47 — clone the plain-text nullable shape)
    - packages/db/src/schema/projects.ts (the `whatsapp` nullable text col L29-34 — the exact analog)
    - packages/db/src/schema/enums.ts (`leadEstadoEnum` — DO NOT modify)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md (schema rows for leads.ts + projects.ts)
  </read_first>
  <files>packages/db/src/schema/leads.ts, packages/db/src/schema/projects.ts</files>
  <action>
    In `leads.ts` add a nullable `desenlace: text("desenlace")` column beside `estado` (per D-03). Do
    NOT add `.notNull()`, no default, and do NOT touch `leadEstadoEnum` — the ganado/perdido domain is
    validated at the Zod boundary in Plan 03, mirroring how `origen` is plain text here. In
    `projects.ts` add a nullable `leadsNotifyEmail: text("leads_notify_email")` column beside `whatsapp`
    (per D-05); no new pgPolicy (existing `projects_tenant` covers it, `projects_anon_published` is
    SELECT-only and this column is panel-private). Keep both columns additive and nullable so the
    migration needs no backfill.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'desenlace' packages/db/src/schema/leads.ts` shows a nullable `text` column and NO change to `leadEstadoEnum`.
    - Source assertion: `grep -n 'leads_notify_email' packages/db/src/schema/projects.ts` shows a nullable `text` column.
    - `pnpm --filter @imbau/db typecheck` passes.
  </acceptance_criteria>
  <done>Both nullable columns declared in the Drizzle schema; enum untouched; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Generate + apply the versioned migration to the live DB</name>
  <read_first>
    - packages/db/drizzle.config.ts (out: "./migrations", entities.roles:true)
    - packages/db/migrations/0005_unit_prices_unit_list_uq.sql (the prior generated migration — shape/naming precedent)
    - packages/db/package.json (scripts `db:generate` = drizzle-kit generate, `db:migrate` = drizzle-kit migrate)
  </read_first>
  <files>packages/db/migrations/0006_leads_desenlace_projects_notify_email.sql</files>
  <action>
    Run `pnpm --filter @imbau/db db:generate` to emit the versioned migration (rename the generated file
    to `0006_leads_desenlace_projects_notify_email.sql` if drizzle names it with a random suffix; keep
    the journal entry consistent). Confirm it contains exactly two `ALTER TABLE ... ADD COLUMN` statements
    (`leads.desenlace`, `projects.leads_notify_email`) and NO drop/type-change/enum DDL. Then run
    `pnpm --filter @imbau/db db:migrate` to apply it to the live dev DB (owner/`DATABASE_URL` role). This
    is versioned migration flow — NEVER `drizzle-kit push`. Both statements are pure additive nullable
    columns → non-interactive; if generate prompts for a rename disambiguation, STOP and flag for manual
    intervention (do not guess). Commit the generated SQL — it is an artifact of this phase.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/db db:migrate && grep -Ec 'add column' packages/db/migrations/0006_leads_desenlace_projects_notify_email.sql</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `pnpm --filter @imbau/db db:migrate` applies 0006 and a second run reports nothing to apply (idempotent).
    - Source assertion: the generated SQL has exactly two `add column` lines and zero `drop`/`type`/`enum` statements (`grep -iE 'drop|alter column .* type|create type' packages/db/migrations/0006_*.sql` returns nothing).
    - Behavior assertion: `information_schema.columns` reports `leads.desenlace` (is_nullable=YES) and `projects.leads_notify_email` (is_nullable=YES) against the live DB.
  </acceptance_criteria>
  <done>Migration 0006 generated, applied to the live DB, idempotent on re-run, columns present and nullable.</done>
</task>

<task type="auto">
  <name>Task 3: Seed desenlace on cerrado leads; confirm seed bypasses the email seam</name>
  <read_first>
    - packages/db/src/seed/content.ts (L327+ `LeadDef` + the 14 lead defs; the 4 estados)
    - packages/db/src/seed/content-rows.ts (L79 leadRows map + L104 `tx.insert(schema.leads)...onConflictDoNothing()` — the direct insert that bypasses the create seam)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md (seed row)
  </read_first>
  <files>packages/db/src/seed/content.ts, packages/db/src/seed/content-rows.ts</files>
  <action>
    Add an optional `desenlace?: "ganado" | "perdido"` field to `LeadDef` (content.ts) and set it on the
    seeded `cerrado` leads (a realistic mix of ganado/perdido; leave non-cerrado leads without it). Map
    the field into `leadRows` (content-rows.ts) so it flows into the existing direct
    `tx.insert(schema.leads)...onConflictDoNothing()`. Do NOT route the seed through any leads router or
    `enqueueLeadEmail` — the direct insert is exactly why a re-seed never spams email (D-01/D-06). Keep
    the seed deterministic and idempotent (safe re-run, no duplicate rows).
  </action>
  <verify>
    <automated>pnpm --filter @imbau/db typecheck && grep -c 'enqueueLeadEmail\|leadsRouter\|\.create(' packages/db/src/seed/content-rows.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `content-rows.ts` inserts leads only via `tx.insert(schema.leads)` — grep for `enqueueLeadEmail`/router `create` calls returns 0.
    - Behavior assertion: after `pnpm db:seed`, the seeded `cerrado` leads have non-null `desenlace`; non-cerrado leads have null `desenlace`.
    - Behavior assertion: a second `pnpm db:seed` inserts zero new rows (idempotent) and performs zero email sends.
  </acceptance_criteria>
  <done>Cerrado leads seeded with desenlace; seed path structurally email-free; re-seed idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live DB | additive DDL crosses into the shared multi-tenant schema |
| seed batch → DB | fictitious lead rows inserted via the owner pool, bypassing the create seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | migration 0006 DDL | medium | mitigate | Additive nullable columns only — no drop/type-change/enum edit; generated (never `push`/manual); reviewed in PR; idempotent apply verified. |
| T-11-02 | Information Disclosure | seeded lead PII | low | accept | Seed uses fictitious "Brigos Recoleta" data via the owner pool; no real buyer PII; anon never reads leads (policy is INSERT-only). |
| T-11-SC | Tampering | npm/pnpm installs | low | mitigate | No new packages introduced; schema + seed use existing `@imbau/db` deps only. No `[ASSUMED]`/`[SUS]` install. |
</threat_model>

<verification>
- `pnpm --filter @imbau/db typecheck` green.
- `pnpm --filter @imbau/db db:migrate` applies 0006 and is idempotent on re-run.
- Live-DB column check: `leads.desenlace` + `projects.leads_notify_email` both nullable.
- `pnpm db:seed` twice → no duplicate rows, cerrado leads carry desenlace, zero emails.
</verification>

<success_criteria>
Both nullable columns exist in schema and live DB via one additive versioned migration; seed carries
desenlace on cerrado leads and remains email-free and idempotent.
</success_criteria>

<output>
Create `.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-01-SUMMARY.md` when done.
</output>
