---
phase: 11-d2-bandeja-de-leads-notificaci-n-por-email
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - packages/api/src/leads/runtime.ts
  - packages/api/src/trpc/routers/leads.ts
  - packages/api/src/trpc/routers/projects.ts
  - packages/api/src/trpc/routers/_app.ts
  - packages/api/tests/leads-role-gate.test.ts
autonomous: true
requirements: [LEADS-01, LEADS-02, LEADS-03, LEADS-04]
must_haves:
  truths:
    - "`leads.listForProject` resolves origen by tenant-scoped joins to `brokers`/`units`/`quotes`; a lead with all pointers null resolves to `Directo`; a cross-org projectId returns empty (RLS), never a leak (LEADS-01)."
    - "`leads.updateEstado` accepts only a destination in the 4-value `z.enum([\"nuevo\",\"contactado\",\"negociacion\",\"cerrado\"])` mirror of `leadEstadoEnum` — 'máquina impuesta' = destination ∈ the 4 values (free transitions incl. reopen, NOT linear); any other value is a 400 (LEADS-02/D-02)."
    - "A transition INTO `cerrado` REQUIRES `desenlace` (`ganado`/`perdido`) via a Zod refine (400 without it); transitioning out of `cerrado` clears/ignores it (D-03)."
    - "Every `updateEstado` appends an auto `LeadNote` (`{ts, autor from ctx.session, estadoPrev, estadoNuevo}`) to `leads.timeline` AND inserts an `events` audit row (stable English `tipo`) in the SAME withTenant tx — an audit row exists iff the write committed (LEADS-03/D-04)."
    - "`leads.addNote` appends a free-text `LeadNote` (`{ts, autor, nota}`) to `leads.timeline` append-only; it emits NO `events` row and triggers NO email (D-06); the timeline renders ordered by `ts` (LEADS-03)."
    - "`leads.create` (alta manual) inserts the lead with `.returning({id})` inside `withTenant`, seeds a first `LeadNote` at t=0, and calls `enqueueLeadEmail` at the mutation TOP LEVEL AFTER the `withTenant(...)` promise resolves — OUTSIDE the tx closure, never nested inside the `withTenant(...)` callback (mirrors quotes.ts L203-222). A rolled-back/failing insert enqueues ZERO jobs; the enqueue only pushes to Redis (no in-request send) — the load-bearing LEADS-04 seam (D-01/D-06)."
    - "The enqueue is idempotent by `jobId = lead:{id}:created`: enqueueing the same lead's created-event twice yields exactly one job / one send; a crash between persist and enqueue never double-sends on idempotent replay, and the mutation never blocks on delivery."
    - "Every leads write (`create`/`updateEstado`/`addNote`) is `requireRole(\"owner\",\"developer\")` over `withTenant`: owner✓ / developer✓ / viewer✗ FORBIDDEN (pre-write) / other-org✗ NOT_FOUND / non-existent id✗ NOT_FOUND — proven by the cross-role matrix vs real Postgres (PANEL-02 mold)."
    - "`projects.updateSettings` is extended with an optional `leadsNotifyEmail: z.email().nullable().optional()` written to `.set(...)` without breaking the Phase 9 canary (estado toggle) test (D-05)."
    - "`leads.ts` imports ONLY `withTenant`/`schema` from `@imbau/db` — never `createOwnerDb`/`appDb` (the Phase 9/10 grep-fence)."
  artifacts:
    - packages/api/src/leads/runtime.ts
    - packages/api/src/trpc/routers/leads.ts
    - packages/api/tests/leads-role-gate.test.ts
  key_links:
    - "leads.create → enqueueLeadEmail (leads/runtime.ts) → LEAD_EMAIL_QUEUE (Plan 02) → worker (Plan 04)."
    - "leadsRouter registered in _app.ts → typed surface consumed by the panel (Plan 05)."
    - "projects.updateSettings.leadsNotifyEmail → projects.leadsNotifyEmail column (Plan 01) → worker recipient resolver (Plan 04)."
  prohibitions:
    - statement: "The notification email MUST fire ONLY on lead creation — `updateEstado` and `addNote` MUST NOT call `enqueueLeadEmail` (state transitions and notes never notify, D-06)."
      category: values
      verification: judgment
    - statement: "The recipient address MUST NOT be taken from client input at create time — `leadsNotifyEmail` is validated by Zod `.email()` on the settings write and the actual recipient is resolved server-side later (worker), so a malformed/attacker value cannot redirect a lead to an unintended address."
      category: privacy
      verification: judgment
---

<objective>
Build the leads router (the LEADS-01/02/03/04 API surface), the enqueue seam, and the extended
`projects.updateSettings`, cloning the Phase 9/10 write mold verbatim and proving authorization +
tenant isolation + no-enumeration + idempotency through a cross-role matrix against real Postgres.

Purpose: this is where the pipeline state machine, the append-only timeline, and the load-bearing
create+enqueue seam live. Output: `leads/runtime.ts`, `leads.ts` router, extended `projects.ts`,
`_app.ts` registration, and `leads-role-gate.test.ts`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-CONTEXT.md
@.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md
</context>

## Edge-probe coverage (spec-less fallback)

Phase 11 is spec-less (no `11-SPEC.md`). The deterministic edge probe returned LEADS-01..04 as
holistic requirements the engine could not auto-decompose, so `unclassified` rows are never
auto-backstopped. The concrete edges were derived by hand from the CONTEXT decisions (D-01..D-06) and
the ROADMAP LEADS success criteria, and each is encoded into `must_haves.truths` above (no edge
dropped). If a reviewer disputes any derived edge, treat it as a flag to resolve, not a silent gap:

- **LEADS-01 origen resolution incl. null → `Directo`** → the `listForProject` truth ("a lead with all pointers null resolves to `Directo`; a cross-org projectId returns empty (RLS)").
- **LEADS-02 destination ∈ the 4-value enum (free transitions, not linear) + `cerrado` requires `desenlace`** → the `updateEstado` enum truth ("destination ∈ the 4 values … NOT linear") and the cerrado-refine truth ("A transition INTO `cerrado` REQUIRES `desenlace` … 400 without it").
- **LEADS-03 timeline append-only, ordered by `ts`** → the `addNote` truth ("appends a free-text `LeadNote` … append-only; the timeline renders ordered by `ts`") plus the auto-note truth.
- **LEADS-04 idempotency by `jobId = lead:{id}:created` (dedup)** → the idempotency truth ("enqueueing the same lead's created-event twice yields exactly one job / one send … never double-sends on idempotent replay").
- **LEADS-04 concurrency — enqueue AFTER commit, OUTSIDE `withTenant`** → the `leads.create` truth ("calls `enqueueLeadEmail` at the mutation TOP LEVEL AFTER the `withTenant(...)` promise resolves — OUTSIDE the tx closure … a rolled-back/failing insert enqueues ZERO jobs"), structurally gated in Task 2 and rollback-tested in Task 3.

## Artifacts this phase produces (this plan)

- `enqueueLeadEmail(data)` enqueue seam (`packages/api/src/leads/runtime.ts`).
- `leadsRouter` with `listForProject`, `create`, `updateEstado`, `addNote` (`packages/api/src/trpc/routers/leads.ts`).
- Stable English `events.tipo` const for lead transitions (e.g. `EVENT_LEAD_ESTADO_CHANGED`).
- Extended `projects.updateSettings` input/`.set` with `leadsNotifyEmail`.
- `leads: leadsRouter` registered in `_app.ts`.
- `packages/api/tests/leads-role-gate.test.ts` cross-role/transition/timeline/idempotency matrix.

<tasks>

<task type="auto">
  <name>Task 1: Enqueue seam (leads/runtime.ts) cloning quotes/runtime enqueuePdf</name>
  <read_first>
    - packages/api/src/quotes/runtime.ts (L39-86 — the LAZY/MEMOIZED getEnv/getQueue + enqueuePdf; strip the R2/S3 half)
    - packages/storage/src/lead-email.ts (Plan 02 — LEAD_EMAIL_QUEUE, leadEmailJobOptions, LeadEmailJobData)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md (`packages/api/src/leads/runtime.ts` row)
  </read_first>
  <files>packages/api/src/leads/runtime.ts</files>
  <action>
    Clone `quotes/runtime.ts` keeping ONLY the Redis half: lazy/memoized `getEnv()` (compose
    `baseEnv` + `redisEnv` — no `r2Env`), `getQueue()` building `new IORedis(REDIS_URL, {
    maxRetriesPerRequest: null })` + `new Queue(LEAD_EMAIL_QUEUE, { connection })` on first use, and
    `export async function enqueueLeadEmail(data: LeadEmailJobData): Promise<void>` calling
    `getQueue().add("notify", data, leadEmailJobOptions(data.leadId))`. Env parsed on first enqueue,
    fails closed with the var NAME (never value). No email send here — this module only pushes the job.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/api typecheck && grep -c 'r2Env\|makeR2Client\|S3Client' packages/api/src/leads/runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `enqueueLeadEmail` calls `getQueue().add("notify", data, leadEmailJobOptions(data.leadId))`.
    - Source assertion: the module composes only `baseEnv`+`redisEnv` — grep for `r2Env`/`makeR2Client`/`S3Client` returns 0.
    - Source assertion: IORedis is built with `maxRetriesPerRequest: null` and lazily (not at module import).
    - `pnpm --filter @imbau/api typecheck` passes.
  </acceptance_criteria>
  <done>Redis-only lazy enqueue seam that pushes a lead-email job with the idempotent options.</done>
</task>

<task type="auto">
  <name>Task 2: leadsRouter (listForProject/create/updateEstado/addNote) + extend projects.updateSettings + register</name>
  <read_first>
    - packages/api/src/trpc/routers/units.ts (write mold: L170-290 listForProject join style, updateEstado UPDATE+.returning() NOT_FOUND, events audit L282-287, grep-fence L27-33, local z.enum mirror L52-54)
    - packages/api/src/trpc/routers/quotes.ts (L185-224 — insert.returning + enqueue-as-side-effect-after-persist shape)
    - packages/api/src/trpc/routers/projects.ts (updateSettings L46-60 — extend input + .set; keep the 0-row NOT_FOUND guard and the Phase 9 estado canary)
    - packages/api/src/trpc/routers/_app.ts (register a new router key)
    - packages/api/src/trpc/init.ts (ctx.session for `autor`; ctx.activeOrgId)
    - packages/db/src/schema/json-schemas.ts (LeadNote shape `{ts, autor?, nota, estadoPrev?, estadoNuevo?}`)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-PATTERNS.md (leads.ts + projects.ts rows)
    - .planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-UI-SPEC.md (timeline copy: auto entry `{autor} movió el lead de {EstadoPrev} a {EstadoNuevo}`; creation entry `{autor} registró el lead`)
  </read_first>
  <files>packages/api/src/trpc/routers/leads.ts, packages/api/src/trpc/routers/projects.ts, packages/api/src/trpc/routers/_app.ts</files>
  <action>
    Create `leadsRouter`. Define a local `LEAD_ESTADOS = ["nuevo","contactado","negociacion","cerrado"]
    as const` mirror and a stable English `EVENT_LEAD_ESTADO_CHANGED` const. `listForProject`
    (protectedProcedure): read leads under `withTenant(ctx.activeOrgId)` with leftJoins to
    `brokers`/`units`/`quotes` on the composite `(id, organizationId)` to resolve origen; a null-pointer
    lead resolves to `Directo`; no app-layer org filter (RLS scopes it). `updateEstado`
    (requireRole owner/developer): input `{projectId, leadId, estado: z.enum(LEAD_ESTADOS), desenlace:
    z.enum(["ganado","perdido"]).optional()}` with a Zod refine requiring `desenlace` iff
    `estado==="cerrado"`; inside one `withTenant` tx read the current estado, UPDATE set estado +
    desenlace + `timeline: [...prev, autoNote]` (autoNote author from `ctx.session`), `.returning({id})`
    0-row → NOT_FOUND, then insert an `events` row (`tipo: EVENT_LEAD_ESTADO_CHANGED`) in the SAME tx;
    transitioning out of `cerrado` sets desenlace null. `addNote` (requireRole owner/developer): same
    NOT_FOUND mold, appends a free-text `LeadNote` to timeline, NO events row, NO enqueue. `create`
    (requireRole owner/developer): input `{projectId, nombre: z.string().min(1), contacto:
    z.string().min(1), origen: z.string(), brokerId/unitId/quoteId: z.uuid().optional()}`. Structure
    the mutation body EXACTLY like quotes.ts create L203-222: call `withTenant(ctx.activeOrgId, (tx) =>
    tx.insert(schema.leads)...returning({id}))` at the mutation TOP LEVEL and bind its resolved value
    to `const inserted`; the insert seeds a t=0 creation `LeadNote` (`{autor} registró el lead`); guard
    `const row = inserted[0]` (INTERNAL_SERVER_ERROR if absent). Then — AFTER the `withTenant(...)`
    promise has RESOLVED and the tx has committed, at the mutation's top level and OUTSIDE the
    `withTenant(...)` closure — `await enqueueLeadEmail({leadId: row.id, organizationId:
    ctx.activeOrgId, projectId: input.projectId})`. This OVERRIDES any sketch that nests the enqueue
    inside the tx callback: `enqueueLeadEmail` MUST NOT appear inside the `withTenant(...)` arrow. A
    pre-commit enqueue inside the tx would fire an email for a lead that later rolls back and let the
    worker race an uncommitted row (D-06); the post-commit placement makes a rolled-back insert enqueue
    zero jobs. A Redis push failure surfaces as the create error (observable). Import ONLY
    `withTenant`/`schema` from `@imbau/db`.
    Extend `projects.updateSettings`: add `leadsNotifyEmail: z.email().nullable().optional()` to the
    input and write only provided keys in `.set(...)`; keep the estado canary + 0-row NOT_FOUND guard.
    Register `leads: leadsRouter` in `_app.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/api typecheck && ! grep -Eq 'createOwnerDb|appDb' packages/api/src/trpc/routers/leads.ts && perl -e 'my $s=do{local $/;<>}; my $ok=0; if($s=~/create:/g){ my $rest=substr($s,pos($s)); if($rest=~/withTenant\s*(\((?:[^()]++|(?1))*+\))/){ $ok = ($1 !~ /enqueueLeadEmail\(/); } } exit($ok?0:1);' packages/api/src/trpc/routers/leads.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: grep-fence — `createOwnerDb`/`appDb` absent in `leads.ts` (`! grep -Eq 'createOwnerDb|appDb' packages/api/src/trpc/routers/leads.ts`).
    - Source assertion: `updateEstado` validates `z.enum(LEAD_ESTADOS)` and refines `desenlace` required on `cerrado`; inserts an `events` row in the same tx.
    - Source assertion: `create` calls `enqueueLeadEmail` exactly once, AFTER the insert `.returning()`; `updateEstado`/`addNote` never call `enqueueLeadEmail`.
    - Structural assertion (load-bearing, D-06): `enqueueLeadEmail(` is invoked OUTSIDE the `withTenant(...)` closure — at the mutation top level, after the `withTenant(...)` promise resolves — mirroring quotes.ts L203-222. The `perl` gate anchors on the `create` procedure, extracts `create`'s `withTenant(...)` balanced-parenthesis argument group (recursive `(?1)` paren matcher), and asserts `enqueueLeadEmail(` does NOT occur inside that group. It PASSES only for the correct (post-commit, outside-`withTenant`) shape and FAILS for the incorrect (pre-commit, nested-inside-`withTenant`) shape — a line-ordering check alone cannot distinguish them, so the balanced-paren match is required.
    - Source assertion: `projects.updateSettings` input includes `leadsNotifyEmail: z.email()...` and the Phase 9 estado canary path is intact.
    - `pnpm --filter @imbau/api typecheck` passes.
  </acceptance_criteria>
  <done>leadsRouter with the 4 procedures, extended updateSettings, and registration — all cloning the settled write mold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: leads-role-gate cross-role + transition + timeline + idempotency matrix vs real Postgres</name>
  <read_first>
    - packages/api/tests/units-role-gate.test.ts (L1-70+ — the harness: createCaller, fixtures makeUserWithActiveOrg/mintMemberInOrg, ownerSql, the owner/developer/viewer/other-org/non-existent matrix, events audit assertion)
    - packages/api/tests/projects-role-gate.test.ts (the Phase 9 canary matrix — keep it green)
    - packages/api/tests/fixtures.ts + packages/api/tests/db.ts (harness helpers)
  </read_first>
  <behavior>
    - listForProject resolves origen: broker pointer → broker join; unit pointer → unit; quote pointer → quote; all-null → `Directo`; cross-org projectId → empty.
    - updateEstado matrix: owner✓ persists + writes an events row; developer✓; viewer✗ FORBIDDEN (pre-write, no events row); other-org owner✗ NOT_FOUND; non-existent leadId✗ NOT_FOUND.
    - updateEstado into `cerrado` without `desenlace` → 400 (refine); with `ganado`/`perdido` → persists desenlace + appends the auto LeadNote (estadoPrev→estadoNuevo).
    - addNote appends a free-text LeadNote in ts order, writes NO events row, and enqueues nothing.
    - create inserts + returns id, seeds the t=0 creation LeadNote, and enqueues exactly one job; enqueuing the same lead twice (mock the queue) yields one job (jobId dedup); updateEstado/addNote enqueue zero jobs.
    - create with a failing/rolled-back insert (e.g. a non-existent/cross-org `projectId` FK, so the tenant tx throws) surfaces the error AND enqueues ZERO jobs — proving the enqueue is a post-commit side-effect placed OUTSIDE the `withTenant(...)` closure (a pre-commit enqueue inside the tx would leak a job for a lead that never persisted).
  </behavior>
  <files>packages/api/tests/leads-role-gate.test.ts</files>
  <action>
    Clone `units-role-gate.test.ts` into `leads-role-gate.test.ts`. Seed a project + broker + unit +
    quote + leads (in FK order via `ownerSql`) for orgA and a second org orgB. Assert every behavior in
    `<behavior>` THROUGH `createCaller` (the app_authenticated role) — the owner pool appears only in
    seeding + post-hoc audit reads, never inside an assertion. For the enqueue assertions, `vi.mock`
    `../src/leads/runtime` (or the storage queue) so no live Redis runs and the call count is asserted.
    Include a rollback case: call `create` with a `projectId` that does not exist in the caller's tenant
    (so the leads→projects FK insert throws inside `withTenant`), assert the call rejects AND the
    `enqueueLeadEmail` mock was called 0 times — the structural proof that the enqueue sits after the
    committed persist, outside the tx. Verify the events audit row exists for a committed transition and
    is absent for a FORBIDDEN one.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/api test -- leads-role-gate projects-role-gate</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `leads-role-gate.test.ts` passes the full matrix (origen resolution, cross-role, cerrado refine, timeline order, events audit, enqueue-once idempotency, transitions/notes enqueue-zero).
    - Test assertion (load-bearing, D-06): a `create` with a rolled-back/failing insert (non-existent/cross-org `projectId`) rejects AND leaves the `enqueueLeadEmail` mock at 0 calls — proving the post-commit, outside-`withTenant` enqueue placement.
    - Test command: `projects-role-gate.test.ts` (Phase 9 canary) still passes after the updateSettings extension.
  </acceptance_criteria>
  <done>Green cross-role/transition/timeline/idempotency matrix vs real Postgres; canary intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel client → tRPC leads mutations | untrusted input (estado, desenlace, notifyEmail, PII) crosses here |
| leads.create → Redis (enqueue) | job payload (ids only) pushed after persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Elevation of Privilege | create/updateEstado/addNote | high | mitigate | `requireRole("owner","developer")` on every write; viewer → FORBIDDEN pre-write; proven by the cross-role matrix (not UI hiding). |
| T-11-06 | Information Disclosure | cross-tenant lead PII (nombre/contacto) | high | mitigate | `withTenant`/RLS on every read + write; cross-org id invisible → 0 rows → NOT_FOUND (no-enumeration); orthogonal to requireRole, both verified. |
| T-11-07 | Tampering | `leadsNotifyEmail` input | high | mitigate | Zod `.email().nullable()` at the boundary (malformed → 400); recipient never client-controlled at send time — the worker resolves it from the validated column / org owners. |
| T-11-08 | Denial of Service | notification-spam amplification | medium | mitigate | Enqueue only on `create` (never transitions/notes); `jobId = lead:{id}:created` dedups retries; a Redis push failure surfaces as the create error (observable), never a silent retry loop. |
| T-11-SC | Tampering | npm/pnpm installs | low | mitigate | No new packages — router reuses existing `@imbau/api` deps (drizzle, trpc, zod, bullmq, ioredis). |
</threat_model>

<verification>
- `pnpm --filter @imbau/api typecheck` green.
- `leads-role-gate.test.ts` + `projects-role-gate.test.ts` green vs real Postgres.
- Grep-fence: no `createOwnerDb`/`appDb` in `leads.ts`.
- Grep: `enqueueLeadEmail` called only in `create`.
</verification>

<success_criteria>
leadsRouter delivers LEADS-01..04 through the settled write mold; authorization, tenant isolation,
no-enumeration, state-machine enforcement, timeline ordering, and enqueue-once idempotency all proven
against real Postgres; the Phase 9 canary stays green.
</success_criteria>

<output>
Create `.planning/phases/11-d2-bandeja-de-leads-notificaci-n-por-email/11-03-SUMMARY.md` when done.
</output>
