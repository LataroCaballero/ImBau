---
phase: 08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ".claude/agents/ (GSD tooling — chore: commit, D-02)"
  - ".claude/commands/gsd/ (GSD tooling — chore: commit, D-02)"
autonomous: true
requirements: [DEBT-01]
must_haves:
  truths:
    - "PR #5 is merged into `main` via a merge commit (NOT squash), preserving the 397-commit Conventional-Commits history and the v1.2 git tag (D-01)."
    - "The `deploy-staging.yml` Actions run triggered by the merge finishes with conclusion `success` and pushes 4 GHCR images (imbau-web / imbau-panel / imbau-worker / imbau-migrate) tagged with the merge commit SHA (DEBT-01)."
    - "The migrate container exits 0 and applies migrations 0004+ BEFORE the web/panel/worker containers are swapped; the app/anon role bootstrap runs between migrate and swap (deploy.sh migrate-before-swap ordering, D-05/D-07)."
    - "Seed 'Brigos Recoleta' exists and is `publicado` in the staging database — run the seed against staging if missing (D-05)."
    - "The GSD-tooling working-tree changes (`.claude/agents/*` modified, `.claude/commands/gsd/*` deleted) and the 6 pending local commits are committed as `chore:` and pushed to origin so `main` reflects the real repo state before merge (D-02)."
    - statement: "A deploy in which the migrate container finds no pending migrations still swaps the app containers cleanly (empty-migration input)."
      verification: backstop
    - statement: "If the migrate container exits non-zero, deploy.sh aborts under `set -e` and leaves the previously-running app images serving staging (interrupted-deploy safe state, D-07)."
      verification: backstop
  prohibitions:
    - statement: "Do not squash-merge PR #5 — squash orphans the v1.2 tag and loses Conventional-Commits traceability (D-01)."
      status: flagged-unverified
    - statement: "Do not delete the `fase-0/foundation` branch during the merge (D-03)."
      status: flagged-unverified
    - statement: "Do not merge while the `quality` branch-protection check is red, and never push directly to `main` (branch protection with enforce_admins, D-07)."
      status: flagged-unverified
    - statement: "Do not deploy a stale `latest` image — the pipeline must pin IMAGE_TAG to the exact merge SHA."
      status: flagged-unverified
    - statement: "On the shared VPS, do not touch the prod nginx/host, and inspect any process's identity before any kill (D-08)."
      status: flagged-unverified
  artifacts:
    - "merge commit on `main` (fase-0/foundation merged)"
    - "chore: commit of GSD tooling on fase-0/foundation, pushed to origin"
    - "4 GHCR images tagged by the merge SHA"
    - "migrations 0004+ applied in the staging Postgres"
    - "seed rows for 'Brigos Recoleta' present + publicado in staging"
  key_links:
    - "merge → push to `main` → deploy-staging.yml trigger"
    - "IMAGE_TAG=<merge SHA> → GHCR images → VPS `git reset --hard origin/main` + `docker compose pull` (exact image parity)"
    - "migrate-before-swap gate (migrate exit 0) → role bootstrap → app container swap"
---

<objective>
Land all of v1.2 on `main` and get it running verified in staging. Bring the working tree to a truthful state (commit + push the pending GSD-tooling changes and 6 local commits as `chore:`, D-02), ensure PR #5 is up-to-date with `main` and the `quality` check is green, then merge PR #5 with a **merge commit** (D-01, not squash — preserves the 397-commit history and the v1.2 tag), keeping the `fase-0/foundation` branch (D-03). Monitor the `deploy-staging.yml` run to green (D-09), then confirm over SSH that the migrate-before-swap gate applied migrations 0004+ and the app containers swapped to the merge SHA, and that seed 'Brigos Recoleta' is `publicado` in staging — running the seed against staging if missing (D-05). Contingency is fix-forward via small PRs; a migrate failure is a safe state that leaves the old images running (D-07). This satisfies DEBT-01.

Purpose: Staging currently runs a pre-fase-5 image; no panel feature (v1.3) may build on unverified infra. This plan makes `main` = the real repo and puts the exact v1.2 images live.
Output: A merge commit on `main`, 4 GHCR images tagged by the merge SHA, migrations 0004+ applied, and seed present in staging.
Note: The human visual pass of fase 6 stays deferred to `/gsd-verify-work 6` (D-06) — out of scope here.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md
@.github/workflows/deploy-staging.yml
@deploy/deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-merge repo hygiene — commit tooling, push, make PR #5 up-to-date + green (D-02)</name>
  <files>.claude/agents/ (chore: commit), .claude/commands/gsd/ (chore: commit)</files>
  <read_first>
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md (D-02, D-07 — real repo state before merge, no direct push)
    - CLAUDE.md (Conventional Commits, English commit messages, Co-Authored-By trailer)
    - .github/workflows/ci.yml (the `quality` job that branch protection requires green)
  </read_first>
  <action>
    On branch `fase-0/foundation`: stage the pending GSD-tooling changes (`git add .claude/agents .claude/commands/gsd` plus any other working-tree GSD changes surfaced by `git status --porcelain`) and commit as `chore: sync GSD tooling (agents + commands)` with the `Co-Authored-By: Claude <noreply@anthropic.com>` trailer (D-02, CLAUDE.md). Do NOT commit `.planning/` phase-8 plan docs here — those are committed by the planner/orchestrator. Push the branch: `git push origin fase-0/foundation` (this carries the 6 local commits + the new chore commit). Then reconcile PR #5 with `main`: run `gh pr view 5 --json mergeStateStatus,mergeable`; PR #5 is currently `BEHIND` (branch protection requires up-to-date), so run `gh pr update-branch 5` to merge `origin/main` into the head branch. Wait for the `quality` check to pass with `gh pr checks 5 --watch` (or poll `gh pr checks 5`). Do not proceed until `quality` is green AND `mergeStateStatus` is no longer `BEHIND`/`DIRTY`. If `update-branch` surfaces a conflict (should not — PR is MERGEABLE), stop and surface it as an infra blocker (D-09) rather than force-resolving.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain | grep -E '^\s*[MD]\s+\.claude/')" && gh pr checks 5 --jq '.[] | select(.name=="quality") | .state' | grep -qx SUCCESS && gh pr view 5 --json mergeStateStatus --jq .mergeStateStatus | grep -Eqx 'CLEAN|UNSTABLE|HAS_HOOKS'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` shows no uncommitted `.claude/agents/*` or `.claude/commands/gsd/*` entries (all committed).
    - `git log origin/fase-0/foundation -1 --pretty=%s` includes the `chore:` tooling commit (branch pushed, origin not behind local).
    - `gh pr checks 5` reports the `quality` check `state` = `SUCCESS`.
    - `gh pr view 5 --json mergeStateStatus --jq .mergeStateStatus` is NOT `BEHIND` and NOT `DIRTY` (i.e. CLEAN / UNSTABLE / HAS_HOOKS — mergeable with green required check).
  </acceptance_criteria>
  <done>Working tree tooling committed as `chore:`, branch pushed, PR #5 up-to-date with `main`, and the `quality` check green — ready to merge.</done>
</task>

<task type="auto">
  <name>Task 2: Merge PR #5 with a merge commit + drive the staging deploy to green (D-01, D-03, D-09)</name>
  <files>main (merge commit — no repo file modified); drives .github/workflows/deploy-staging.yml (read-only)</files>
  <read_first>
    - .github/workflows/deploy-staging.yml (push-to-main trigger, 4-image matrix, SSH deploy with IMAGE_TAG=github.sha)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md (D-01 merge commit, D-03 keep branch, D-09 Claude drives the run)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-PATTERNS.md ("Merge + deploy trigger")
  </read_first>
  <action>
    Merge PR #5 with the **merge-commit** strategy: `gh pr merge 5 --merge` (D-01 — NOT `--squash`, NOT `--rebase`; squash orphans the v1.2 tag). Do NOT pass `--delete-branch` — keep `fase-0/foundation` (D-03). Confirm merge: `gh pr view 5 --json state` returns `MERGED` and capture the merge commit SHA (`gh pr view 5 --json mergeCommit --jq .mergeCommit.oid`). The push to `main` triggers `deploy-staging.yml`; identify the run with `gh run list --workflow=deploy-staging.yml --branch main --limit 1 --json databaseId,headSha,status` (headSha must equal the merge SHA) and follow it to completion with `gh run watch <databaseId>` (D-09). If the run fails, apply fix-forward via a small PR to `main` (D-07) — do NOT push directly to `main`. On success, confirm all 4 images were pushed to GHCR tagged by the merge SHA (query the packages via `gh api` for `imbau-web/imbau-panel/imbau-worker/imbau-migrate`, or accept the run's build-job success as the image-publish signal). This satisfies DEBT-01 (merged + deploy triggered).
  </action>
  <verify>
    <automated>gh pr view 5 --json state --jq .state | grep -qx MERGED && gh run list --workflow=deploy-staging.yml --branch main --limit 1 --json conclusion --jq '.[0].conclusion' | grep -qx success</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view 5 --json state --jq .state` returns `MERGED`.
    - `gh pr view 5 --json mergeCommit --jq .mergeCommit.oid` is non-empty and the merge is a merge commit (the merge commit has 2 parents: `git cat-file -p <sha>` shows two `parent` lines) — confirms NOT squash (D-01).
    - `gh api repos/{owner}/{repo}/branches/fase-0/foundation` returns 200 (branch still exists, D-03).
    - `gh run list --workflow=deploy-staging.yml --branch main --limit 1 --json conclusion,headSha` shows `conclusion=success` and `headSha` equal to the merge commit SHA.
  </acceptance_criteria>
  <done>PR #5 merged via merge commit, branch retained, and the `deploy-staging.yml` run for the merge SHA finished green with all 4 images published.</done>
</task>

<task type="auto">
  <name>Task 3: Verify migrate-before-swap + seed 'Brigos Recoleta' on staging over SSH (D-05, D-07, D-08)</name>
  <files>staging VPS Postgres (migrations 0004+, seed rows) — no repo file modified; reads deploy/deploy.sh</files>
  <read_first>
    - deploy/deploy.sh (steps 6-8: migrate-before-swap gate → role bootstrap → app swap; the exact container/compose invocations)
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-PATTERNS.md ("Migrate-before-swap gate", "SSH inspection convention")
    - .planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-CONTEXT.md (D-05 seed prerequisite, D-08 shared prod VPS safety)
  </read_first>
  <action>
    SSH to the staging VPS as `root@31.97.175.128` with key `id_vps_andescode` (D-08 — VPS is SHARED with prod `andescode.com.ar`: never touch prod nginx/host; inspect any process identity before any kill). Confirm the deploy landed the merge SHA: `cd /opt/imbau && git rev-parse HEAD` equals the merge commit SHA, and the running app containers use the SHA-tagged images (`docker compose -f deploy/compose.staging.yml ps` + `docker inspect` image tag on web/panel/worker). Confirm the migrate-before-swap gate applied migrations 0004+: inspect the one-off migrate container's last-run logs and query the Drizzle migrations bookkeeping table inside the postgres container (`docker compose -f deploy/compose.staging.yml exec -T postgres psql -U imbau -d imbau -c "select count(*) from drizzle.__drizzle_migrations;"` — assert the count reflects 0004+; the migrate container exited 0, D-05/D-07). Confirm seed 'Brigos Recoleta' is present and publicado: query `select estado from projects where slug='brigos-recoleta';` and assert a `publicado` row exists. If the seed is missing, run the deterministic idempotent seed against staging per README (`pnpm db:seed` equivalent inside the worker/migrate context on the box, or `docker compose ... run --rm` the seed entrypoint) — it is safe to re-run (D-05). Do NOT run drizzle-kit or hand-edit schema (migrate container is the only schema path). Capture the raw command output for reuse as evidence in plan 08-02's UAT doc (D-10).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/id_vps_andescode -o StrictHostKeyChecking=accept-new root@31.97.175.128 "cd /opt/imbau && docker compose -f deploy/compose.staging.yml exec -T postgres psql -U imbau -d imbau -tAc \"select estado from projects where slug='brigos-recoleta'\"" | grep -qx publicado</automated>
  </verify>
  <acceptance_criteria>
    - `cd /opt/imbau && git rev-parse HEAD` on the VPS equals the merge commit SHA from Task 2.
    - `docker inspect` on the web/panel/worker containers shows image refs tagged with the merge SHA (not `a599bb7`/pre-fase-5, not stale `latest`).
    - The Drizzle migrations table count on staging reflects migrations 0004+ applied (strictly greater than the pre-fase-5 baseline).
    - `select estado from projects where slug='brigos-recoleta'` returns a row with `publicado` (seed present; run seed if it was missing).
    - No prod process was killed and the prod nginx/host was untouched (only imbau compose containers inspected — D-08).
  </acceptance_criteria>
  <done>Staging runs the merge-SHA images, migrations 0004+ are applied via the migrate-before-swap gate, and seed 'Brigos Recoleta' is publicado — DEBT-01 complete and the cotizador prerequisite is in place for 08-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git branch → `main` (CI/CD) | The merge decides exactly what code the deploy pipeline builds and ships. |
| CI runner → GHCR → VPS | Image supply chain: what is built, tagged, pulled, and swapped. |
| operator SSH → shared prod VPS | Privileged root access to a box that also hosts live prod (`andescode.com.ar`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | merge to `main` / deploy-staging.yml | high | mitigate | Require `quality` check green + PR up-to-date (`gh pr update-branch 5`) before `gh pr merge --merge`; branch protection (enforce_admins) blocks direct push; no `--squash` (D-01). |
| T-08-02 | Tampering | GHCR image tag → VPS pull | medium | mitigate | Pin IMAGE_TAG to the exact merge SHA (workflow invariant); verify the deployed container images are tagged with the merge SHA over SSH (Task 3). |
| T-08-03 | Elevation of Privilege | SSH `root@` shared prod VPS | high | mitigate | Read-only inspection of imbau compose containers only; never touch prod nginx/host; inspect any process identity before any kill (D-08, MEMORY never-kill-unverified-pid). |
| T-08-04 | Information Disclosure | SOPS-decrypted `deploy/.env` on VPS | medium | mitigate | deploy.sh writes the env `chmod 600`; do not `cat`/echo secret values into logs or evidence capture. |
| T-08-05 | Denial of Service | migrate-before-swap on shared box | low | accept | migrate failure aborts under `set -e` leaving old images running (safe state, D-07); `free -m` RAM headroom checked in deploy.sh on the co-tenant prod box. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — the v1.2 lockfile is already merged and was audited during v1.2; no new dependencies added. Legitimacy checkpoint N/A (no `[ASSUMED]`/`[SUS]` installs). |
</threat_model>

<verification>
- DEBT-01 satisfied: `gh pr view 5 --json state` = `MERGED` (merge commit, 2 parents), `deploy-staging.yml` run for the merge SHA green, 4 GHCR images published, staging containers on the merge SHA, migrations 0004+ applied, seed 'Brigos Recoleta' publicado.
- Edge-probe coverage (spec-less fallback): DEBT-01 adjacency (merge-commit even at history parity, D-01) → truth; ordering (migrate before swap) → truth; empty (no-pending-migration deploy still swaps) → backstop; interruption/concurrency (migrate failure = old images stay) → backstop. No edge item dropped.
- Prohibitions recorded (descriptor-less): no squash, keep branch, no red-check/direct-push merge, no stale `latest`, no prod-host touch.
</verification>

<success_criteria>
ROADMAP Phase 8 criterion 1 is met: `fase-0/foundation` is merged to `main` and the automatic deploy left the 4 v1.2 images running on `staging.tours.andescode.com.ar`, with migrations 0004+ applied and seed present — verified over SSH.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08-01)

- **Merge commit on `main`** — PR #5 merged via merge-commit strategy (D-01), 2 parents, v1.2 tag preserved, `fase-0/foundation` retained (D-03).
- **`chore:` commit** of GSD tooling (`.claude/agents/*`, `.claude/commands/gsd/*`) on `fase-0/foundation`, pushed to origin (D-02).
- **4 GHCR images** tagged by the merge SHA: `imbau-web`, `imbau-panel`, `imbau-worker`, `imbau-migrate`.
- **Deployed staging containers** (web/panel/worker) swapped to the merge SHA on the VPS (`/opt/imbau` reset to `origin/main`).
- **Applied migrations 0004+** in the staging Postgres (durable DB state via migrate-before-swap).
- **Seed rows for 'Brigos Recoleta'** present + `publicado` in staging (run against staging if missing, D-05).
</artifacts_produced>

<output>
Create `.planning/phases/08-deuda-v1-2-merge-a-main-re-verificaci-n-en-staging/08-01-SUMMARY.md` when done (record the merge commit SHA, the deploy run id, the applied-migration count, and the seed status; carry the captured SSH/psql output forward for 08-02's UAT evidence).
</output>
