---
phase: 01-monorepo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .nvmrc
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
autonomous: true
requirements: [PROC-01, MONO-01]
must_haves:
  truths:
    - "El estado untracked del repo (.claude/, docs/) está commiteado en main antes de cualquier scaffolding"
    - "Existe la rama fase-0/foundation creada desde el main commiteado"
    - "Corepack activa pnpm@11.6.0 y `pnpm --version` reporta exactamente 11.6.0"
    - "El workspace root declara apps/* y packages/* y los scripts root delegan a turbo"
    - "turbo.json usa la key `tasks` (no `pipeline`) con dev, build, lint, typecheck, test"
    - "D-15: la rama `fase-0/foundation` que este plan abre se cierra al final de la fase vía PR en GitHub + merge `--no-ff` (acción de ship-time, fuera del scope de tasks de scaffolding; preserva los commits atómicos de GSD)"
  artifacts:
    - path: "package.json"
      provides: "Root workspace manifest con packageManager pin y scripts → turbo"
      contains: "\"packageManager\": \"pnpm@11.6.0\""
    - path: "pnpm-workspace.yaml"
      provides: "Declaración de workspaces apps/* + packages/*"
      contains: "packages:"
    - path: "turbo.json"
      provides: "Task graph Turborepo 2.x con key tasks"
      contains: "\"tasks\""
    - path: ".nvmrc"
      provides: "Pin de Node 22 LTS"
      contains: "22"
    - path: ".gitignore"
      provides: "Ignora node_modules, .next, .turbo, dist, .env*"
      contains: ".env"
  key_links:
    - from: "package.json scripts"
      to: "turbo"
      via: "turbo run <task>"
      pattern: "turbo run"
    - from: "pnpm-workspace.yaml"
      to: "apps/*, packages/*"
      via: "workspace globs"
      pattern: "packages/\\*"
---

<objective>
Sentar la base del workspace antes de cualquier código: cumplir PROC-01 (commitear el estado untracked y crear la rama `fase-0/foundation`), activar la toolchain pinneada (Corepack → pnpm 11.6.0, Node 22 vía `.nvmrc`), y crear el esqueleto raíz del monorepo (root `package.json`, `pnpm-workspace.yaml`, `turbo.json` con key `tasks`, `.gitignore`). Sin este andamiaje, ningún paquete ni app puede instalarse o compilar.

Purpose: Es el Wave 1 que desbloquea todo. PROC-01 exige literalmente commitear antes de ramificar (Pitfall 5), y la raíz del workspace es el contenedor que todos los planes posteriores extienden. Establece la convención de toolchain (Corepack-pinned pnpm, Node 22) que las fases 2-4 heredan.
Output: Rama `fase-0/foundation` desde un main limpio; root manifest + workspace config + turbo task graph + `.gitignore` + `.nvmrc` commiteables.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (newly introduced — not drift):
- File `package.json` (root) — fields: `packageManager`, `engines.node`, scripts `dev`/`build`/`lint`/`typecheck`/`test`/`format`
- File `pnpm-workspace.yaml` — `packages:` globs `apps/*`, `packages/*`
- File `turbo.json` — `tasks` key with tasks `dev`, `build`, `lint`, `typecheck`, `test`
- File `.nvmrc` — content `22`
- File `.gitignore` — entries for `node_modules`, `.next`, `.turbo`, `dist`, `.env*`
- Git branch `fase-0/foundation`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Commitear estado untracked y crear rama fase-0/foundation (PROC-01)</name>
  <files>.git (rama fase-0/foundation) — operaciones git + corepack, sin archivos fuente</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-13 a D-16: flujo de ramas; D-16 fija el nombre literal `fase-0/foundation`)
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md (Pitfall 5: commitear antes de ramificar; Pitfall 6: pnpm 0.34.1 en PATH vs pin; Open Question 3: D-16 vence al template config.json)
    - CLAUDE.md (Convenciones: Conventional Commits, ramas `fase-N/descripcion`)
  </read_first>
  <action>
    Cumplir PROC-01 en el orden literal del CONTEXT (Pitfall 5) — todo vía CLI, autónomo:
    1. Estando en `main`, stagear y commitear el estado untracked actual (`.claude/` y `docs/`) con un Conventional Commit `chore: commit initial repo state before fase-0 scaffolding`. Los artefactos previos al branch (CONTEXT.md, RESEARCH.md ya en `.planning/`) deben quedar en main (D-14). No incluir todavía ningún archivo de scaffolding — este commit es solo el estado actual.
    2. Crear y cambiar a la rama `fase-0/foundation` desde ese main commiteado (`git checkout -b fase-0/foundation`). El nombre es LITERAL por D-16 y vence el `phase_branch_template` de config.json (Open Question 3 — D-16 gana). No usar el template `gsd/phase-{phase}-{slug}` para esta fase.
    3. Activar la toolchain pinneada: `corepack enable` y `corepack prepare pnpm@11.6.0 --activate` para que el pnpm 0.34.1 que está en PATH (Pitfall 6) quede sombreado por el shim de Corepack. Verificar con `pnpm --version` que reporte exactamente 11.6.0; si no, no continuar (el resto del plan depende del pnpm correcto).
  </action>
  <acceptance_criteria>
    - `git log --oneline -1 main` muestra un commit `chore: commit initial repo state before fase-0 scaffolding` (o equivalente Conventional Commit) que incluye `.claude/` y `docs/`
    - `git rev-parse --abbrev-ref HEAD` imprime `fase-0/foundation`
    - `git status --porcelain` no lista `.claude/` ni `docs/` como untracked (quedaron tracked en el commit de main)
    - `pnpm --version` imprime exactamente `11.6.0`
  </acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = "fase-0/foundation" && test "$(pnpm --version)" = "11.6.0" && echo OK</automated>
  </verify>
  <done>El repo está en la rama `fase-0/foundation` creada desde un main que tiene el estado inicial commiteado, y pnpm 11.6.0 está activo vía Corepack.</done>
</task>

<task type="auto">
  <name>Task 2: Crear la raíz del workspace (package.json, pnpm-workspace.yaml, turbo.json, .nvmrc, .gitignore)</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, .nvmrc, .gitignore</files>
  <read_first>
    - CLAUDE.md (sección "Technology Stack": pnpm 11.6.0 vía packageManager + Corepack, Turborepo 2.9.18 con key `tasks` no `pipeline`, Node 22 LTS en `.nvmrc` + `engines`, TS 5.9.x; "What NOT to Use": prohibido `pipeline`, prohibido instalar pnpm global vía npm)
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md ("Recommended Project Structure"; "Pattern 1: pnpm workspace + Turborepo task graph" con el ejemplo de turbo.json; "Runtime State Inventory": `.gitignore` desde el inicio; Pitfall 1: `.nvmrc`=22 + `engines.node` >=22 como warning)
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-08: `pnpm dev` = `turbo dev`, puertos web 3000 panel 3001; Claude's Discretion: estructura interna de turbo.json patrón estándar 2.x; naming `@imbau/*`)
  </read_first>
  <action>
    Crear los archivos raíz del monorepo (Discretion D: estructura interna de turbo.json y naming `@imbau/*`):
    - Root `package.json`: `"name": "imbau"`, `"private": true`, `"packageManager": "pnpm@11.6.0"` (NO instalar pnpm global), `"engines": { "node": ">=22" }` (Pitfall 1 — warning en fase 1, hard-enforce llega con CI en fase 4 per Open Question 1). Scripts que delegan TODO a turbo (D-08): `dev` → `turbo run dev`, `build` → `turbo run build`, `lint` → `turbo run lint`, `typecheck` → `turbo run typecheck`, `test` → `turbo run test`, `format` → `prettier --write .` (la config de prettier la cablea el plan 02). Agregar `turbo@2.9.18` y `typescript@5.9` como devDependencies del root (las versiones exactas de tooling compartido las instala el plan 02; acá solo el pin de turbo para el task runner).
    - `pnpm-workspace.yaml`: `packages:` con globs `apps/*` y `packages/*`.
    - `turbo.json`: key `tasks` (NUNCA `pipeline` — removido en Turborepo 2.x). Definir tasks `build` (`dependsOn: ["^build"]`, `outputs: [".next/**", "!.next/cache/**", "dist/**"]`), `typecheck` (`dependsOn: ["^typecheck"]` — esta arista impone el orden de deps sin TS project references, D-07), `lint` (sin deps), `test` (`dependsOn: ["^build"]`), `dev` (`cache: false`, `persistent: true`). Incluir el `$schema` `https://turborepo.dev/schema.json`.
    - `.nvmrc`: contenido `22` (Pitfall 1).
    - `.gitignore`: ignorar `node_modules`, `.next`, `.turbo`, `dist`, y `.env*` (este último es requisito de seguridad V14 — secrets nunca commiteados; el plan 03 commiteará solo `.env.example`). Excluir `.env.example` del ignore (`!.env.example`) para que sí se versione.
  </action>
  <acceptance_criteria>
    - `package.json` contiene `"packageManager": "pnpm@11.6.0"` y un `engines.node` `>=22`
    - El bloque de scripts de `package.json` referencia `turbo run` en al menos 4 scripts (dev, build, lint, typecheck, test)
    - `turbo.json` contiene la cadena `"tasks"` y NO contiene `"pipeline"`
    - `pnpm-workspace.yaml` contiene `apps/*` y `packages/*`
    - `.nvmrc` contiene `22`
    - `.gitignore` contiene `node_modules`, `.next`, `.turbo`, `dist`, un patrón `.env*` y la negación `!.env.example`
    - `pnpm install` completa sin error (instala turbo + typescript en root)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"packageManager": "pnpm@11.6.0"' package.json && test "$(grep -c 'turbo run' package.json)" -ge 4 && grep -q '"tasks"' turbo.json && ! grep -q '"pipeline"' turbo.json && grep -q 'apps/\*' pnpm-workspace.yaml && grep -q '!.env.example' .gitignore && pnpm install >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>El root manifest pinnea pnpm 11.6.0 y Node 22, los scripts delegan a turbo, `turbo.json` usa `tasks`, el workspace declara apps/* + packages/*, `.env*` está ignorado salvo `.env.example`, y `pnpm install` corre limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → control de versiones | Lo que se commitea queda en el historial; secrets nunca deben cruzar este límite |
| toolchain de PATH → ejecución | Un binario `pnpm` viejo (0.34.1 en PATH) puede sombrear el pin de Corepack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.gitignore` / archivos `.env` | mitigate | `.gitignore` ignora `.env*` desde el primer commit; solo `.env.example` (sin valores reales, vía `!.env.example`) se versiona (V14, D-04). El plan 03 crea los `.env.example`. |
| T-01-02 | Tampering | pnpm en PATH (0.34.1) vs pin 11.6.0 | mitigate | Corepack `enable` + `prepare pnpm@11.6.0 --activate` + `packageManager` field; verificación `pnpm --version` == 11.6.0 (Pitfall 6). |
| T-01-SC | Tampering | instalaciones npm/pnpm | mitigate | Package Legitimacy Audit de RESEARCH.md ya corrió (todos OK; dos `too-new` son falsos positivos explicados). No hay `[ASSUMED]`/`[SUS]` bloqueantes — `typescript-eslint` quedó verificado en 8.61.0 al planear. |
</threat_model>

<verification>
- `git rev-parse --abbrev-ref HEAD` == `fase-0/foundation`
- `pnpm --version` == `11.6.0`
- `turbo.json` usa `tasks`, no `pipeline`
- `pnpm install` corre sin error desde la raíz
- `.gitignore` cubre `.env*` y exceptúa `.env.example` (no se filtran secrets)
</verification>

<success_criteria>
- PROC-01 cumplido: estado inicial commiteado en main + rama `fase-0/foundation` creada
- Toolchain pinneada y verificada (pnpm 11.6.0, Node 22 declarado)
- Raíz del workspace existe y `pnpm install` corre limpio
- `turbo.json` con key `tasks` y task graph estándar 2.x
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation/01-01-SUMMARY.md` when done
</output>
