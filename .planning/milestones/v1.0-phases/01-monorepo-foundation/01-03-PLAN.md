---
phase: 01-monorepo-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/env.ts
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/env.test.ts
  - apps/web/.env.example
  - apps/panel/package.json
  - apps/panel/tsconfig.json
  - apps/panel/next.config.ts
  - apps/panel/env.ts
  - apps/panel/app/layout.tsx
  - apps/panel/app/page.tsx
  - apps/panel/.env.example
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/worker/tsup.config.ts
  - apps/worker/src/env.ts
  - apps/worker/src/env.test.ts
  - apps/worker/src/index.ts
  - apps/worker/.env.example
autonomous: true
requirements: [MONO-01, MONO-03]
must_haves:
  truths:
    - "Las tres apps (web, panel, worker) existen y compilan consumiendo packages vía la cadena de deps descendente"
    - "Cada app valida su env con @t3-oss/env componiendo presets compartidos desde packages/config; cada app declara solo lo que usa"
    - "Arrancar una app con una env faltante o inválida falla rápido con un mensaje agregado que lista todas las variables fallidas con nombre y motivo"
    - "web y panel muestran una página de status mínima en es-AR (nombre, entorno, env validó)"
    - "El worker es un shell deployable que bootea logueando ok, sin lógica de jobs BullMQ"
    - "Existe un .env.example por app, consistente con su schema, versionado como doc viva"
    - "Un test Vitest verifica que dos vars inválidas surfacean ambos nombres (error agregado)"
  artifacts:
    - path: "apps/web/env.ts"
      provides: "Validación de env de web vía @t3-oss/env-nextjs (server + NEXT_PUBLIC_*)"
      exports: ["env"]
    - path: "apps/worker/src/env.ts"
      provides: "Validación de env del worker vía @t3-oss/env-core (DATABASE_URL, REDIS_URL, NODE_ENV)"
      exports: ["env"]
    - path: "apps/web/app/page.tsx"
      provides: "Página de status es-AR de web"
    - path: "apps/panel/app/page.tsx"
      provides: "Página de status es-AR de panel"
    - path: "apps/worker/src/index.ts"
      provides: "Shell deployable del worker (boot + log ok, sin jobs)"
    - path: "apps/worker/src/env.test.ts"
      provides: "Test de error agregado de env (dos vars inválidas → ambos nombres)"
  key_links:
    - from: "apps/web/env.ts, apps/panel/env.ts, apps/worker/src/env.ts"
      to: "@imbau/config/env/presets"
      via: "import + composición de presets"
      pattern: "@imbau/config/env/presets"
    - from: "apps/worker/src/index.ts"
      to: "./env"
      via: "import de env primero para validar al boot"
      pattern: "import.*env"
    - from: "apps/web/next.config.ts, apps/panel/next.config.ts"
      to: "transpilePackages [@imbau/ui, @imbau/config, ...]"
      via: "consumo JIT de packages workspace"
      pattern: "transpilePackages"
---

<objective>
Crear las tres apps (`web`, `panel`, `worker`) en la cima del DAG, cerrando MONO-01 (deps estrictamente descendentes apps → api → db/ui → config, todo compila) y entregando MONO-03 (validación de env tipada con Zod vía `@t3-oss/env` que falla rápido al boot con errores agregados). web y panel muestran una página de status es-AR (D-09); el worker es un shell deployable sin lógica de jobs (out of scope). Cada app tiene su `.env.example` versionado y un test que prueba el error agregado.

Purpose: MONO-03 es el último requirement de la fase y la prueba de fuego de la costura: cada app compone solo los presets de env que usa (D-02 — web no valida `REDIS_URL`, worker no valida `NEXT_PUBLIC_*`) y falla al boot/build con un mensaje accionable (D-04). Las apps son la cima del grafo: que compilen consumiendo ui/config valida toda la cadena descendente. El status page es-AR es la primera UI visible y aplica la convención de idioma desde el día uno.
Output: web/panel (Next 16) con env.ts + status page es-AR + .env.example; worker (tsx/tsup) shell + env.ts + .env.example; tests de env agregado; `pnpm build`, `pnpm lint`, `pnpm typecheck`, `pnpm test` verdes en todo el workspace.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols/files this plan creates (newly introduced — not drift):
- App `apps/web` — files: `env.ts` (exports `env`), `next.config.ts` (`transpilePackages`, `output: 'standalone'`), `app/layout.tsx`, `app/page.tsx` (status es-AR), `env.test.ts`, `.env.example`
- App `apps/panel` — mirror of web on port 3001
- App `apps/worker` — files: `src/env.ts` (exports `env`), `src/index.ts` (boot shell), `src/env.test.ts`, `tsup.config.ts`, `.env.example`
- Env var names introduced: `NODE_ENV`, `NEXT_PUBLIC_APP_ENV` (web/panel), `DATABASE_URL`, `REDIS_URL` (worker)
- Scripts: per-app `dev`, `build`, `lint`, `typecheck`, `test`; worker `tsx watch` (dev) + `tsup` (build)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker — shell deployable + env-core con error agregado (MONO-03, D-06)</name>
  <files>apps/worker/package.json, apps/worker/tsconfig.json, apps/worker/tsup.config.ts, apps/worker/src/env.ts, apps/worker/src/env.test.ts, apps/worker/src/index.ts, apps/worker/.env.example</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md ("Code Examples → Worker env — @t3-oss/env-core" con `createEnv` + `runtimeEnv` + `skipValidation`; "Worker boot" `src/index.ts` import env primero; "Vitest test for aggregated env error" exacto; "Standard Stack": @t3-oss/env-core 0.13.11, tsx 4.22.4, tsup 8.5.1; Pitfall 4: error agregado; A2: t3-env agrega issues por default)
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-01: @t3-oss/env-core para worker, no helper propio; D-02: worker valida DATABASE_URL/REDIS_URL/NODE_ENV, NO NEXT_PUBLIC_*; D-03: SKIP_ENV_VALIDATION=1 solo en build Docker fase 3, al boot valida siempre; D-04: error agregado + .env.example; D-06: tsx watch en dev, tsup para build prod; D-10: test real de env agregado)
    - packages/config/env/presets.ts (creado en plan 02: baseEnv, dbEnv, redisEnv — el worker los compone)
    - .planning/REQUIREMENTS.md (APP-03 / Out of Scope: worker shell deployable SIN lógica de jobs BullMQ — fase 0 solo el shell)
  </read_first>
  <behavior>
    - Test 1: `createEnv` con `DATABASE_URL` inválido ("not-a-url") y `REDIS_URL` faltante lanza un error cuyo mensaje contiene AMBOS nombres (`DATABASE_URL` y `REDIS_URL`) — error agregado, no solo el primero
    - Test 2 (boot ok): con env válido, importar `./env` no lanza y `env.NODE_ENV` es uno de development/test/production
  </behavior>
  <action>
    Crear `apps/worker` como package `@imbau/worker` (`"type": "module"`, `"private": true`), deps: `@t3-oss/env-core@0.13.11`, `zod@4`, `@imbau/config` (`workspace:*`); devDeps: `tsx@4.22.4`, `tsup@8.5.1`. tsconfig extiende `@imbau/config/tsconfig/node.json` (D-06).
    - `src/env.ts`: `createEnv` de `@t3-oss/env-core` componiendo `baseEnv.server`, `redisEnv.server`, `dbEnv.server` desde `@imbau/config/env/presets` (D-02 — worker valida DATABASE_URL, REDIS_URL, NODE_ENV). Pasar `runtimeEnv: process.env`. Honrar `SKIP_ENV_VALIDATION` SOLO así: `skipValidation: process.env.SKIP_ENV_VALIDATION === "1"` (D-03 — para el build de imagen de fase 3; al boot del contenedor valida siempre). NO override de `onValidationError` que imprima solo el primer error (Pitfall 4, A2 — t3-env agrega por default). El mensaje de error NUNCA debe imprimir el VALOR de la var, solo nombre + motivo (requisito de seguridad V7).
    - `src/index.ts`: importar `./env` PRIMERO (así valida al boot), luego loguear JSON estructurado de boot ok (ej: `{"msg":"worker boot ok","node_env":env.NODE_ENV}`). SIN lógica de jobs BullMQ (out of scope, APP-03 es solo el shell — eso llega con media/PDFs).
    - `src/env.test.ts`: el test de error agregado del bloque `<behavior>` (dos vars mal → ambos nombres en el mensaje).
    - `tsup.config.ts`: config para bundlear el worker + sus workspace packages (`@imbau/*`) en un solo output (D-06 — listo para la imagen Docker de fase 3). `noExternal` para los `@imbau/*`.
    - Scripts en package.json: `dev` → `tsx watch src/index.ts`, `build` → `tsup`, `typecheck` → `tsc --noEmit`, `lint` → `eslint .`, `test` → `vitest run`.
    - `.env.example`: lista `DATABASE_URL=`, `REDIS_URL=`, `NODE_ENV=development` con valores placeholder (NUNCA reales), consistente con el schema (D-04). El `.gitignore` del plan 01 ya ignora `.env*` excepto `.env.example`.
  </action>
  <acceptance_criteria>
    - `apps/worker/src/env.ts` importa de `@t3-oss/env-core` y de `@imbau/config/env/presets`
    - `apps/worker/src/env.ts` contiene `skipValidation` ligado a `SKIP_ENV_VALIDATION === "1"` y NO contiene `NEXT_PUBLIC_`
    - `apps/worker/src/index.ts` importa `./env` antes de cualquier lógica y NO contiene referencias a BullMQ/jobs
    - `apps/worker/src/env.test.ts` assertea que un error con DATABASE_URL inválido + REDIS_URL faltante menciona ambos nombres
    - `apps/worker/.env.example` contiene `DATABASE_URL=`, `REDIS_URL=` y `NODE_ENV=` con placeholders (sin valores reales)
    - `pnpm --filter @imbau/worker test` pasa
    - `pnpm --filter @imbau/worker build` produce un output de tsup sin error
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@t3-oss/env-core' apps/worker/src/env.ts && grep -q 'SKIP_ENV_VALIDATION' apps/worker/src/env.ts && ! grep -q 'NEXT_PUBLIC_' apps/worker/src/env.ts && grep -q 'env' apps/worker/src/index.ts && ! grep -qi 'bullmq' apps/worker/src/index.ts && pnpm --filter @imbau/worker test >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>El worker es un shell deployable que valida su env con env-core (DATABASE_URL/REDIS_URL/NODE_ENV) al boot con error agregado, loguea boot ok, bundlea con tsup, tiene .env.example versionado y test verde, sin lógica de jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: web + panel — Next 16, env-nextjs, status page es-AR (MONO-01, MONO-03, D-09)</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/env.ts, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/env.test.ts, apps/web/.env.example, apps/panel/package.json, apps/panel/tsconfig.json, apps/panel/next.config.ts, apps/panel/env.ts, apps/panel/app/layout.tsx, apps/panel/app/page.tsx, apps/panel/.env.example</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation/01-RESEARCH.md ("Code Examples → Next app env (web/panel)" con `createEnv` de env-nextjs + `experimental__runtimeEnv` para NEXT_PUBLIC_*; "status page (D-09) es-AR voseo"; "JIT package exports" → `transpilePackages: ["@imbau/ui","@imbau/config",...]`; "Standard Stack": Next 16.2.x, React 19.2.x, @t3-oss/env-nextjs 0.13.11; Pitfall 3: server var leak al client; A4: verificar `experimental__runtimeEnv` contra README instalado)
    - .planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-01: env-nextjs para apps Next; D-02: web/panel NO validan REDIS_URL; D-08: puertos web 3000, panel 3001; D-09: status page es-AR con nombre app + entorno + check de env validó, sin branding/layout; D-12: ui componente trivial usado por las apps)
    - packages/config/env/presets.ts y packages/config/tsconfig/next.json (creados en plan 02)
    - packages/ui/src/index.tsx (componente trivial creado en plan 02 — web/panel lo consumen para probar la arista app → ui)
    - CLAUDE.md (idioma: UI en es-AR voseo; "What NOT to Use": no leer process.env directo en client; "Architecture-adjacent": `output: 'standalone'` para Docker fase 3)
  </read_first>
  <behavior>
    - Test 1 (web env): `createEnv` con `NEXT_PUBLIC_APP_ENV` inválido (valor fuera del enum) lanza un error que menciona `NEXT_PUBLIC_APP_ENV`
    - Test 2 (web env): con `NEXT_PUBLIC_APP_ENV` válido el env resuelve sin lanzar
  </behavior>
  <action>
    Crear `apps/web` (`@imbau/web`) y `apps/panel` (`@imbau/panel`), ambas Next 16 App Router, deps: `next@16.2`, `react@19.2`, `react-dom@19.2`, `@t3-oss/env-nextjs@0.13.11`, `zod@4`, `@imbau/config` + `@imbau/ui` (`workspace:*`). tsconfig extiende `@imbau/config/tsconfig/next.json`.
    - `env.ts` (por app): `createEnv` de `@t3-oss/env-nextjs` componiendo `baseEnv.server` (NODE_ENV) y declarando en `client` solo `NEXT_PUBLIC_APP_ENV: z.enum(["development","staging","production"])`. Cablear `experimental__runtimeEnv: { NEXT_PUBLIC_APP_ENV: process.env.NEXT_PUBLIC_APP_ENV }` (Next inlinea los NEXT_PUBLIC_*; verificar el nombre exacto de la API contra el README del paquete instalado — A4). web/panel NO declaran `REDIS_URL` (D-02). NUNCA poner un secret server-only con prefijo `NEXT_PUBLIC_` (Pitfall 3, V7).
    - `next.config.ts` (por app): `transpilePackages: ["@imbau/ui","@imbau/config"]` (consumo JIT de packages, D-05) y `output: 'standalone'` (para Docker fase 3). panel fija el puerto 3001 en su script dev (D-08), web 3000.
    - `app/layout.tsx`: layout mínimo con `<html lang="es-AR">` (sin branding/design system, D-09).
    - `app/page.tsx`: página de status es-AR (voseo) que importa `env` desde `../env` y un componente trivial desde `@imbau/ui` (prueba la arista app → ui, D-12): muestra el nombre de la app (ImBau · Web / ImBau · Panel), el entorno actual (`env.NEXT_PUBLIC_APP_ENV`) y un texto confirmando que el env se validó al iniciar. Server Component leyendo el `env` validado (no `process.env` directo).
    - `apps/web/env.test.ts`: el test del bloque `<behavior>` (NEXT_PUBLIC_APP_ENV inválido → mensaje lo menciona). panel reusa el mismo patrón de env; un test en web es suficiente para el gate (mismo schema compuesto).
    - `.env.example` por app: `NODE_ENV=development`, `NEXT_PUBLIC_APP_ENV=development` con placeholders, consistente con el schema (D-04).
    Scripts por app: `dev` (next dev con puerto correcto — panel `-p 3001`), `build` → `next build` (valida env en build, D-03), `typecheck` → `tsc --noEmit`, `lint` → `eslint .`, `test` → `vitest run`.
  </action>
  <acceptance_criteria>
    - `apps/web/env.ts` y `apps/panel/env.ts` importan de `@t3-oss/env-nextjs` y de `@imbau/config/env/presets`
    - `apps/web/env.ts` declara `NEXT_PUBLIC_APP_ENV` en el bloque client y NO declara `REDIS_URL`
    - `apps/web/next.config.ts` y `apps/panel/next.config.ts` contienen `transpilePackages` y `output: 'standalone'`
    - `apps/web/app/layout.tsx` contiene `lang="es-AR"`
    - `apps/web/app/page.tsx` importa `env` y un símbolo desde `@imbau/ui`, y el texto de la página está en español (contiene `Entorno` o equivalente es-AR)
    - `apps/panel` corre en puerto 3001 (su script dev contiene `3001`)
    - `apps/web/.env.example` y `apps/panel/.env.example` contienen `NEXT_PUBLIC_APP_ENV=` con placeholder y NO contienen `REDIS_URL`
    - `apps/web/env.test.ts` assertea que `NEXT_PUBLIC_APP_ENV` inválido aparece en el mensaje de error
    - `pnpm build` completa para web y panel; `pnpm lint`, `pnpm typecheck` y `pnpm test` pasan en todo el workspace
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@t3-oss/env-nextjs' apps/web/env.ts && grep -q 'NEXT_PUBLIC_APP_ENV' apps/web/env.ts && ! grep -q 'REDIS_URL' apps/web/env.ts && grep -q 'transpilePackages' apps/web/next.config.ts && grep -q "output: 'standalone'" apps/web/next.config.ts && grep -q 'lang="es-AR"' apps/web/app/layout.tsx && grep -q '@imbau/ui' apps/web/app/page.tsx && grep -q '3001' apps/panel/package.json && pnpm typecheck >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>web y panel existen como apps Next 16 que validan su env con env-nextjs (NEXT_PUBLIC_APP_ENV, sin REDIS_URL), muestran una status page es-AR consumiendo @imbau/ui, transpilan los packages JIT, tienen .env.example y test de env, y todo el workspace pasa build/lint/typecheck/test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → app | Las variables de entorno son entrada no confiable; se validan en el boundary con Zod/t3-env antes de usarse |
| server → client bundle | Una var server-only no debe filtrarse al bundle del cliente de Next |
| repo → git | Solo `.env.example` (placeholders) se versiona; los `.env` reales nunca cruzan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | env de web/panel (server var → client bundle) | mitigate | Split client/server de t3-env; solo `NEXT_PUBLIC_*` en el bloque client; nunca leer `process.env` directo en client; web no declara `REDIS_URL` (Pitfall 3, D-02). |
| T-03-02 | Information Disclosure | mensaje de error de validación de env | mitigate | El error imprime nombre de var + motivo, NUNCA el valor ofensor (V7, D-04); no override de onValidationError que filtre valores. |
| T-03-03 | Information Disclosure | archivos `.env` con secrets reales | mitigate | `.gitignore` (plan 01) ignora `.env*`; solo `.env.example` con placeholders se versiona (V14, D-04). |
| T-03-04 | Denial of Service (fail-open) | boot con env inválido | mitigate | `@t3-oss/env` valida al import (boot/build) y falla cerrado; `SKIP_ENV_VALIDATION` solo en build Docker fase 3, nunca al boot del contenedor (D-03, MONO-03). |
| T-03-SC | Tampering | instalaciones npm de las apps | mitigate | Todos los paquetes (`@t3-oss/env-*`, next, react, tsx, tsup) aprobados en el Package Legitimacy Audit de RESEARCH.md; ningún `[ASSUMED]`/`[SUS]` bloqueante. |
</threat_model>

<verification>
- `pnpm build` valida env en build para web y panel (D-03) y produce salida standalone
- `pnpm test` pasa: tests de env agregado (web + worker) verifican que dos/varias vars fallidas surfacean todos los nombres (D-04, D-10)
- web/panel renderizan status page es-AR consumiendo `@imbau/ui` (arista app → ui probada)
- Ninguna app declara variables que no usa (web sin REDIS_URL, worker sin NEXT_PUBLIC_*) — D-02
- Cada app tiene `.env.example` consistente con su schema y ningún `.env` real se versiona
</verification>

<success_criteria>
- MONO-01 completo: las tres apps compilan consumiendo la cadena descendente apps → api/ui → config; `pnpm build` verde en todo el workspace
- MONO-03 completo: env validado por app con t3-env, falla rápido al boot/build con error agregado accionable, `.env.example` por app
- D-09: web y panel muestran status page es-AR (smoke test visual para deploys futuros)
- worker es shell deployable sin lógica de jobs
- `pnpm lint`, `pnpm typecheck`, `pnpm test` verdes en todo el workspace (prerequisito de CI fase 4)
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation/01-03-SUMMARY.md` when done
</output>
