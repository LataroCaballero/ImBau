---
phase: 03-auth-api-app-surfaces
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - apps/web/Dockerfile
  - apps/panel/Dockerfile
  - apps/worker/Dockerfile
  - apps/web/.dockerignore
  - apps/panel/.dockerignore
  - apps/worker/.dockerignore
autonomous: true
requirements: [APP-04]

must_haves:
  truths:
    - "Each app (web, panel, worker) has a multi-stage Dockerfile following prune → install/build → slim runner (CLAUDE.md)"
    - "web/panel runners copy the Next standalone output (.next/standalone + .next/static + public); the worker runner copies the tsup dist/"
    - "No Dockerfile runs `pnpm install --prod` inside .next/standalone (would break pnpm-symlinked traced node_modules)"
    - "Dockerfiles are authored and statically valid; the image build is verified in CI (Phase 4) — no local Docker build"
  artifacts:
    - path: "apps/panel/Dockerfile"
      provides: "multi-stage Dockerfile for the panel (turbo prune + Next standalone)"
      contains: "turbo prune"
    - path: "apps/web/Dockerfile"
      provides: "multi-stage Dockerfile for the web (turbo prune + Next standalone)"
      contains: "turbo prune"
    - path: "apps/worker/Dockerfile"
      provides: "multi-stage Dockerfile for the worker (turbo prune + tsup dist)"
      contains: "turbo prune"
  key_links:
    - from: "apps/panel/Dockerfile"
      to: "turbo prune @imbau/panel --docker"
      via: "pruner stage emits out/json + out/full"
      pattern: "turbo prune .*panel"
    - from: "apps/worker/Dockerfile"
      to: "tsup dist/index.js"
      via: "runner copies dist and runs node dist/index.js"
      pattern: "dist/index.js"
---

<objective>
Author the multi-stage Dockerfile for each of the three apps (web, panel, worker) following CLAUDE.md's prune → install/build → slim runner pattern with `turbo prune --docker` and Next.js standalone. These are AUTHORED ONLY this phase — there is no local Docker daemon, so the image build is verified in CI (Phase 4, D-16). The goal is correct, deployable Dockerfiles, not a local build.

web/panel use the Next standalone output (`output: 'standalone'` is already set); the worker uses its tsup `dist/`. No stage runs `pnpm install --prod` inside `.next/standalone` (it breaks the pnpm-symlinked @vercel/nft-traced node_modules — CLAUDE.md "What NOT to Use", Pitfall 5).

Purpose: APP-04 — each app produces a deployable image via a multi-stage Dockerfile. Phase 4's CI-03 builds/pushes these images and INFRA deploys them.
Output: three Dockerfiles + matching .dockerignore files.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-auth-api-app-surfaces/03-CONTEXT.md
@.planning/phases/03-auth-api-app-surfaces/03-RESEARCH.md
@.planning/phases/03-auth-api-app-surfaces/03-PATTERNS.md
@.planning/phases/03-auth-api-app-surfaces/03-03-SUMMARY.md
@.planning/phases/03-auth-api-app-surfaces/03-04-SUMMARY.md
@apps/panel/next.config.ts
@apps/web/next.config.ts
@apps/worker/tsup.config.ts
@apps/worker/package.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author web + panel multi-stage Dockerfiles (turbo prune + Next standalone) + .dockerignore</name>
  <read_first>
    - CLAUDE.md §"Architecture-adjacent configuration notes (phase 0)" (prune → install/build → runner; Node 22 Alpine; copy .next/standalone + .next/static + public; do NOT pnpm install --prod in standalone)
    - CLAUDE.md §"What NOT to Use" (no `pnpm install --prod` inside .next/standalone; no turbo.json `pipeline` key)
    - apps/panel/next.config.ts + apps/web/next.config.ts (output: 'standalone' set; outputFileTracingRoot pinned to workspace root)
    - package.json (packageManager pnpm@11.6.0 via corepack; turbo 2.9.18)
    - 03-RESEARCH.md Pattern 7 (the canonical 3-stage Dockerfile: pruner/builder/runner; CMD node apps/<app>/server.js), Pitfall 5
  </read_first>
  <action>
    Create `apps/panel/Dockerfile` and `apps/web/Dockerfile` per RESEARCH Pattern 7 / CLAUDE.md, three stages on `node:22-alpine`:
    - `pruner`: `corepack enable`; `COPY . .`; `RUN pnpm dlx turbo prune @imbau/panel --docker` (web uses `@imbau/web`) → `out/json` + `out/full`.
    - `builder`: `corepack enable`; copy `out/json/` then `pnpm install --frozen-lockfile`; copy `out/full/`; `RUN pnpm turbo build --filter=@imbau/panel` (web: `--filter=@imbau/web`). Pass `SKIP_ENV_VALIDATION=1` for the build step (env is validated at container boot, not build — matches the worker env comment).
    - `runner`: `NODE_ENV=production`; copy `.next/standalone` → `./`, `.next/static` → `./apps/<app>/.next/static`, `public` → `./apps/<app>/public`; `CMD ["node", "apps/<app>/server.js"]`. NEVER `pnpm install --prod` inside standalone (Pitfall 5).
    Create `apps/panel/.dockerignore` and `apps/web/.dockerignore` excluding `node_modules`, `.next`, `.turbo`, `dist`, `*.log`, `.env*`. Author only — do NOT run `docker build` (no local daemon; verified in CI Phase 4).
  </action>
  <verify>
    <automated>test -f apps/panel/Dockerfile && test -f apps/web/Dockerfile && grep -q "turbo prune @imbau/panel --docker" apps/panel/Dockerfile && grep -q "turbo prune @imbau/web --docker" apps/web/Dockerfile && ! grep -q "install --prod" apps/panel/Dockerfile && ! grep -q "install --prod" apps/web/Dockerfile && echo "dockerfiles ok"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/panel/Dockerfile` and `apps/web/Dockerfile` exist with three stages (pruner/builder/runner) on `node:22-alpine`.
    - Each pruner stage runs `turbo prune @imbau/<app> --docker`; each builder runs `pnpm install --frozen-lockfile` then `pnpm turbo build --filter=@imbau/<app>`.
    - Each runner copies `.next/standalone` + `.next/static` + `public` and `CMD`s `node apps/<app>/server.js`.
    - No Dockerfile contains `pnpm install --prod`.
    - `.dockerignore` files exist for both apps.
    - The verify command prints `dockerfiles ok`.
  </acceptance_criteria>
  <done>web and panel have correct multi-stage Dockerfiles authored (build verified in CI Phase 4).</done>
</task>

<task type="auto">
  <name>Task 2: Author worker multi-stage Dockerfile (turbo prune + tsup dist) + .dockerignore</name>
  <read_first>
    - apps/worker/tsup.config.ts (outDir dist; noExternal bundles @imbau/* — the runner only needs dist/index.js)
    - apps/worker/package.json (build script = tsup; bullmq/ioredis deps from Plan 03-04)
    - CLAUDE.md §"Architecture-adjacent configuration notes" (worker variant copies tsup dist and runs node dist/index.js)
    - 03-RESEARCH.md Pattern 7 note (worker variant: copy tsup dist/, run node dist/index.js)
    - apps/panel/Dockerfile (Task 1 — same pruner/builder skeleton to mirror)
  </read_first>
  <action>
    Create `apps/worker/Dockerfile` mirroring the Task 1 pruner/builder skeleton on `node:22-alpine` but for the worker: `pruner` runs `pnpm dlx turbo prune @imbau/worker --docker`; `builder` runs `pnpm install --frozen-lockfile` then `pnpm turbo build --filter=@imbau/worker` (tsup → `dist/`), with `SKIP_ENV_VALIDATION=1` for the build. `runner` (Node 22 Alpine, `NODE_ENV=production`) copies `apps/worker/dist` and `CMD ["node", "apps/worker/dist/index.js"]`. Because tsup `noExternal` inlines `@imbau/*`, the runner does not need the workspace node_modules for internal packages; copy any external runtime deps (bullmq/ioredis) as traced — keep it minimal and do NOT `pnpm install --prod` inside a standalone tree (this is tsup, not Next standalone, but keep the same no-prod-install discipline). Create `apps/worker/.dockerignore` (exclude node_modules, dist, .turbo, *.log, .env*). Author only — verified in CI Phase 4.
  </action>
  <verify>
    <automated>test -f apps/worker/Dockerfile && grep -q "turbo prune @imbau/worker --docker" apps/worker/Dockerfile && grep -q "dist/index.js" apps/worker/Dockerfile && echo "worker dockerfile ok"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/Dockerfile` exists with pruner/builder/runner stages on `node:22-alpine`.
    - The pruner runs `turbo prune @imbau/worker --docker`; the builder runs `pnpm turbo build --filter=@imbau/worker`.
    - The runner runs `node apps/worker/dist/index.js`.
    - `apps/worker/.dockerignore` exists.
    - The verify command prints `worker dockerfile ok`.
  </acceptance_criteria>
  <done>The worker has a correct multi-stage Dockerfile authored using its tsup dist output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build context → image | The Docker build copies the repo into the image; secrets/dev artifacts must not leak in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Information Disclosure | secrets/.env or dev artifacts baked into the image | mitigate | `.dockerignore` excludes `.env*`, `node_modules`, `.next`, `.turbo`, `dist`, logs; env is supplied at container boot, never baked. |
| T-03-21 | Tampering | broken standalone tree from `pnpm install --prod` | mitigate | No stage runs `pnpm install --prod` inside `.next/standalone` (Pitfall 5); acceptance criteria grep-assert its absence. |
| T-03-22 | Tampering | image build not actually validated this phase | accept | No local Docker daemon (D-16); the build is verified in CI (Phase 4, CI-03). Phase 3 authors + statically validates only. |
</threat_model>

<verification>
- All three Dockerfiles exist; grep confirms `turbo prune @imbau/<app> --docker` per app and absence of `pnpm install --prod`.
- Worker Dockerfile runs `node apps/worker/dist/index.js`; web/panel run `node apps/<app>/server.js`.
- `.dockerignore` present for each app.
- (Optional) `hadolint apps/*/Dockerfile` if available — non-blocking.
</verification>

<success_criteria>
- Each app has a multi-stage Dockerfile (prune → build → slim runner) following CLAUDE.md (APP-04).
- web/panel package the Next standalone output; worker packages the tsup dist.
- No `pnpm install --prod` inside standalone.
- Image build deferred to CI Phase 4 (authored + statically valid here).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (from this plan)

**Files created:**
- `apps/web/Dockerfile`, `apps/panel/Dockerfile`, `apps/worker/Dockerfile`
- `apps/web/.dockerignore`, `apps/panel/.dockerignore`, `apps/worker/.dockerignore`

**Symbols/exports:** none (build config only)
</artifacts_produced>

<output>
Create `.planning/phases/03-auth-api-app-surfaces/03-05-SUMMARY.md` when done.
</output>
