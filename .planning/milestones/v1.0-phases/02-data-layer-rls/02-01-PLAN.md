---
phase: 02-data-layer-rls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yml
  - .env.example
  - packages/db/package.json
  - packages/db/.env.example
  - packages/db/tsconfig.json
  - packages/db/drizzle.config.ts
  - packages/db/src/env.ts
  - packages/config/env/presets.ts
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "docker compose up -d brings up Postgres 16 and Redis 7 with one command and both pass their healthcheck"
    - "packages/db has the pinned data-layer dependencies installed and exact-pinned (no ^/~)"
    - "Importing packages/db env with a missing DATABASE_APP_URL or DATABASE_ANON_URL fails fast with the variable name in the message"
    - "drizzle-kit reads packages/db/drizzle.config.ts with entities.roles enabled and the owner/migration DATABASE_URL"
  artifacts:
    - path: "compose.yml"
      provides: "Postgres 16-alpine + Redis 7-alpine local services with healthchecks and named pgdata volume"
      contains: "postgres:16-alpine"
    - path: "packages/db/package.json"
      provides: "Pinned data-layer deps (drizzle-orm, postgres, better-auth) + dev tooling (drizzle-kit, @better-auth/cli) + db:generate/db:migrate scripts"
      contains: "drizzle-orm"
    - path: "packages/db/drizzle.config.ts"
      provides: "drizzle-kit config: schema+migrations paths, entities.roles:true, owner DATABASE_URL"
      contains: "entities"
    - path: "packages/db/src/env.ts"
      provides: "Fail-fast env composition for the three DB connection strings"
      contains: "createEnv"
    - path: "packages/config/env/presets.ts"
      provides: "dbEnv extended to owner + app + anon connection-string schemas"
      contains: "DATABASE_APP_URL"
  key_links:
    - from: "packages/db/src/env.ts"
      to: "@imbau/config/env/presets"
      via: "spread dbEnv.server into createEnv"
      pattern: "dbEnv"
    - from: "packages/db/drizzle.config.ts"
      to: "process.env.DATABASE_URL"
      via: "owner/migration connection string for DDL"
      pattern: "DATABASE_URL"
---

<objective>
Stand up the local data-layer infrastructure and the `packages/db` toolchain that every later plan in this phase builds on: a one-command Docker Compose stack (Postgres 16 + Redis 7), the pinned Drizzle / postgres / Better Auth dependencies, the three-connection-string env contract that D-04's role split requires, and a `drizzle-kit` config with `entities.roles` enabled.

Purpose: DATA-01 (Compose up Postgres + Redis) and the DATA-02 scaffolding (versioned-migration toolchain) must exist before any schema, policy, or test can be written. This plan creates no SQL and no schema — it makes the next two plans runnable.
Output: `compose.yml`, extended `dbEnv` preset (owner/app/anon URLs), `packages/db` deps + scripts + `drizzle.config.ts` + `src/env.ts` + `.env.example`, and a verified `docker compose up -d` + `pg_isready`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-layer-rls/02-CONTEXT.md
@.planning/phases/02-data-layer-rls/02-RESEARCH.md
@.planning/phases/02-data-layer-rls/02-PATTERNS.md
@CLAUDE.md

# Phase-1 analogs this plan mirrors exactly (read before editing):
@packages/config/env/presets.ts
@apps/worker/src/env.ts
@apps/worker/package.json
@packages/db/package.json
@packages/db/tsconfig.json
</context>

<artifacts_this_phase_produces>
This plan contributes the following symbols (full phase inventory continues in plans 02 and 03):
- File `compose.yml` (repo root) — services `postgres` (image `postgres:16-alpine`) and `redis` (image `redis:7-alpine`), named volume `pgdata`, healthchecks for both.
- Env vars (schemas in `dbEnv`): `DATABASE_URL` (owner/migration — pre-existing, repurposed), `DATABASE_APP_URL` (`app_authenticated`), `DATABASE_ANON_URL` (`anon`). Plus Compose env: `POSTGRES_PASSWORD`.
- File `packages/db/drizzle.config.ts` — drizzle-kit config object (`entities.roles: true`).
- File `packages/db/src/env.ts` — `env` export (fail-fast `createEnv`).
- File `packages/db/.env.example` and root `.env.example` additions for the three URLs.
- Deps added to `packages/db/package.json`: `drizzle-orm@0.45.2`, `postgres@3.4.9`, `better-auth@1.6.18`, `@t3-oss/env-core@0.13.11`, `zod@4.4.3` (deps); `drizzle-kit@0.31.10`, `@better-auth/cli@1.4.21`, `@types/node@22.18.13` (devDeps). Scripts `db:generate`, `db:migrate`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author compose.yml with Postgres 16 + Redis 7 and verify it boots</name>
  <files>compose.yml, .env.example</files>
  <read_first>
    - .planning/phases/02-data-layer-rls/02-RESEARCH.md — "Code Examples > Docker Compose (DATA-01)" (the verified service shape) and the Port note + Assumption A4
    - .planning/phases/02-data-layer-rls/02-CONTEXT.md — D-12 (no seed), Claude's Discretion (Redis 7 image, optional healthcheck)
  </read_first>
  <action>
    Create `compose.yml` at the repo root (no compose file exists yet — verified) defining two services. `postgres`: image pinned to `postgres:16-alpine` (NEVER float to 17/18 — CLAUDE.md), env `POSTGRES_USER: imbau` (this is the owner/migration role), `POSTGRES_PASSWORD` sourced from `${POSTGRES_PASSWORD:-dev}`, `POSTGRES_DB: imbau`, a named volume `pgdata` mounted at `/var/lib/postgresql/data`, and a healthcheck running `pg_isready -U imbau -d imbau` (interval 5s, timeout 3s, retries 10). `redis`: image pinned to `redis:7-alpine` with a `redis-cli ping` healthcheck (Redis is container-only this phase — no app code touches it, D discretion).

    CRITICAL host-port conflict (resolves Assumption A4 — verified at plan time): host port 6379 is ALREADY occupied by a local Homebrew redis-server, and per user memory another project (CLINICAL) owns 3000/3001. Host port 5432 is free. Therefore: map Postgres `5432:5432` but map Redis to a NON-default host port to avoid the clash — publish `6380:6379` (container stays 6379; only the host binding moves). Document this remap in a comment in `compose.yml`.

    Do NOT add a Postgres init/seed mount (D-12: no dev seed this phase). Do NOT add Traefik/Loki/Grafana/Uptime-Kuma services — those are phase 4 (INFRA-01); keep service names `postgres`/`redis` stable so phase 4 can extend this same file.

    Update the root `.env.example` to document `POSTGRES_PASSWORD` (placeholder value only, e.g. `dev`) so the Compose env is discoverable. Real secret encryption (SOPS/age) is phase 4 (INFRA-03) — placeholders only here, and `.env`/`.env.*` are already gitignored except `.env.example`.
  </action>
  <verify>
    <automated>docker compose -f compose.yml config >/dev/null && docker compose up -d && (for i in $(seq 1 20); do docker compose exec -T postgres pg_isready -U imbau -d imbau && break || sleep 2; done) && docker compose exec -T redis redis-cli ping | grep -q PONG && echo COMPOSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f compose.yml config` validates with no error.
    - `docker compose up -d` starts both services with a single command.
    - `compose.yml` contains the literal string `postgres:16-alpine` and `redis:7-alpine`.
    - `pg_isready -U imbau -d imbau` exits 0 inside the postgres container; `redis-cli ping` returns `PONG`.
    - Redis host port is published as `6380` (not `6379`) to avoid the occupied host port; a comment in `compose.yml` explains the remap.
    - No seed mount, no Traefik/observability services present.
  </acceptance_criteria>
  <done>One-command `docker compose up -d` brings up healthy Postgres 16 and Redis 7 locally without colliding with the host's existing Redis on 6379.</done>
</task>

<task type="auto">
  <name>Task 2: Extend dbEnv preset to the three-role connection-string contract</name>
  <files>packages/config/env/presets.ts</files>
  <read_first>
    - packages/config/env/presets.ts — current `dbEnv` (single `DATABASE_URL`) and the header comment forbidding speculative presets
    - .planning/phases/02-data-layer-rls/02-RESEARCH.md — "Pattern 4: Two connection strings / two roles" and the env-preset extension example
    - .planning/phases/02-data-layer-rls/02-PATTERNS.md — "packages/config/env/presets.ts (config — modify)" pattern assignment
  </read_first>
  <action>
    Extend the existing `dbEnv` object's `server` block (per D-04 and D-06) to declare three connection-string schemas: keep `DATABASE_URL` as the owner/migration role (used by `drizzle.config.ts` for DDL — its semantics change from "the app DB URL" to "the owner/migration DB URL"); add `DATABASE_APP_URL` for the `app_authenticated` runtime role; add `DATABASE_ANON_URL` for the `anon` published-only read role. Each is `z.string().url()`, matching the existing idiom exactly: declare variable NAMES + Zod schemas only, never values; preserve `as const`; keep one preset object per concern. Update the surrounding comment to note these three are justified by D-04/D-06 (not speculative). Do NOT touch `redisEnv` or `baseEnv`. These exact variable names are within Claude's discretion (CONTEXT) — if you rename, keep all three concepts (owner, app, anon) and update every downstream reference in this plan.
  </action>
  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs query nop >/dev/null 2>&1; grep -qE "DATABASE_APP_URL" packages/config/env/presets.ts && grep -qE "DATABASE_ANON_URL" packages/config/env/presets.ts && pnpm --filter @imbau/config typecheck && echo PRESET_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/config/env/presets.ts` contains `DATABASE_URL`, `DATABASE_APP_URL`, and `DATABASE_ANON_URL`, each a `z.string().url()` inside `dbEnv.server`.
    - No values are present, only Zod schemas; `as const` retained.
    - `pnpm --filter @imbau/config typecheck` passes.
  </acceptance_criteria>
  <done>The shared `dbEnv` preset declares the owner, app, and anon connection-string contract that the role-split (D-04) and anon-read path (D-06) require.</done>
</task>

<task type="auto">
  <name>Task 3: Install pinned db deps, add scripts, write src/env.ts + drizzle.config.ts + .env.example, widen tsconfig</name>
  <files>packages/db/package.json, packages/db/src/env.ts, packages/db/drizzle.config.ts, packages/db/.env.example, packages/db/tsconfig.json</files>
  <read_first>
    - apps/worker/src/env.ts — exact fail-fast `createEnv` composition pattern to replicate (do NOT override onValidationError)
    - apps/worker/package.json — deps/devDeps split, exact version pinning, script shapes
    - packages/db/package.json — current shape (single `.` export, `@imbau/config` + `typescript` devDeps)
    - packages/db/tsconfig.json — current `include: ["src"]` (drizzle.config.ts + tests fall outside)
    - .planning/phases/02-data-layer-rls/02-RESEARCH.md — Standard Stack installation block + "Recommended Project Structure" (drizzle.config.ts uses owner DATABASE_URL; entities.roles:true)
    - .planning/phases/02-data-layer-rls/02-PATTERNS.md — "packages/db/src/env.ts", "packages/db/package.json", "packages/db/tsconfig.json" pattern assignments
  </read_first>
  <action>
    Add the pinned data-layer dependencies to `packages/db/package.json` at EXACT versions (no `^`/`~`, mirroring the worker's pinning): `dependencies` += `drizzle-orm@0.45.2`, `postgres@3.4.9`, `better-auth@1.6.18`, `@t3-oss/env-core@0.13.11`, `zod@4.4.3`; `devDependencies` += `drizzle-kit@0.31.10`, `@better-auth/cli@1.4.21`, `@types/node@22.18.13` (keep existing `@imbau/config: workspace:*` and `typescript: 5.9.3`). `@better-auth/cli` is `1.4.21` — the 1.4.x line, NOT 1.6.x (FLAG-1: no 1.6.x CLI exists); it is dev-only for `generate`. Add scripts `db:generate` (`drizzle-kit generate`) and `db:migrate` (`drizzle-kit migrate`) — NEVER a `db:push` script (CLAUDE.md: versioned migrations only). Keep `lint`/`typecheck`; add `test` (`vitest run`) for plan 03. Run `pnpm install` to update the lockfile.

    Write `packages/db/src/env.ts` mirroring `apps/worker/src/env.ts`: import `createEnv` from `@t3-oss/env-core` and `baseEnv, dbEnv` from `@imbau/config/env/presets`; spread ONLY `baseEnv.server` and `dbEnv.server` into `createEnv`'s `server`; set `runtimeEnv: process.env`; gate `skipValidation` on `process.env.SKIP_ENV_VALIDATION === "1"`. Do NOT override `onValidationError` (default formatter prints variable NAME + reason, never the value — security V7). Use `import type` where applicable (`verbatimModuleSyntax`).

    Write `packages/db/drizzle.config.ts` using `defineConfig` from `drizzle-kit`: `dialect: "postgresql"`; `schema` pointing at the schema folder glob (`./src/schema/*.ts` — the folder is created in plan 02; the config may reference it now); `out: "./migrations"`; `entities: { roles: true }` (REQUIRED so policies/roles DDL is emitted — RESEARCH Standard Stack); `dbCredentials: { url: process.env.DATABASE_URL! }` — the OWNER/migration string (NOT the app URL), because migrations run as the privileged owner role (D-04). Read `DATABASE_URL` from `process.env` directly here (drizzle-kit runs this file as a CLI tool, outside the app boot path).

    Widen `packages/db/tsconfig.json` `include` so `drizzle.config.ts` (and later `auth.ts`/`tests/` from plans 02-03) are type-checked: change `include` from `["src"]` to cover `src`, `drizzle.config.ts`, `auth.ts`, and `tests` (PATTERNS note: otherwise typecheck/lint skip them). Keep `extends: "@imbau/config/tsconfig/base.json"`.

    Write `packages/db/.env.example` documenting `DATABASE_URL`, `DATABASE_APP_URL`, `DATABASE_ANON_URL`, `NODE_ENV` with placeholder localhost values (port 5432, db `imbau`, distinct users `imbau`/`app_authenticated`/`anon`) — NEVER real passwords (A5; SOPS is phase 4). Mirror the worker's `.env.example` doc-comment style.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false >/dev/null 2>&1 && grep -q '"drizzle-orm": "0.45.2"' packages/db/package.json && grep -q '"@better-auth/cli": "1.4.21"' packages/db/package.json && grep -qE "entities" packages/db/drizzle.config.ts && ! grep -q 'db:push' packages/db/package.json && SKIP_ENV_VALIDATION=1 pnpm --filter @imbau/db exec node -e "import('./src/env.ts').then(()=>console.log('ENV_IMPORTS_OK')).catch(e=>{console.error(e);process.exit(1)})" 2>/dev/null; pnpm --filter @imbau/db typecheck && echo DB_SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/package.json` lists `drizzle-orm` `0.45.2`, `postgres` `3.4.9`, `better-auth` `1.6.18`, `@t3-oss/env-core` `0.13.11`, `zod` `4.4.3` in `dependencies`; `drizzle-kit` `0.31.10`, `@better-auth/cli` `1.4.21`, `@types/node` `22.18.13` in `devDependencies`. All exact (no `^`/`~`).
    - Scripts include `db:generate` and `db:migrate`; there is NO `db:push` or `push` script anywhere.
    - `packages/db/drizzle.config.ts` sets `entities.roles: true` and reads `process.env.DATABASE_URL` (owner string), not the app URL.
    - `packages/db/src/env.ts` composes `baseEnv` + `dbEnv` via `createEnv` and does not override `onValidationError`.
    - `packages/db/tsconfig.json` `include` covers `drizzle.config.ts`/`auth.ts`/`tests` in addition to `src`.
    - `pnpm --filter @imbau/db typecheck` passes; `pnpm-lock.yaml` is updated and committed.
  </acceptance_criteria>
  <done>`packages/db` has its pinned data-layer toolchain installed, a fail-fast env module for the three connection strings, a `drizzle-kit` config with role emission enabled, and a tsconfig that type-checks the config/test files — ready for schema work in plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → Compose Postgres/Redis | Local containers expose DB/cache ports on the host; only owner-role credentials live in local `.env` (placeholders in `.env.example`). |
| repo → secrets | `.env`/`.env.*` gitignored (except `.env.example`); real role passwords never committed (SOPS is phase 4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `.env.example` / compose env | mitigate | Only placeholder values committed; `.gitignore` already blocks `.env`/`.env.*` except `.env.example`. Real passwords injected at runtime (A5). |
| T-02-02 | Tampering | unpinned dependency drift | mitigate | All deps exact-pinned (no `^`/`~`); `pnpm install` updates the committed lockfile; versions match the RESEARCH-verified matrix. |
| T-02-03 | Denial of Service | host-port clash on 6379 | mitigate | Redis host port remapped to 6380 (verified clash with host redis-server); Postgres 5432 confirmed free. |
| T-02-SC | Tampering | npm installs (drizzle-orm, postgres, better-auth, drizzle-kit, @better-auth/cli) | accept | RESEARCH "Package Legitimacy Audit" verdicts all OK/approved; `better-auth` SUS is patch-recency only on the locked stack dependency; no `postinstall` scripts. No blocking checkpoint warranted (locked stack, audit table present). |
</threat_model>

<verification>
- `docker compose up -d` → both services healthy (`pg_isready` exits 0, `redis-cli ping` → PONG).
- `pnpm --filter @imbau/db typecheck` and `pnpm --filter @imbau/config typecheck` pass.
- `packages/db/package.json` has exact pins and `db:generate`/`db:migrate` (no `push`).
- `dbEnv` declares the three connection-string schemas.
- Importing `packages/db/src/env.ts` with `SKIP_ENV_VALIDATION=1` succeeds; without it and with a missing app/anon URL, the boot would fail with the variable name (default t3-env formatter).
</verification>

<success_criteria>
- DATA-01 satisfied: one-command Compose brings up Postgres 16 + Redis 7, both healthy, no host-port clash.
- DATA-02 toolchain in place: `packages/db` has pinned Drizzle/postgres/Better-Auth deps, `drizzle.config.ts` with `entities.roles:true` on the owner URL, `db:generate`/`db:migrate` scripts, and NO `push` path.
- Env contract for D-04's role split exists (owner/app/anon URLs) and fails fast on missing vars.
</success_criteria>

<output>
Create `.planning/phases/02-data-layer-rls/02-01-SUMMARY.md` when done.
</output>
