---
phase: 01-schema-completo-rls
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - apps/worker/src/partitions.ts
  - apps/worker/src/partitions.test.ts
  - apps/worker/src/index.ts
autonomous: true
requirements: [SCHEMA-06]
must_haves:
  truths:
    - "A pure helper computes next month's [from, to) UTC bounds and the events_YYYY_MM partition name for any reference date"
    - "The partition-creation SQL is idempotent (CREATE TABLE IF NOT EXISTS ... PARTITION OF events) so re-running never errors"
    - "A repeatable BullMQ job is registered on the worker that pre-creates next month's events partition; Phase 1 is the skeleton only"
  artifacts:
    - apps/worker/src/partitions.ts
    - apps/worker/src/partitions.test.ts
  key_links:
    - "the job runs CREATE TABLE IF NOT EXISTS against the events parent via the owner connection; the DEFAULT partition (D-05) guarantees inserts never fail even if the job is late"
---

<objective>
Add the repeatable events-partition maintenance job to the existing worker shell (D-06): a small, idempotent BullMQ job that pre-creates next month's `events_YYYY_MM` partition. Phase 1 delivers the skeleton job + the pure date/SQL helpers only — retention/detach of old partitions is deferred.

Purpose: SCHEMA-06 (events particionada por mes — the ongoing maintenance half; the initial partitions + DEFAULT ship in the 01-04 migration). The DEFAULT partition (D-05) already guarantees no insert ever fails, so this job is the observability-friendly pre-creation path, not a hard dependency.
Output: a pure partition helper (unit-tested), a repeatable job registration in the worker boot.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-completo-rls/01-RESEARCH.md

# Worker shell analog + the partition DDL style to mirror
@apps/worker/src/index.ts
@apps/worker/src/index.test.ts
@packages/db/migrations/0001_rls.sql
</context>

> Depends on 01-04: the `events` parent table + its initial partitions exist in the DB. See plan 01-01 "Resolved phase decisions" for D-05 (DEFAULT partition) and D-06 (no pg_partman; small idempotent job).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure partition helper (date bounds + idempotent DDL) with unit tests</name>
  <files>apps/worker/src/partitions.ts, apps/worker/src/partitions.test.ts</files>
  <read_first>
    - apps/worker/src/index.ts (worker module style: factored pure functions + exported factories, no side effects at import)
    - apps/worker/src/index.test.ts (the vitest style already used in the worker — clone for partitions.test.ts)
    - packages/db/migrations/0001_rls.sql (idempotent DDL style: `--> statement-breakpoint`, IF NOT EXISTS)
    - .planning/phases/01-schema-completo-rls/01-RESEARCH.md (Pattern 2 partition naming `events_YYYY_MM`, FOR VALUES FROM (..) TO (..); Open Q2 — current + 3 months, idempotent)
  </read_first>
  <behavior>
    - nextMonthPartitionSpec(new Date('2026-12-15Z')) returns name `events_2027_01`, from `2027-01-01`, to `2027-02-01` (year rollover correct, UTC)
    - the generated DDL string is `CREATE TABLE IF NOT EXISTS "events_YYYY_MM" PARTITION OF "events" FOR VALUES FROM (..) TO (..)` (idempotent)
    - bounds are always the first day of the month at 00:00 UTC; `to` is exclusive (next month's first day)
  </behavior>
  <action>
    Create `partitions.ts` with PURE, side-effect-free helpers: a function that, given a reference `Date`, computes next month's partition spec — the partition name `events_YYYY_MM` (UTC year/month of the next month, zero-padded) and the `[from, to)` bounds as `YYYY-MM-01` strings (from = next month's first day, to = the month after, exclusive). A second function renders the idempotent `CREATE TABLE IF NOT EXISTS "<name>" PARTITION OF "events" FOR VALUES FROM ('<from>') TO ('<to>')` SQL string from a spec. Keep these pure (no DB/Redis) so they unit-test without infra. Create `partitions.test.ts` (clone `index.test.ts` vitest style) asserting: the year-rollover case (Dec → next-Jan), zero-padding of single-digit months, exclusive upper bound, and that the rendered SQL contains `IF NOT EXISTS` and `PARTITION OF "events"`.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/worker test && pnpm --filter @imbau/worker typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/src/partitions.test.ts` asserts the Dec→Jan rollover produces `events_2027_01` with bounds `2027-01-01`/`2027-02-01`
    - the rendered DDL string contains `CREATE TABLE IF NOT EXISTS` and `PARTITION OF "events"`
    - `pnpm --filter @imbau/worker test` exits 0
  </acceptance_criteria>
  <done>The partition math + idempotent DDL are pure and unit-tested independent of any running DB.</done>
</task>

<task type="auto">
  <name>Task 2: Register the repeatable pre-create job on the worker (skeleton)</name>
  <files>apps/worker/src/index.ts, apps/worker/src/partitions.ts</files>
  <read_first>
    - apps/worker/src/index.ts (the `boot()` factory + Queue/Worker registration to extend — add the repeatable job without breaking the health shell or the auto-boot guard)
    - .planning/phases/01-schema-completo-rls/01-RESEARCH.md (D-06 skeleton scope: register the repeatable job + the executor that runs the helper SQL via the owner connection; retention deferred)
  </read_first>
  <action>
    In `partitions.ts`, add a job executor that opens an owner DB connection (reuse the owner connection-string env var the migrate path uses), runs the idempotent next-month partition DDL from Task 1, logs a structured pino line on success, and closes the connection. In `index.ts`, register a repeatable BullMQ job (e.g. queue `partitions-maintenance`, a monthly repeat pattern) inside `boot()` alongside the existing health queue, and stand up a Worker whose processor calls the executor. Keep the existing health shell and the `import.meta.url` auto-boot guard intact (no second connection leak in tests). Phase 1 scope: registration + executor only; do NOT add retention/detach. Errors must surface (pino + Sentry already wired via `./instrument`), never be swallowed.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/worker typecheck && pnpm --filter @imbau/worker test && pnpm --filter @imbau/worker lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/src/index.ts` registers a repeatable job for events-partition maintenance inside `boot()` without removing the health queue/worker
    - the job executor calls the pure helper from `partitions.ts` and runs its DDL via the owner connection
    - `pnpm --filter @imbau/worker test` exits 0 and the existing worker smoke test still passes (no open-handle leak)
  </acceptance_criteria>
  <done>The worker carries an idempotent monthly partition pre-creation job; the DEFAULT partition remains the safety net so this job is never on the critical insert path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (owner connection) → DB | runs DDL (CREATE partition); must be idempotent and never destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Denial of Service | missing future partition for events insert | low | mitigate | DEFAULT partition (D-05) catches any ts; the job pre-creates next month idempotently |
| T-01-17 | Tampering | non-idempotent DDL corrupting partitions on re-run | medium | mitigate | `CREATE TABLE IF NOT EXISTS ... PARTITION OF` only; pure helper unit-tested; no DROP/DETACH this phase |
</threat_model>

<verification>
- `pnpm --filter @imbau/worker test` (partition helper + worker smoke) green; `typecheck` + `lint` clean.
</verification>

<success_criteria>
- A unit-tested pure partition helper exists; a repeatable idempotent pre-create job is registered on the worker without breaking the health shell.
</success_criteria>

<output>
Create `.planning/phases/01-schema-completo-rls/01-05-SUMMARY.md` when done.
</output>
