---
phase: 01-schema-completo-rls
plan: 06
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - packages/db/tests/helpers.ts
  - packages/db/tests/cross-tenant.test.ts
autonomous: true
requirements: [SCHEMA-07, SCHEMA-08]
must_haves:
  truths:
    - "For every new tenant table, org A reads zero org B rows, and cross-tenant INSERT raises 42501 / cross-tenant UPDATE affects 0 rows — asserted as the unprivileged app role"
    - "anon reads ONLY rows of publicado projects across catalog/content tables and zero borrador rows; anon SELECT on quotes, cac_index, leads, events raises 42501"
    - "anon INSERT into leads/events succeeds against a publicado project and is rejected (42501) against a borrador project"
    - "an event inserted with an out-of-range ts lands in the DEFAULT partition (no error) and is readable by its tenant; querying the events parent as the wrong tenant returns 0 rows"
    - "the full @imbau/db suite is green in CI against real Postgres 16 with NOBYPASSRLS roles — the phase exit gate"
  artifacts:
    - packages/db/tests/helpers.ts
    - packages/db/tests/cross-tenant.test.ts
  key_links:
    - "fixtures seed via the OWNER connection (setup only); ALL assertions run through withTenant/withAnon as app_authenticated/anon with rolbypassrls=false (the role guard stays intact)"
---

<objective>
Extend the existing cross-tenant absence suite to every new table — the phase exit gate (SCHEMA-08) — and add the anon published-only assertions (SCHEMA-07), the anon insert-only assertions for leads/events, the no-anon-SELECT assertions for the tenant-private tables, and the events partition-routing/isolation cases.

This plan does NOT weaken any assertion to make it pass: if a case fails, the fix belongs in the schema/migration plans (01-01..01-04), not here.

Purpose: SCHEMA-07 (anon reads only publicado rows; verified by test) and SCHEMA-08 (cross-tenant isolation extended to all new tables, green in CI vs real Postgres 16 with roles without BYPASSRLS).
Output: extended `helpers.ts` (13 fixtures) + extended `cross-tenant.test.ts`; `pnpm --filter @imbau/db test` green.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-completo-rls/01-RESEARCH.md
@.planning/phases/01-schema-completo-rls/01-VALIDATION.md

# The suite + fixtures to extend (do NOT rewrite — clone the existing cases)
@packages/db/tests/cross-tenant.test.ts
@packages/db/tests/helpers.ts
@packages/db/tests/db.ts
@packages/db/src/with-tenant.ts
</context>

> Depends on 01-04: the full journal (0000–0003) migrates from zero in the suite's globalSetup. See plan 01-01 "Resolved phase decisions" for the anon coverage map (catalog/content = anon SELECT published; leads/events = anon INSERT-only; quotes/cac_index = no anon).

<tasks>

<task type="auto">
  <name>Task 1: Extend helpers.ts with owner-seeded fixtures for every new table</name>
  <files>packages/db/tests/helpers.ts</files>
  <read_first>
    - packages/db/tests/helpers.ts (the EXACT fixture style to clone: `makeProject`/`makeMember`, owner connection via `ownerDb()`, unique `randomUUID` ids, no rollback)
    - packages/db/src/schema/index.ts (after 01-04 — import the new tables from the barrel)
    - .planning/phases/01-schema-completo-rls/01-RESEARCH.md (Wave 0 Gaps — the fixture list)
  </read_first>
  <action>
    Add owner-seeded fixtures cloning `makeProject` (insert via `ownerDb().db.insert(<table>).values({...})`, unique `randomUUID()` ids, return the id): `makeFloor(orgId, projectId)`, `makeUnit(orgId, projectId, floorId)`, `makePriceList(orgId, projectId)`, `makeUnitPrice(orgId, projectId, unitId, priceListId)`, `makePaymentPlan(orgId, projectId)`, `makeCacIndex(orgId)`, `makeQuote(orgId, projectId, unitId, paymentPlanId)`, `makeBroker(orgId, projectId)`, `makeLead(orgId, projectId)`, `makeProgressPost(orgId, projectId)`, `makeGallery(orgId, projectId)`, `makeMedia(orgId, projectId)`, `makeEvent(orgId, projectId, ts?)` (ts defaults to now; accept an override so the DEFAULT-partition routing case can pass a far-future ts). Each fixture sets `organizationId` + the parent FK column(s) so the composite FKs are satisfied. Seeding runs as OWNER (intentional — only assertions run unprivileged). Do NOT modify the existing fixtures or `closeFixtures`.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/tests/helpers.ts` exports `makeFloor`, `makeUnit`, `makeUnitPrice`, `makePaymentPlan`, `makeCacIndex`, `makeQuote`, `makeBroker`, `makeLead`, `makeProgressPost`, `makeGallery`, `makeMedia`, `makeEvent`, `makePriceList`
    - `makeEvent` accepts an optional `ts` override
    - each fixture inserts via `ownerDb()` and returns a `randomUUID`-based id
    - `pnpm --filter @imbau/db typecheck` exits 0
  </acceptance_criteria>
  <done>Fresh, unique owner-seeded fixtures exist for every new table, mirroring the established style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend cross-tenant.test.ts — absence, anon-published, anon insert-only, events routing (SCHEMA-07/08)</name>
  <files>packages/db/tests/cross-tenant.test.ts</files>
  <read_first>
    - packages/db/tests/cross-tenant.test.ts (clone the existing cases: the role guard lines 92-124, `rlsViolationInChain` lines 71-89, read-isolation (a)/(b), write (c) INSERT-throws + UPDATE-0-rows, anon-published (d))
    - packages/db/src/with-tenant.ts (`withTenant`/`withAnon` — the only sanctioned access; assertions run through these)
    - .planning/phases/01-schema-completo-rls/01-RESEARCH.md (Validation Architecture — the Req→Test map; D-16 case list)
  </read_first>
  <behavior>
    - read A→B and B→A over every new tenant table: zero rows of the other org (absence, not just "no error")
    - cross-tenant INSERT raises 42501 (assert via rlsViolationInChain); cross-tenant UPDATE returns 0 rows (.returning().length)
    - anon SELECT over each catalog/content table returns only publicado-project rows, zero borrador
    - anon SELECT over quotes, cac_index, leads, events raises 42501 (no grant)
    - anon INSERT into leads/events against a publicado project succeeds; against a borrador project raises 42501
    - an event with a far-future ts inserts without error (DEFAULT partition) and is read back by its tenant; the events parent queried as the wrong tenant yields 0 rows
  </behavior>
  <action>
    Extend the suite (do NOT rewrite). In `beforeAll`, expand the seeded scenario: for both org A and org B, seed a publicado project and a borrador project, and under each project seed one row of every new table (use the Task-1 fixtures; for leads/events seed via owner so the absence/read cases have data). Add test cases mirroring the existing (a)-(d) pattern: (1) per new tenant table — read A→B and B→A return zero rows of the other org (assert as app role via `withTenant`); (2) per new tenant table — cross-tenant INSERT (claiming org B while scoped to org A) raises an RLS violation (`rlsViolationInChain` true) and cross-tenant UPDATE of an org-B row affects 0 rows (`.returning().length` === 0); (3) anon published-only — for floors, units, price_lists, unit_prices, payment_plans, brokers, progress_posts, galleries, media: `withAnon` select returns rows ONLY for publicado projects and zero rows tied to borrador projects; (4) no-anon — `withAnon` select on quotes, cac_index, leads, events each rejects with 42501; (5) anon insert-only — `withAnon` insert into leads with a publicado project_id succeeds, into a borrador project_id rejects (42501); same for events; (6) events partition — `withTenant(orgA)` insert a `makeEvent`-shaped row with a far-future ts succeeds and is read back; an event row of org B is invisible to org A querying the events PARENT (Pitfall 6 — parent isolation). Keep the existing role guard test intact (proves rolbypassrls=false). Do NOT switch any assertion to the owner role and do NOT weaken a check to pass — a failure means a schema/migration fix in 01-01..01-04.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/db test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @imbau/db test` exits 0 with the extended suite (migrates 0000–0003 from zero in globalSetup, runs all assertions as app/anon)
    - the suite asserts anon SELECT on quotes, cac_index, leads, and events each raises a 42501 / row-level-security rejection
    - the suite asserts anon INSERT into leads and events succeeds vs a publicado project and is rejected (42501) vs a borrador project
    - the suite asserts a far-future-ts event inserts without error and is read back, and that an org-B event is absent when org A queries the events parent
    - the role-guard test (current_user app_authenticated/anon, rolbypassrls=false) remains and passes
  </acceptance_criteria>
  <done>The cross-tenant absence suite covers every new table plus the anon read/write boundary and events partition behavior; green here is the phase exit gate (SCHEMA-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web (anon) → DB | the suite proves anon can read only publicado catalog rows, write only publicado-gated leads/events, and read nothing private |
| panel/API (app_authenticated) → DB | the suite proves org A can never read/write org B across every new table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Information Disclosure | cross-tenant read leak on a new table | high | mitigate | per-table absence assertions (read A→B / B→A) as the unprivileged role — the exit gate |
| T-01-19 | Tampering | cross-tenant write on a new table | high | mitigate | per-table INSERT-42501 + UPDATE-0-rows assertions |
| T-01-20 | Information Disclosure | anon enumerating private/unpublished data | high | mitigate | anon-published assertions + no-anon-SELECT (quotes/cac_index/leads/events) + borrador-rejection assertions |
| T-01-21 | Repudiation | absence test passing as owner (false green) | medium | mitigate | role guard asserts current_user is app/anon with rolbypassrls=false; assertions never run as owner |
</threat_model>

<verification>
- `pnpm --filter @imbau/db test` green (full extended suite, real Postgres 16, NOBYPASSRLS roles).
- Wave/phase merge: `pnpm test && pnpm typecheck && pnpm lint` green; the CI `quality` gate is the phase exit gate before `/gsd-verify-work`.
</verification>

<success_criteria>
- Every new table has cross-tenant absence + write-rejection coverage; anon read/write boundary fully asserted; events routing/isolation asserted; suite green in CI.
</success_criteria>

<output>
Create `.planning/phases/01-schema-completo-rls/01-06-SUMMARY.md` when done.
</output>
