---
phase: 03-seed-del-edificio-ficticio
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - packages/db/tests/seed.idempotency.test.ts
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [SEED-04]
user_setup: []

must_haves:
  truths:
    - "running the full seed twice yields identical per-table count(*) for every seeded table (row-count invariance) — the non-negotiable SEED-04 gate"
    - "no duplicate ids across re-runs; deterministic ids are stable"
    - "withAnon reads seeded floors/units/galleries/media because the project is publicado; a different-tenant GUC reads 0 seeded rows (RLS-correct isolation)"
    - "pnpm db:seed is documented with the full prerequisite list (compose postgres+redis+worker, R2 env by NAME) (SEED-04)"
  artifacts:
    - packages/db/tests/seed.idempotency.test.ts
    - README.md (## Comandos db:seed section)
  key_links:
    - "onConflictDoNothing across all inserts + deterministic seedId => second runSeed() is a no-op => count invariance"
    - "runSeed({ skipMedia: true }) lets CI validate row invariance without R2/worker; the db:seed CLI path stays full-pipeline + D-05 fail-fast"
---

<objective>
Prove and document the seed's idempotency (SEED-04). Add the row-count-invariance gate — run the full seed twice and assert every seeded table's `count(*)` is identical — plus RLS-correctness assertions (anon reads the publicado project's rows; a foreign tenant GUC reads zero seeded rows). Document `pnpm db:seed` and its prerequisites in the README `## Comandos` section so an operator can run it reproducibly.

Purpose: Idempotency is the phase's exit criterion; without a run-twice invariance test the "no duplica filas" guarantee is unproven. This plan turns the deterministic-id + onConflictDoNothing design (built across plans 01-02) into an enforced, automated gate and a documented command.
Output: A green idempotency + isolation test suite and a documented, discoverable `pnpm db:seed`.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-seed-del-edificio-ficticio/03-CONTEXT.md
@.planning/phases/03-seed-del-edificio-ficticio/03-RESEARCH.md
@.planning/phases/03-seed-del-edificio-ficticio/03-PATTERNS.md
@.planning/phases/03-seed-del-edificio-ficticio/03-01-PLAN.md
@.planning/phases/03-seed-del-edificio-ficticio/03-02-PLAN.md
</context>

<artifacts_this_plan_produces>
New files/symbols this plan creates:

- `packages/db/tests/seed.idempotency.test.ts` — the SEED-04 gate: run-twice per-table `count(*)` invariance + no-duplicate-ids, plus withAnon published-visibility and foreign-tenant isolation assertions.
- `README.md` (`## Comandos`) — documents `pnpm db:seed`, prerequisites (compose postgres+redis+worker, R2 env vars by NAME), and points at `packages/db/src/seed/assets/LICENSES.md` for image provenance.
- `CLAUDE.md` (`## Comandos`) — add `pnpm db:seed` to the running command list (kept current per project convention).
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Row-count-invariance gate + RLS-correctness assertions (SEED-04)</name>
  <files>packages/db/tests/seed.idempotency.test.ts</files>
  <read_first>
    - packages/db/tests/db.ts (connectAs/ownerUrl/appUrl/anonUrl + `_test` DB guard — reuse; NEVER run seed against a non-`_test` DB in tests)
    - packages/db/tests/helpers.ts (fixture/query style; makeOrg for a second foreign tenant if needed for isolation)
    - packages/db/seed.ts (runSeed signature + skipMedia option — from plans 01/02)
    - packages/db/src/with-tenant.ts + packages/db/src/client.ts (withAnon reads publicado-only; withTenant scoped by GUC)
    - .planning/phases/03-seed-del-edificio-ficticio/03-RESEARCH.md (Validation Architecture §487-521 — row-count-invariance is the non-negotiable gate; skipIf strategy; Pattern 1 §190-207)
    - .planning/phases/03-seed-del-edificio-ficticio/03-PATTERNS.md (tests §176-185; idempotency shared pattern §199-208)
    - CLAUDE.md (idempotencia por diseño; RLS respetada)
  </read_first>
  <action>
    Create `packages/db/tests/seed.idempotency.test.ts` against the `_test` harness (reuse `packages/db/tests/db.ts`; respect the `_test`-DB name guard). Always-on CI gate: call `runSeed({ skipMedia: true })` twice, capturing per-table `count(*)` for every seeded table (organization, projects, floors, units, price_lists, unit_prices, payment_plans, cac_index, brokers, leads, progress_posts, galleries, events) after run 1 and after run 2, and assert the counts are pairwise identical (row-count invariance — the SEED-04 gate). Also assert no duplicate ids appear (e.g. `count(*) === count(distinct id)` per table where applicable). Add an env-gated `describe.skipIf` block that runs the FULL `runSeed()` (media on) twice when R2/worker are present and asserts the `media` row count is also invariant (covers the media-object idempotency path). RLS-correctness assertions: after a seed, use `withAnon` to read floors/units/galleries/media and assert non-empty (the project is `publicado`); then read the same tables under a DIFFERENT tenant GUC (a foreign org id via `withTenant(foreignOrgId, ...)`) and assert zero seeded rows are visible (cross-tenant isolation — withCheck org = GUC). Keep the media assertions in the env-gated block only.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && pnpm --filter @imbau/db test -- --run seed.idempotency && pnpm --filter @imbau/db typecheck</automated>
  </verify>
  <done>seed.idempotency.test.ts green: two `skipMedia` runs produce identical per-table counts and no duplicate ids; anon reads the publicado rows; a foreign tenant GUC reads zero seeded rows; media invariance covered in the env-gated block.</done>
  <acceptance_criteria>
    - Running `runSeed({ skipMedia: true })` twice yields identical per-table `count(*)` for every seeded table (asserted; this is the SEED-04 gate).
    - No table gains rows on the second run; `count(*) === count(distinct id)` holds.
    - `withAnon` reads seeded floors/units/galleries/media (project publicado) return non-empty; a foreign-tenant GUC returns 0 seeded rows.
    - The test uses the `_test` DB harness and never targets a non-`_test` database.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document pnpm db:seed + prerequisites (SEED-04)</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (existing structure; where a `## Comandos` / commands section lives or should live)
    - CLAUDE.md (`## Comandos` list — kept current per project convention; add db:seed)
    - packages/db/package.json + root package.json (the `db:seed` scripts added in plan 01)
    - packages/db/src/seed/prerequisites.ts (the exact required env var NAMES the guard enforces — D-05)
    - packages/db/src/seed/assets/LICENSES.md (image provenance to reference)
    - .planning/phases/03-seed-del-edificio-ficticio/03-RESEARCH.md (Environment Availability §476-485; Pattern 3 §230-236 docs requirement)
  </read_first>
  <action>
    Add a `## Comandos` entry (README.md, and mirror the one-liner into CLAUDE.md's `## Comandos` list) documenting `pnpm db:seed`: what it does (siembra determinista e idempotente del edificio "Brigos Recoleta"), that it is safe to re-run (idempotent — no duplica filas), and the full prerequisite list required by the D-05 fail-fast guard: the DB must be migrated first (`pnpm db:migrate`), the media pipeline must be up (`docker compose up -d postgres redis worker`), and the required env vars must be present — list them by NAME only (`DATABASE_URL`, `DATABASE_APP_URL`, `DATABASE_ANON_URL`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_PUBLIC_BASE_URL`, `REDIS_URL`), never example values. Note that the seed aborts with an explanatory error if a prerequisite is missing, and point at `packages/db/src/seed/assets/LICENSES.md` for stock-image provenance/licensing (D-03). Keep the prose es-AR (voseo) consistent with the rest of the docs.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && grep -q 'db:seed' README.md && grep -q 'db:seed' CLAUDE.md && grep -q 'REDIS_URL' README.md && grep -q 'db:migrate' README.md</automated>
  </verify>
  <done>README `## Comandos` documents `pnpm db:seed` with its prerequisites (db:migrate, compose up, R2 env by NAME) and points at the license manifest; CLAUDE.md `## Comandos` lists db:seed.</done>
  <acceptance_criteria>
    - `README.md` documents `pnpm db:seed`, states it is idempotent/re-runnable, and lists all prerequisites (migrate first, compose up postgres+redis+worker, the R2/DB/Redis env vars by NAME).
    - `CLAUDE.md` `## Comandos` includes `pnpm db:seed`.
    - The docs reference `packages/db/src/seed/assets/LICENSES.md` for image provenance and contain no secret values.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process to Postgres | Runs against the `_test` DB only (harness guard); never a real/prod DB |
| operator to seed CLI | Docs must state prerequisites + that db:seed targets the configured DB; idempotent by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | non-idempotent re-run duplicating rows | high | mitigate | Row-count-invariance test is the enforced SEED-04 gate; deterministic ids + onConflictDoNothing established in plans 01-02 |
| T-03-10 | Tampering | seeding into a wrong/prod DB by accident | low | accept | Docs state db:seed targets the configured DB; the test harness enforces the `_test` DB name guard; idempotent by design limits blast radius |
| T-03-11 | Elevation | cross-tenant contamination | high | mitigate | Isolation assertion: a foreign-tenant GUC reads 0 seeded rows; withCheck (org = GUC) structurally rejects a wrong org id |
| T-03-12 | Information Disclosure | secret values in docs | low | mitigate | Document required env vars by NAME only, never example values (ASVS V7) |
</threat_model>

<verification>
- `pnpm --filter @imbau/db test` green including `seed.idempotency`.
- Two `skipMedia` seed runs produce identical per-table counts (SEED-04 gate); with R2/worker up, media count is also invariant.
- Anon reads the publicado project's rows; a foreign tenant reads none.
- README + CLAUDE document `pnpm db:seed` and its prerequisites; no secret values in docs.
- Full phase gate: `pnpm --filter @imbau/db test` (all seed suites) green before `/gsd-verify-work`.
</verification>

<success_criteria>
- The seed is proven idempotent by an automated run-twice row-count-invariance test (SEED-04).
- RLS correctness (published visibility + cross-tenant isolation) is asserted.
- `pnpm db:seed` is documented and discoverable with its full prerequisite list.
- SEED-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-seed-del-edificio-ficticio/03-03-SUMMARY.md` when done.
</output>
