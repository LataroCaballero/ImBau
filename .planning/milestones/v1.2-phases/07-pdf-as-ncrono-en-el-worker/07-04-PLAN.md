---
phase: 07-pdf-as-ncrono-en-el-worker
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - apps/web/env.ts
  - apps/web/env.test.ts
  - apps/web/vitest.config.ts
  - deploy/compose.staging.yml
  - apps/web/components/cotizador-simulator.tsx
autonomous: true
requirements: [PDF-01]
tags: [web, trpc, polling, presigned-download, env, compose, staging]

must_haves:
  truths:
    - "apps/web/env.ts validates REDIS_URL + R2_* in the SERVER block (never NEXT_PUBLIC_) because D-02 runs the enqueue (producer) + presign (status) inside the web process (Pitfall 3)."
    - "The staging web service depends_on redis and inherits REDIS_URL/R2_* from the SOPS .env; the worker service receives WEB_PUBLIC_BASE_URL for the QR deep-link."
    - "Tapping 'Descargar PDF' emits/reuses a quoteId (D-01), shows 'Generando PDF…', polls quotes.pdfStatus (~2s, inside nginx zone=quotes), and auto-downloads the presigned URL when ready (D-03)."
    - "A ~30-45s timeout or a failed job shows a soft es-AR voseo message ('No pudimos generar el PDF, probá de nuevo en un rato') and resets the button; the WhatsApp CTA stays visible — the PDF is never the critical path (D-10)."
    - "Changing unit/plan/modalidad invalidates the retained quoteId so the next trigger emits a fresh quote with its own PDF (D-01)."
  artifacts:
    - "apps/web/env.ts with redisEnv + r2Env in the server block"
    - "deploy/compose.staging.yml — web depends_on redis + worker WEB_PUBLIC_BASE_URL"
    - "apps/web/components/cotizador-simulator.tsx — real PDF wiring replacing the 'Próximamente' placeholder"
  key_links:
    - "quotes.pdfStatus is polled via trpc.quotes.pdfStatus.queryOptions with TanStack Query refetchInterval; the path /api/trpc/quotes.pdfStatus stays on the dedicated isQuotesOp link → inside the nginx quotes throttle"
    - "quoteId is shared between the WhatsApp CTA and the PDF button (D-01) — both reuse a retained id; a selection change resets it"
    - "auto-download = presigned R2 GET (from pdfStatus) opened via an anchor click; fallback 'Descargar' button if mobile blocks the auto-download"
  prohibitions:
    - statement: "R2_*/REDIS_URL are never exposed to the browser bundle (server block only)"
      status: resolved
      verification: "apps/web/env.ts adds redisEnv/r2Env under `server:` — not `client:`, never NEXT_PUBLIC_ (T-03-01)"
    - statement: "quotes calls stay on the dedicated quotes link so the nginx rate limit still applies"
      status: resolved
      verification: "pdfStatus is a quotes.* procedure → isQuotesOp matches → dedicated httpBatchLink → path /api/trpc/quotes.pdfStatus (QUOTE-03)"
---

<objective>
Close the loop for the buyer (PDF-01): declare the new server env `apps/web` needs now that the enqueue +
presign run in the web process (D-02), wire the staging deploy config, and replace the fase-6 "Descargar PDF ·
Próximamente" placeholder with the real flow — emit/reuse a quoteId (D-01), poll `quotes.pdfStatus`, and
auto-download the presigned R2 URL when ready (D-03), degrading softly on timeout/failure (D-10) while the
WhatsApp CTA stays live. The PDF is never the demo-critical path.

Purpose: This is the only surface the buyer touches; everything upstream (producer, worker, status) exists to
serve this download. It depends on the pdfStatus procedure shipped in 07-03.
Output: apps/web/env.ts (+redisEnv +r2Env), compose.staging.yml (web depends_on redis + worker WEB_PUBLIC_BASE_URL), cotizador-simulator.tsx wiring.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-CONTEXT.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-RESEARCH.md
@.planning/phases/07-pdf-as-ncrono-en-el-worker/07-PATTERNS.md

# Files to modify (read current state first)
@apps/web/env.ts
@apps/web/components/cotizador-simulator.tsx
@apps/web/lib/trpc-client.tsx
@apps/web/lib/trpc-split.ts
@deploy/compose.staging.yml

# Reference for the env presets + producer/presign that run in the web process
@packages/config/env/presets.ts
@packages/api/src/media/runtime.ts
@deploy/nginx/staging.tours.andescode.com.ar.conf
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- **Modified:** `apps/web/env.ts` server block spreads `...redisEnv.server` + `...r2Env.server`
- **Modified:** `deploy/compose.staging.yml` — `web.depends_on` adds redis; `worker.environment` adds `WEB_PUBLIC_BASE_URL`
- **Modified:** `apps/web/components/cotizador-simulator.tsx` — new client state/handlers for quoteId retention, pdfStatus polling, auto-download, soft-fail (replacing the disabled placeholder button)
- **New client behavior (no new exported symbol):** PDF download flow inside the existing CotizadorSimulator island
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: apps/web server env (REDIS_URL + R2_*) + staging compose (web depends_on redis + worker WEB_PUBLIC_BASE_URL)</name>
  <files>apps/web/env.ts, apps/web/env.test.ts, apps/web/vitest.config.ts, deploy/compose.staging.yml</files>
  <read_first>
    - apps/web/env.ts (phase-5 A1 widening pattern; server block where DATABASE_APP_URL/ANON_URL live)
    - apps/web/env.test.ts (asserts the web env composition — MUST be updated to include the new vars in its valid runtimeEnv)
    - apps/web/vitest.config.ts (no test.env block today — add one only if a web test loads env.ts and now needs the new vars)
    - packages/config/env/presets.ts (redisEnv, r2Env — already imported by media/runtime; reuse them)
    - deploy/compose.staging.yml (web service depends_on postgres only; worker service environment block)
  </read_first>
  <action>
    In `apps/web/env.ts`: import `redisEnv, r2Env` from `@imbau/config/env/presets` and spread `...redisEnv.server`
    + `...r2Env.server` into the existing `server:` block (alongside DATABASE_ANON_URL/DATABASE_APP_URL). These are
    server-only (D-02: the quotesRouter enqueue + presign run in the web process) — NEVER `NEXT_PUBLIC_` (T-03-01).
    Add a comment explaining D-02 (web now hosts the PDF producer + presigned-GET). Do NOT add DATABASE_URL (owner) —
    web never needs it. Do NOT wire these into experimental__runtimeEnv (server vars, not client-inlined).

    Update `apps/web/env.test.ts` to keep the web env suite green: add REDIS_URL + the five R2_* vars to the valid-case
    runtimeEnv (dummy non-secret values, e.g. REDIS_URL "redis://localhost:6380", R2_* "test"/"https://cdn.example.test"),
    and add them to the composed server schema under test so parity with env.ts holds. If ANY other web vitest suite imports
    apps/web/env.ts (run the web suite to check), add a `test.env` block to apps/web/vitest.config.ts injecting those dummy
    values (mirror apps/worker/vitest.config.ts); otherwise leave vitest.config.ts untouched.

    In `deploy/compose.staging.yml`: (1) the `web` service — add `redis: { condition: service_healthy }` to its
    `depends_on` (keeping the existing postgres condition) and add a comment that REDIS_URL + R2_* are inherited from the
    SOPS-decrypted `.env` via the existing `env_file: [.env]` (worker/media already use those values — no new secret to
    generate). (2) the `worker` service — add an `environment:` entry `WEB_PUBLIC_BASE_URL: https://staging.tours.andescode.com.ar`
    (non-secret; the public origin of apps/web used to build the QR deep-link — D-08) alongside `APP_NAME: worker`.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/web test && pnpm --filter @imbau/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/env.ts server block spreads `...redisEnv.server` and `...r2Env.server`; no R2_/REDIS var appears under `client:` or in experimental__runtimeEnv
    - apps/web/env.test.ts valid case includes REDIS_URL + R2_* and passes
    - deploy/compose.staging.yml web service depends_on includes redis (service_healthy)
    - deploy/compose.staging.yml worker service environment includes `WEB_PUBLIC_BASE_URL`
    - `pnpm --filter @imbau/web test` and `pnpm --filter @imbau/web typecheck` both exit 0
  </acceptance_criteria>
  <done>apps/web validates the Redis + R2 env it now needs at runtime (server-only), and the staging topology wires the web→redis dependency plus the worker's QR deep-link origin — the deploy will not fail-fast on a missing var.</done>
</task>

<task type="auto">
  <name>Task 2: cotizador-simulator — quoteId reuse, pdfStatus polling, auto-download, soft failure</name>
  <files>apps/web/components/cotizador-simulator.tsx</files>
  <read_first>
    - apps/web/components/cotizador-simulator.tsx (create mutation L103, onWhatsapp L188-219, softError slot L273-280, placeholder button L304-311, selection effects L110-176)
    - apps/web/lib/trpc-client.tsx (useTRPC; quotes on the dedicated split link)
    - apps/web/lib/trpc-split.ts (isQuotesOp — pdfStatus stays on the quotes link)
    - .planning/phases/07-pdf-as-ncrono-en-el-worker/07-PATTERNS.md (cotizador-simulator section — reuse readTrpcError, softError, seqRef)
    - .planning/phases/07-pdf-as-ncrono-en-el-worker/07-CONTEXT.md (D-01/D-03/D-10 — quoteId lifecycle, polling+auto-download, soft failure)
  </read_first>
  <action>
    Replace the disabled placeholder button (L304-311) with the real PDF flow. Mechanics are Claude's discretion
    (CONTEXT) but the contract is fixed:

    quoteId reuse (D-01): lift the emitted quoteId into shared state so BOTH the WhatsApp CTA (existing onWhatsapp) and
    the new PDF button reuse one emission. Extract a helper that returns the retained quoteId or awaits `create.mutateAsync(
    {projectId, unitId, paymentPlanId, modalidad})` and retains `created.quoteId`. Have onWhatsapp use this helper too (it
    already calls create — now it stores the id). Reset the retained quoteId to null whenever {unitId, planId, modalidad}
    changes (extend the existing selection effect / handlers) so a changed selection forces a fresh emission.

    PDF button + polling (D-03): on click set a "generando" state, call the helper to ensure a quoteId, then poll
    `trpc.quotes.pdfStatus.queryOptions({ projectId: project.id, quoteId }, { enabled, refetchInterval })` with TanStack Query
    (useQuery) — refetchInterval ~2000ms while not ready, stop when ready or on timeout. On `{ready:true, url}` trigger an
    auto-download: create an anchor with href=url + download attribute and click it; ALSO expose a visible "Descargar" link/button
    to that url as a fallback when a mobile browser blocks the programmatic download. The pdfStatus path stays on the dedicated
    quotes link (isQuotesOp) so it remains inside the nginx zone=quotes throttle.

    Soft failure (D-10): cap polling at ~30-45s (a poll counter or a deadline). On timeout OR a job that never produces a key,
    show the soft es-AR voseo message "No pudimos generar el PDF, probá de nuevo en un rato" (reuse the existing softError slot),
    reset the button to its initial state so the buyer can retry, and keep the WhatsApp CTA visible. Reuse `readTrpcError` (L62-75)
    to tolerate a 429 on the poll (soft-retry, never a raw error). The PDF is never the critical path — no failure blocks the screen or WhatsApp.

    Keep the file a client island ("use client"); do not introduce server-only imports. Preserve all existing UI-01..06/WA-01 behavior.
  </action>
  <verify>
    <automated>pnpm --filter @imbau/web typecheck && pnpm --filter @imbau/web lint && SKIP_ENV_VALIDATION=1 pnpm --filter @imbau/web build</automated>
    <human-check>On staging (post-merge to main → web/worker image on the VPS): open the cotizador for a published unit, tap "Descargar PDF", confirm it shows "Generando PDF…", then auto-downloads a one-page PDF with correct es-AR accents, both legal leyendas, the header (proyecto/unidad/piso/tipología/m²/fecha/CAC/ref), and the footer deep-link + scannable QR. Confirm the WhatsApp CTA stays usable throughout and a forced failure shows the soft es-AR message.</human-check>
  </verify>
  <acceptance_criteria>
    - The "Descargar PDF · Próximamente" disabled placeholder is removed; a working PDF button + optional fallback download link exist
    - cotizador-simulator.tsx calls `trpc.quotes.pdfStatus` (query) with a refetchInterval and reuses a retained quoteId across the WhatsApp + PDF triggers
    - A selection change ({unitId, planId, modalidad}) resets the retained quoteId
    - Timeout/failure path renders the soft es-AR voseo message and keeps the WhatsApp CTA visible
    - `pnpm --filter @imbau/web typecheck`, `lint`, and `SKIP_ENV_VALIDATION=1 … build` all exit 0
  </acceptance_criteria>
  <done>The buyer can tap "Descargar PDF" and receive the generated PDF via poll + auto-download, with graceful es-AR degradation and the WhatsApp path always live — the fase-6 placeholder is gone (PDF-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser bundle ← env | Server secrets must not reach the client bundle |
| client poll → edge | pdfStatus polling must stay inside the rate limit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Information Disclosure | R2/Redis secret leaking to the browser | high | mitigate | REDIS_URL + R2_* declared in the server block only (t3-env split) — never NEXT_PUBLIC_; presign/enqueue run server-side. |
| T-07-04 | Denial of Service | aggressive pdfStatus polling | medium | mitigate | pdfStatus stays on the quotes.* dedicated link → nginx zone=quotes (10r/s burst 20); ~2s per client is trivial; polling capped at a deadline. |
| T-07-15 | Availability | PDF failure blocks the demo path | medium | mitigate | Soft-fail (D-10): timeout/error resets the button and shows an es-AR message; WhatsApp CTA never depends on the PDF. |
</threat_model>

<verification>
- `pnpm --filter @imbau/web test` + `typecheck` + `lint` + build green.
- Manual staging UAT (post-merge): end-to-end tap → generate → auto-download with correct accents/leyendas/QR (recorded at the phase gate).
</verification>

<success_criteria>
- The buyer downloads the PDF via poll + auto-download; the flow degrades softly and never blocks screen/WhatsApp (PDF-01).
- No server secret reaches the browser; polling stays within the edge rate limit.
</success_criteria>

<output>
Create `.planning/phases/07-pdf-as-ncrono-en-el-worker/07-04-SUMMARY.md` when done.
</output>
