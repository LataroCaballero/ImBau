---
phase: 06-ui-p-blica-del-cotizador-cta-whatsapp
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - packages/api/src/trpc/routers/picker.ts
  - packages/api/src/trpc/routers/_app.ts
  - packages/api/tests/picker-router.test.ts
autonomous: true
requirements: [UI-01, UI-05]
tags: [api, trpc, rls, anon, picker]

must_haves:
  truths:
    - "An anonymous caller can read the published project (with its whatsapp number), its floors, its units, and its payment plans through the picker router"
    - "The anon RLS policies gate every picker read to publicado projects — a borrador/archivado project yields no rows"
    - "payment_plans read by the picker carry notasLegales (the UI-05 leyenda source) and the preset fields (anticipoPct, cuotas, ajuste, refuerzos)"
  artifacts:
    - "packages/api/src/trpc/routers/picker.ts with getPublishedProject/listFloors/listUnits/listPlans"
    - "packages/api/src/trpc/routers/_app.ts mounting picker → extends AppRouter"
    - "packages/api/tests/picker-router.test.ts (integration, caller against real Postgres)"
  key_links:
    - "picker procedures route through withAnon → the anon policy filters to publicado (clone of projects.listPublished)"
    - "getPublishedProject selects projects.whatsapp (the new column from Plan 01)"
    - "picker mounted in _app.ts extends AppRouter so the web client gets end-to-end types with no codegen"
  prohibitions:
    - statement: "picker.ts imports ONLY withAnon and schema from @imbau/db — never createOwnerDb or appDb (fence T-03-09/T-05-07)"
      status: resolved
      verification: "grep of import statements in picker.ts shows no createOwnerDb / appDb / createAppDb; only { withAnon, schema } from @imbau/db"
    - statement: "No app-layer estado='publicado' WHERE is added — the RLS anon policy does the filtering"
      status: resolved
      verification: "grep shows picker.ts has no `estado` / `publicado` literal in its query where-clauses"
---

<objective>
Add the anon read seam the piso→unidad picker needs (D-08, UI-01): a new `picker` tRPC router with
`getPublishedProject` (including the new `whatsapp` column), `listFloors`, `listUnits`, `listPlans` —
all clones of the `projects.listPublished` `publicProcedure + withAnon` pattern — mounted into the
AppRouter so the web client gets end-to-end types.

Purpose: the RSC page (Plan 05) resolves the picker/project data server-side through these reads. The
plans read here carry `notasLegales` (the UI-05 leyenda) and the preset fields the snap slider maps.
Output: a fenced, RLS-safe anon picker router + integration test.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-CONTEXT.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 03)

New/changed symbols introduced by this plan (excluded from drift verification downstream):
- `packages/api/src/trpc/routers/picker.ts` exports: `pickerRouter`
- procedures: `picker.getPublishedProject`, `picker.listFloors`, `picker.listUnits`, `picker.listPlans`
- `_app.ts` gains a `picker: pickerRouter` mount (extends `AppRouter`)
- `packages/api/tests/picker-router.test.ts` — new integration test

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: picker router (anon published reads) + mount in AppRouter</name>
  <files>packages/api/src/trpc/routers/picker.ts, packages/api/src/trpc/routers/_app.ts</files>
  <read_first>
    - packages/api/src/trpc/routers/projects.ts (listPublished — the exact withAnon pattern to clone + the fence header comment)
    - packages/api/src/trpc/routers/quotes.ts (quoteInputSchema uuid inputs; how it imports only withAnon/withTenant/schema)
    - packages/api/src/trpc/routers/_app.ts (the router({...}) composition to extend)
    - packages/db/src/schema/units.ts (units_anon_published EXISTS-on-parent policy; estado enum disponible/reservado/vendido; identificador/tipologia/m2/ambientes/orden columns)
    - packages/db/src/schema/floors.ts (floors_anon_published; numero/nombre columns)
    - packages/db/src/schema/payment-plans.ts (payment_plans_anon_published; anticipoPct/cuotas/ajuste/refuerzos/notasLegales columns)
    - packages/db/src/schema/projects.ts (the whatsapp column from Plan 01)
    - 06-PATTERNS.md picker.ts section (the concrete clone shape + fence) and 06-RESEARCH.md Code Examples
  </read_first>
  <action>
    Create packages/api/src/trpc/routers/picker.ts. Mirror the projects.ts fence header comment (imports
    ONLY `withAnon`/`schema` from @imbau/db — never the owner/app-pool clients — grep-verified). Imports:
    `z` from zod, `eq` from drizzle-orm, `{ withAnon, schema }` from @imbau/db, `{ router, publicProcedure }`
    from `../init`. Export `pickerRouter = router({ ... })` with four publicProcedures, each routing
    through `withAnon((tx) => tx.select()...)` (the anon RLS policy does the publicado filtering — do NOT
    add an app-layer estado where):
    - `getPublishedProject`: input `z.object({ slug: z.string() })`; select `{ id, nombre, whatsapp }`
      from projects where `eq(schema.projects.slug, input.slug)` limit 1. (whatsapp is the new column.)
    - `listFloors`: input `z.object({ projectId: z.uuid() })`; select all from floors where
      `eq(schema.floors.projectId, input.projectId)`.
    - `listUnits`: input `z.object({ floorId: z.uuid() })`; select all from units where
      `eq(schema.units.floorId, input.floorId)`. (Return ALL fields incl. `estado`,`identificador`,
      `tipologia`,`m2`,`ambientes`,`orden`; the component decides only `disponible` is selectable — do
      NOT filter estado in the query.)
    - `listPlans`: input `z.object({ projectId: z.uuid() })`; select all from paymentPlans where
      `eq(schema.paymentPlans.projectId, input.projectId)`. (Carries `notasLegales`, the UI-05 leyenda,
      plus anticipoPct/cuotas/ajuste/refuerzos for the snap slider.)
    Then edit _app.ts: `import { pickerRouter } from "./picker";` and add `picker: pickerRouter,` inside
    `router({...})`. Update the head comment's procedure inventory to list the four picker procedures.
  </action>
  <verify>
    <automated>cd packages/api && grep -Eq '^import .*\{ *withAnon, *schema *\}' src/trpc/routers/picker.ts && ! grep -Eq '^import.*createOwnerDb|^import.*appDb|^import.*createAppDb' src/trpc/routers/picker.ts && ! grep -Eq "publicado|estado" src/trpc/routers/picker.ts && grep -q 'getPublishedProject' src/trpc/routers/picker.ts && grep -q 'whatsapp: schema.projects.whatsapp' src/trpc/routers/picker.ts && grep -q 'picker: pickerRouter' src/trpc/routers/_app.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - picker.ts imports exactly `{ withAnon, schema }` from @imbau/db and NOTHING else from that module
    - picker.ts has NO `createOwnerDb`/`appDb`/`createAppDb` import (fence)
    - picker.ts has NO `estado`/`publicado` literal in its queries (RLS does the filtering)
    - picker.ts exports pickerRouter with getPublishedProject (selecting whatsapp), listFloors, listUnits, listPlans
    - _app.ts mounts `picker: pickerRouter` and AppRouter type still compiles
    - `pnpm --filter @imbau/api typecheck` exits 0
  </acceptance_criteria>
  <done>The picker router exposes the four anon reads through withAnon and is mounted into AppRouter.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: picker router integration test (caller vs real Postgres)</name>
  <files>packages/api/tests/picker-router.test.ts</files>
  <read_first>
    - packages/api/tests/quotes-router.test.ts (the integration pattern: createCaller({ headers: new Headers() }), ownerSql seeding in FK order, makeUserWithActiveOrg)
    - packages/api/tests/db.ts (ownerSql helper)
    - packages/api/tests/fixtures.ts (makeUserWithActiveOrg, SessionFixture)
    - packages/api/vitest.config.ts (test runner config)
    - packages/db/src/schema/projects.ts (estado enum — seed one publicado + one borrador project)
  </read_first>
  <action>
    Create packages/api/tests/picker-router.test.ts following quotes-router.test.ts. Seed via the owner
    pool in FK order: an org, a `publicado` project (with a whatsapp value) + a `borrador` project, a
    floor + units (mix of `disponible`/`reservado`/`vendido`) + a payment plan with notasLegales, all
    for the publicado project; seed a floor/unit under the borrador project too. Drive assertions through
    the ANON caller (`createCaller({ headers: new Headers() })` → `caller.picker.*`):
    - getPublishedProject(slug of publicado) returns the row with the correct whatsapp; getPublishedProject(slug of borrador) returns no row (empty array / undefined).
    - listFloors(publicado projectId) returns the seeded floor(s); listFloors(borrador projectId) returns [].
    - listUnits(floorId of publicado) returns all seeded units INCLUDING non-disponible ones (the query does not filter estado); listUnits(floorId under borrador) returns [].
    - listPlans(publicado projectId) returns the plan with notasLegales populated and the preset fields present.
    Use exact equality on ids/whatsapp/notasLegales (no fuzzy matchers).
  </action>
  <verify>
    <automated>cd packages/api && pnpm test -- picker-router</automated>
  </verify>
  <acceptance_criteria>
    - The test seeds a publicado and a borrador project and asserts the anon caller sees ONLY publicado rows across all four procedures
    - getPublishedProject returns the whatsapp value for the publicado project
    - listUnits returns non-disponible units too (proves estado is not filtered server-side)
    - listPlans returns notasLegales and the preset fields
    - `pnpm --filter @imbau/api test -- picker-router` exits 0
  </acceptance_criteria>
  <done>An integration test proves the picker reads are anon-safe (publicado-only) and carry the fields the UI needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → picker router | Untrusted slug/uuid inputs cross here; RLS anon policies gate visibility |
| picker router → @imbau/db | Data access must stay on the withAnon path (fence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-INFO | Information disclosure | Enumerating unpublished floors/units/plans via the picker | high | mitigate | Every read routes through withAnon; the anon RLS policies (units_anon_published / floors_anon_published / payment_plans_anon_published) gate to publicado; integration test asserts borrador rows are invisible |
| T-06-03-ELEV | Elevation of privilege | picker accidentally importing the owner/app pool bypasses RLS | high | mitigate | Import fence (only withAnon/schema); grep-verified in Task 1 verify |
| T-06-03-TAMPER | Tampering | Malformed slug/uuid inputs | low | mitigate | Inputs validated as z.string()/z.uuid() at the tRPC boundary |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages in this plan; RESEARCH Package Legitimacy Audit covers phase packages (all approved) |
</threat_model>

<verification>
- picker.ts fence holds (only withAnon/schema; no owner/app-pool import) — grep clean
- No app-layer estado filter in picker queries — grep clean
- `pnpm --filter @imbau/api typecheck` and `pnpm --filter @imbau/api test -- picker-router` exit 0
- Anon caller sees publicado rows only across all four procedures
</verification>

<success_criteria>
The picker router serves the four anon published reads the buyer's picker needs (project+whatsapp,
floors, units, plans+notasLegales), fenced to the withAnon path and proven RLS-safe by integration test.
</success_criteria>

<output>
Create `.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-03-SUMMARY.md` when done.
</output>
