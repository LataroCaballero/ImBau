---
phase: 06-ui-p-blica-del-cotizador-cta-whatsapp
plan: 04
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - apps/web/lib/whatsapp.ts
  - apps/web/lib/whatsapp.test.ts
  - apps/web/lib/plan-snap.ts
  - apps/web/lib/plan-snap.test.ts
  - apps/web/components/quote-cards.tsx
  - apps/web/components/plan-slider.tsx
  - apps/web/components/quote-cards.test.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-04, UI-05, UI-06, WA-01]
tags: [frontend, presentational, whatsapp, quote-result, es-ar-format, slider]

must_haves:
  truths:
    - "The wa.me URL builder produces https://wa.me/<digits>?text=<url-encoded> from a QuoteResult + project number, digits-only, and returns null when the number is absent"
    - "The plan→slider-index map places the slider value on a preset planId index only (never a free term); a single-plan project degenerates to a fixed/disabled state"
    - "quote-cards renders precio USD, anticipo (USD + %), cuotas, primera cuota ARS, refuerzos and totals for a financiado result, and the precio for contado, formatted ONLY via formatUsd/formatArs"
    - "quote-cards shows the contado and financiado cards side by side (desktop) / stacked (mobile) with the compareQuotes savings band and the notasLegales + 'no vinculante' leyenda"
  artifacts:
    - "apps/web/lib/whatsapp.ts (buildWhatsappUrl) + test"
    - "apps/web/lib/plan-snap.ts (planToIndex/indexToPlan helpers) + test"
    - "apps/web/components/quote-cards.tsx (presentational) + render test"
    - "apps/web/components/plan-slider.tsx (native range snap-by-index)"
  key_links:
    - "quote-cards + whatsapp builder consume ONE QuoteResult via @imbau/quoting (formatUsd/formatArs/compareQuotes/toWhatsAppText) — never re-format or re-compute"
    - "plan-slider value is an index into the sorted payment_plans[]; onChange selects plans[i].id"
  prohibitions:
    - statement: "No Intl / toLocaleString / currency-style formatting is used in these components — only formatUsd/formatArs from @imbau/quoting (UI-06, Pitfall 3)"
      status: resolved
      verification: "grep shows no `toLocaleString` and no `Intl.NumberFormat` in quote-cards.tsx / whatsapp.ts"
    - statement: "The WhatsApp URL host is the fixed literal https://wa.me/ — only digits from the project number are interpolated (no open redirect)"
      status: resolved
      verification: "buildWhatsappUrl always starts the URL with the https://wa.me/ literal; the number is passed through a digits-only strip before interpolation"
    - statement: "The slider never emits a free anticipo/plazo value — only a preset planId (UI-04, Out of Scope free terms)"
      status: resolved
      verification: "plan-slider onChange maps an integer index to plans[index].id; there is no numeric anticipo/cuotas input bound to the API"
---

<objective>
Build the pure/presentational layer of the cotizador: the wa.me URL builder (WA-01), the
plan→slider-index snap map (UI-04), the quote-cards render surface (UI-02/UI-03/UI-05/UI-06) and the
snap-to-preset slider (UI-04) — all driven by the single `QuoteResult` and the deterministic
`@imbau/quoting` formatters, mockup-faithful (D-12) with the ImBau brand look (D-11).

Purpose: front-load every testable presentation/derivation unit so the interactive island (Plan 05)
just wires state → these leaves. Keeping money formatting and the WhatsApp text on the shared engine
functions is the single largest correctness guard of the phase (server==client, no drift).
Output: tested presentational components + pure helpers the simulator composes.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-CONTEXT.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-PATTERNS.md
@packages/quoting/src/index.ts
@packages/quoting/src/types.ts
</context>

## Artifacts this phase produces (Plan 04)

New/changed symbols introduced by this plan (excluded from drift verification downstream):
- `apps/web/lib/whatsapp.ts` exports: `buildWhatsappUrl`
- `apps/web/lib/plan-snap.ts` exports: `planToIndex`, `indexToPlan` (or equivalent snap helpers)
- `apps/web/components/quote-cards.tsx` exports: `QuoteCards`
- `apps/web/components/plan-slider.tsx` exports: `PlanSlider`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wa.me URL builder + plan-snap map (pure helpers, tested)</name>
  <files>apps/web/lib/whatsapp.ts, apps/web/lib/whatsapp.test.ts, apps/web/lib/plan-snap.ts, apps/web/lib/plan-snap.test.ts</files>
  <read_first>
    - packages/quoting/src/serialize.ts (toWhatsAppText — the body of the message; do NOT re-implement)
    - packages/quoting/src/types.ts (QuoteResult, PlanInput/payment plan shape, ContadoResult/FinanciadoResult)
    - 06-RESEARCH.md Pattern 5 (wa.me handoff) + Pattern 4 (snap-to-preset) + Security Domain (open-redirect note)
    - packages/api/src/trpc/routers/quotes.ts (input only accepts paymentPlanId — free terms are impossible)
  </read_first>
  <behavior>
    - buildWhatsappUrl({ whatsapp, unitIdentificador, projectNombre, result, deepLinkUrl }) returns `https://wa.me/<digits>?text=<encoded>` where digits is the number stripped to [0-9]
    - The encoded text = encodeURIComponent(`${header}\n${toWhatsAppText(result)}\n${deepLinkUrl}`) with header `Hola! Me interesa la unidad <id> de <proyecto>.`
    - buildWhatsappUrl returns null when whatsapp is null/empty (D-02 — no dead button)
    - The URL always begins with the literal `https://wa.me/` (no open redirect)
    - planToIndex(plans, planId) returns the index of the plan in the sorted array; indexToPlan(plans, index) returns plans[index]
    - For plans.length === 1, the snap helpers report a fixed/single-stop state (degenerate case, D-04)
  </behavior>
  <action>
    Create apps/web/lib/whatsapp.ts exporting `buildWhatsappUrl` per the behavior block: strip the
    project number to digits via a `[^\d]` replace, build the header + `toWhatsAppText(result)` (imported
    from @imbau/quoting — never inline the amounts) + the deepLinkUrl, `encodeURIComponent` the whole
    text, and return the fixed-host `https://wa.me/${digits}?text=${text}`. Return `null` when the number
    is absent so the caller hides the CTA (D-02). Create apps/web/lib/plan-snap.ts exporting the snap
    helpers: sort the payment_plans deterministically (by `anticipoPct` ascending, tie-break by `cuotas`),
    map a plan id ↔ its integer index, and expose an `isSingle(plans)` (or `stops` count) so the slider
    knows the degenerate case (D-04). Never expose a free anticipo/plazo value — the only output is a
    preset planId. Write the two `.test.ts` files asserting the full behavior block, including the
    null-number case and the single-plan degenerate case, with exact string equality on the built URL.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'https://wa.me/' lib/whatsapp.ts && grep -q 'toWhatsAppText' lib/whatsapp.ts && ! grep -q 'toLocaleString' lib/whatsapp.ts && pnpm test -- whatsapp plan-snap</automated>
  </verify>
  <acceptance_criteria>
    - buildWhatsappUrl returns a `https://wa.me/<digits>?text=<encoded>` URL for a valid number and null for an absent one
    - The message body comes from `toWhatsAppText(result)` (not re-implemented) and the header + deep link are appended and URL-encoded
    - whatsapp.ts contains no `toLocaleString`/`Intl`
    - plan-snap maps planId ↔ index over a deterministically sorted plans array and flags the single-plan case
    - `pnpm --filter @imbau/web test -- whatsapp plan-snap` exits 0
  </acceptance_criteria>
  <done>The wa.me builder and the snap map are pure, tested helpers driven by the engine's toWhatsAppText.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quote-cards presentational component (render + comparison + leyenda) with render test</name>
  <files>apps/web/components/quote-cards.tsx, apps/web/components/quote-cards.test.tsx</files>
  <read_first>
    - packages/quoting/src/format.ts (formatUsd → `US$ 1.234`, formatArs → `$ 1.234.560,00` — the ONLY formatters)
    - packages/quoting/src/compare.ts (compareQuotes → { ahorroUsd, ahorroPct }; sign convention)
    - packages/quoting/src/types.ts (ContadoResult/FinanciadoResult fields: precioUsd, anticipoUsd, saldoUsd, cuotas[], refuerzos[], totals, cac)
    - docs/mockup.html §s-cotizador (lines 541-573: quote-out grid, hl highlight card, cta-row, wa-preview) — the literal layout reference (D-12)
    - docs/marca/brand-book.md (JetBrains Mono for ALL financial figures; cobre accent; dark grafito)
    - apps/web/app/globals.css (the @theme tokens the utilities resolve — from Plan 02)
  </read_first>
  <behavior>
    - Given a financiado result, the card shows: precio USD, anticipo (USD + the % from the plan), the cuota count, the primera cuota ARS ("al valor del mes"), the refuerzos schedule, and the totals — every amount via formatUsd/formatArs
    - Given a contado result, the card shows the precio USD
    - Given both results, the two cards render side-by-side (desktop) / stacked (mobile) with a savings band from compareQuotes(ahorroUsd + ahorroPct)
    - The notasLegales (plan leyenda) and a "Cotización no vinculante" line are visible
    - A financiado result with cuotas[0].ars === null (fijo plan) renders the primera cuota as USD, not ARS
  </behavior>
  <action>
    Create apps/web/components/quote-cards.tsx exporting `QuoteCards`. Props include the contado
    QuoteResult, the financiado QuoteResult, the selected plan's `notasLegales`, and the plan's
    `anticipoPct`. Render two cards (Contado / Financiado) mirroring the mockup's `quote-out` grid and
    the `hl` highlighted total (D-12): for financiado show precioUsd, anticipoUsd + the `%` label,
    `cuotas.length` CUOTAS, the primera cuota (formatArs(cuotas[0].ars) when non-null else
    formatUsd(cuotas[0].usd)), the refuerzos lines, and totals.totalUsd; for contado show precioUsd.
    Below/between them render the compareQuotes savings band (ahorro USD + `ahorroPct%`). Render the
    `notasLegales` text and a literal "Cotización no vinculante" line (UI-05). Format EVERY amount with
    formatUsd/formatArs from @imbau/quoting — NEVER toLocaleString/Intl (UI-06, Pitfall 3). Apply the
    brand look with Tailwind utilities resolving the @theme tokens: JetBrains Mono (`font-mono`) on all
    figures, carbón surfaces on grafito, cobre accent on the highlighted total; stacked on mobile,
    two-column on desktop (a `sm:`/`md:` grid). Motion, if any, transform/opacity only with
    prefers-reduced-motion respected (D-13). Write quote-cards.test.tsx (jsdom + @testing-library/react)
    asserting the behavior block: financiado renders the expected formatUsd/formatArs strings, the
    savings band shows the compareQuotes figures, the "no vinculante" + notasLegales text is present,
    and the fijo-plan primera-cuota-as-USD case.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'formatUsd' components/quote-cards.tsx && grep -q 'formatArs' components/quote-cards.tsx && grep -q 'compareQuotes' components/quote-cards.tsx && ! grep -q 'toLocaleString' components/quote-cards.tsx && ! grep -q 'Intl.NumberFormat' components/quote-cards.tsx && grep -q 'no vinculante' components/quote-cards.tsx && pnpm test -- quote-cards</automated>
  </verify>
  <acceptance_criteria>
    - quote-cards.tsx formats every amount via formatUsd/formatArs and contains NO toLocaleString/Intl.NumberFormat
    - The financiado card shows precio, anticipo (USD + %), cuotas count, primera cuota ARS, refuerzos, and totals
    - The comparison band renders compareQuotes ahorroUsd + ahorroPct
    - The notasLegales and a "Cotización no vinculante" line are rendered (UI-05)
    - JetBrains Mono utility (`font-mono`) is applied to the financial figures
    - `pnpm --filter @imbau/web test -- quote-cards` exits 0
  </acceptance_criteria>
  <done>quote-cards renders the full result + contado/financiado comparison + leyendas from one QuoteResult with es-AR formatting, proven by a render test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: plan-slider snap-to-preset control</name>
  <files>apps/web/components/plan-slider.tsx</files>
  <read_first>
    - apps/web/lib/plan-snap.ts (the snap map from Task 1 — the source of index↔plan)
    - docs/mockup.html §s-cotizador (lines 560-565: pay-opts + cz-sliders markup — the visual reference)
    - packages/db/src/schema/payment-plans.ts (plan fields shown as the slider label: anticipoPct, cuotas)
    - 06-RESEARCH.md Pattern 4 + Assumption A1 (native range with step, Radix optional)
  </read_first>
  <action>
    Create apps/web/components/plan-slider.tsx exporting `PlanSlider`. Props: the sorted `plans`, the
    current `planId`, and an `onSelect(planId)` callback. Render a native `<input type="range">` with
    `min={0} max={plans.length - 1} step={1}` whose value is the current plan's index (via plan-snap
    Task 1). `onChange` maps the integer index back to `plans[index].id` and calls `onSelect` — it NEVER
    emits a free anticipo/plazo value (UI-04). Show a label with the selected plan's `anticipoPct`% and
    `cuotas` cuotas (mockup style). Degenerate case (D-04): when `plans.length < 2`, render the control
    `disabled` (fixed single stop) — never synthesize plans. Style with brand utilities (cobre thumb
    accent), motion transform/opacity only (D-13). Keep it a client component (`"use client"`).
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'type="range"' components/plan-slider.tsx && grep -Eq 'plans.length - 1|plans.length-1' components/plan-slider.tsx && grep -q 'disabled' components/plan-slider.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - plan-slider.tsx renders a native range with `step={1}` and max `plans.length - 1`
    - onChange maps an integer index to plans[index].id via the plan-snap helper and calls onSelect (no free numeric term bound)
    - When plans.length < 2 the control is disabled/fixed
    - The label shows the selected plan's anticipoPct% and cuotas
    - `pnpm --filter @imbau/web typecheck` exits 0
  </acceptance_criteria>
  <done>The slider snaps only to preset planIds and degenerates to a fixed state for a single plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → wa.me | The built URL navigates the browser off-site; only the fixed wa.me host + digits are trusted |
| QuoteResult → screen | The rendered amounts must match the server-computed result byte-for-byte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-REDIRECT | Tampering | Open redirect via the WhatsApp link | medium | mitigate | Host is the fixed literal `https://wa.me/`; only digits stripped from project.whatsapp are interpolated (Task 1) |
| T-06-04-DRIFT | Tampering/Info | On-screen amount drifts from server value / the WhatsApp text | high | mitigate | All amounts via formatUsd/formatArs; the message body via toWhatsAppText(result); never re-format/re-compute (Tasks 1-2, UI-06) |
| T-06-04-XSS | Tampering | Reflected content in the rendered cards/wa-preview | low | mitigate | React auto-escapes; no dangerouslySetInnerHTML; values are engine-formatted strings |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages beyond Plan 02's vetted set; RESEARCH Package Legitimacy Audit covers them |
</threat_model>

<verification>
- whatsapp.ts + quote-cards.tsx contain no `toLocaleString`/`Intl.NumberFormat` (grep clean)
- buildWhatsappUrl uses the fixed `https://wa.me/` host + digits-only number
- plan-slider snaps to integer indices only (no free anticipo/plazo)
- `pnpm --filter @imbau/web test` (whatsapp, plan-snap, quote-cards) and `typecheck` exit 0
</verification>

<success_criteria>
The presentational layer renders the full financiado/contado comparison + leyendas from one
QuoteResult with es-AR formatting, builds a safe pre-filled wa.me URL, and snaps the anticipo/plazo
control to preset plans only — all covered by unit/render tests.
</success_criteria>

<output>
Create `.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-04-SUMMARY.md` when done.
Record the exported prop shapes of QuoteCards / PlanSlider so Plan 05 wires them without guessing.
</output>
