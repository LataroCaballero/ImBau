---
phase: 06-ui-p-blica-del-cotizador-cta-whatsapp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/postcss.config.mjs
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
  - apps/web/lib/trpc-client.tsx
  - apps/web/vitest.config.ts
  - apps/web/playwright.config.ts
  - apps/web/tests/trpc-client-split.test.ts
  - packages/ui/src/tokens.css
  - packages/ui/src/index.tsx
autonomous: true
requirements: [UI-06]
tags: [frontend, tailwind, next-font, trpc, tanstack-query, design-system, test-infra]

must_haves:
  truths:
    - "apps/web renders the ImBau brand look: dark grafito background, Space Grotesk display, Inter UI, JetBrains Mono for figures, self-hosted via next/font (no runtime Google fetch)"
    - "apps/web has a browser tRPC client whose quotes.* procedures ONLY ever batch with each other so the HTTP path stays /api/trpc/quotes.* (nginx limit_req keeps throttling them)"
    - "apps/web can run unit tests (vitest) and e2e tests (Playwright) via its own scripts"
  artifacts:
    - "apps/web/app/globals.css: @import tailwindcss + @theme mapping the brand tokens"
    - "apps/web/postcss.config.mjs loading @tailwindcss/postcss"
    - "apps/web/lib/trpc-client.tsx: TRPCReactProvider + useTRPC with a dual splitLink"
    - "apps/web/app/layout.tsx: next/font variables + dark grafito body"
    - "apps/web/vitest.config.ts + apps/web/playwright.config.ts + a test:e2e script"
    - "packages/ui/src/tokens.css (design tokens, no longer placeholder)"
  key_links:
    - "splitLink condition op.path.startsWith('quotes.') → dedicated httpBatchLink keeps quotes on their own URL path (Pitfall 1 / nginx location ^~ /api/trpc/quotes)"
    - "next/font CSS variables (--font-space-grotesk / --font-inter / --font-jetbrains) wired into @theme so Tailwind font utilities resolve them"
  prohibitions:
    - statement: "No tailwind.config.js is created — configuration is CSS-first via @theme (D-10)"
      status: resolved
      verification: "no tailwind.config.* file exists under apps/web; globals.css contains @import \"tailwindcss\" and an @theme block"
    - statement: "The layout/app never links to Google Fonts over the network (D-13 budget)"
      status: resolved
      verification: "grep shows no fonts.googleapis.com or <link ... fonts in apps/web/app; fonts come from next/font/google (self-hosted at build)"
---

<objective>
Stand up the apps/web public-UI shell: all new dependencies + scripts, Tailwind v4 CSS-first with
the ImBau brand tokens, self-hosted brand fonts via next/font, the browser tRPC client with the
load-bearing dual `splitLink`, and the vitest + Playwright test infrastructure apps/web still lacks.

Purpose: every downstream cotizador plan renders into this shell. The dual splitLink is THE reason
this phase needs a client-wiring task — it keeps quotes requests on the `/api/trpc/quotes.*` path so
the nginx `limit_req` (QUOTE-03) actually throttles them. es-AR consistency (UI-06) is delivered here
by wiring the deterministic `formatUsd`/`formatArs` path and JetBrains Mono for all figures.
Output: a themed, testable web app with a rate-limit-safe tRPC client.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-CONTEXT.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-RESEARCH.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02)

New/changed symbols introduced by this plan (excluded from drift verification downstream):
- `apps/web/lib/trpc-client.tsx` exports: `TRPCProvider`, `useTRPC`, `TRPCReactProvider`
- `apps/web/app/globals.css` — new Tailwind v4 entry + `@theme` brand token mapping
- `apps/web/postcss.config.mjs` — new PostCSS config
- `apps/web/vitest.config.ts`, `apps/web/playwright.config.ts` — new test configs
- `packages/ui/src/tokens.css` — new tokens file; `packages/ui` exports it
- Brand CSS custom properties in `@theme`: `--color-grafito`, `--color-carbon`, `--color-hormigon`, `--color-cobre`, `--color-cobre-profundo`, `--color-blueprint`, `--color-disponible`, `--color-reservado`, `--color-vendido`, `--font-display`, `--font-text`, `--font-mono`
- new `apps/web/package.json` scripts: `test:e2e`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dependencies, Tailwind v4 CSS-first, brand tokens</name>
  <files>apps/web/package.json, apps/web/postcss.config.mjs, apps/web/app/globals.css, packages/ui/src/tokens.css, packages/ui/src/index.tsx</files>
  <read_first>
    - apps/web/package.json (current deps: @trpc/server@11.17.0, next@16.2.9, react@19.2.7 already present)
    - apps/web/next.config.ts (transpilePackages includes @imbau/ui; output standalone)
    - docs/marca/tokens.css (the design token source of truth — paleta, tipografía, radios, sombras)
    - docs/marca/brand-book.md (dark-first grafito, cobre accent never as background, motion 200-800ms)
    - packages/ui/package.json + packages/ui/src/index.tsx (placeholder today; peerDeps react 19.2.x)
    - 06-RESEARCH.md Pattern 6 (Tailwind v4 @import + @theme) and the Standard Stack install block
  </read_first>
  <action>
    Add to apps/web/package.json dependencies (exact pins, verified on npm 2026-07-04):
    `@trpc/client@11.17.0`, `@tanstack/react-query@5.101.0`, `@trpc/tanstack-react-query@11.17.0`.
    Add to devDependencies: `tailwindcss@4.3.2`, `@tailwindcss/postcss@4.3.2`, `@playwright/test@1.60.0`,
    `vitest@4.1.8`, `jsdom@26.1.0`, `@testing-library/react@16.3.0`, `@testing-library/jest-dom@6.9.0`
    (use the latest patch of each testing lib compatible with react 19 / vitest 4 — verify on npm at
    install time; pin exact versions, no `^`). Add ALL new apps/web deps here in ONE task so no other
    plan touches package.json. Add a `"test:e2e": "playwright test"` script. Run
    `pnpm install` from the repo root after editing.
    Create apps/web/postcss.config.mjs exporting a default object whose `plugins` is
    `{ "@tailwindcss/postcss": {} }` (the single v4 plugin — do NOT add autoprefixer/postcss-import;
    v4 folds them in). Do NOT create any tailwind.config.js (CSS-first per D-10).
    Create apps/web/app/globals.css: first line `@import "tailwindcss";` then an `@theme { ... }`
    block mapping the brand tokens (RESEARCH Pattern 6): `--color-grafito:#14181E`,
    `--color-carbon:#1D232C`, `--color-hormigon:#F2EFE9`, `--color-cobre:#D98A4F`,
    `--color-cobre-profundo:#C0703A`, `--color-blueprint:#4C7DF0`, `--color-disponible:#2FA26E`,
    `--color-reservado:#E3A63C`, `--color-vendido:#D65454`, and font vars
    `--font-display: var(--font-space-grotesk), system-ui, sans-serif`,
    `--font-text: var(--font-inter), system-ui, sans-serif`,
    `--font-mono: var(--font-jetbrains), ui-monospace, monospace`. Values come from docs/marca/tokens.css.
    Copy docs/marca/tokens.css into packages/ui/src/tokens.css (D-10 — packages/ui stops being a
    placeholder) and export it from packages/ui via a `./tokens.css` entry in packages/ui/package.json
    exports (or re-export path); keep the existing AppStatus export intact in index.tsx. To AVOID the
    Tailwind v4 purge pitfall (Pitfall 5), keep all cotizador COMPONENTS inside apps/web (later plans);
    globals.css is self-contained (@theme inline), so no `@source` into packages/ui is required.
  </action>
  <verify>
    <automated>cd apps/web && grep -q '@trpc/client' package.json && grep -q '@tanstack/react-query' package.json && grep -q '@tailwindcss/postcss' package.json && grep -q '"test:e2e"' package.json && test -f postcss.config.mjs && grep -q '@import "tailwindcss"' app/globals.css && grep -q '@theme' app/globals.css && grep -q -- '--color-grafito' app/globals.css && ! ls tailwind.config.* 2>/dev/null && test -f ../../packages/ui/src/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies include @trpc/client, @tanstack/react-query, @trpc/tanstack-react-query at the pinned versions
    - apps/web/package.json devDependencies include tailwindcss, @tailwindcss/postcss, @playwright/test, vitest, jsdom, @testing-library/react
    - apps/web/package.json has a `test:e2e` script
    - apps/web/postcss.config.mjs exists with the single `@tailwindcss/postcss` plugin
    - apps/web/app/globals.css starts with `@import "tailwindcss";` and has an `@theme` block containing `--color-grafito` and the three `--font-*` variables
    - NO `apps/web/tailwind.config.*` file exists
    - packages/ui/src/tokens.css exists (copied tokens); AppStatus export still present in index.tsx
    - `pnpm install` completes and the lockfile updates
  </acceptance_criteria>
  <done>apps/web has all cotizador deps + scripts, Tailwind v4 CSS-first with brand tokens, and packages/ui carries the tokens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Brand fonts in layout + dual-splitLink tRPC browser client</name>
  <files>apps/web/app/layout.tsx, apps/web/lib/trpc-client.tsx</files>
  <read_first>
    - apps/web/app/layout.tsx (current minimal layout, keep lang="es-AR")
    - apps/panel/lib/trpc-client.tsx (the client to CLONE — createTRPCContext, getBaseUrl, TRPCReactProvider, QueryClient)
    - apps/panel/app/(dashboard)/page.tsx (how the provider wraps an island — the web page follows this, not the layout)
    - packages/api/src/trpc/routers/_app.ts (AppRouter type imported by the client)
    - deploy/nginx/staging.tours.andescode.com.ar.conf (the `location ^~ /api/trpc/quotes` this split protects)
    - 06-RESEARCH.md Pattern 1 (dual splitLink) + Pattern 7 (next/font) + Pitfall 1
  </read_first>
  <action>
    layout.tsx: import `Space_Grotesk`, `Inter`, `JetBrains_Mono` from `next/font/google`, each with
    `subsets:["latin"]`, `display:"swap"` and a `variable` (`--font-space-grotesk`, `--font-inter`,
    `--font-jetbrains`). `import "./globals.css"`. Apply the three font `.variable` classNames on
    `<html>` (keep `lang="es-AR"`) and set the dark grafito look on `<body>` via Tailwind utilities
    that resolve the `@theme` tokens (e.g. a `bg-grafito text-hormigon font-text` body). Do NOT add a
    `<link>` to Google Fonts (self-hosted at build — D-13). Do NOT mount the tRPC provider in the
    layout (the cotizador page mounts it around its island, mirroring panel).
    trpc-client.tsx: clone apps/panel/lib/trpc-client.tsx verbatim (the `"use client"`,
    `createTRPCContext<AppRouter>()` → export `{ TRPCProvider, useTRPC }`, `getBaseUrl()`,
    `TRPCReactProvider` with `useState(() => new QueryClient())`). CHANGE ONLY the links array:
    import `splitLink` alongside `httpBatchLink` from `@trpc/client`, and make `links` a single
    `splitLink` whose `condition` is `(op) => op.path.startsWith("quotes.")`, with `true` = a
    dedicated `httpBatchLink({ url })` and `false` = a default `httpBatchLink({ url })`, both pointing
    at `${getBaseUrl()}/api/trpc`. This is the load-bearing decision (Pitfall 1): it keeps every
    quotes request path prefixed `/api/trpc/quotes.*` so it never escapes the nginx location by
    co-batching with a picker read.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'next/font/google' app/layout.tsx && grep -q 'Space_Grotesk' app/layout.tsx && grep -q 'JetBrains_Mono' app/layout.tsx && ! grep -qi 'fonts.googleapis' app/layout.tsx && grep -q 'splitLink' lib/trpc-client.tsx && grep -q 'op.path.startsWith("quotes.")' lib/trpc-client.tsx && grep -q 'createTRPCContext' lib/trpc-client.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports Space_Grotesk, Inter, JetBrains_Mono from next/font/google with `variable` + `display:"swap"`
    - layout.tsx applies the font variables on `<html>` and a dark grafito body; keeps `lang="es-AR"`
    - layout.tsx has NO reference to fonts.googleapis.com or a font `<link>`
    - lib/trpc-client.tsx exports `TRPCProvider`, `useTRPC`, `TRPCReactProvider`
    - lib/trpc-client.tsx uses a single `splitLink` with `condition: (op) => op.path.startsWith("quotes.")` and two httpBatchLinks
    - `pnpm --filter @imbau/web typecheck` exits 0
  </acceptance_criteria>
  <done>The web layout self-hosts the brand fonts on a dark grafito body, and the browser tRPC client keeps quotes on their own batch path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: apps/web test infrastructure (vitest + Playwright) with a splitLink guard test</name>
  <files>apps/web/vitest.config.ts, apps/web/playwright.config.ts, apps/web/tests/trpc-client-split.test.ts</files>
  <read_first>
    - apps/panel/vitest.config.ts (self-contained config excluding e2e/** — the pattern to clone)
    - apps/panel/playwright.config.ts (webServer, PORT reassignment to avoid CLINICAL collision, DATABASE_* env defaults)
    - MEMORY clinical-project-ports (CLINICAL owns :3000/:3001 — web must reassign its e2e port)
    - apps/web/lib/trpc-client.tsx (the split condition under test — from Task 2)
  </read_first>
  <behavior>
    - Test: the splitLink `condition` predicate returns true for an op path `quotes.compute` and `quotes.create`
    - Test: the split condition returns false for `picker.listUnits` and `projects.listPublished`
    - (Guards Pitfall 1: only quotes.* procedures take the dedicated batch link)
  </behavior>
  <action>
    Create apps/web/vitest.config.ts cloning apps/panel/vitest.config.ts: self-contained
    (`defineConfig` from `vitest/config`, do NOT import the repo-root vitest.config — it is pruned by
    turbo prune), `test.exclude` widened with `"e2e/**"`, and set `test.environment` to `"jsdom"` so
    component/render assertions work. Create apps/web/playwright.config.ts cloning
    apps/panel/playwright.config.ts but with `testDir: "./e2e"`, a REASSIGNED port (use
    `WEB_E2E_PORT ?? 3110` — NOT 3000/3001 which CLINICAL owns), `webServer.command` building+starting
    web (`pnpm run build && pnpm exec next start -p <port>`), and env defaulting DATABASE_URL /
    DATABASE_APP_URL / DATABASE_ANON_URL / NEXT_PUBLIC_APP_ENV=development / NODE_ENV=production to the
    Compose dev DB (mirror panel). Extract the split condition into a small exported predicate in
    lib/trpc-client.tsx (e.g. `export const isQuotesOp = (path: string) => path.startsWith("quotes.")`)
    and use it inside the splitLink `condition`, so the unit test can import and assert it without
    booting a client. Write apps/web/tests/trpc-client-split.test.ts asserting the behavior block above.
  </action>
  <verify>
    <automated>cd apps/web && test -f vitest.config.ts && test -f playwright.config.ts && grep -q 'e2e/\*\*' vitest.config.ts && grep -Eq '3110|WEB_E2E_PORT' playwright.config.ts && ! grep -Eq '3000|3001' playwright.config.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/vitest.config.ts is self-contained, excludes `e2e/**`, and sets the jsdom environment
    - apps/web/playwright.config.ts uses testDir ./e2e and a port that is NOT 3000/3001
    - lib/trpc-client.tsx exports the split predicate used by the splitLink condition
    - apps/web/tests/trpc-client-split.test.ts asserts true for quotes.* and false for picker.*/projects.*
    - `pnpm --filter @imbau/web test` runs and passes (the split-predicate test is green)
  </acceptance_criteria>
  <done>apps/web has vitest + Playwright configured, and a unit test proves only quotes.* takes the dedicated batch link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/trpc | The tRPC browser client crosses into the server; the batch URL path decides whether nginx limit_req sees it |
| build → npm registry | New dependencies are installed at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-DOS | Denial of service | quotes.compute co-batched with picker reads escapes nginx rate limit | high | mitigate | Dual splitLink keeps quotes on the /api/trpc/quotes.* path (Task 2); unit test guards the predicate (Task 3); live 429 re-check happens in the e2e plan |
| T-06-02-PERF | Denial of service (self-inflicted) | render-blocking Google Fonts busts the <3s-4G budget | medium | mitigate | next/font self-hosts at build; no network font link (Task 2, D-13) |
| T-06-SC | Tampering | npm installs (tailwind, trpc client, tanstack, playwright, vitest, testing-library) | high | mitigate | RESEARCH Package Legitimacy Audit ran on all new packages — all approved (SUS `too-new` are release-recency false positives on official high-volume packages; no SLOP, no postinstall). No blocking-human checkpoint required. |
</threat_model>

<verification>
- `apps/web/app/globals.css` has `@import "tailwindcss"` + `@theme` with brand tokens; no tailwind.config.* exists
- `apps/web/lib/trpc-client.tsx` uses `splitLink` gated on `quotes.` and exports the predicate
- `apps/web/app/layout.tsx` self-hosts the three brand fonts via next/font; no Google Fonts link
- `pnpm --filter @imbau/web typecheck` and `pnpm --filter @imbau/web test` exit 0
</verification>

<success_criteria>
The web app boots with the ImBau dark-grafito brand look and self-hosted fonts, has a browser tRPC
client whose quotes procedures always batch alone (rate-limit-safe), and can run both vitest and
Playwright — proven by a green split-predicate unit test.
</success_criteria>

<output>
Create `.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-02-SUMMARY.md` when done.
Record the final pinned versions of the testing libraries chosen at install time.
</output>
