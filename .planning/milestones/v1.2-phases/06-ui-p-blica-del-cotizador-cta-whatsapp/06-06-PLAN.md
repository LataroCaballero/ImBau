---
phase: 06-ui-p-blica-del-cotizador-cta-whatsapp
plan: 06
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - apps/web/e2e/cotizador.spec.ts
  - apps/web/e2e/picker.spec.ts
  - apps/web/e2e/whatsapp-cta.spec.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, WA-01]
tags: [e2e, playwright, verification, cotizador]

must_haves:
  truths:
    - "An e2e run drives the piso→unidad picker and reaches the result view (UI-01)"
    - "A deep-link /p/[slug]/cotizador?u=&plan= renders the result view directly, no picker (UI-01)"
    - "The result view shows precio USD, anticipo (USD + %), cuotas, primera cuota ARS, refuerzos, totals and the contado-vs-financiado comparison (UI-02/UI-03)"
    - "Adjusting the preset slider updates the result; the leyenda (notasLegales + 'no vinculante') is visible (UI-04/UI-05)"
    - "Amounts render es-AR (US$ / $) as produced by formatUsd/formatArs (UI-06)"
    - "The 'Consultar por WhatsApp' CTA navigates to a wa.me URL with an encoded text param (WA-01)"
  artifacts:
    - "apps/web/e2e/picker.spec.ts, cotizador.spec.ts, whatsapp-cta.spec.ts"
  key_links:
    - "e2e runs against the seeded Brigos Recoleta project (pnpm db:seed) via the Playwright webServer + Compose dev DB"
    - "the WhatsApp CTA is asserted by inspecting the wa.me href / navigation target (no real WhatsApp needed)"
  prohibitions:
    - statement: "e2e does not assert money strings via toLocaleString/Intl — it asserts the exact formatUsd/formatArs output (US$ / $ es-AR)"
      status: resolved
      verification: "spec assertions match the `US$ ` / `$ ` literal-labelled es-AR strings, not Intl currency output"
---

<objective>
Prove the demo-critical cotizador path end to end with Playwright against the seeded Brigos Recoleta
project: picker flow, deep-link, full result render + contado/financiado comparison, preset slider
adjust, leyenda visibility, es-AR amounts, and the WhatsApp CTA target — closing the phase's Nyquist
gaps (RESEARCH Validation Architecture: apps/web e2e specs).

Purpose: the per-component unit tests (Plans 03-04) cover the leaves; this plan verifies the wired
flow a buyer actually experiences and the QUOTE-03 429 tolerance in the browser.
Output: green e2e specs covering UI-01..06 + WA-01.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-CONTEXT.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 06)

New/changed symbols introduced by this plan (excluded from drift verification downstream):
- `apps/web/e2e/picker.spec.ts`, `apps/web/e2e/cotizador.spec.ts`, `apps/web/e2e/whatsapp-cta.spec.ts` — new Playwright specs

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Picker + deep-link e2e (UI-01)</name>
  <files>apps/web/e2e/picker.spec.ts</files>
  <read_first>
    - apps/web/playwright.config.ts (webServer command, port, DATABASE_* env — from Plan 02)
    - apps/panel/e2e (existing Playwright spec style: page.goto, locators, expect)
    - packages/db/src/seed/content.ts + packages/db/seed.ts (the seeded Brigos Recoleta slug + a disponible unit's identificador to target)
    - 06-RESEARCH.md Validation Architecture (Req → Test map) + Environment Availability (seed via pnpm db:seed)
  </read_first>
  <action>
    Create apps/web/e2e/picker.spec.ts. Precondition: the seeded Brigos Recoleta project must exist
    (document that `pnpm db:seed` is run against the Compose dev DB before the suite, or add a
    globalSetup that seeds; Claude's discretion — mirror how the panel e2e obtains its data). Spec 1
    (picker): `page.goto('/p/<slug>/cotizador')` with NO params → assert the floor step renders, pick a
    floor, assert its units render with status badges, assert a `vendido`/`reservado` unit is not
    clickable and a `disponible` unit is, click a disponible unit → assert the result view appears and
    the URL now carries `?u=`. Spec 2 (deep-link): `page.goto('/p/<slug>/cotizador?u=<unitId>&plan=<planId>')`
    → assert the result view renders directly with NO picker step. Use the real seeded slug/unit ids
    (resolve them in setup or via a known-seed helper). es-AR text assertions.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'cotizador' e2e/picker.spec.ts && pnpm test:e2e -- picker</automated>
  </verify>
  <acceptance_criteria>
    - No-params visit opens the floor→unit picker; a disponible unit is selectable, reservado/vendido are not
    - Selecting a unit reveals the result view and updates the URL with `?u=`
    - A `?u=&plan=` deep-link renders the result view directly without the picker
    - `pnpm --filter @imbau/web test:e2e -- picker` passes
  </acceptance_criteria>
  <done>The picker flow and the shareable deep-link both reach the result view (UI-01).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Result render + comparison + leyenda + es-AR + slider e2e (UI-02..06)</name>
  <files>apps/web/e2e/cotizador.spec.ts</files>
  <read_first>
    - packages/quoting/src/format.ts (the exact `US$ ` / `$ ` es-AR output the spec asserts)
    - apps/web/components/quote-cards.tsx (the rendered labels/structure to target — from Plan 04)
    - apps/web/components/plan-slider.tsx (the range control to drive — from Plan 04)
    - apps/web/components/cotizador-simulator.tsx (the live-recompute + 429 handling — from Plan 05)
    - 06-RESEARCH.md Pitfall 4 (429 tolerance) + Req → Test map
  </read_first>
  <action>
    Create apps/web/e2e/cotizador.spec.ts driving a deep-linked result view. Assert (UI-02) the
    financiado card shows precio USD, anticipo with a `%`, cuotas count, a primera cuota in ARS, refuerzos
    and totals; (UI-03) both a Contado and a Financiado card are visible side by side with a savings band;
    (UI-06) amounts match the formatUsd/formatArs shape — assert a `US$ ` prefixed value and a `$ ` ARS
    value with es-AR grouping (dot thousands / comma decimals), NOT Intl currency output; (UI-04) moving
    the preset slider to another stop changes the displayed cuota/anticipo (and the URL `?plan=`), and a
    single-plan project would leave it disabled; (UI-05) the notasLegales text and a "no vinculante" line
    are visible. 429 tolerance (Pitfall 4, optional but recommended): rapidly drive the slider and assert
    the UI never shows a raw error/stack (a soft es-AR message at most). Keep assertions on the seeded
    Brigos Recoleta numbers where exact values are known.
  </action>
  <verify>
    <automated>cd apps/web && grep -Eq 'US\\$|no vinculante' e2e/cotizador.spec.ts && pnpm test:e2e -- cotizador</automated>
  </verify>
  <acceptance_criteria>
    - The result view shows precio/anticipo(%)/cuotas/primera cuota ARS/refuerzos/totals (UI-02)
    - Contado and Financiado cards render together with a compareQuotes savings band (UI-03)
    - Amounts assert the `US$ ` / `$ ` es-AR formatter output, not Intl currency style (UI-06)
    - Moving the preset slider changes the result and the `?plan=` param (UI-04)
    - notasLegales + "no vinculante" leyenda is visible (UI-05)
    - `pnpm --filter @imbau/web test:e2e -- cotizador` passes
  </acceptance_criteria>
  <done>The result view, comparison, slider adjust, leyenda and es-AR formatting are verified end to end (UI-02..06).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: WhatsApp CTA e2e (WA-01) + phase gate</name>
  <files>apps/web/e2e/whatsapp-cta.spec.ts</files>
  <read_first>
    - apps/web/components/cotizador-simulator.tsx (the CTA handler + create.mutate + window.location.href — from Plan 05)
    - apps/web/lib/whatsapp.ts (buildWhatsappUrl output shape — from Plan 04)
    - packages/db/seed.ts (the seeded project whatsapp number — from Plan 01)
    - 06-RESEARCH.md Pattern 5 + Security Domain (open-redirect note)
  </read_first>
  <action>
    Create apps/web/e2e/whatsapp-cta.spec.ts on a deep-linked result view. Intercept/assert the CTA
    navigation target instead of really opening WhatsApp: prevent the top-level navigation (e.g. capture
    the `window.location.href` set, or a `route`/`popup`/navigation listener — Playwright pattern of
    choice) and assert the target is `https://wa.me/<digits>?text=<encoded>` where the digits match the
    seeded project number and the decoded text contains the unit + proyecto header and the amounts from
    toWhatsAppText. Assert the CTA is PRESENT for the seeded project (it has a number). Assert the PDF
    button is visible but disabled ("Próximamente"). Then run the whole apps/web suite as the phase gate
    (`pnpm --filter @imbau/web test` + `pnpm --filter @imbau/web test:e2e`) and confirm green. Document
    in the SUMMARY that the live staging UAT of QUOTE-03 429 depends on PR #1 → main landing the fase-5
    quotes route (STATE.md) and note the deploy ordering.
  </action>
  <verify>
    <automated>cd apps/web && grep -q 'wa.me' e2e/whatsapp-cta.spec.ts && pnpm test:e2e -- whatsapp-cta && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - The CTA navigation target is a `https://wa.me/<digits>?text=` URL whose digits match the seeded number and whose text decodes to the unit/proyecto header + toWhatsAppText amounts (WA-01)
    - The CTA is present for the seeded (numbered) project; the PDF button renders disabled
    - `pnpm --filter @imbau/web test:e2e` (all specs) and `pnpm --filter @imbau/web test` pass
  </acceptance_criteria>
  <done>The WhatsApp CTA opens a correctly pre-filled wa.me URL and the full apps/web test suite is green (phase gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e browser → app | The e2e run exercises the untrusted-client path against the seeded DB |
| CTA → wa.me | The spec asserts the off-site navigation target is the fixed wa.me host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-REDIRECT | Tampering | Open redirect via the WhatsApp link | medium | mitigate | The spec asserts the target begins with the fixed `https://wa.me/` host and only seeded digits appear |
| T-06-06-DOS | Denial of service | 429 under burst not tolerated | high | accept (verify) | The slider-burst assertion confirms the UI degrades softly; the authoritative live 429 re-check is the staging UAT gated on PR #1 → main (STATE.md) |
| T-06-SC | Tampering | npm installs | high | mitigate | No new packages; RESEARCH Package Legitimacy Audit covers @playwright/test (approved) |
</threat_model>

<verification>
- `pnpm --filter @imbau/web test:e2e` runs picker + cotizador + whatsapp-cta specs green
- `pnpm --filter @imbau/web test` (unit) green
- CTA target asserted as a fixed-host wa.me URL with the seeded number + encoded summary
- es-AR amount assertions match formatUsd/formatArs output (not Intl)
</verification>

<success_criteria>
The full buyer flow — picker/deep-link → live contado-vs-financiado result → preset slider → leyenda →
WhatsApp CTA — is verified end to end against the seeded project, covering UI-01..06 + WA-01, with the
apps/web suite green as the phase gate.
</success_criteria>

<output>
Create `.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-06-SUMMARY.md` when done.
Record the staging UAT dependency (PR #1 → main) and how the seed is provisioned for the e2e run.
</output>
