---
phase: 06-ui-p-blica-del-cotizador-cta-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/projects.ts
  - packages/db/migrations/0004_project_whatsapp.sql
  - packages/db/migrations/meta/_journal.json
  - packages/db/seed.ts
autonomous: true
requirements: [WA-01]
tags: [database, drizzle, migration, seed, whatsapp]

must_haves:
  truths:
    - "projects table has a nullable whatsapp text column readable by the anon role with no new policy"
    - "The seeded Brigos Recoleta project row carries a WhatsApp number so demo/staging always render the CTA"
    - "Re-running pnpm db:seed does not duplicate rows (idempotent, onConflictDoNothing preserved)"
  artifacts:
    - "packages/db/src/schema/projects.ts with a whatsapp text column"
    - "packages/db/migrations/0004_project_whatsapp.sql (drizzle-kit generated, additive ALTER)"
    - "packages/db/seed.ts project insert carrying whatsapp"
  key_links:
    - "projects_anon_published (table-level SELECT to anonRole) already exposes the new column to anon — no policy change"
    - "drizzle-kit generate → migrate is the ONLY path that alters the schema (versioned migrations, never hand-edit)"
  prohibitions:
    - statement: "No new pgPolicy is added to the projects table for the whatsapp column"
      status: resolved
      verification: "grep shows exactly two pgPolicy( declarations in projects.ts (projects_tenant, projects_anon_published) — unchanged count"
    - statement: "The migration SQL is not hand-edited — it is drizzle-kit generated"
      status: resolved
      verification: "0004 migration body is a single additive ALTER TABLE ... ADD COLUMN, matching drizzle-kit output; meta/_journal.json updated by the tool"
---

<objective>
Add the `whatsapp` source-of-truth column to `projects` (D-01) so the WhatsApp CTA (WA-01) has a
per-project default number, and seed "Brigos Recoleta" with a number so demo/staging always render
the CTA (D-02 — no dead buttons).

Purpose: WA-01 needs a project-scoped number. The broker routing of the maestro phase 5 overrides
it later — this column is exactly the "slot listo para routing" of WA-01.
Output: nullable `whatsapp` column + versioned migration `0004_project_whatsapp.sql` + seeded number.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-CONTEXT.md
@.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01)

New/changed symbols introduced by this plan (excluded from drift verification downstream):
- `projects.whatsapp` — new nullable `text("whatsapp")` column in `packages/db/src/schema/projects.ts`
- `packages/db/migrations/0004_project_whatsapp.sql` — new additive migration file
- seed value `whatsapp: "+5491155551234"` on the Brigos Recoleta project insert in `packages/db/seed.ts`

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Add projects.whatsapp column, generate migration, apply it</name>
  <files>packages/db/src/schema/projects.ts, packages/db/migrations/0004_project_whatsapp.sql, packages/db/migrations/meta/_journal.json</files>
  <read_first>
    - packages/db/src/schema/projects.ts (current schema + the two existing pgPolicy declarations; `text` is already imported line 7)
    - packages/db/src/schema/brokers.ts (has its OWN whatsapp column — DO NOT confuse the two tables)
    - packages/db/drizzle.config.ts (schema globs, entities.roles:true, DATABASE_URL is the OWNER connection)
    - packages/db/migrations/0003_rls_domain.sql (latest existing migration — the new one is 0004)
    - CLAUDE.md ("Migraciones de DB versionadas (Drizzle), nunca cambios manuales al schema")
  </read_first>
  <action>
    Add a single nullable column to the `projects` table columns object in projects.ts, after
    `estado` (line 28): `whatsapp: text("whatsapp")` (nullable — no `.notNull()`). `text` is already
    imported. Do NOT add, remove, or edit any `pgPolicy` — the existing `projects_anon_published`
    (table-level SELECT to `anonRole`) already exposes the new column to anon (D-01 / RESEARCH
    Runtime State Inventory); adding a policy would be wrong. Keep the column comment about it being
    the WA-01 slot that broker routing overrides later.
    Then generate the versioned migration with `pnpm --filter @imbau/db db:generate` (drizzle-kit
    generate). Expected emitted body in 0004: a single `ALTER TABLE "projects" ADD COLUMN "whatsapp" text;`
    (additive, nullable, no backfill) plus the `meta/_journal.json` update. NEVER hand-edit the SQL.
    Rename the generated file to `0004_project_whatsapp.sql` only if drizzle-kit's random suffix name
    differs AND update `meta/_journal.json` accordingly — otherwise keep the tool's name and record it
    in the SUMMARY. This is the [BLOCKING] step: apply it with `pnpm --filter @imbau/db db:migrate`
    against the dev database (Docker Postgres up; `DATABASE_URL` owner connection set — see MEMORY
    local-dev recipe). Build + typecheck alone pass WITHOUT the applied migration (types come from
    the schema file, not the live DB) — the migrate step is mandatory or verification is a false
    positive.
  </action>
  <verify>
    <automated>cd packages/db && grep -c 'pgPolicy(' src/schema/projects.ts | grep -qx 2 && grep -q 'whatsapp: text("whatsapp")' src/schema/projects.ts && ls migrations/0004_*.sql && grep -qi 'add column "whatsapp"' migrations/0004_*.sql && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - projects.ts contains `whatsapp: text("whatsapp")` and NO `.notNull()` on it
    - projects.ts still has EXACTLY two `pgPolicy(` declarations (projects_tenant, projects_anon_published) — unchanged
    - A migration file `migrations/0004_*.sql` exists whose body is an additive `ALTER TABLE "projects" ADD COLUMN "whatsapp" text`
    - migrations/meta/_journal.json lists the 0004 entry
    - `pnpm --filter @imbau/db db:migrate` applies cleanly against the dev DB (column present in live `projects`)
    - `pnpm --filter @imbau/db typecheck` exits 0
  </acceptance_criteria>
  <done>projects has a nullable whatsapp column, a drizzle-kit-generated 0004 migration exists and is applied to the dev DB, and no RLS policy was added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Seed Brigos Recoleta with a WhatsApp number (idempotent)</name>
  <files>packages/db/seed.ts</files>
  <read_first>
    - packages/db/seed.ts (the project insert at lines 100-113 — `.insert(schema.projects).values({...}).onConflictDoNothing()`)
    - packages/db/src/seed/content.ts (BUILDING.project — nombre/slug source; broker whatsapp values live here too, do not reuse the broker field)
    - packages/db/src/schema/projects.ts (the new whatsapp column from Task 1)
  </read_first>
  <action>
    In seed.ts, add `whatsapp: "+5491155551234"` to the project `.values({...})` object (after
    `estado: "publicado"`, before the closing brace), so demo/staging always render the CTA (D-01/D-02).
    Use a digits+`+` string like the broker seed style. Do NOT change the `.onConflictDoNothing()` —
    the seed must stay idempotent (safe re-run, no duplicate rows). Prefer sourcing the literal from
    `BUILDING.project` if a whatsapp field is added there; otherwise inline the number in seed.ts with
    a comment referencing D-01. Then run the seed twice to prove idempotency: `pnpm db:seed` (needs
    Docker Postgres/Redis up + the DATABASE_URL/DATABASE_APP_URL/DATABASE_ANON_URL/R2_*/REDIS_URL envs;
    it fail-fasts naming anything missing).
  </action>
  <verify>
    <automated>cd packages/db && grep -q 'whatsapp: "+549' seed.ts && grep -q 'onConflictDoNothing' seed.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - seed.ts project `.values({...})` includes `whatsapp: "+549..."` (a non-null digits string)
    - The project insert still ends with `.onConflictDoNothing()` (idempotency preserved)
    - Running `pnpm db:seed` twice leaves exactly one Brigos Recoleta project row with the whatsapp value set (no duplicates)
    - `pnpm --filter @imbau/db typecheck` exits 0
  </acceptance_criteria>
  <done>The seeded project carries a WhatsApp number and re-running the seed is a no-op (idempotent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon role → projects | The public anon role reads the new whatsapp column via the existing table-level SELECT policy |
| migration tool → DB schema | drizzle-kit generate/migrate is the only sanctioned DDL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-INFO | Information disclosure | projects.whatsapp exposed to anon | low | accept | The number is intended to be public (it is the CTA target); anon SELECT on projects is already published-only (projects_anon_published), so only publicado projects expose it |
| T-06-01-TAMPER | Tampering | hand-edited migration SQL drifting from schema | medium | mitigate | Migration is drizzle-kit generated (not hand-authored); verify greps for the additive ALTER only; CLAUDE.md versioned-migrations rule enforced |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in this plan (drizzle-kit/tsx already present); RESEARCH Package Legitimacy Audit covers all phase packages (all approved, no SLOP) |
</threat_model>

<verification>
- `grep -c 'pgPolicy(' packages/db/src/schema/projects.ts` returns 2 (no policy added)
- `packages/db/migrations/0004_*.sql` exists and is an additive ADD COLUMN
- `pnpm --filter @imbau/db db:migrate` applies cleanly; live `projects` has the whatsapp column
- `pnpm db:seed` run twice → one project row, whatsapp set, no duplicates
- `pnpm --filter @imbau/db typecheck` exits 0
</verification>

<success_criteria>
projects.whatsapp exists as a nullable column (no new policy), the 0004 migration is generated and
applied, and the seeded Brigos Recoleta project carries a WhatsApp number that survives idempotent
re-seeding.
</success_criteria>

<output>
Create `.planning/phases/06-ui-p-blica-del-cotizador-cta-whatsapp/06-01-SUMMARY.md` when done.
Record the exact generated migration filename and whether it was renamed.
</output>
