---
phase: 05-emisi-n-y-persistencia-server-side-api-rls-rate-limit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/storage/src/quote-pdf.ts
  - packages/storage/src/keys.ts
  - packages/storage/src/index.ts
autonomous: true
requirements: [QUOTE-02]
must_haves:
  truths:
    - "packages/storage exports the PDF pipeline contract — QUOTE_PDF_QUEUE, QuotePdfJobData, quotePdfJobOptions, quotePdfKey — so the fase-7 producer and consumer share one source of truth (D-13)."
    - "@imbau/storage still has NO bullmq dependency: the contract is a const + interface + pure helpers, mirroring the MEDIA_QUEUE molecule."
    - "quotes.create does NOT enqueue anything this phase — no producer exists (D-13: contrato sí, producer no)."
  artifacts:
    - packages/storage/src/quote-pdf.ts
    - packages/storage/src/keys.ts
    - packages/storage/src/index.ts
  key_links:
    - "index.ts barrel re-exports the new contract (runtime via export, types via export type — verbatimModuleSyntax)"
    - "quotePdfJobOptions(quoteId) sets jobId = quoteId so a fase-7 BullMQ retry/re-enqueue dedups (idempotencia por quoteId, PDF-02 groundwork)"
    - "quotePdfKey is deterministic per (orgId, projectId, quoteId) so retries overwrite the same R2 object"
---

<objective>
Add the quote-PDF queue contract to `packages/storage` — `QUOTE_PDF_QUEUE`, `QuotePdfJobData`, `quotePdfJobOptions()`, `quotePdfKey()` — cloning the "shared contract, no bullmq import" molecule of `queue.ts`/`keys.ts` (D-13). This is the dependency fase 7 consumes: the fase-7 producer (enqueue in `quotes.create`) and consumer (worker processor) will both build against these values, so they can never drift onto mismatched channel names, payload shapes, or R2 keys.

Purpose: Fase 7's PDF pipeline needs a contract frozen now, while deliberately NOT enqueuing anything this phase (no consumer exists — jobs would rot in Redis).
Output: `quote-pdf.ts` (new), `keys.ts` + `index.ts` (extended).
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-CONTEXT.md

# Analogs to clone
@packages/storage/src/queue.ts
@packages/storage/src/keys.ts
@packages/storage/src/index.ts
</context>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift/source-grounding):

- File `packages/storage/src/quote-pdf.ts` (NEW)
- Const `QUOTE_PDF_QUEUE = "quote-pdf"`
- Interface `QuotePdfJobData` (fields: `quoteId`, `organizationId`, `projectId` — all readonly strings)
- Function `quotePdfJobOptions(quoteId)` returning `{ jobId, attempts, backoff }`
- Function `quotePdfKey(orgId, projectId, quoteId)` added to `packages/storage/src/keys.ts`
- Barrel re-exports in `packages/storage/src/index.ts`: `quotePdfKey`, `QUOTE_PDF_QUEUE`, `quotePdfJobOptions` (runtime) and `QuotePdfJobData` (type-only)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create quote-pdf.ts — queue name, job payload, job options</name>
  <files>packages/storage/src/quote-pdf.ts</files>
  <read_first>
    - packages/storage/src/queue.ts (the molecule to mirror byte-for-byte in structure: header comment explaining "shared by producer and consumer, intentionally no bullmq import", const, readonly interface, pure options helper with jobId-dedup + 5-attempt exponential backoff rationale)
  </read_first>
  <action>
    Create `packages/storage/src/quote-pdf.ts` mirroring `queue.ts`. Header comment: this is the quote-PDF pipeline contract shared by the fase-7 producer (`quotes.create` enqueue) and consumer (worker processor); this phase ships the contract ONLY — no producer, no consumer (D-13) — and the module deliberately does not import bullmq so `@imbau/storage` stays Redis-free. Declare `export const QUOTE_PDF_QUEUE = "quote-pdf";`. Declare `export interface QuotePdfJobData { readonly quoteId: string; readonly organizationId: string; readonly projectId: string; }` — organizationId travels in the payload because the worker has no session to derive the tenant from (it feeds withTenant, same rationale as MediaJobData). Declare `export function quotePdfJobOptions(quoteId: string): { jobId: string; attempts: number; backoff: { type: "exponential"; delay: number } }` returning `{ jobId: quoteId, attempts: 5, backoff: { type: "exponential", delay: 2000 } }` — jobId = quoteId gives BullMQ dedup so a retried enqueue can never stack duplicate PDF jobs for one quote (the PDF-02 idempotency seam).
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && pnpm --filter @imbau/storage typecheck && grep -c "QUOTE_PDF_QUEUE" packages/storage/src/quote-pdf.ts && ! grep -rn "bullmq" packages/storage/src/</automated>
  </verify>
  <acceptance_criteria>
    - `packages/storage/src/quote-pdf.ts` contains `export const QUOTE_PDF_QUEUE = "quote-pdf"`, `export interface QuotePdfJobData`, and `export function quotePdfJobOptions(`.
    - `QuotePdfJobData` has exactly three readonly string fields: quoteId, organizationId, projectId.
    - `quotePdfJobOptions` returns jobId equal to its quoteId argument, attempts 5, exponential backoff delay 2000.
    - `grep -rn "bullmq" packages/storage/src/` returns nothing (no bullmq import anywhere in the package source).
    - `pnpm --filter @imbau/storage typecheck` exits 0.
  </acceptance_criteria>
  <done>The queue contract exists as a pure, dependency-free module fase 7 can import from both sides.</done>
</task>

<task type="auto">
  <name>Task 2: Add quotePdfKey to keys.ts and re-export the contract from the barrel</name>
  <files>packages/storage/src/keys.ts, packages/storage/src/index.ts</files>
  <read_first>
    - packages/storage/src/keys.ts (originalKey/variantKey style: pure functions, deterministic keys, doc comments explaining the idempotent-on-retry rationale)
    - packages/storage/src/index.ts (barrel convention: `export { ... }` for runtime, `export type { ... }` for type-only — verbatimModuleSyntax)
    - packages/storage/src/quote-pdf.ts (the symbols created in Task 1)
  </read_first>
  <action>
    In `keys.ts`, add `export function quotePdfKey(orgId: string, projectId: string, quoteId: string): string` returning the template literal `quotes/${orgId}/${projectId}/${quoteId}.pdf`, with a doc comment in the existing style: deterministic per quoteId so a retried fase-7 job overwrites the SAME R2 object instead of duplicating (mirrors variantKey's Pitfall-5 rationale); org/project segments scope the object for human-readable bucket browsing — authority stays with RLS + the worker, not the key path. In `index.ts`, extend the existing keys export line to `export { originalKey, variantKey, quotePdfKey } from "./keys";` and add `export { QUOTE_PDF_QUEUE, quotePdfJobOptions } from "./quote-pdf";` plus `export type { QuotePdfJobData } from "./quote-pdf";` — mirroring the MEDIA_QUEUE/MediaJobData export split exactly.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && pnpm --filter @imbau/storage typecheck && pnpm --filter @imbau/storage lint && grep -c "quotePdfKey" packages/storage/src/index.ts && grep -c "export type { QuotePdfJobData }" packages/storage/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/storage/src/keys.ts` contains `export function quotePdfKey(` returning the `quotes/` -prefixed `.pdf` key built from orgId, projectId, quoteId.
    - `packages/storage/src/index.ts` re-exports `quotePdfKey`, `QUOTE_PDF_QUEUE`, `quotePdfJobOptions` as runtime exports and `QuotePdfJobData` via `export type`.
    - Existing exports (`makeR2Client`, `originalKey`, `variantKey`, `MEDIA_QUEUE`, `mediaJobOptions`, `MediaJobData`, `R2ClientEnv`) remain untouched in the barrel.
    - `pnpm --filter @imbau/storage typecheck` and `pnpm --filter @imbau/storage lint` exit 0.
    - `pnpm --filter @imbau/api typecheck` still exits 0 (no api-side consumer was added — grep confirms `packages/api/src` has no import of `quote-pdf` or `QUOTE_PDF_QUEUE`).
  </acceptance_criteria>
  <done>The full PDF contract (queue name, payload, job options, R2 key) is exported from @imbau/storage with zero runtime dependencies and zero producers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure contract module — no I/O, no network, no queue connection this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Denial of Service | orphaned Redis jobs | medium | mitigate | NO producer is wired this phase (D-13) — enqueue lands in fase 7 together with the consumer, so jobs can never accumulate unconsumed |
| T-05-10 | Tampering | producer/consumer contract drift | low | mitigate | Single shared const/interface in @imbau/storage — both sides of fase 7 import the same symbols (mirrors MEDIA_QUEUE precedent) |
| T-05-SC | Tampering | npm installs | high | accept | No packages installed — the module deliberately has no bullmq/Redis dependency |
</threat_model>

<verification>
- `pnpm --filter @imbau/storage typecheck` and `lint` green.
- No `bullmq` string anywhere under `packages/storage/src/`.
- No consumer/producer added: `grep -rn "QUOTE_PDF_QUEUE" packages/api/src apps/` returns nothing (only packages/storage defines/exports it this phase).
</verification>

<success_criteria>
- Fase 7 can import `QUOTE_PDF_QUEUE`, `QuotePdfJobData`, `quotePdfJobOptions`, `quotePdfKey` from `@imbau/storage` on both the producer and consumer side.
- `quotes.create` (05-01) remains enqueue-free — verified by grep in this plan and by the 05-02 suite staying green.
</success_criteria>

<output>
Create `.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-03-SUMMARY.md` when done.
</output>