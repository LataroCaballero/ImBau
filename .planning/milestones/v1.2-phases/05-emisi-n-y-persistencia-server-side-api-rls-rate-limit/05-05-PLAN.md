---
phase: 05-emisi-n-y-persistencia-server-side-api-rls-rate-limit
plan: 05
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - deploy/nginx/staging.tours.andescode.com.ar.conf
autonomous: true
requirements: [QUOTE-03]
must_haves:
  truths:
    - "The versioned staging vhost declares a per-IP limit_req zone and applies it to a dedicated location matching the quotes tRPC path on the WEB server block only (D-10/D-11)."
    - "The reject status is explicitly 429 via limit_req_status — nginx's default reject is 503, which would break the fase-6 retry contract (RESEARCH Pitfall 2, CITED)."
    - "Panel vhost, prod vhosts, and the rest of the web vhost are untouched — the limit applies only to the quotes path (D-10)."
    - "The conf in deploy/nginx/ is the source of truth; application on the VPS is manual (sites-available + config test + reload) and the 429 burst is a human UAT step (D-12)."
  artifacts:
    - deploy/nginx/staging.tours.andescode.com.ar.conf
  key_links:
    - "limit_req_zone declared at the top of the included vhost file (sites-enabled files are included inside the http{} context, so the zone directive is valid there)"
    - "location ^~ /api/trpc/quotes inside the web :443 server block proxies to the same loopback backend as location / (127.0.0.1:8092)"
    - "the location prefix matches tRPC's quotes-only batched POST path /api/trpc/quotes.compute,quotes.create (RESEARCH Assumption A1 — fase 6 must keep quotes calls on a quotes-only link)"
---

<objective>
Activate the edge rate limit for the anonymous quote endpoint (QUOTE-03): add a `limit_req` zone (`rate=10r/s`) and a dedicated `location ^~ /api/trpc/quotes` block (`burst=20 nodelay`, `limit_req_status 429`) to the WEB server block of the versioned staging vhost, following the pattern pre-documented in the file since 04-07. The conf change is versioned here; the apply happens by hand on the VPS and the 429 burst is verified as a human UAT step — the limit lives in infra, not CI (D-10/D-11/D-12).

Purpose: The quotes path is the project's first anonymous write funnel; without an edge throttle, quotes.create is an open spam/DoS vector (Pitfall 6, T-05-14).
Output: Updated `deploy/nginx/staging.tours.andescode.com.ar.conf` + documented manual-apply/UAT instructions.
</objective>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-CONTEXT.md
@.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-RESEARCH.md

# File to modify (the pre-documented limit_req skeleton lives in its comments)
@deploy/nginx/staging.tours.andescode.com.ar.conf
</context>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift/source-grounding):

- `limit_req_zone` directive `zone=quotes:10m rate=10r/s` at the top of `deploy/nginx/staging.tours.andescode.com.ar.conf`
- `location ^~ /api/trpc/quotes` block (with `limit_req zone=quotes burst=20 nodelay;` and `limit_req_status 429;`) inside the web :443 server block
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the quotes limit_req zone + location to the web vhost</name>
  <files>deploy/nginx/staging.tours.andescode.com.ar.conf</files>
  <read_first>
    - deploy/nginx/staging.tours.andescode.com.ar.conf (current structure: HTTP :80 redirect block, web :443 block with `location /` proxying 127.0.0.1:8092, panel :443 block proxying 127.0.0.1:8093; the deferred rate-limit comments in both :443 blocks; the header rules — applied by hand, never certbot --nginx, prod untouched)
    - .planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-RESEARCH.md (the nginx Code Example: exact zone + location directives, the 503-default pitfall, the tRPC batched-path note)
  </read_first>
  <action>
    Two scoped edits to the versioned conf:

    1. At the top of the file (after the header comment block, before the first `server {`), add the zone with an explanatory comment: `limit_req_zone $binary_remote_addr zone=quotes:10m rate=10r/s;` — note in the comment that this directive is valid here because sites-enabled files are included INSIDE the host nginx's http{} context (zone directives must live at http level), that the zone is per-IP (`$binary_remote_addr`), 10m ≈ 160k IP slots, and that rate/burst are D-11 starting points tunable by hand during UAT without changing the decision.

    2. Inside the web :443 server block (`server_name staging.tours.andescode.com.ar`), BEFORE the existing `location /`, add a dedicated block:
       - `location ^~ /api/trpc/quotes { ... }` containing `limit_req zone=quotes burst=20 nodelay;`, `limit_req_status 429;`, then the SAME proxy directives as the existing `location /` (proxy_pass `http://127.0.0.1:8092`, Host / X-Real-IP / X-Forwarded-For / X-Forwarded-Proto headers, `proxy_http_version 1.1`).
       - Comment the block: `limit_req_status 429` is REQUIRED because nginx's default reject status for limit_req is 503 and the fase-6 client + QUOTE-03 UAT key off 429 (CITED); the `^~` prefix matches tRPC's quotes-only batched POST path (`/api/trpc/quotes.compute,quotes.create?batch=1`) — fase 6 must keep quotes calls on a quotes-only client link or this prefix will not throttle co-batched calls (RESEARCH Assumption A1); the throttle is web-only (QUOTE-03: anonymous funnel) — the panel block stays untouched.

    Update the stale "Edge rate-limit ... DEFERRED" comment in the WEB block only: it now refers to the future events/leads endpoints, while quotes is active above (rewrite that comment to point at the active quotes block as the in-file example). Leave the panel :443 block and its comments completely untouched. Do NOT touch the HTTP :80 block, TLS directives, or anything else.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && grep -c "limit_req_zone \$binary_remote_addr zone=quotes:10m rate=10r/s" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -c "limit_req_status 429" deploy/nginx/staging.tours.andescode.com.ar.conf && grep -c "location \^~ /api/trpc/quotes" deploy/nginx/staging.tours.andescode.com.ar.conf && awk '/panel.staging.tours.andescode.com.ar;$/,0' deploy/nginx/staging.tours.andescode.com.ar.conf | grep -c "limit_req zone=quotes" | grep -qx 0 && echo PANEL_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - The conf contains exactly one `limit_req_zone` directive with `zone=quotes:10m rate=10r/s`, placed before the first `server {` block.
    - The web :443 server block contains `location ^~ /api/trpc/quotes` with `limit_req zone=quotes burst=20 nodelay;`, `limit_req_status 429;`, and a `proxy_pass http://127.0.0.1:8092;` matching the backend of `location /`.
    - The panel :443 server block contains no active (uncommented) `limit_req` directive (the awk/grep gate above prints PANEL_UNTOUCHED).
    - `nginx -t`-style syntax sanity: if a local nginx binary or docker is available, `docker run --rm -v "$PWD/deploy/nginx":/etc/nginx/conf.d:ro nginx:alpine nginx -t` may be used as best-effort; otherwise the grep gates above are the CI-side evidence (the authoritative `nginx -t` runs on the VPS at apply time, D-12).
  </acceptance_criteria>
  <done>The versioned vhost is the complete, apply-ready source of truth for the QUOTE-03 edge limit, returning 429 (not 503) on burst.</done>
</task>

<task type="auto">
  <name>Task 2: Document the manual apply + 429 UAT procedure in the conf header</name>
  <files>deploy/nginx/staging.tours.andescode.com.ar.conf</files>
  <read_first>
    - deploy/nginx/staging.tours.andescode.com.ar.conf (as left by Task 1 — the header comment block documenting the 04-07 apply procedure)
  </read_first>
  <action>
    Extend the file's header comment with a short "Phase 5 rate-limit apply" note so the operator has the full procedure next to the config: (1) copy/update the file in /etc/nginx/sites-available/ on the VPS (root@andescode.com.ar), keeping the existing symlink; (2) run the nginx config test and reload via systemctl — same procedure as 04-07, never certbot --nginx, prod vhosts untouched; (3) UAT: from a shell, fire a burst of >30 rapid POSTs at `https://staging.tours.andescode.com.ar/api/trpc/quotes.compute?batch=1` (e.g. a `for` loop of `curl -s -o /dev/null -w "%{http_code}\n"`) and confirm the first ~20 pass through (200/400s from the app) while the excess returns 429; (4) rate/burst values may be tuned in place during UAT (D-11) — the repo file must be updated to match whatever lands. Note that this UAT belongs to the phase's human verification (end-of-phase), not CI.
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/ImBau && grep -c "Phase 5 rate-limit apply" deploy/nginx/staging.tours.andescode.com.ar.conf</automated>
    <human-check>On the VPS: apply the vhost (sites-available + nginx config test + systemctl reload nginx), then run the curl burst against https://staging.tours.andescode.com.ar/api/trpc/quotes.compute?batch=1 and confirm HTTP 429 appears after ~20 rapid requests while normal traffic still passes. Record the observed statuses in the phase UAT.</human-check>
  </verify>
  <acceptance_criteria>
    - The header comment contains a "Phase 5 rate-limit apply" section listing the sites-available update, config-test + reload, and the curl-burst 429 UAT with the exact staging URL.
    - The note states that tuned values must be synced back to this file (source of truth, D-12).
  </acceptance_criteria>
  <done>An operator can apply and verify the limit end-to-end from the conf file alone; the 429 burst is queued as the phase's human UAT step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → host nginx (staging web vhost) | All anonymous traffic to the quotes path crosses here — the throttle tier |
| Repo conf → VPS nginx | Manual apply boundary: the versioned file is truth, the box is a copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Denial of Service | anonymous quotes funnel | high | mitigate | Per-IP limit_req zone rate=10r/s burst=20 nodelay on the dedicated quotes location; 429 on excess (QUOTE-03/D-10/D-11) |
| T-05-15 | Denial of Service | wrong reject status breaking client retry | medium | mitigate | Explicit `limit_req_status 429` — nginx default reject is 503 (CITED, RESEARCH Pitfall 2) |
| T-05-16 | Tampering | prod/panel vhost regression during apply | high | mitigate | Web-block-only edit, panel/prod untouched (grep-gated); manual apply per 04-07 procedure with config test before reload, never certbot --nginx |
| T-05-17 | Denial of Service | co-batched tRPC calls dodging the prefix | low | accept | Documented for fase 6 (RESEARCH Assumption A1): quotes client must use a quotes-only link, or the limit widens to /api/trpc/ — revisit at fase-6 client wiring |
| T-05-SC | Tampering | npm installs | high | accept | No packages installed — config-only change |
</threat_model>

<verification>
- Grep gates: zone directive, 429 status, quotes location present; panel block clean.
- Human UAT (end-of-phase, D-12): VPS apply (config test + reload) and curl burst demonstrating 429 — record in VERIFICATION/UAT, cannot run in CI.
</verification>

<success_criteria>
- QUOTE-03: the anonymous quote endpoint is rate-limited at the edge via nginx limit_req with 429 on burst, config versioned in deploy/nginx/, prod and panel untouched.
</success_criteria>

<output>
Create `.planning/phases/05-emisi-n-y-persistencia-server-side-api-rls-rate-limit/05-05-SUMMARY.md` when done.
</output>
